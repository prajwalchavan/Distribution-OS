# Batch 2 — new findings (2026-09-13)

Findings raised while batch 2 was being planned and built. They are outside the scope the founder approved on 2026-09-13, so each
needs its own approval before any product change (Charter A.6).

### DOS-166 — A salesperson can record a money receipt through the offline upload, which the normal receipt endpoint refuses
Category: security | Priority: P0 | Role: Sales Rep (Warehouse also passes the role check, see Actual) | Platform: API

User: Sales Rep (rahul.deshmukh, tenant tarsun)
Platform: API — HTTP requests against the local services (sales-service :3003, warehouse-service :3004, delivery-service :3005, retailer-service :3006)
Environment: local, database dos_qa (QA test data), main at 19e2c12, 2026-09-13 10:20 IST
Steps:
  1. Sign in as rahul.deshmukh (POST :3000/auth/login).
  2. POST :3003/sync/upload with one op: PUT table `receipts`, a new UUIDv7 id, data {retailer_id: a tarsun shop, mode: cash,
     amount_paise: 100, received_at: now, status: collected} — the shape the delivery app queues (frontend/delivery-app/src/lib/queue.ts).
  3. Query dos_qa for the receipt, its journal entry, its allocation and the sync_ops row.
  4. Control: POST :3003/receipts with the same token and the same money.
Expected: the upload refuses the op as a sync rejection (2xx + sync_errors), just as the HTTP route refuses it, because a salesperson
never collects money (docs/22 §6 and never-list #2; docs/17 §D4).
Actual: 200 {accepted: 1}. dos_qa holds RCPT-0708 (100 paise, cash, collected, received_by rahul.deshmukh whose role is salesperson) with a
balanced journal entry, an allocation to a live bill, a ReceiptRecorded outbox event and a rendered PDF. The HTTP control answered 403
"the salesperson role may not call POST /receipts". A warehouse token (dinesh.patil, :3004) also passed every role check and was stopped
only by a journal insert policy, surfacing as 500 "Internal server error" instead of a sync rejection. The retailer was refused 403 because
the upload itself is staff-only. The delivery control (ganesh.more) was accepted as RCPT-0707, as it should be.
Business impact: the founder's most-repeated money rule holds on the normal endpoint and not on the offline route every field app ships.
A salesperson's phone can post cash receipts into the books, reduce a shop's outstanding and print a genuine-looking receipt: an
embezzlement and books-integrity hole. Any staff role may write any synced table wherever a database policy happens to allow the insert.
Severity: P0
Evidence: QA/evidence/batch2/sync-role-probe/ — 02-salesperson-receipts.json and 02-salesperson-receipts-db.txt (accepted, RCPT-0708),
05-salesperson-receipts-http.json (403 control), 01-delivery-receipts.* (legitimate control), 03-warehouse-receipts.* (500),
04-retailer-receipts.json (403), 06-salesperson-trip_expenses.* (refused only by the trips row policy), side-effects.md, probe.mjs.
Re-checked by the main session: the JSON statuses above and `SELECT receipt_no, amount_paise, username … WHERE receipt_no IN
('RCPT-0707','RCPT-0708')` → RCPT-0707|100|ganesh.more, RCPT-0708|100|rahul.deshmukh.
Root cause (the probe agent's code reading, to be confirmed by the architect's design): `sync.upload` is STAFF
(backend/libs/contracts/src/permissions.ts:451); `SyncService.upload` dispatches by table with no per-op role check
(backend/libs/core/src/modules/sync/sync.service.ts:108-148); `applyReceiptSync` calls `recordReceipt` directly
(backend/libs/core/src/modules/receivables/receivables.sync.ts:36), while `requireRole(MONEY_COLLECTORS)` exists only on the HTTP path
(receivables.service.ts:709).
Suggested fix: check every upload op against the permission matrix before dispatch. Each synced table names the procedure it stands for,
and a role the matrix refuses gets a sync rejection (2xx + sync_errors, never 4xx). The architect's design is being written to
QA/evidence/batch2/verdicts/DOS-166-design.md. The architect first suspected this while signing off DOS-115
(QA/evidence/batch2/verdicts/DOS-115.md); the probe proved it.
Side effects in dos_qa: RCPT-0707 and RCPT-0708 (100 paise each, with journal entries, allocations and PDFs) and one sync_errors row.
Ledgers are append-only, so they stay until dos_qa is rebuilt after DOS-032+059 merges.

### DOS-167 — After sign-out, the next user on the same device sees the previous user's shops, orders and dues, including another distributor's after an app restart
Category: security | Priority: P0 | Role: Sales Rep (any field app on a shared device) | Platform: Web with a persistent store; Android and iOS NOT TESTED

User: Sales Rep. First rahul.deshmukh (tarsun), then kiran.mhatre (sai-distributors). Same-tenant variant: amit.pawar (tarsun).
Platform: Web. The sales app (expo start --web :5175) ran in Chromium with COOP/COEP headers, so its offline store persists in OPFS. That is the kind of store a hosted web build keeps, and the native apps persist theirs in expo-sqlite. Tested at desk 1280x800 and phone 390x844.
Environment: local, database dos_qa (fixed seed, rebuilt 2026-09-13 16:11), services on main 3d7cb36. Probe run wf_ca15a053-341, 2026-09-13 17:23–18:05 IST.
Steps:
  1. Sign in as rahul.deshmukh. Wait for sync ("Updated just now"). Confirm Chavan Kirana Stores and SO-0875 are on the phone.
  2. Sign out through the app's own menu.
  3. (V5C) Reload the page (an app restart). Sign in as kiran.mhatre of Sai Distributors on the same device.
  4. (V5A) Same tab, no reload. Sign in as amit.pawar of the same distributor.
Expected: the next user starts from an empty device store and sees only their own data. Sign-out removes the previous user's rows and sync cursor.
Actual:
  - V5C, cross-tenant after a restart (both runs):
    - Within 0.4 s of kiran's sign-in, under the header "Sai Distributors, Dombivli / salesperson", Shops listed 30 Tarsun shops with codes, beats and outstanding dues. Examples: Balaji Wholesale Stores R-0010 ₹7,20,804.00; Chavan Kirana Stores R-0024 ₹21,892.00.
    - It cleared once the manifest handshake re-snapshotted. kiran then held 24 shops, matching SQL.
  - V5A, same tenant, no reload:
    - amit inherited rahul's store and sync cursor. His first pull carried rahul's since, and 0 rows came back.
    - Shops showed 30, rahul's list, including Chavan Kirana, which is never on amit's beat.
    - Orders read "newest 100 of 363", which is rahul's count; amit's own count in SQL is 380.
    - Rahul's SO-0875 detail rendered with its lines and a total of ₹1,01,531.00, while the server answered 404 to amit.
    - amit's beat read "No beat today" and none of his own 29 shops arrived, while the screen said "Updated just now".
  - Not reproduced:
    - V1–V4 on the plain dev web build: its store is in memory and new at each sign-in.
    - V5B, cross-tenant in the same tab with no reload: the tenant check re-snapshotted before any row showed.
Business impact:
  - A shared or reassigned phone shows one distributor's customer book and receivables to another distributor's rep. That is tenant data leaking on the device (the never-list).
  - Within one distributor, a rep silently gets a colleague's shops and orders and never receives his own beat. He cannot work his route, and nothing on screen tells him.
Severity: P0
Evidence: QA/evidence/batch2/s-098/SUMMARY.md, which lists every screenshot and results JSON of V1–V5 (the v5 runs are the confirmed ones). Script: QA/tools/e2e/s-098-shared-device.mjs.
Root cause (architect's code reading at the DOS-080 merge review, consistent with the probe):
  - frontend/libs/offline/src/engine.ts:200: wipe() has no caller, so sign-out keeps the rows and the cursor.
  - The local store is named per app, not per user or tenant: sales-app/app/_layout.tsx:299, delivery-app :299, warehouse-app :305.
  - The tenant check re-snapshots only after the first manifest, so rows render before it.
Suggested fix (for the architect to design):
  - Wipe the device store and cursor on sign-out, and on any user or tenant change, before the first render.
  - Key the store by user and tenant.
  - Render nothing from the store until the manifest handshake confirms the same user and tenant.
  - Do the same in all three field apps (sales, delivery, warehouse).
  - Prove the fix on Android and iOS as well as web.
Not tested: Android (expo-sqlite) and iOS. They run the same engine code, but per Charter A.4 that result is not inferred.


### DOS-168 — Two simultaneous POST /receipts/deposit calls for the same receipt both return 200: the receipt is banked twice, BANK doubles and CASH/CHEQUES go negative
Category: bug | Priority: P0 | Role: Money desk: Accountant and Manager (the Owner can also bank) | Platform: API (core spec on a test database: HTTP through Fastify inject into ReceivablesModule, real TenantGuard, permission matrix and DbModule)
User: Money desk: Accountant and Manager (the Owner can also bank)
Platform: API (core spec on a test database: HTTP through Fastify inject into ReceivablesModule, real TenantGuard, permission matrix and DbModule)
Environment: Worktree qa/b2-suspects at c679499 (= main, 'git merge --ff-only main' printed 'Already up to date.'). Libraries already built. Postgres 17 on 127.0.0.1:5439, database dos_test_b2_sus (a copy of dos_test_batch2b_template with 48 of 48 migrations applied), default_transaction_isolation = read committed. Node 24, vitest 4.1.11, DATABASE_POOL_MAX=3. Probe tenants: s75b-5ce32634 (run 1) and s75b-118938be (run 2), both removed afterwards by recreating the database from the template.
Steps:
  1. As the accountant, record an office receipt of Rs 100 (10 000 paise): POST /receipts with mode cash (or cheque with reference and bankName), no trip. Reply is 200, status 'collected', tripId null.
  2. At the same moment, send two requests: the accountant sends POST /receipts/deposit {id: batchA, idempotencyKey: dep-s75b-batchA, receiptIds: [receipt], depositRef: DEP-A-n} and the manager sends the same with batchB, its own key and DEP-B-n (Promise.all of two app.inject calls with pre-signed desk tokens).
  3. Read the receipt row (status, deposit_ref), the deposit journal_entries for batchA and batchB with their journal_lines by account code, and the ChequeDeposited outbox_events for the receipt.
  4. Run A: repeat steps 1-3 thirty times, alternating cash and cheque.
  5. Run B: repeat five more times while a third connection holds 'select ... for update' on the receipt row for 1.5 s. Both desk requests then pass their status check before either writes; count the backends waiting on a lock in pg_stat_activity, then release.
  6. Run C (control): bank the same receipt twice one after the other, accountant then manager.
  7. Read the probe tenant's totals: receipts by mode, account balances for BANK/CASH/CHEQUES/AR, deposit entries, unbalanced entries.
Expected: For each concurrent pair, exactly one call answers 200 and the other 409 'receipt ... is deposited, not collected'. The receipt carries exactly one deposit journal entry (BANK +10 000, CASH or CHEQUES -10 000) and one ChequeDeposited event. Tenant totals: BANK equals the Rs 3 600 actually collected, and CASH and CHEQUES are 0.
Actual: Run 2 (per-run log in probe-log.txt):
- Run A: 30 of 30 pairs both 200 (each 'updated=1,total=10000'). Every receipt shows receipt.status=deposited, depositEntries=2, BANK=20000, CASH or CHEQUES=-20000 and ChequeDepositedEvents=2. deposit_ref is whichever desk wrote last (DEP-B-n in 26 runs, DEP-A-n in 4).
- Run B: 5 of 5 pairs both 200. pg_stat_activity showed backendsWaitingOnLock=2, both inside 'update "receipts" set "status" = ...', so both had already passed the status check.
- Run C (sequential): first 200, second '409(receipt RCPT-0036 is deposited, not collected)'.
- Tenant totals after run 2: 36 receipts, Rs 3 600 collected (cash 210 000 + cheque 150 000 paise). 71 deposit journal entries. BANK +710 000 (Rs 7 100), CASH -200 000, CHEQUES -150 000, AR -360 000, 0 unbalanced entries.
- SQL for RCPT-0001 and RCPT-0002: each has two deposit entries, DEP-A-n posted_by the accountant (...73c1) and DEP-B-n posted_by the manager (...73c0), each BANK +10000 / CASH or CHEQUES -10000, and two ChequeDeposited events.

Run 1 (database only; vitest hid the console lines): 34 of 35 concurrent pairs doubled. 70 deposit entries for 36 receipts, BANK +700 000 against 360 000 collected, CASH -190 000, CHEQUES -150 000.

Overall, 69 of 70 concurrent pairs banked the same receipt twice. Each sequential control banked once.
Business impact: A shop pays Rs 100 in cash. The manager presses 'Bank this batch' on Day-end while the accountant presses 'Bank it' on the receipt panel (the owner's Money > Receipts screen can bank too). Both screens say it worked. The books now show Rs 200 in the bank and Rs -100 cash in hand for Rs 100 of real money. The bank statement will never reconcile, cash in hand and cheques in hand go negative, and BANK is overstated in every balance and report built from the journal. One deposit call accepts up to 200 receipts, so a single clash on a full Day-end batch doubles the whole day's banking (for example Rs 2 lakh shows as Rs 4 lakh). For a cheque, a later bounce reverses only one of the two deposits, so BANK stays overstated. The journal is append-only, so the fix is a manual reversing entry, and nothing tells anyone it happened. ChequeDeposited fires twice, so anything that consumes it acts twice. The receipt's deposit_ref and deposited_at are silently overwritten by the second desk, so the receipt points at a different batch than the first journal entry. This is financial corruption in normal two-desk use, hence P0 by the charter; the row's P1 was a pre-verification guess.
Severity: P0
Evidence: QA/evidence/batch2/suspects/S-75b/ (probe spec and its run output); skeptic re-run: QA/evidence/batch2/suspects/S-75b/skeptic/ where present. Probe: temporary spec backend/libs/core/src/modules/receivables/zz-probe-S-75b.spec.ts (copy saved in scratchpad, deleted from the worktree after the run).

Command: pnpm --filter @dos/core exec vitest run src/modules/receivables/zz-probe-S-75b.spec.ts --silent=false, with DATABASE_URL=.../dos_test_b2_sus. Result: 'Test Files 1 passed (1) Tests 4 passed (4)'. The probe records outcomes and does not assert them.

Log lines from probe-log.txt:
- '[S-75b] database={"d":"dos_test_b2_sus","iso":"read committed"}'
- 'A run 01 cash accountant=200(updated=1,total=10000) manager=200(updated=1,total=10000) | receipt.status=deposited deposit_ref=DEP-B-1 depositEntries=2 BANK=20000 CASH=-20000 ChequeDepositedEvents=2'
- 'A summary: runs=30 bothSucceeded=30 oneRefused409=0 other=0'
- 'B run 1 cash backendsWaitingOnLock=2 ["update \"receipts\" set \"status\" = $1, \"de", ...] accountant=200 manager=200 | depositEntries=2 BANK=20000 CASH=-20000'
- 'B summary: runs=5 bothSucceeded=5'
- 'C sequential first=200(updated=1,total=10000) second=409(receipt RCPT-0036 is deposited, not collected) | depositEntries=1 BANK=10000 CASH=-10000'
- 'D account balances (paise): BANK 710000, CASH -200000, CHEQUES -150000, AR -360000'
- 'D journal entries by ref_type: deposit 71, receipt 36'
- 'D unbalanced entries: 0'

Read-only psql after run 2 (db-after-run2-receipts-1-2.txt):
- RCPT-0001 cash, status deposited, deposit_ref DEP-B-1. Journal: DEP-A-1 (posted_by accountant) BANK 10000 / CASH -10000, and DEP-B-1 (posted_by manager) BANK 10000 / CASH -10000.
- RCPT-0002 cheque: DEP-A-2 and DEP-B-2, each BANK 10000 / CHEQUES -10000.
- Two ChequeDeposited events per receipt.

Read-only psql after run 1 (db-after-run1.txt): BANK 700000, CASH -190000, CHEQUES -150000, 70 deposit entries for 36 receipts. Two cash receipts carry one event (the control plus one pair where the guard held); the other 34 carry two.

Code read on this worktree: withTenant (backend/libs/database/src/client.ts:61-75) opens a plain db.transaction, so READ COMMITTED. idempotent() (backend/libs/core/src/platform/idempotency.ts:57-61) only serialises requests that reuse the same key.

Grep of the frontend: three desk screens call receivables.receipts.deposit, each with depositedAt: new Date().toISOString() — frontend/manager-app/app/money/day-end.tsx:135, frontend/manager-app/app/money/index.tsx:156 and frontend/owner-app/app/money/receipts.tsx:85. None of these screens was run.

Cleanup: the probe file was removed and dos_test_b2_sus was dropped and recreated from dos_test_batch2b_template ('migrations applied', 'probe tenants left: 0'). git status --short printed nothing.
Root cause: backend/libs/core/src/modules/receivables/receivables.service.ts, depositReceipts (line 870):
- :876-879 reads the batch's receipts with a plain SELECT, without FOR UPDATE, inside withTenant's READ COMMITTED transaction.
- :883-888 checks status 'collected' against that unlocked snapshot, so a second desk whose read happens before the first desk commits also sees 'collected'.
- :933-942 updates receipts filtering only on tenant_id and id. There is no status = 'collected' condition and the updated row count is never checked. When the first transaction commits, the second one's UPDATE (which was waiting on the row lock) re-checks its WHERE clause, still matches, and overwrites status, deposit_ref and deposited_at.
- :943-956 posts the journal with idempotencyKey `journal:deposit:${input.id}`. That key is unique per batch id, so a second batch posts a second Dr BANK / Cr CASH or CHEQUES entry.
- :957-963 emits ChequeDeposited again.
- The only serialisation point, idempotent() in backend/libs/core/src/platform/idempotency.ts:57-61, applies to requests that reuse the same key, not to two desks with different keys.
Suggested fix: In depositReceipts, lock the batch before checking it. Change the select at receivables.service.ts:876-879 to `...where(...).orderBy(receipts.id).for('update')`. The ordering stops two overlapping batches from deadlocking. The second desk then waits for the first to commit, reads the committed status 'deposited', and gets the existing 409. As a second guard, add `eq(receipts.status, 'collected')` to the UPDATE's WHERE at :942 with `.returning({ id: receipts.id })`, and throw ORPCError CONFLICT (which rolls back the journal and events) when the returned count is not input.receiptIds.length. No schema or contract change is needed. The same read-then-update pattern is worth checking in undoReceipt (reverse and bounce) as a separate item.
Test plan: Add a red test to backend/libs/core/src/modules/receivables/receivables.spec.ts.

Deterministic case:
1. Record an office cash receipt of 10 000 paise as the accountant.
2. From the spec's own pool, open a gate connection: begin; select id from receipts where id = $1 for update.
3. Start the accountant's and the manager's POST /receipts/deposit for that receipt, with different ids and idempotency keys, without awaiting.
4. Poll pg_stat_activity until 2 backends have wait_event_type = 'Lock', then commit the gate and await both calls.
5. Expect the sorted statuses to be [200, 409], and the 409 message to contain 'is deposited, not collected'.
6. Expect exactly one journal_entries row with ref_type 'deposit' among the two batch ids, with lines BANK +10 000 and CASH -10 000.
7. Expect exactly one ChequeDeposited outbox event for the receipt, and receipts.deposit_ref equal to the winning batch's depositRef.
8. Repeat once with a cheque (CHEQUES -10 000).

Unassisted case: 20 iterations of Promise.all over the two desk calls, asserting that no receipt ever has two deposit entries.

On current main both cases fail: both calls return 200 and each receipt gets two entries, as the probe showed in 69 of 70 concurrent pairs.
NOT TESTED: No screen was driven on web, Android or iOS: the manager app's Day-end 'Bank this batch', its Money receipt panel 'Bank it', and the owner app's Money > Receipts. The three call sites were found by grep only.; A single desk double-tapping 'Bank it': whether the api-client gives the second tap a new idempotency key (each call site builds depositedAt at click time) was not run.; Trip receipts from a settled trip (the DOS-132 path banked from CASH) were not raced; only office cash and cheques were.; Two multi-receipt batches overlapping in a different order (deadlock or partial overlap behaviour) were not tested.; A bounce of a cheque that was banked twice (whether BANK stays overstated by the second deposit) was not run.; Two separate service processes: both requests went through one Nest app on separate pooled connections. The database interleaving is the same, but two processes were not run.; Owner or manager reports and daybook screens showing the doubled BANK balance were not checked.; Part (a) of S-75 (undoReceipt crediting CASH_VAN after the trip is settled) is outside this probe.
Source: architect suspect S-75 part (b); probe and skeptic run wf_309501e5-2f5 (2026-09-14). Skeptic: UPHELD (real user path: True; priority P0). Upheld: I could not refute it. When two desks bank the same collected receipt at the same moment, both calls succeed. Each call posts its own Dr BANK / Cr CASH or CHEQUES entry and emits its own ChequeDeposited event.

The probe takes the same path as the app:
- Same endpoint: POST /receipts/deposit, the call made by the manager app's Day-end (day-end.tsx:135), its Money panel (money/index.tsx:156) and the owner app's Money > Receipts (owner-app receipts.tsx:85).
- Same roles: MONEY_DESK at permissions.ts:604 admits owner, manager and accountant.
- Same body: id, idempotencyKey, receiptIds, depositedAt, depositRef.
- Same keys: each device's useMutation builds its own id and key (api-client client.ts:90-91), so two desks always send different keys.
- Deposit has no offline-sync handler (receivables.sync.ts has none), so this online call is the only path.

Nothing else in the product prevents the outcome. Queried on dos_test_b2_sus:
- Triggers on receipts: only receipts_tombstone and receipts_touch_updated_at.
- The receipts_write_update policy checks only the tenant and a non-retailer role.
- journal_entries has only UNIQUE(tenant_id, idempotency_key). The deposit key is `journal:deposit:${batchId}`, so each batch is unique and nothing ties a receipt to a single deposit.
- ChequeDeposited is listed as AUDIT_ONLY at backend/worker/src/jobs/outbox-coverage.test.ts:40, so no job reconciles afterwards.
- The server has no FOR UPDATE and no status guard (receivables.service.ts:876-879 and :933-942, as the prober cited).

Reproduction:
- Prober's spec re-run: 34 of 35 concurrent pairs banked twice.
- My probe through the real manager and owner services: 18 of 20 simultaneous pairs banked twice.
- On a bounce after a double deposit, the cheque's net is still BANK +10000 and CHEQUES -10000.
- The owner's GET /receivables/accounts shows the inflated BANK.

Why P0 stays: by the charter this is financial corruption (BANK overstated, CASH and CHEQUES negative), it is silent, and the product has no way to post a correcting entry.

How often it happens is narrower than the prober says. The second confirm must reach the server while the first call's transaction is still open. Locally that window was under 5 ms for one receipt: pairs sent 5 to 320 ms apart were refused in 28 of 28 cases. For a 200-receipt Day-end batch it was about 25 to 50 ms. It is still a real path for two people on two devices, and a production database adds network latency, which should widen the window; that was not measured.

A double tap from ONE desk is NOT a path. useMutation reuses the same key for the same input, and the button is disabled while the call is loading. In 10 of 10 same-key pairs one call answered 200 and the other 409.
Skeptic corrections (binding on this record): 1. How often it happens. The businessImpact says this occurs in normal two-desk use, but the second desk's confirm must reach the server while the first deposit's transaction is still open.
   - Measured locally: under 5 ms for a single receipt. At d=0, 4/4 pairs banked twice; at d=5 to 320 ms, 0/28 did, each answered 409.
   - For a 200-receipt Day-end batch: about 25 to 50 ms. The batch took 47 ms; singles sent at 0 and 25 ms banked twice, singles at 50 to 1000 ms were refused 8/8.
   - The 'Rs 2 lakh shows as Rs 4 lakh' example needs both whole batches to start inside that window.
   - A production database's network latency widens the window; not measured.
   - Priority stays P0 on the charter's impact definition, but the report should state the timing condition.

2. 'ChequeDeposited fires twice, so anything that consumes it acts twice' overstates it. ChequeDeposited is in AUDIT_ONLY at backend/worker/src/jobs/outbox-coverage.test.ts:40. No worker handler claims it, so the second event is only a duplicate audit row.

3. 'The journal is append-only, so the fix is a manual reversing entry.' The product has no endpoint to post one: permissions.ts:624-625 hold only the reads receivables.accounts.list and receivables.journal.list. A reverse or bounce undoes one deposit only (confirmed in E5), so correcting the books needs a code change or a database-level correction.

4. The prober's notTested item on a single desk double-tapping 'Bank it' is now resolved: it is not a path. The screens build depositedAt inside the mutation function, not in the intent input, so useMutation (api-client react/index.tsx) keeps the same idempotencyKey. The Dialog confirm is disabled while busy (ui web/feedback.tsx:318 → controls.tsx Button disabled when loading). API check: 10/10 same-key pairs gave one 200 and one 409 'idempotencyKey was already used with a different request', 1 entry; 5/5 identical bodies banked once. Only two users, devices or dialog openings reach the race; the manager Money panel calls deposit.reset() when the dialog opens.

5. The bounce claim is verified. After a cheque was banked twice and bounced once, its net journal was AR 0, BANK +10000, CHEQUES -10000.

6. 'Every balance and report' is verified only for GET /receivables/accounts (trial balance): BANK balancePaise 2930000 against 2680000 collected. The Tally export and day book were not checked.

7. The probe's shape: the prober used bootTestApp([ReceivablesModule, SyncModule]) in one app. I reproduced it through the real createServiceApp compositions (two manager-service instances plus owner-service, separate pools): 18/20 pairs banked twice. So the harness is not a shortcut.

8. Citations: the idempotent() insert is at idempotency.ts:58-60 (the prober wrote 57-61; minor). The others check out:
   - receivables.service.ts:870, 876-879, 883-888, 933-942, 943-956 and 957-963
   - client.ts:61-75
   - day-end.tsx:135, money/index.tsx:156, owner-app receipts.tsx:85
   - receiptIds is capped at 200 (contracts receivables.ts:481).

9. Run counts on my re-run of the prober's spec: A 29/30 banked twice (not 30/30), B 5/5, C refused. D: BANK 700000, CASH -190000, CHEQUES -150000, 70 deposit entries. Consistent with the prober's run 1.
Status: NEW — confirmed on the API, not yet approved by the founder.

### DOS-169 — Trip settlement ignores cash receipts uploaded offline: an honest crew shows as cash over and needs the owner, and cash a crew keeps settles green with no variance
Category: business-logic | Priority: P0 | Role: Delivery crew (takes cash at the door with no signal), then Accountant / Manager / Owner at Day-end settlement | Platform: API (core spec on a test database: NestJS app from src/testing/app.ts). No device or browser was driven.
User: Delivery crew (takes cash at the door with no signal), then Accountant / Manager / Owner at Day-end settlement
Platform: API (core spec on a test database: NestJS app from src/testing/app.ts). No device or browser was driven.
Environment: Worktree qa/b2-suspects fast-forwarded to main 199952b. Node 24, Postgres 17 on 127.0.0.1:5439, database dos_test_b2_sus (a copy of dos_test_batch2b_template, migrated). Each run used a fresh probe tenant with the default settlement tolerance of 10,000 paise. Each bill was 60 pcs x Rs 10 + 12% GST = 67,200 paise. The probe ran twice (run 2 and run 3) with the same numbers.
Steps:
  Set up a tenant with owner, manager, accountant, warehouse, salesperson, three delivery users and shops A and B. Make real bills of 67,200 paise each: POST /orders, then /orders/:id/submit, then /warehouse/orders/:id/pack.
  Trip 1: the manager plans a trip with a float of 50,000 paise and two stops (bill A at shop A, bill B at shop B). Warehouse runs start-loading. The driver gives GPS consent, departs, and records both deliveries (POST /delivery/deliveries, both 'delivered').
  As the driver, call GET /sync/manifest.
  Stop A with a signal (the online branch of collect.tsx): the driver calls POST /delivery/collections with cash 67,200 for shop A.
  Stop B with no signal (the offline branch of collect.tsx, useQueueReceipt in queue.ts, sent as engine.ts toWireOp builds it): the driver calls POST /sync/upload with protocol 1 and one op: table 'receipts', op PUT, data {retailer_id B, trip_id, mode 'cash', amount_paise 67200, received_at, received_by, device_id, status 'collected', client_receipt_no}, plus clientTime.
  The driver calls POST /delivery/trips/:id/return. The accountant calls GET /delivery/trips/:id/settlement and GET /receipts?tripId&mode=cash&status=collected.
  The accountant calls POST /delivery/trips/:id/settle with handedOverCashPaise 184,400 (float 50,000 + 67,200 + 67,200) and counted = expectedVanStock.
  The owner sends the same settle with acceptVariance true. Read approvals, trip_settlements, trips.state, the settlement journal lines and the tenant CASH_VAN / CASH / CASH_SHORT balances.
  Trip 2: a second driver with a float of 20,000 paise and one delivered stop. One offline cash receipt of 67,200 goes through /sync/upload. Return. The accountant settles with handedOverCashPaise 20,000, meaning the crew keeps the shop's cash.
  Trip 3 (is there a workaround?): one offline cash receipt of 67,200. Then the accountant, and next the manager, call POST /delivery/collections with receiptId set to the offline receipt's id. Then settle with 67,200.
Expected: Settlement counts every cash receipt recorded against the trip, whichever path wrote it. Trip 1: preview cashCollectedPaise 134,400 and expectedCashPaise 184,400. Handing over 184,400 is variance 0, so the trip ends 'settled' with no approval row. The journal credits CASH_VAN 134,400 with nothing on CASH_SHORT, and CASH_VAN nets to 0. Trip 2: expected cash is 87,200. Handing over only 20,000 is 67,200 short, so the settle answers 409 settlement_needs_owner and files a pending approval.
Actual: Run 2 (run 3 gave the same numbers).
TRIP 1:
- Manifest for the delivery role: receipts writable=true, collections not published. Writable tables are only receipts, trip_stops and deliveries.
- The online collection returned 200 (RCPT-0001). /sync/upload returned 200 {accepted:1, rejected:[]} (RCPT-0002).
- The receipts table has 2 rows, both carrying trip_id. Each posted Dr CASH_VAN 67,200 / Cr AR 67,200. The collections table has 1 row, for the online receipt only.
- Preview: cashCollectedPaise 67,200, collectionsCount 1, expectedCashPaise 117,200. For the same trip, GET /receipts totals.countedPaise is 134,400 over 2 receipts.
- Accountant settle with 184,400: 409 settlement_needs_owner, 'cash is 67200 paise off (tolerance 10000)', cashVariancePaise +67,200. An approvals row of kind trip_settlement is filed as pending (payload expected 117,200, handed 184,400).
- Owner with acceptVariance: 200. The trip ends settled_with_variance (hasVariance true, approved_by owner).
- Settlement journal: CASH +134,400, CASH_SHORT -67,200 (memo 'cash over'), CASH_VAN -67,200.
- Tenant balances after: CASH_VAN +67,200 left behind, CASH_SHORT -67,200 (a fictitious cash-over gain).
TRIP 2:
- The offline receipt was accepted (RCPT-0003). Collections has 0 rows. The invoice state is 'paid'.
- Preview: cashCollectedPaise 0, expectedCashPaise 20,000.
- Settle with 20,000: 200, tripState 'settled', cashVariancePaise 0, hasVariance false. No approvals row and no settlement journal entry.
- GET /receipts with withCrew=false (cash in hand) lists the 67,200 receipt as in the office.
- CASH_VAN rose by another 67,200. The 67,200 the crew kept went unnoticed.
TRIP 3:
- Accountant POST /delivery/collections with the stop id: 404 'stop ... not found'.
- Manager with the same call: 200. It replayed the existing receipt (still one receipt, no second journal entry) and added the collections row.
- The settle with 67,200 then ends 'settled' and the CASH_VAN change is 0.
- No desk app calls delivery.collections.record (grep of the owner, manager, admin and warehouse apps found nothing), so this repair exists only as a hand-made API call that needs the receipt id.
Business impact: Vans often lose signal, and the delivery app's own offline path records a doorstep payment as a 'receipts' op. The Day-end cockpit does not see any of that cash.
- Honest crew: each offline cash receipt over Rs 100 shows as cash over. The accountant or manager cannot close the trip, it goes to the owner's approval queue, and it closes red (settled_with_variance). When the owner accepts, the shop's payment is booked as a made-up cash-over gain (CASH_SHORT, overstating income), and the same amount stays in CASH_VAN for good (overstating van cash). Every offline receipt makes the books worse.
- Dishonest or careless crew: they hand over only the float and keep the offline cash. The settlement is green, variance 0, no approval. Meanwhile the shop's bill shows paid and the accountant's cash-in-hand list says the money is in the office. At Rs 672 a bill, a driver with 10 offline cash stops could keep Rs 6,720 in one day with nothing flagged.
This is money going missing and the ledger being wrong in normal daily work, so it meets the charter's P0 'financial corruption'. That is above the P1 in the suspect row, because trip 2 shows theft goes unnoticed.
The only repair is an API call no screen offers (manager, before settlement, knowing the receipt id), and the accountant role gets 404 on it.
Severity: P0
Evidence: QA/evidence/batch2/suspects/S-76/ (probe spec and its run output); skeptic re-run: QA/evidence/batch2/suspects/S-76/skeptic/ where present. Command (run twice at HEAD 199952b): cd backend && pnpm --filter @dos/core exec vitest run src/modules/delivery/zz-probe-S-76.spec.ts. Both runs ended 'Test Files 1 passed, Tests 3 passed'. The assertions check that the defect is present: preview cashCollectedPaise == 67,200; accountant settle 409 with cashVariancePaise == 67,200; owner settle 200; tenant CASH_VAN == 67,200; trip 2 settle 200 'settled' with variance 0 and CASH_VAN +67,200.
The test preset hides console output, so every observation was appended to a log file:
- scratchpad/suspects/S-76/probe-log-run2.jsonl, trip 1 lines 1-24: manifest (collections null) at line 3; upload accepted at 8; receipts 2 rows vs collections 1 row at 9-10; receipt journals at 11; preview 67,200 / 117,200 at 14; receipts list 134,400 at 15; 409 +67,200 with approval at 17-18; owner settled_with_variance at 19-22; settlement journal CASH +134,400 / CASH_SHORT -67,200 / CASH_VAN -67,200 at 23; books CASH_VAN 67,200 at 24.
- Same file, trip 2 lines 25-40: collections [] at 31; invoice paid at 33; preview 0 / 20,000 at 34; settled variance 0 at 35; no approval and no journal at 36-37; receipt listed as in hand at 38; CASH_VAN delta +67,200 at 40.
- scratchpad/suspects/S-76/probe-log-run3.jsonl: same trip 1 and 2 numbers. Trip 3: accountant attach 404 'stop not found', manager attach 200, collections row added, settlement 'settled', CASH_VAN delta 0.
Grep of frontend/{owner,manager,admin,warehouse}-app for collections.record returned nothing.
Cleanup: the probe was deleted and git status --porcelain is empty. dos_test_b2_sus was dropped, recreated from dos_test_batch2b_template and migrated; select count(*) from tenants where slug like 'p76-%' returns 0.
Root cause: 1. backend/libs/core/src/modules/delivery/settlement.service.ts:348-372: figures() takes cash/UPI/cheque totals only from `collections` rows for the trip (lines 349-352, 360). So expectedCashPaise (368) and cashVariancePaise (446-449) leave out any trip receipt that has no collections row. settle() then posts Cr CASH_VAN = plan.cashCollectedPaise (235-239) and puts the rest on CASH_SHORT (230-234).
2. backend/libs/core/src/modules/receivables/receivables.sync.ts:60-78: applyReceiptSync calls ReceivablesService.recordReceipt with tripId (line 72). That posts Dr CASH_VAN (receivables.service.ts:280-283 receiptAccountCode, and 686) but writes no collections row. Only CollectionsService.collectInTx writes one (collections.service.ts:127-144).
3. The delivery app can queue only `receipts` offline: frontend/delivery-app/app/stop/[id]/collect.tsx:154-172 calls useQueueReceipt in frontend/delivery-app/src/lib/queue.ts:240-274. A `collections` upload handler does exist (delivery.sync.ts:176-211, registered in delivery.module.ts:93-97). But `collections` is not in SYNC_PULL_TABLES (backend/libs/database/src/sync-tables.ts lists receipts at :94 and has no collections), so the manifest never publishes it as writable. The probe confirmed this: collections null.
The gap is described in comments (queue.ts:224-236, collect.tsx:14-21) but not handled.
Suggested fix: Make the settlement count what was actually posted to the van's accounts, not only collections rows.
- Add an exported ReceivablesService method, for example tripReceiptTotals(tx, tripId). It returns per-mode totals of the trip's receipts net of reversals, which are exactly the rows whose journal lines hit CASH_VAN / UPI / CHEQUES. SettlementService already injects ReceivablesService.
- In SettlementService.figures(), take cashCollectedPaise, upiCollectedPaise and chequeCollectedPaise from that method. Keep collectionsCount for display only.
This covers receipts already uploaded offline, older app builds and the online path in one place. The settle journal's Cr CASH_VAN then equals the receipts' Dr CASH_VAN.
Optional follow-up: also let the delivery app queue the existing `collections` op, so the stop-level row exists. The settlement must not depend on that row.
Test plan: Red test in backend/libs/core/src/modules/delivery/delivery.spec.ts, which fails today:
1. On an active trip with float F, the driver records one online POST /delivery/collections of Y cash.
2. The driver uploads POST /sync/upload with {table:'receipts', op:'PUT', data:{trip_id, retailer_id, mode:'cash', amount_paise:X, device_id, client_receipt_no}}.
3. After return, assert that the preview has cashCollectedPaise == X+Y and expectedCashPaise == F+X+Y.
4. The accountant settles with F+X+Y. Assert 200, tripState 'settled', hasVariance false, and no approvals row for the trip.
5. Assert the settlement journal has CASH_VAN == -(X+Y) and CASH_SHORT 0, and that CASH_VAN over the trip's receipts plus the settlement nets to 0.
Twin test: a second trip whose only money is an offline cash receipt X, settled with F alone. It must answer 409 settlement_needs_owner with cashVariancePaise == -X and file a pending trip_settlement approval.
Today the first test gets preview 67,200 and 409 +67,200, and the twin gets 200 'settled' with variance 0.
NOT TESTED: The delivery app on Android, iOS or web: the offline collect screen and the outbox flush were not driven. The wire op was copied from queue.ts useQueueReceipt and libs/offline engine.ts toWireOp and sent by API.; The manager app's Day-end settle screen (money/day-end.tsx) and the owner app's approval and trip screens were not driven; only the API was.; Offline UPI and cheque receipts (only cash was probed). upiCollectedPaise / chequeCollectedPaise read the same collections-only query, but that was not run.; Reversing an offline receipt before settlement, and depositing a stranded offline receipt after settlement (Dr BANK / Cr CASH): not run.; The accountant's 404 'stop not found' on POST /delivery/collections with a stopId was seen in passing and not investigated. It is outside S-76.; Van-sale collections and trip expenses sent through sync: not run.; Live dos / dos_qa data was not queried for real offline receipts that are missing a collections row.
Source: architect suspect S-76 and S-09 (one defect); probe and skeptic run wf_309501e5-2f5 (2026-09-14). Skeptic: UPHELD (real user path: True; priority P0). I could not refute it. It reproduces through the same path the delivery app takes, and nothing in the product catches or repairs it afterwards.

REAL PATH: collect.tsx:154-185 takes the offline branch when status.online is false. That flag is radio && reachable (engine.ts:591), so it is a real no-signal van. The branch queues exactly the op the probe sends (queue.ts:240-274 useQueueReceipt, engine.ts:1498 toWireOp). The prober used an online delivery plus an offline receipt. My own probe went fully offline. One /sync/upload batch carried the ops in the order the app queues them: trip_stops PATCH started, trip_stops PATCH arrived, deliveries PUT on the planned row id with lines and proof inline, then receipts PUT. The batch returned accepted 4, rejected 0. The stop and the delivery ended 'delivered', and RCPT-0001 was written with trip_id. The collections table stayed empty. Preview: cashCollectedPaise 0, expectedCashPaise 30,000. Settling with the honest 97,200 answered 409 settlement_needs_owner, cashVariancePaise +67,200, for the accountant and for the manager. Both got the same approvalId, so no second approval row was filed. The owner with acceptVariance got 200 and the trip ended settled_with_variance.

WHERE I LOOKED FOR A GUARD, AND FOUND NONE:
- Only collections.service.ts:130 inserts into collections. No worker job and no outbox handler writes that row later.
- Nothing refuses the upload: DOS-166 permits receipts for the delivery role (receivables.module.ts:35-37), and the manifest marks receipts writable.
- Trip return does not check for receipts that have no collections row.
- In the whole frontend only the delivery app calls collections.record (collect.tsx:111). No owner, manager or admin screen can attach a collection.
- The driver is not warned. The only copy is 'd.offlineWrite' and 'd5.offlineNoNumber' (strings.ts:96, :273-274), and neither mentions check-in. queue.ts:233-235 claims the screen warns the driver; it does not.
- docs/23-app-screens-and-api-gaps.md:954 (§10 #6) records only that collections is not a pull table. It says nothing about the settlement or ledger effect, so this is not an intended behaviour.

BANKING MAKES IT PERMANENT (verified). Both settlement paths put the stranded receipt into the Day-end bank register (withCrew=false), and the desk can bank it with deposit 200, posted Dr BANK / Cr CASH (receivables.service.ts:929-930 never credits CASH_VAN).
- Honest crew, owner accepts, then the desk banks: CASH_VAN +67,200 stranded for good, CASH_SHORT -67,200 booked as a fake cash-over gain, BANK +67,200, CASH 0.
- Crew keeps the cash (settled green, variance 0, no approval): the register still lists RCPT-0002 as office cash and GET /receipts/:id says withCrew false. Banking it takes CASH to -67,200 while CASH_VAN stays +67,200.
The ledger is append-only and nothing corrects it later.

PRIORITY: P0, financial corruption under charter A.7. Normal offline doorstep collection leaves wrong balances for good (van cash overstated, fake income), and a crew that keeps the cash passes the day-end cash control green. The only repair is a manager API call that needs the receipt id; no screen offers it. The accountant gets 404 on that call, and the owner's accept path is exactly what books the fake gain. That is not a usable workaround, so P2 does not apply.
Skeptic corrections (binding on this record): 1. 'Nothing flagged / theft unnoticed' goes too far. The settlement flags nothing, but afterwards the kept receipt sits in the Day-end bank register as office cash (withCrew=false; receipts.get withCrew false). The desk can bank it: deposit 200 posts Dr BANK / Cr CASH and takes CASH to -67,200. The gap therefore shows up only as a drawer that comes up short at the bank, or a negative CASH balance, and nothing ties it to the crew or the trip, which is already closed green. Add to the impact: banking makes the corruption permanent. After the owner accepts and the desk banks, CASH_VAN +X and CASH_SHORT -X stay; in the kept-cash case CASH goes negative.
2. When the delivery is queued offline too, the case the app really produces with no signal, the settlement journal has NO CASH_VAN line at all: CASH +X / CASH_SHORT -X, because cashCollectedPaise is 0. The manager's settle also gets 409, not only the accountant's. The second refusal reuses the same pending approval (fileApproval dedupes it).
3. queue.ts:233-235 says 'The collect screen says so in one line'. It does not. strings.ts:96 'd.offlineWrite' and :273-274 'd5.offlineNoNumber' say only that the phone keeps the receipt and the office numbers it; neither mentions check-in or settlement, so the driver is not warned.
4. Link the filing to docs/23-app-screens-and-api-gaps.md:954 (§10 row #6). That row records the missing collections pull table but not its money effect. The suggested fix should say that publishing collections alone does not repair receipts already uploaded or older app builds, so the settlement has to read the trip's receipts.
5. File:line nits. collections.service.ts writes the collections row at :129-145 (.insert at :130), not :127-144. The deposit credits CASH, never CASH_VAN, at receivables.service.ts:929-930. Every other cited line checks out: settlement.service.ts:348-373, 360, 368, 446-449, 230-239; receivables.sync.ts:60-78 (tripId at :72); receivables.service.ts:280-283, 686; sync-tables.ts:94; collect.tsx:154-185; queue.ts:240-274.
6. The desk-app grep is confirmed and can be stated exactly: in the whole frontend the only caller of delivery.collections.record is frontend/delivery-app/app/stop/[id]/collect.tsx:111.
Status: NEW — confirmed on the API, not yet approved by the founder.

### DOS-170 — Reversing a trip cash receipt after its trip settled credits CASH_VAN again: van cash goes negative and office CASH stays overstated
Category: business-logic | Priority: P1 | Role: Accountant (money desk; owner and manager take the same MONEY_DESK route POST /receipts/{id}/reverse) | Platform: API (core spec on a test database)
User: Accountant (money desk; owner and manager take the same MONEY_DESK route POST /receipts/{id}/reverse)
Platform: API (core spec on a test database)
Environment: Worktree .claude/worktrees/b2-suspects, branch qa/b2-suspects at bcc7fda (the tip of main; git merge --ff-only main said "Already up to date"). Postgres 17 on 127.0.0.1:5439, database dos_test_b2_sus (a copy of dos_test_batch2b_template, migrated). Node 24, vitest 4.1.11, DATABASE_POOL_MAX=3. The spec harness src/testing/app.ts booted DeliveryModule, WarehouseModule, BillingModule, OrdersModule, InventoryModule, ReceivablesModule, FilesModule and SyncModule, so the trip-settled predicate from DeliveryModule.onModuleInit was registered, as it is on the real services.
Steps:
  1. Fixture: a fresh tenant with bootstrapTenant, one shop, one variant at Rs 10 a piece with 12% GST, and 1000 pieces of opening stock at the godown.
  2. Salesperson: POST /orders with 1 case, then POST /orders/{id}/submit. Warehouse: POST /warehouse/orders/{id}/pack. This issued INV/0001 for 13400 paise.
  3. Owner: POST /delivery/vehicles. Driver: POST /delivery/consents. Warehouse: POST /delivery/trips with one stop carrying INV/0001 and openingCashPaise 0, then POST /delivery/trips/{id}/start-loading. Driver: POST /delivery/trips/{id}/depart. The trip is now active.
  4. Driver: POST /delivery/stops/{id}/start, POST /delivery/stops/{id}/arrive, and POST /delivery/deliveries (full delivery, signature as proof of delivery).
  5. Driver: POST /delivery/collections with mode cash and 13400 paise. This created RCPT-0001 with Dr CASH_VAN 13400 / Cr AR 13400.
  6. Driver: POST /delivery/trips/{id}/return, so the trip is closing. Accountant: GET /delivery/trips/{id}/settlement read expected cash 13400. Accountant: POST /delivery/trips/{id}/settle with handedOverCashPaise 13400. Trip state became settled with no variance.
  7. Accountant: GET /receipts/{id} returned status collected, depositedAt null, withCrew false (the cash is in the office). Balances were read by SQL (sum of journal_lines per account) and by GET /receivables/accounts.
  8. Accountant: POST /receipts/{id}/reverse with reversalId and reason 'entered against the wrong shop'.
  9. Read the receipt_reversal journal lines and the CASH, CASH_VAN and AR balances again, by SQL and by GET /receivables/accounts.
Expected: The settlement already moved the trip's cash from CASH_VAN to CASH (Dr CASH 13400 / Cr CASH_VAN 13400). The reversal should take the money out of CASH, where it now is: Dr AR 13400 / Cr CASH 13400. Afterwards CASH_VAN should be 0, CASH should be 0 and AR 13400, and the trial balance should show no negative cash with the crews.
Actual: Book after collection: CASH_VAN 13400, CASH 0, AR 0. Settlement lines: CASH +13400, CASH_VAN -13400. Book after settle, by SQL and by GET /receivables/accounts: CASH 13400, CASH_VAN 0, AR 0. The reverse call returned 200 and created RCPT-0002 (cash, -13400, status cancelled); the original RCPT-0001 is now cancelled. Reversal lines: AR +13400, CASH_VAN -13400, and no CASH line. Book after undo, identical by SQL and by the desk's GET /receivables/accounts: AR 13400, CASH 13400, CASH_VAN -13400. The undo changed CASH_VAN by -13400 and CASH by 0. A second run on a new tenant (s75a-22028645) gave the same rows and balances as the first (s75a-21993993).
Business impact: Undoing a doorstep cash receipt after Day-end is routine: a receipt entered against the wrong shop, or cash handed back. Each time, the accountant's trial balance shows negative cash with the delivery crews (-Rs 134 here, which is physically impossible) and office cash Rs 134 higher than what is in the till or was refunded. At the next cash count the desk looks short by the reversed amount, and the accountant takes the blame. If the desk then records the corrected receipt at the office, CASH is debited again and the overstatement stays. Total cash (CASH + CASH_VAN) and AR are right, so no money is lost or paid twice. But the split between office cash and crew cash is wrong in an append-only journal, and it can only be fixed with a manual correcting entry. That is a major operational issue for the Day-end reconciliation, not a total-ledger corruption, hence P1.
Severity: P1
Evidence: QA/evidence/batch2/suspects/S-75a/ (probe spec and its run output); skeptic re-run: QA/evidence/batch2/suspects/S-75a/skeptic/ where present. Command: cd backend && pnpm --filter @dos/core exec vitest run src/modules/receivables/zz-probe-S-75a.spec.ts --silent=false --reporter=verbose. Result: Test Files 1 passed, Tests 1 passed. Key logged lines: "[S-75a] settlement journal lines: [{CASH 13400},{CASH_VAN -13400}]", "[S-75a] book after settle (SQL): {AR 0, BANK 0, CASH 13400, CASH_VAN 0}", "[S-75a] receipt before undo: {RCPT-0001, status collected, depositedAt null, withCrew false}", "[S-75a] reversal journal lines: [{AR 13400},{CASH_VAN -13400}]", "[S-75a] book after undo (SQL): {AR 13400, BANK 0, CASH 13400, CASH_VAN -13400}", "[S-75a] desk trial balance after undo (GET /receivables/accounts): {AR 13400, BANK 0, CASH 13400, CASH_VAN -13400}", "delta CASH_VAN (undo): -13400", "delta CASH (undo): 0". Read-only psql on dos_test_b2_sus before cleanup: for both probe tenants, the receipt_reversal entry has the lines AR 13400 and CASH_VAN -13400, and the balances are CASH 13400, CASH_VAN -13400, AR 13400. The receipts rows are RCPT-0001 (13400, cancelled, trip state settled, deposited_at null) and RCPT-0002 (-13400, cancelled). The probe spec's own assertions (CASH_VAN 0 and CASH 13400 after settle, a reversal CASH_VAN line of -13400 with no CASH line, CASH_VAN -13400 after undo) all passed. The run took 755 ms.
Root cause: backend/libs/core/src/modules/receivables/receivables.service.ts:1921-1923 in undoReceipt picks the source account as `original.depositedAt ? 'BANK' : receiptAccountCode(original.mode, original.tripId)`. receiptAccountCode (:280-283) returns 'CASH_VAN' for any cash receipt with a tripId, and undoReceipt never checks whether that trip has settled. The service already has that check: `this.tripSettled`, registered by DeliveryModule.onModuleInit (delivery/delivery.module.ts:79) and used by getReceipt at :791 and depositReceipts at :902-907. The settlement already posted Cr CASH_VAN for the trip's cash (delivery/settlement.service.ts:235-239), so the reversal credits CASH_VAN a second time. depositReceipts has the same situation and handles it correctly by passing a null tripId (:928-930, the DOS-132 fix); undoReceipt was not updated in that fix (git blame: lines 1920-1924 are unchanged since 8ea449a1, 2026-09-05).
Suggested fix: In undoReceipt, before building the lines, check the trip when the receipt is not deposited, has a tripId and is cash. Run the same predicate getReceipt uses (`select (${this.tripSettled(sql`${original.tripId}`, tenantId)}) as settled`) inside the same transaction. If the trip has settled, use receiptAccountCode(mode, null), which gives CASH; otherwise keep CASH_VAN. Keep the depositedAt → BANK branch first. To stop a reversal racing a settlement on the same trip, take the trip-settled read under the lock settle takes (read the trip row FOR SHARE, or through a delivery-supplied locking predicate) so the account choice and the settlement's Cr CASH_VAN cannot disagree. No contract or schema change is needed.
Test plan: Add a red spec in delivery.spec.ts, right after the DOS-132 settled-trip test, or in a receivables spec that boots DeliveryModule. Take a trip cash receipt through POST /delivery/collections, return the trip, settle it with the accountant (POST /delivery/trips/{id}/settle), then have the accountant call POST /receipts/{id}/reverse. Assert that the receipt_reversal entry for reversalId is exactly {AR: +amount, CASH: -amount} with no CASH_VAN line. Assert that CASH_VAN summed over the receipt, settlement and reversal entries is 0, and that GET /receivables/accounts shows CASH back at its pre-collection balance and CASH_VAN not negative. Add two controls: a cash receipt reversed while its trip is still active still credits CASH_VAN, and a settled trip receipt that was deposited and then reversed credits BANK. The spec must fail on bcc7fda (today the reversal line is CASH_VAN -amount) and pass with the fix.
NOT TESTED: Part (b) of S-75 (depositReceipts reads without FOR UPDATE, so two desks could bank the same receipt twice): out of scope for this probe.; Reversal of a trip cash receipt BEFORE the trip settles: not executed. From reading the code only, settlement figures (settlement.service.ts:348-360) sum collections rows regardless of receipt status, so a pre-settle reversal may be double-counted at settlement. Unverified.; Trip closed as settled_with_variance (owner acceptVariance): not executed. The predicate includes that state, so the same defect is expected but not proven.; Reversal of a settled trip receipt that was deposited first (BANK branch): not executed.; Owner and manager roles calling reverse: only the accountant was driven.; Any app screen or device (manager or owner app receipt panel, Android, iOS, web): not run. No UI result is claimed.; Integrations or Tally export of the resulting journal: not executed.
Source: architect suspect S-75 part (a); probe and skeptic run wf_309501e5-2f5 (2026-09-14). Skeptic: UPHELD (real user path: True; priority P1). I could not refute it. I re-ran the probe on current main (c679499; the only commit after bcc7fda changes QA/STATE.md, so the product code is the same) and got the same book: after settle CASH 13400 / CASH_VAN 0; the reversal lines are AR +13400 and CASH_VAN -13400 with no CASH line; after the undo AR 13400, CASH 13400, CASH_VAN -13400. My own read-only SQL on tenant s75a-22312210 shows the same rows.

Checks that the path is real:
(1) The spec harness sends real HTTP through app.inject with a signed bearer token, and both controllers carry @UseGuards(TenantGuard) (receivables.controller.ts:9, delivery.controller.ts:17). So the permission matrix was enforced: reverse and settle are MONEY_DESK (owner, manager, accountant; permissions.ts:52, :603, :733), and collections.record is MONEY_COLLECTORS (:746).
(2) The delivery app takes cash at the door through the same endpoint, api.api.delivery.collections.record (frontend/delivery-app/app/stop/[id]/collect.tsx:111).
(3) The desk sends the same body shape as the probe: {id, reversalId, reason, idempotencyKey} (manager-app/app/money/index.tsx:135-142, owner-app/app/money/receipts.tsx:107-114).
(4) No UI guard stops it. The Reverse button is disabled only when status is cancelled (manager index.tsx:353, owner receipts.tsx:249), so it is live for a collected receipt of a settled trip, which GET /receipts/{id} reports as withCrew false.
(5) Nothing corrects it later. ReceiptReversed has no consumer (it appears only in the worker's outbox-coverage.test.ts), no migration has a trigger or check on CASH_VAN (grep of the migrations found nothing), and undoReceipt never consults this.tripSettled.
(6) The state is normal business: cash stays in the office till between settle and banking.
(7) The DOS-132 architect verdict (QA/evidence/batch2/verdicts/DOS-132.md) deliberately left the reversal as it was ("the reversal half is filed below"), and it was never filed before S-75. That confirms the defect is known and unfixed, not intended.

Priority: P1 is right and consistent with DOS-132 (the same mis-posting class: CASH_VAN credited twice, CASH overstated), which was approved at P1. AR and total cash stay correct, and no money is lost or paid twice. The office-cash versus crew-cash split is wrong in the append-only journal, and the desk's trial balance shows negative crew cash. That is a major Day-end reconciliation problem, not a total-ledger corruption.

Scope of this result: a real-DB core spec only. No app screen or device was run (NOT TESTED). The delivery app's offline receipt path (queue.ts, which posts via sync with no collections row) is S-76 and was not exercised.
Skeptic corrections (binding on this record): 1. Environment: main has moved to c679499. The only change after bcc7fda is QA/STATE.md (git diff --stat HEAD main showed QA/STATE.md alone), so the product code is the same and the result holds on current main.
2. Steps 3 and 6, roles: POST /delivery/trips is TRIP_PLANNERS (permissions.ts:724) and settle is MONEY_DESK (:733). The probe's warehouse and accountant callers are allowed, and the calls went through TenantGuard (receivables.controller.ts:9, delivery.controller.ts:17). The spec does not bypass the guard.
3. Every file:line claim checks out: receivables.service.ts:1921-1923 (sourceCode), :280-283 (receiptAccountCode), :791-792 (getReceipt predicate), :902-907 and :928-930 (depositReceipts), delivery.module.ts:79, settlement.service.ts:235-239 and :348-360, git blame 8ea449a1 dated 2026-09-05.
4. Add to the root cause: the DOS-132 architect verdict (QA/evidence/batch2/verdicts/DOS-132.md, Decisions) explicitly kept the reversal unchanged ("the reversal half is filed below"). It was never filed until S-75.
5. Add these guards, all checked and absent: the Reverse button is disabled only for cancelled receipts (frontend/manager-app/app/money/index.tsx:353, frontend/owner-app/app/money/receipts.tsx:249); ReceiptReversed has no event consumer; no migration has a trigger on CASH_VAN.
6. Scope note for the suggested fix: the bounceCheque path shares undoReceipt but is unaffected, because a cheque maps to CHEQUES whether or not it has a tripId. Only cash with a tripId on a settled trip is wrong.
7. The delivery app's offline receipt path (frontend/delivery-app/src/lib/queue.ts, which enqueues a receipts PUT with no collections row) is a different path covered by S-76 and was not exercised here.
Status: NEW — confirmed on the API, not yet approved by the founder.

### DOS-171 — Van sale screen shows the pre-GST net as 'Sale total': a 12-piece sale at 12% GST reads Rs 221.40, but the bill it issues is Rs 248.00
Category: bug | Priority: P1 | Role: Delivery crew (delivery role: driver or helper) at the shop door | Platform: API (core spec on a test database, run through the bootTestApp harness as the delivery role) plus a read of the D6 screen source. The screen itself was NOT run on web, Android or iOS.
User: Delivery crew (delivery role: driver or helper) at the shop door
Platform: API (core spec on a test database, run through the bootTestApp harness as the delivery role) plus a read of the D6 screen source. The screen itself was NOT run on web, Android or iOS.
Environment: Worktree .claude/worktrees/b2-suspects at 199952b (same as main 199952b). Node 24, vitest 4.1.11, Postgres 17 on 127.0.0.1:5439, database dos_test_b2_sus (a copy of dos_test_batch2b_template, migrated). Harness modules: Delivery, Warehouse, Billing, Orders, Pricing, Inventory, Receivables, Files, Sync. The probe made its own tenant: seller in state 27; one shop in state 27 with a GSTIN; one variant on an HSN at 12% GST (gstBps 1200); default price list at 1845 paise a piece; 1000 pcs opening stock at the godown, 48 pcs loaded on the van.
Steps:
  Owner: POST /delivery/vehicles. Driver: POST /delivery/consents. Warehouse: POST /delivery/trips (vanSalesEnabled true, one stop for the shop), then POST /delivery/trips/:id/start-loading. Manager: POST /warehouse/load-sheets with 48 pcs of van stock, then POST /warehouse/load-sheets/:id/confirm. Driver: POST /delivery/trips/:id/depart. Result: trip active, vanSalesAllowed true.
  As the driver, POST /pricing/quote exactly as van-sale.tsx:150-157 sends it: {retailerId, lines:[{lineId: variantId, variantId, qtyPcs: 12}]}.
  Take totals.netPaise from the reply. van-sale.tsx:193 puts this value in <Money testID="d6-total"> under t('d6.total') = 'Sale total' (lines 215-217).
  As the driver, POST /delivery/van-sales exactly as van-sale.tsx:163-178 sends it: stopId, retailerId, invoiceId, deliveryId, deviceId, one line of 12 pieces, no collect.
  Read the invoice in the reply, then read-only SQL on invoices, sales_orders, retailer_outstanding_summary and outbox_events (VanSaleInvoiced).
  As the driver, POST /delivery/collections for cash with amountPaise set to the 'Sale total' figure, the amount the crew would ask for. Then re-read the shop's outstanding and the invoice state.
Expected: 'Sale total' shows what the van sale will bill and what the shop has to pay: pricing.quote totals.totalPaise = 24 800 paise (Rs 248.00), the same as the invoice total. The screen's own header comment promises this (van-sale.tsx:13-15: the price shown at the van door is the bill's price "to the paisa"). Collecting the amount shown pays the bill in full.
Actual: pricing.quote (200) returned totals {grossPaise 22140, discountPaise 0, bargainPaise 0, netPaise 22140, taxPaise 2657, roundOffPaise 3, totalPaise 24800}. Its line had lineNetPaise 22140, gstBps 1200, taxPaise 2657, lineTotalPaise 24797. The screen renders netPaise, so 'Sale total' reads Rs 221.40. The van sale (200) issued INV/0001, source van_sale: subtotal 22140, taxable 22140, CGST 1328, SGST 1328, IGST 0, round-off 4, total 24800. The order went to delivered with total_paise 24800, retailer_outstanding_summary.outstanding_paise became 24800 (1 open bill), and the outbox VanSaleInvoiced event carried totalPaise 24800. A cash collection of the shown 22140 (200, RCPT-0001) left outstanding_paise 2660 and the invoice in state partially_paid. The gap between the bill and 'Sale total' is 2660 paise (Rs 26.60): the 12% GST plus round-off.
Business impact: Every van sale of a taxable item shows the crew and the shopkeeper a figure that leaves out the GST (5-28% depending on the item) and the round-off. The crew asks for and takes that figure at the door. A sale meant to be settled at the door (cash or UPI) then leaves a due behind: here Rs 26.60 on a Rs 248 bill. The printed bill then disagrees with the price quoted at the door, which starts an argument with the shopkeeper. The accountant has to chase many small dues, and the crew's receipts come in short of the bills. With 20-40 van sales a day at 12-18% GST, that is thousands of rupees a day of credit nobody meant to give. The books stay correct: invoice, receivables and outstanding all carry the true Rs 248.00. So this is not financial corruption (P0), but it hits a normal daily workflow (P1).
Severity: P1
Evidence: QA/evidence/batch2/suspects/S-28/ (probe spec and its run output); skeptic re-run: QA/evidence/batch2/suspects/S-28/skeptic/ where present. 1) Screen source. `grep -n` on frontend/delivery-app/app/stop/[id]/van-sale.tsx at 199952b gave: `193:  const netPaise = quote.data?.totals.netPaise ?? null`, `215:              {t('d6.total')}`, `217:            <Money value={netPaise} size="moneyL" testID="d6-total" />`, `221:            label={t('d6.create')}`. `grep -n` on frontend/delivery-app/src/strings.ts gave: `283: 'd6.total': 'Sale total'`, `285: 'd6.collectNow': 'Take the money now'`, `286: 'd6.create': 'Bill it and hand it over'`. grep finds d6.collectNow only in strings.ts, so the screen never uses it. The button under 'Sale total' is 'Bill it and hand it over', so the row's 'Take the money now' wording is out of date, but the defect stands. Line 263 also shows each line's lineNetPaise (before GST). `git log -- van-sale.tsx` lists only 6a42c5a and 2f8a5a5, so no DOS-096 commit (7f7fc8b, merge 7d9ee4e) touched this screen. 2) Contract: backend/libs/contracts/src/pricing.ts:354-361 documents netPaise as 'Before GST.' and totalPaise as 'What the shop pays: netPaise + taxPaise + roundOffPaise'. 3) Executed probe: `pnpm --filter @dos/core exec vitest run src/modules/delivery/zz-probe-S-28.spec.ts --silent=false --reporter=verbose` gave 1 test passed. Its assertions (all passed): taxPaise > 0; netPaise < totalPaise; invoice.totalPaise === quote totalPaise; invoice.totalPaise !== netPaise. Output lines: `[S-28] pricing.quote totals: {\"grossPaise\":22140,...,\"netPaise\":22140,\"taxPaise\":2657,\"roundOffPaise\":3,\"totalPaise\":24800}`; `[S-28] screen \"Sale total\" (totals.netPaise): 22140`; `[S-28] van sale invoice: {\"invoiceNo\":\"INV/0001\",\"subtotalPaise\":22140,\"taxablePaise\":22140,\"cgstPaise\":1328,\"sgstPaise\":1328,\"igstPaise\":0,\"roundOffPaise\":4,\"totalPaise\":24800,...}`; `[S-28] SQL invoices row: {...\"source\":\"van_sale\",...\"total_paise\":\"24800\"}`; `[S-28] SQL sales_orders row: {\"state\":\"delivered\",\"total_paise\":\"24800\"}`; `[S-28] SQL retailer_outstanding_summary after sale: {\"outstanding_paise\":\"24800\",\"open_bills\":1}`; `[S-28] SQL outbox VanSaleInvoiced: {\"total_paise\":\"24800\"}`; `[S-28] collections.record: {\"amountPaise\":22140,\"receiptNo\":\"RCPT-0001\"}`; `[S-28] SQL retailer_outstanding_summary after collecting the shown figure: {\"outstanding_paise\":\"2660\",\"open_bills\":1}`; `[S-28] SQL invoice after collecting the shown figure: {\"total_paise\":\"24800\",\"state\":\"partially_paid\"}`; `[S-28] gap between the bill and \"Sale total\" (paise): 2660`. The full output is in probe-output.txt. Incidental, not investigated: the quote splits its tax as 2657 with round-off 3, while the bill has CGST+SGST 2656 with round-off 4. Both total 24800. Cleanup: probe copied to the scratchpad and deleted from the worktree; dos_test_b2_sus dropped (0 other connections), recreated from dos_test_batch2b_template and migrated ('migrations applied'; 0 's28-%' tenants left); `git status --short` is empty.
Root cause: frontend/delivery-app/app/stop/[id]/van-sale.tsx:193 reads `quote.data?.totals.netPaise`, which the contract documents as 'Before GST.' (backend/libs/contracts/src/pricing.ts:354-355). Line 217 renders it under 'Sale total' (strings.ts:283). Since DOS-096 the quote also returns totals.totalPaise (pricing.ts:360-361), computed at backend/libs/core/src/modules/pricing/quote.service.ts:158,166 with the same roundToRupee the bill uses (backend/libs/core/src/modules/billing/invoices.service.ts:1555-1559). The van sale bills through BillingService.issueFromLocation (backend/libs/core/src/modules/delivery/vansales.service.ts:97-104), whose total includes GST and round-off; that total also caps the doorstep allocation (:192) and goes into VanSaleInvoiced (:207). DOS-096 never updated this screen (last changed in 6a42c5a), so it still shows the pre-GST field. Line 263 does the same per line with lineNetPaise.
Suggested fix: In van-sale.tsx, render `quote.data?.totals.totalPaise` under 'Sale total' instead of totals.netPaise (line 193, and rename the variable). Optionally add a small line under it showing GST (totals.taxPaise). For the lines, show `quoted?.lineTotalPaise`, or keep lineNetPaise with an 'excl. GST' label, so the lines and the total agree. No backend change is needed: the quote already returns the payable figure the bill uses.
Test plan: Red test first. The frontend `pnpm test` covers only the kit, client and offline specs, not app screens. So move the figure choice into a pure helper in frontend/delivery-app/src/lib (for example `saleTotalPaise(quote)`) and add a vitest. Given a quote with totals {netPaise 22140, taxPaise 2657, roundOffPaise 3, totalPaise 24800}, the helper must return 24800; it returns 22140 today. Backend guard: in backend/libs/core/src/modules/delivery/delivery.spec.ts, for a GST-bearing van sale, assert that the invoice totalPaise equals the pricing.quote totals.totalPaise the delivery role got for the same lines, and differs from netPaise. Then walk the delivery app on web and on the Pixel 7: build a 12-piece van sale on a 12% item and check that the d6-total text equals the issued bill's total (Rs 248.00).
NOT TESTED: The D6 screen on web, Android (Pixel 7) or iOS. Dev servers and emulators were not allowed, so the 'Sale total' text is read from the source, not seen on a screen.; The collect screen's expected amount after a van sale (collect.tsx:100 uses planned_collection_paise, else outstanding_paise), on any device.; Inter-state (IGST) shops, items with cess, schemes, free goods, bargains, and sales with several lines.; The van-sales `collect` path at the door (the screen never sends it).; How the short collection shows up in trip settlement and cash variance at day end.; The 1-paisa split difference between the quote's tax/round-off (2657/3) and the bill's CGST+SGST/round-off (2656/4); seen in passing, not investigated.
Source: architect suspect S-28; probe and skeptic run wf_309501e5-2f5 (2026-09-14). Skeptic: UPHELD (real user path: True; priority P1). I could not refute it. It reproduces on the calls the real app makes, and nothing downstream catches it.

1) Screen source at 199952b. van-sale.tsx:193 is `const netPaise = quote.data?.totals.netPaise ?? null`. Line 215 is t('d6.total') ('Sale total', strings.ts:283). Line 217 is `<Money value={netPaise} size="moneyL" testID="d6-total" />`. Line 263 shows each line's lineNetPaise. Nothing on D6 labels the figure as before GST. The panel meta only says 'Priced by the office rules' (strings.ts:284). The header comment at lines 13-15 promises the shown figure is the bill's figure 'to the paisa'. `git log` on the file lists only 6a42c5a and 2f8a5a5. The DOS-096 commits (7f7fc8b, f41bb3a, 7d9ee4e, b9485c8) never touched it.

2) Same path as the app. The probe's /pricing/quote body matches van-sale.tsx:150-157. Its /delivery/van-sales body matches lines 163-178 (tripId, stopId, retailerId, invoiceId, deliveryId, deviceId, piece lines, no collect). It ran as the delivery role. The harness `call()` (testing/app.ts:157-170) sends a signed Bearer token through app.inject, so TenantGuard and PERMISSIONS apply. The live delivery service serves this call: definitions.ts puts PricingModule and the 'pricing' contract key in deliveryServiceDefinition (roles ['delivery'], port 3005), and permissions.ts:516 has 'pricing.quote': ANY_MEMBER. The setup is one the product allows: trip with van sales on, confirmed van stock, departed.
- One difference: the idempotency key is a string, where the app sends a UUIDv7. It does not affect any amount.

3) My re-run (output in skeptic/rerun-probe-output.txt): 1 test passed, exit 0.
- Quote totals: netPaise 22140, taxPaise 2657, roundOffPaise 3, totalPaise 24800.
- Van sale: INV/0001, source van_sale, taxable 22140, CGST 1328, SGST 1328, round-off 4, total 24800.
- The order and the VanSaleInvoiced event both carry 24800. Outstanding after the sale was 24800.
- Collecting the shown 22140 left outstanding at 2660 and the invoice partially_paid.
- The SQL read-back (skeptic/rerun-sql-readback.txt) confirms the invoice at 24800 partially_paid and outstanding 2660 / 1 bill.

4) No guard elsewhere. The books stay correct, so this is not P0, but nothing corrects the figure the crew sees. The workaround is weaker than it looks:
- D6 records no money; D5 does. D5's headline amount is `planned_collection_paise ?? outstanding_paise` (collect.tsx:100). This stop has planned_collection_paise = 0, not null: trips.service.ts:1139-1140 sums an empty invoiceIds list to 0, and a stop the van sale adds itself is also written with 0 (vansales.service.ts:256). So D5 would read 'Owed on the bills here' Rs 0.00, not Rs 248.00 (source read and SQL; not seen on a device).
- The correct figures appear only in D5's bills list and the 'Owes' chip. Both come from the phone's offline copy, which refreshes on a 60 s timer by default (offline engine.ts:114), and D6's refresh list ['trip','van-stock','settlement'] does not touch that copy.
- The product has already named this exact risk. sales-app orders/new.tsx:366-372 labels its net figure 'before GST' because showing the net as the total is what a rep would read out across the counter.

5) Priority P1 holds. DOS-096, the same net-shown-as-total defect on the retailer order screen, was rated business-logic P1 (findings/06-walkthrough-retailer.md:79). Books are correct, so not P0. It applies to every van sale of a taxable item, at the moment money changes hands, and the one fallback figure on D5 reads 0 for this stop.
- The prober's 'thousands of rupees a day' and 'the crew takes that figure' are extrapolations, not measured. The screen walk on web, Android and iOS is NOT TESTED.
Skeptic corrections (binding on this record): 1) The suspect's wording says 'above Take the money now'. That is wrong: 'd6.collectNow' exists only in strings.ts:285, and the button under 'Sale total' is 'Bill it and hand it over' (van-sale.tsx:221, strings.ts:286). D6 records no collection; money is taken on D5 (collect.tsx).

2) Contract lines: 'Before GST.' is the comment on pricing.ts:354, with netPaise on 355; 'What the shop pays' is 360, with totalPaise on 361. Totals run 353-362, not 354-361.

3) Step 4 of the prober's list leaves out tripId, but the probe and the app both send it (van-sale.tsx:166).

4) Business impact needs one addition and one softening.
- Addition: D5's headline 'Owed on the bills here' reads planned_collection_paise, which is 0 for a van-sale stop planned with no bills (trips.service.ts:1139-1140) and for a stop the van sale adds itself (vansales.service.ts:256). The `??` at collect.tsx:100 does not fall through on 0, so D5 would not show Rs 248.00 there either. Only the bills list and the 'Owes' chip, both from the offline copy on a 60 s pull (engine.ts:114), would show it. This is a source read, NOT TESTED on a device.
- Softening: 'thousands of rupees a day' and 'the crew asks for and takes that figure' are extrapolations, not measured.

5) Same-pattern context for the fix. The sales app shows the net and labels it 'before GST' (sales-app orders/new.tsx:364-375, strings 's3.beforeGst'). The retailer screen shows before GST, GST and total (retailer-app order.tsx:554-559). D6 has neither.

6) Priority calibration: DOS-096, the same defect on the retailer screen, is P1 business-logic (QA/findings/06-walkthrough-retailer.md:79). The category could be business-logic rather than bug to match it.

7) The 1-paisa difference in how tax is split into CGST, SGST and round-off stands as observed and is out of scope: the quote gives taxPaise 2657 with round-off 3, the bill gives CGST+SGST 2656 with round-off 4, and both total 24800.

8) Every other file:line citation checked is correct: van-sale.tsx 13-15, 150-157, 163-178, 193, 215, 217, 221, 263; strings.ts 283-286; quote.service.ts 158 and 166; invoices.service.ts 1555-1559; vansales.service.ts 97, 192, 207.
Status: NEW — confirmed on the API, not yet approved by the founder.

### DOS-172 — A bill that comes back undelivered after its load sheet was confirmed can never go on another load sheet (409 'already on load sheet'). W7 hides it, and the next trip carries it off with no count, no challan and no e-way bill check
Category: business-logic | Priority: P1 | Role: Warehouse (godown loader building the W7 load sheet); the Manager desk gets the same refusal | Platform: API (core spec on a test database: vitest + Nest/Fastify spec harness src/testing/app.ts, same endpoints and roles the apps use)
User: Warehouse (godown loader building the W7 load sheet); the Manager desk gets the same refusal
Platform: API (core spec on a test database: vitest + Nest/Fastify spec harness src/testing/app.ts, same endpoints and roles the apps use)
Environment: Worktree .claude/worktrees/b2-suspects, branch qa/b2-suspects at 199952b (main after every batch-2 merge). Node 24, Postgres 17 on 127.0.0.1:5439, database dos_test_b2_sus (a copy of dos_test_batch2b_template, 48/48 migrations), libs built (turbo FULL TURBO cache hit). Probe tenant 01a09c27-5a3a-7157-bda8-3fa640d0a6cb, run on 2026-09-14. The probe lowered its own tenant's ewb_intra_state_threshold to 26800 paise (= bill R + bill F).
Steps:
  1. The rep places and submits SO-0001 (bill R, shop A, 13400 paise) and SO-0002 (bill F, shop B, 13400 paise). The warehouse packs both through POST /warehouse/orders/{id}/pack.
  2. Manager: POST /delivery/trips creates trip 1 (TRIP-0001) with one stop for bill R. Warehouse: POST /delivery/trips/{trip1}/start-loading.
  3. Warehouse: POST /warehouse/load-sheets {tripId: trip1, orderIds:[R]} returns 200 draft. Manager: POST .../approve returns 200. Warehouse: POST .../confirm {countedPackages:1} returns 200 confirmed, challan DC-0001, dispatched [R].
  4. Delivery: POST /delivery/trips/{trip1}/depart returns 200. Then start, arrive and POST /delivery/stops/{stop}/fail {failureReason:'shop_closed'} return 200, and order R is back to packed.
  5. Delivery: POST /delivery/trips/{trip1}/return returns 200 (closing). Accountant: POST /delivery/trips/{trip1}/settle {handedOverCashPaise:0, counted:[]} returns 200 settled.
  6. Warehouse: GET /warehouse/packs?status=awaiting_load (W7 'Packed orders'). Warehouse: GET /delivery/trip-planning (W10/M7 board).
  7. Warehouse: POST /delivery/trips creates trip 2 (TRIP-0002) with stops for R and F, then POST start-loading.
  8. Warehouse: POST /warehouse/load-sheets {tripId: trip2, orderIds:[R, F]}. Then warehouse with [R], manager with [R], and manager POST /warehouse/load-sheets/{sheet1}/cancel.
  9. Warehouse: POST /warehouse/load-sheets {tripId: trip2, orderIds:[F]}, then approve (manager) and confirm (warehouse) with no ewbNo.
  10. Delivery: POST /delivery/trips/{trip2}/depart. Read load_sheets, delivery_challans, deliveries and order_state_transitions.
Expected: Once the failed stop has sent bill R back to packed and trip 1 is settled, three things should hold. W7's 'Packed orders' (packs.list status=awaiting_load) offers R. POST /warehouse/load-sheets for trip 2 accepts R together with F (200). The re-attempt then goes through the manager's approval, the blind package count, a delivery challan listing both bills, and the e-way bill check on the full load value (R+F = 26800 paise, which reaches the threshold, so ewbRequired = true). Only a DRAFT sheet that already carries R should be refused.
Actual: Step 4: the stop failed (200, shop_closed) and order R went dispatched -> packed. Step 5: trip 1 settled (200, hasVariance false) and R stayed packed. Step 6: W7 awaiting_load returned 200 with offersR=false (1 row, F only), but the planning board returned 200 with offersR=true. Step 7: trip 2 was created with R and F (200). Step 8: POST /warehouse/load-sheets returned 409 'order(s) already on load sheet 01a09c27-5d71-7216-afcf-c2b9b348543c' for warehouse [R, F], for warehouse [R] and for manager [R]. Cancelling that old sheet returned 409 'load sheet … is confirmed; the goods have left with challan DC-0001 and come back through delivery, not here'. Step 9: the only sheet the godown could build held F alone: loadValuePaise 13400, ewbRequired false, expectedPackages 1. It confirmed with no e-way bill number as DC-0002 (value_paise 13400, 1 line), dispatched [F]. Step 10: depart trip 2 returned 200 with loadSheetIds [sheet2 only], and R moved packed -> dispatched with transition reason 'trip depart' (trips.depart dispatchIfPacked). Rows: load_sheets lists R only on sheet1 (confirmed, trip1, DC-0001). delivery_challans DC-0002 has 1 line worth 13400 paise, while the van carries 26800 paise, which equals the threshold. order_state_transitions for R: dispatch(null) > return_undelivered(shop_closed) > dispatch(trip depart). The onALiveSheet query for R still returns sheet1 (confirmed). The seeded template copy already holds four bills in this state: tarsun SO-0845 and SO-0850 (DC-0080), kalyan-agencies SO-0169 (DC-0037), sai-distributors SO-0435 (DC-0050), each 'dispatch > return_undelivered(Stop failed; goods back on the dock)'.
Business impact: Shops are routinely closed, short of cash or refusing stock, and every such bill that went out on a confirmed load sheet is affected. When it comes back, the godown cannot load it again from W7: it drops out of 'Packed orders' for good, even though the planning board still offers it. The re-attempt goes out with no manager load-out approval, no blind package count and no challan line. It leaves only because trips.depart dispatches it without saying so. The van's paperwork then understates the load. In the run, the challan and sheet declared ₹134 and ewbRequired=false while ₹268 of bills were on the van, which equals the e-way bill threshold. A real re-delivery that pushes a load to or past the ₹1 lakh default would leave without the e-way bill number being asked for, which risks the vehicle being detained and a GST penalty. A carton short on the re-attempt is also never counted at the dock. In the demo data, 4 bills across 3 distributors are already stuck like this.
Severity: P1
Evidence: QA/evidence/batch2/suspects/S-03/ (probe spec and its run output); skeptic re-run: QA/evidence/batch2/suspects/S-03/skeptic/ where present. Probe spec run: pnpm exec vitest run src/modules/delivery/zz-probe-S-03.spec.ts --reporter=verbose passed (1 test, 976 ms). Its step-by-step log with the replies and rows is in scratchpad/suspects/S-03/probe-log.txt, and the vitest output in probe-output.txt. Key lines: '14. POST /warehouse/load-sheets (warehouse) trip2 [R, F]: {"status":409,…"message":"order(s) already on load sheet 01a09c27-5d71-7216-afcf-c2b9b348543c"}'; '16. … (manager) trip2 [R]: {"status":409,…}'; '17. POST cancel sheet1 (manager): {"status":409,…"is confirmed; the goods have left with challan DC-0001…"}'; '10. GET /warehouse/packs?status=awaiting_load (W7): {"offersR":false,"offersF":true,"count":1}'; '11. GET /delivery/trip-planning: {"offersR":true,"offersF":true}'; '18. … trip2 [F]: {"loadValuePaise":13400,"ewbRequired":false}' against threshold 26800; '20. POST depart trip2: {"status":200,"loadSheetIds":["01a09c27-5ec0-…"]}'; 'order_state_transitions for order R: … return_undelivered reason shop_closed … dispatch reason "trip depart"'. Read-only SQL on dos_test_b2_sus before the reset (sql-template-packed-on-live-sheet.txt) found the 4 seeded bills listed under Actual. The existing spec backend/libs/core/src/modules/warehouse/warehouse.spec.ts:2002-2049 asserts this 409 for a returned bill as the intended result.
Root cause: backend/libs/core/src/modules/warehouse/load-sheets.service.ts:795-807. onALiveSheet treats every sheet with status <> 'cancelled' as holding its orders, including a CONFIRMED sheet whose order was later sent back by return_undelivered (dispatched -> packed). create refuses on it at :185-189. The same rule is duplicated as onLiveLoadSheet in packing.service.ts:388-391, used at :183-188, so W7's awaiting_load hides the bill. Nothing releases the order from its confirmed sheet: trips.service.ts:1052-1059 (failStopInTx, shared by stops.fail and trips.return) and deliveries.service.ts:166-176 (a refused-at-door delivery) only move the order state, and load-sheets.service.ts:489-501 refuses to cancel a confirmed sheet. The only way out is trips.service.ts:484-508 with dispatchIfPacked at :983-997, which dispatches any packed bill planned on the trip with no sheet, count or challan. The planning board (trips.service.ts:388-394, plannedOnOpenTrips in delivery.internals.ts:385-409) still offers the bill, because the failed delivery has an outcome and trip 1 is settled.
Suggested fix: Only a DRAFT sheet should block a packed order. loadSheets.confirm dispatches every order on the sheet in the same transaction (load-sheets.service.ts:428-438), so a packed order listed on a confirmed sheet can only be one that came back undelivered. Change onALiveSheet (load-sheets.service.ts:801) from `ls.status <> 'cancelled'` to `ls.status = 'draft'`; create has already checked every order is packed at :165-171. Make the same change in onLiveLoadSheet (packing.service.ts:390), which already sits alongside orderInState 'packed'. Update both doc comments and flip warehouse.spec.ts step (e): the returned bill is offered and accepted. A draft sheet still returns 409, and confirmed sheets stay untouched as the record of the first load-out.
Test plan: Red test (delivery.spec.ts, or warehouse.spec.ts with DeliveryModule booted), mirroring the probe. Pack R and F. Trip 1: sheet [R], approve, confirm, depart, stop fail shop_closed, return, settle. Then assert: (1) GET /warehouse/packs?status=awaiting_load offers R. (2) With the tenant's EWB threshold set to R+F, POST /warehouse/load-sheets {tripId: trip2, orderIds:[R, F]} returns 200, loadValuePaise = R+F, ewbRequired = true, expectedPackages = 2. (3) Confirm without ewbNo returns 400 ewb_required. Confirm with ewbNo returns 200, the challan has 2 lines worth R+F, and dispatched = [R, F]. (4) Depart trip 2 returns 200, and R's latest order_state_transitions row is dispatch with reason null (from the sheet), not 'trip depart'. (5) A second draft sheet carrying R still returns 409 'already on load sheet'. Also flip warehouse.spec.ts:2002-2049 (e) from 409 to acceptance, keep the oracle query tied to the new draft-only rule, and run the DOS-043 depart gate and DOS-133 list tests again.
NOT TESTED: The W7 Load screen, W10 and M7 planning screens on web, Android or iOS (this was API-only; no device or browser was run); A refused-at-door delivery (POST /delivery/deliveries with every line zero, deliveries.service.ts:166-176) on a sheet-loaded bill: only the code was read; trips.return failing a still-open stop on a sheet-loaded bill: only the code was read; the probe failed the stop explicitly before returning; The offline /sync/upload variants of stops.fail and loadSheets.create; The e-way bill gate at the real default threshold (10,000,000 paise); the probe lowered the threshold on its own tenant to 26800 paise; dos and dos_qa databases (off-limits); the seeded rows were checked only on the dos_test_b2_sus template copy
Source: architect suspect S-03; probe and skeptic run wf_309501e5-2f5 (2026-09-14). Skeptic: UPHELD (real user path: True; priority P1). I could not refute it. The defect reproduces through the same endpoints, roles and order of steps the apps use, and also through the paths the delivery app really takes.

Re-run: the prober's spec, copied back unchanged except for the log path, passed. Every observation matched: stop fail sends R back to packed; W7 awaiting_load leaves R out (offersR=false); the planning board offers R; loadSheets.create answers 409 'already on load sheet' for warehouse [R,F], warehouse [R] and manager [R]; cancelling the confirmed sheet answers 409; sheet2 holds F only (13400 paise, ewbRequired false); trip 2 departs 200 and R goes packed -> dispatched with reason 'trip depart'. The onALiveSheet query still returns sheet1 (confirmed).

Extra refutation run: I wrote a variants spec for the three doorstep paths the prober only read. (1) The delivery app's offline stop failure: /sync/upload trip_stops PATCH started/arrived/failed, the shape in frontend/delivery-app/src/lib/local.ts. (2) Refused at the door: /sync/upload deliveries PUT with 0 delivered. (3) trips.return with the stop still open. All 6 sync ops were accepted, none rejected. All three bills went back to packed and both trips settled. W7 awaiting_load returned count 0, while the manager's planning board offered all three. POST /warehouse/load-sheets for each bill on trip 2 answered 409 'already on load sheet'. Trip 2 then departed 200 with loadSheetIds [] (no sheet at all), and every bill was dispatched with reason 'trip depart'.

No guard prevents this in real use:
- W7 (warehouse-app/app/load/index.tsx:95,107-113) only narrows the awaiting_load rows, so it can never offer the bill.
- No worker job touches load_sheets. A grep of backend/worker/src finds these event names only in outbox-coverage.test.ts.
- order_ids has no trigger. The only load_sheets trigger is the approval guard (0013:63).
- trips.depart blocks only on DRAFT sheets (trips.service.ts:494-499) and has no e-way bill check.

Seed check (read-only, dos_test_b2_sus): 4 packed bills sit on confirmed sheets after dispatch > return_undelivered, with 0 open plans: kalyan-agencies SO-0169 (DC-0037), sai-distributors SO-0435 (DC-0050), tarsun SO-0845 and SO-0850 (DC-0080).

Priority stays P1.
- Not P0: nothing is corrupted. Packed pieces left the godown at pack (DOS-039), the invoice and receivable are unchanged, and the bill is still delivered.
- Not P2: no path in the product puts a returned bill back under the load-out controls the founder decided (manager approval 2026-09-05, counted load-out DOS-043, 'a load sheet is built for a trip, from the bills on it' 2026-09-13), and it happens on every failed delivery, which is routine.
- The only partial workaround is setting an e-way bill number by hand on the invoice (billing.invoices.setEwayBill, permissions.ts:646). Nothing prompts for it, and there is no workaround for the count or the approval.

Not tested: I ran no screen. W7, W10, M7 and D3 on web, Android and iOS are NOT TESTED; this was API only.
Skeptic corrections (binding on this record): 1) File:line references. Every one the prober cited checks out on 199952b: load-sheets.service.ts:795-807, :185-189, :165-171, :428-438, :489-501; packing.service.ts:388-391 and :183-188; trips.service.ts:484-508, :983-997, :1052-1059, :388-394; delivery.internals.ts:385-409; deliveries.service.ts:166-176; warehouse.spec.ts:2002-2049. The environment also checks out: 48 migrations in dos_test_b2_sus. Small precisions:
   - packing.service.ts:183-185 is the orderInState half; the sheet anti-join is at :186-188.
   - The stop-fail entry points are stops.fail and the sync handler (delivery.sync.ts:100-118 -> trips.failStopFromInput :889 -> failStopInTx :900), plus trips.return (:553).

2) notTested items the prober listed, now TESTED at the API:
   - The offline /sync/upload stop failure (the delivery app's real path: frontend/delivery-app/app/stop/[id]/index.tsx:114-127, src/lib/local.ts).
   - Refused at the door (a deliveries PUT with 0 delivered).
   - trips.return failing a stop still open.
   All three reproduce the defect.
   - An offline loadSheets.create is not a real path: W7 calls it online (warehouse-app/app/load/index.tsx:120).

3) Worse than reported: a trip made only of re-attempts departs with no load sheet at all (loadSheetIds []). depart blocks only drafts (trips.service.ts:494-499; see the comment at :462-463), and trips.service.ts contains no e-way bill check. So the worst case is no paperwork at all, not only an understated challan. The one manual escape for the e-way bill is billing.invoices.setEwayBill (permissions.ts:646, BILLING_ISSUERS). Nothing prompts for it.

4) The 409 in warehouse.spec.ts:2002-2049 (step e) is not a product decision. It pins list/create parity, and it reaches return_undelivered through a direct applyFulfilmentEvent call, not a delivery endpoint. The DOS-133 plan notes (QA/evidence/batch2/plans/DOS-133.json 'Residuals … (a)') and verdict (QA/evidence/batch2/verdicts/DOS-133.md:39) already recorded 'a bill returned undelivered can never be loaded again' as an unfixed P1. The code comments at load-sheets.service.ts:790-793 and packing.service.ts:383-386 name exactly this change. It is already logged as S-03 and S-82 in QA/findings/12-batch2-new-findings.md:99 and :183. Merge S-82 into this finding, since they are the same defect.

5) Product intent needs one call before the fix. docs/22 §4 D5 (line 128) says 'Failed: reason, stock stays on vehicle', and deliveries.service.ts:179-181 says the pieces stay on the van for the next attempt. But the seed records 'goods back on the dock' (seed-demo/sales.ts:2847), no stock ledger row holds packed goods on the vehicle (DOS-039), and the planning board lets any vehicle take the re-attempt. This does not refute the defect: the bill still departs with no paper and no count. It does mean the suggested draft-only rule is a behaviour change. The architect or founder should confirm that a re-attempt rides on a new sheet for its trip (as the 2026-09-13 decision 'a load sheet is built for a trip, from the bills on it' implies) before it is built.

6) Suggested fix check (grep only). Only two predicates in core modules read load_sheets.order_ids by order: load-sheets.service.ts:800-801 and packing.service.ts:390. A draft-only rule breaks no other order-to-sheet lookup that grep found. The owner-connection oracle in warehouse.spec.ts:2052-2065 (`ls.status <> 'cancelled'`) must change together with step (e).

7) Minor. The probe's idempotency keys are free strings, not the app's UUIDv7 keys. The server keys any string the same way, so the path is unchanged. The probe's runtime in my run was 1120 ms, not 976 ms.
Status: NEW — confirmed on the API, not yet approved by the founder.

### DOS-173 — Manager billing desk counts a pack parked without a bill twice in 'left to bill': it is in both the billing queue and the unbilled-packs list
Category: bug | Priority: P3 | Role: manager (the accountant, who shares the screen, gets the same replies and the same figure) | Platform: API (core spec on a test database) plus the screen's own count helpers run over the replies. The billing screen itself was not opened in a browser or on a device.
User: manager (the accountant, who shares the screen, gets the same replies and the same figure)
Platform: API (core spec on a test database) plus the screen's own count helpers run over the replies. The billing screen itself was not opened in a browser or on a device.
Environment: Worktree .claude/worktrees/b2-suspects on branch qa/b2-suspects at 199952b (same as main). Postgres 17 on 127.0.0.1:5439, database dos_test_b2_sus (a copy of dos_test_batch2b_template, migrated). Node 24, vitest 4.1.11 through `pnpm --filter @dos/core exec vitest run src/modules/billing/zz-probe-S-106.spec.ts --silent=false`, DATABASE_POOL_MAX=3. A new tenant for each run, set up with the real endpoints and the salesperson, warehouse, manager and accountant roles. The database was dropped and recreated from the template afterwards.
Steps:
  1. As a salesperson, POST /orders (1 case) and POST /orders/{id}/submit. This is order P, SO-0001.
  2. As warehouse, POST /warehouse/orders/{P}/pack {packages:1, issueInvoice:false}. This is the warehouse app's 'Pack without a bill' button (frontend/warehouse-app/app/pack/[orderId].tsx:317). The reply has invoice null. In SQL, P is 'packed' and its pack_confirmations.invoice_id is null.
  3. Control: order N (SO-0002) is packed the normal way, with issueInvoice true, and gets an issued bill.
  4. As manager, and again as accountant, make the two reads the billing desk makes (index.tsx:107-110): GET /billing/queue {limit:200} (billing.invoices.queue) and GET /warehouse/packs {invoiced:false, limit:50} (warehouse.packs.list).
  5. Run the screen's own count over the two replies: remaining = addCounts(pagedCount(queue), pagedCount(unbilledPacks)) (index.tsx:182), shown as 'm6.remaining' = '{count} left to bill' (index.tsx:290). The probe copies pagedCount, countText and addCounts from ui.tsx:626-644 and first checks that the source files still contain those exact lines.
  6. Order C (SO-0003) is packed and billed, then the manager cancels the bill with POST /invoices/{id}/cancel. Read the desk again.
  7. Bill P once from the packs panel with POST /warehouse/packs/{packId}/invoice. Read the desk again. Then try billing P a second time the way a queue row does: look up the pack with GET /warehouse/packs {orderId:P, limit:1}, then POST the invoice.
  8. Bill C the queue-row way and read the desk again.
Expected: 'Left to bill' should count each order that still needs a bill once. With only the parked order P waiting, the header should say '1 left to bill'. With P and the cancelled-bill order C, it should say '2 left to bill'.
Actual: READ 1, with parked P and billed N:
- GET /billing/queue returned 200 with items [SO-0001, state packed, orderId 01a09c39-1bba…] and nextCursor null.
- GET /warehouse/packs?invoiced=false returned 200 with items [pack 01a09c39-1c7b… for orderId 01a09c39-1bba…, SO-0001, invoiceId null] and nextCursor null.
- So the same order is in both replies. pagedCount is 1 for each list, addCounts gives 2, and the header reads '2 left to bill'.
- Distinct orders across both replies: 1. The SQL count of orders needing a bill: 1. The accountant got the same replies and '2 left to bill'.
- The queue row's lookup (GET /warehouse/packs {orderId:P}) returns pack 01a09c39-1c7b…, the same pack the panel row bills.

READ 2, after C's bill was cancelled:
- The queue returned [SO-0001, SO-0003]; the packs list returned [SO-0001].
- The header reads '3 left to bill'. Distinct orders: 2. SQL: 2.

READ 3, after one bill for P (INV/0003, issued):
- The queue returned [SO-0003]; the packs list was empty.
- The header reads '1 left to bill'. One bill took the header down by 2.
- The second attempt on P through the queue-row path returned 409 already_invoiced ('pack … is already billed as INV/0003'). P still has exactly 1 live bill.

READ 4, after C was billed: the header reads '0 left to bill' and SQL says 0.
Business impact: At the billing desk, every pack the warehouse sends out with 'Pack without a bill' is counted as two bills still to issue. The same order (for example SO-0001 for one shop) also shows in both the 'Billing desk' register and the 'Packed, not billed' panel. With one parked pack the header says 2 when 1 bill is left; with a parked pack and a cancelled bill it says 3 when 2 are left. The screen code itself calls this header the one number the desk is paced by, so a manager judging how much billing remains before the vans load gets an inflated backlog.

No document, stock or money figure is wrong:
- Billing the pack once clears it from both lists.
- A second bill from the duplicate row was refused with 409 already_invoiced, and only 1 live bill existed.
- The queue's rupee total sums queue rows only.
- The header reaches 0 once everything is billed, and each panel shows its own correct count.

Hence P3: the count is wrong, but the rows and panel counts give a workaround.
Severity: P3
Evidence: QA/evidence/batch2/suspects/S-106/ (probe spec and its run output); skeptic re-run: QA/evidence/batch2/suspects/S-106/skeptic/ where present. Final run output (probe-output.txt): 'Test Files 1 passed (1) / Tests 1 passed (1)', exit 0. The probe's assertions match the numbers observed. Lines from probe-log.txt:
- `[S-106] READ 1 (parked P + billed N) [manager] screen arithmetic: {"pagedCountQueue":{"count":1,"more":false},"pagedCountPacks":{"count":1,"more":false},"remaining":{"count":2,"more":false},"header":"2 left to bill",...,"ordersInBothLists":["01a09c39-1bba-704c-9572-42a5e0e76ca9"],"distinctOrdersAcrossBothReplies":1,"sqlOrdersNeedingABill":1}`
- The two reads for READ 1: GET /billing/queue items [{orderNo SO-0001, state packed}] and GET /warehouse/packs {invoiced:false} items [{packId 01a09c39-1c7b-7033-95b8-c49cdd647bfe, orderNo SO-0001, invoiceId null}], both with nextCursor null.
- `openQueueRow lookup ... {"packId":"01a09c39-1c7b-…","panelRowPackId":"01a09c39-1c7b-…"}`
- `SUMMARY: [{read 1, header "2 left to bill", distinctOrders 1, sqlTruth 1, accountantHeader "2 left to bill"}, {read 2, "3 left to bill", 2, 2}, {read 3, "1 left to bill", 1, 1}, {read 4, "0 left to bill", 0, 0}]`
- `issueForPack P again via the queue-row path: {"status":409,"code":"already_invoiced","message":"pack 01a09c39-1c7b-… is already billed as INV/0003","liveBillsForP":1}`

The SQL read-back after step 3: SO-0001 is packed with pack_id set and invoice_id null; SO-0002 is packed with an issued invoice.

Source checks passed ('screenLinesFound 5, uiLinesFound 3, m6remaining "{count} left to bill"'), so the copied helpers are the screen's code on this tree.

Run 1 failed only because of an error in the probe's own SQL (string_agg on an enum needed ::text); that output is kept as probe-run-1-probe-sql-cast-error.txt. Read-only SQL on the template copy before the probe: the tarsun tenant has queue_rows 1, unbilled_pack_rows 0 and in_both 0, so the seeded demo data does not show the overlap.

Cleanup: the probe was deleted, `git status --short` shows 0 lines, and dos_test_b2_sus was recreated from the template and migrated (0 s106 tenants).
Root cause: The two lists behind the header overlap on every parked pack whose order is still packed, and the screen adds their lengths without checking which orders they hold.

- frontend/manager-app/app/billing/index.tsx:182 `const remaining = addCounts(pagedCount(queue), pagedCount(unbilledPacks))`, shown at :290 as '{count} left to bill'. The queue read is at :107 and the unbilled-packs read at :108-110.
- frontend/manager-app/src/lib/ui.tsx:641-644 `addCounts` returns `(a.count ?? 0) + (b.count ?? 0)`, with no knowledge of which orders are in each list.
- backend/libs/core/src/modules/billing/invoices.service.ts:699-744 `queue` lists orders with state = 'packed' (:706) and no invoice other than cancelled or draft (:710-711). A parked pack's order meets both conditions.
- backend/libs/core/src/modules/warehouse/packing.service.ts:133 skips the bill when issueInvoice is false, while the order still moves to packed. packing.service.ts:171-175 lists `invoiced:false` as `invoice_id IS NULL`, which includes that same pack.

The overlap existed before DOS-022 (commit 7865d6c narrowed the queue to packed orders but kept parked packs in it). It is reachable in normal use through the warehouse app's 'Pack without a bill' (frontend/warehouse-app/app/pack/[orderId].tsx:317).
Suggested fix: Count orders, not rows, on the billing screen.

1. Extract a pure helper, for example `billingRemaining(queueReply, packsReply)`, into frontend/manager-app/src/lib/. It should return `addCounts(pagedCount(queue), { count: packs.items.filter(p => !queueOrderIds.has(p.orderId)).length, more: pagedCount(packs).more })`. A parked pack whose order is already a queue row is the same piece of work.
2. Use the helper at index.tsx:182.
3. In the 'Packed, not billed' panel, leave out those same rows (or mark them) so SO-0001 is not listed twice. Its meta count should use the same filtered length.

A capped queue (nextCursor set) still says 'N+', so the rule about showing a cap honestly is unchanged. No contract change is needed. A backend change to either list would shift the counts on other screens that read the same procedures.
Test plan: Red tests a fix must add:

1. A new frontend/manager-app/src/lib/billing-remaining.test.ts, run by the manager-app's vitest `test` script:
   - (a) queue reply [order P] plus packs reply [pack of P] gives count 1 and '1 left to bill' (2 today);
   - (b) queue [P, C] plus packs [pack of P] gives 2 (3 today);
   - (c) queue [] plus packs [pack of D, whose order has already left packed] gives 1;
   - (d) a queue reply with nextCursor keeps the '+';
   - (e) a check on index.tsx's source, like review-desk.test.ts does, that 'm6.remaining' and the packs panel use the helper.
2. In backend/libs/core/src/modules/billing/billing.spec.ts, a spec that pins the overlap the screen must handle: a pack confirmed with issueInvoice:false puts its order in GET /billing/queue AND in GET /warehouse/packs?invoiced=false, and after POST /warehouse/packs/{packId}/invoice it is in neither.

Re-run the S-106 probe scenario through the helper: expect 1, 2, 1, 0 instead of 2, 3, 1, 0.
NOT TESTED: The billing screen in a browser (desk or phone width) was not opened. The '2 left to bill' header and the double listing of SO-0001 across the two panels come from running the screen's code on the API replies, not from looking at the page.; The Android and iOS apps were not run.; Whether other surfaces add the same two lists (the owner app, manager nav badges or tiles) was not checked. One grep over the frontend with --include failed in zsh and was not repeated.; A parked pack whose order has moved past packed (dispatched or delivered) was not tried; it would be in the packs list only.; Paged cases where the queue has more than 200 rows or the packs list more than 50 were not tried.; The screen's refresh after billing (the useMutation invalidation of ['billing'] and ['warehouse']) was not run in a client.
Source: architect suspect S-106; probe and skeptic run wf_309501e5-2f5 (2026-09-14). Skeptic: UPHELD (real user path: True; priority P3). I could not refute it. The bug reproduces on HEAD 199952b, which is main.

**The two lists overlap on a parked pack.**
- A pack confirmed with issueInvoice:false leaves the order `packed` with no live bill. That puts it in `billing.invoices.queue` (invoices.service.ts:699-744, state filter at :706).
- The same pack has invoice_id null, so it is also in `warehouse.packs.list {invoiced:false}` (packing.service.ts:133 skips the bill, :175 filters on `isNull`).
- The screen reads both at billing/index.tsx:107 and :108-110, then adds their lengths without looking at order ids (:182, `addCounts` in ui.tsx:641-644). The result is shown as '{count} left to bill' (:290).

**The probe follows the path the app takes.**
- The pack call is the same one the warehouse app's 'Pack without a bill' button makes: POST /warehouse/orders/{id}/pack with issueInvoice:false (w6-park at [orderId].tsx:162-169, the dialog sends it at :317). It is reachable whenever the order is not yet packed.
- The two reads use the same GET routes and inputs as the screen (billing.ts:808 `/billing/queue`, warehouse.ts:907 `/warehouse/packs`). They go through the test harness with TenantGuard.
- manager-service (definitions.ts:109-149) mounts BillingModule and WarehouseModule, with the billing and warehouse contract keys, for manager and accountant. So the real screen reaches the same handlers.

**Guards I looked for and did not find:**
- No worker job bills a parked pack: `OrderPacked` is listed as audit-only (worker/src/jobs/outbox-coverage.test.ts:48), and there is no issueForPack in the worker.
- Nothing de-duplicates the two lists on the client.
- The stale comment at index.tsx:326 still calls the queue 'the confirmed orders'. The design assumed the two lists never overlap.
- The overlap predates DOS-022: the 7865d6c diff shows the old filter (confirmed, picking and packed with no live bill) also included parked packs.

**Priority stays P3.**
- Only the header number is wrong. Each panel shows its own correct count.
- A second bill for P was refused with 409 already_invoiced, and P kept exactly 1 live bill.
- The queue's rupee total sums queue rows only.
- There is no money, stock or document damage.
Skeptic corrections (binding on this record): 1. **The impact statement needs a time limit.** 'Every pack sent out with Pack without a bill is counted as two' holds only while the order is still `packed`. load-sheets.service.ts create (about :165-180) accepts a packed order that has a pack confirmation, whether or not it has a bill. Once a trip departs, the order leaves `packed` and drops out of /billing/queue. From then on it is only in the packs list and is counted once. So the double count lasts from the pack until dispatch. This comes from reading the code; the dispatched case was not run.

2. **Button location.** The 'Pack without a bill' button is at frontend/warehouse-app/app/pack/[orderId].tsx:162-169 (testID w6-park at :168). Line :317 is the dialog's confirm, which sends `issueInvoice: ask !== 'park'`.

3. **Other screens (resolves one of the prober's NOT TESTED items, by grep only).** A quoted `--include` grep over frontend/ found that only manager-app/app/billing/index.tsx:182 adds the two lists.
   - warehouse-app/app/pack/index.tsx:40 and warehouse-app/app/index.tsx:59 read `packs.list {invoiced:false}` on their own.
   - manager-app/app/money/day-end.tsx:183 uses addCounts for cash plus cheque, which is unrelated.
   - So the double count is limited to the billing desk header. No other screen was opened.

4. **Probe shortcut that does not matter.** The probe packs orders straight from `confirmed` with no picklist, while the warehouse app packs after picking. Neither the queue filter nor `packs.list {invoiced:false}` looks at picklists, so the overlap is the same.

5. **Verified as correct.** These citations in the report match the tree: index.tsx:107, 108-110, 182, 290; ui.tsx pagedCount :626, countText :636, addCounts :641-644; invoices.service.ts queue :699-744 with state 'packed' at :706; packing.service.ts :133 and :171-175 (isNull at :175). The strings are 'm6.remaining' '{count} left to bill' (strings.ts:440), 'm6.queueRows' '{count} packed, no bill yet' (:441) and 'm6.packRows' '{count} packs already gone out' (:442).

6. **No other changes.** Every number in the prober's report matches my run: headers 2, 3, 1, 0 against true counts 1, 2, 1, 0, and the 409 on the second bill.
Status: NEW — confirmed on the API, not yet approved by the founder.

### DOS-174 — Closing the load-out panel leaves a challan poll running: reopening it opens a challan for the sheet that was closed (was S-108)
Category: bug | Priority: P2 | Role: manager | Platform: manager app, web and phone widths (source guard plus unit test; the browser walk is owed)
User: manager
Platform: manager app M7 load-out panel
Environment: Worktree .claude/worktrees/b2-s108 on branch qa/b2-s108, merged into main as `c3b4ec1`.
Steps:
  1. On M7, open a draft load sheet's panel and press Print the challan. The first answer is `queued` (the worker renders the PDF), so the screen polls.
  2. Close the panel before the poll ends (the poll window is about 60 s).
  3. Open another sheet's panel.
Expected: the poll for the closed sheet stops; nothing opens for a sheet the manager is no longer looking at.
Actual: the timer kept running and a late answer opened the CLOSED sheet's challan over the newly opened panel.
Fix (merged): the panel's `onClose` bumps `challanToken.current`, so any answer belonging to a closed panel is dropped and nothing further is scheduled. Guard: `frontend/manager-app/src/lib/s-108-challan-close.guard.test.ts` — proven red on the merged tree by deleting the single `challanToken.current += 1` line.
Owed: the manual walks (web 1280x800 and 390x844, Android, iOS, and the Safari attempt-0 check).

### DOS-175 — A receipt can be recorded against a trip that does not exist, and is then permanently un-bankable
Category: bug | Priority: P1 (money) | Role: accountant, manager, owner | Platform: API (`pnpm smoke` on a fresh seeded database, main 6e5c7c5)
User: whoever records the receipt; the accountant is the one who can never bank it
Platform: API
Environment: Fresh database `dos_smoke_money` (created, migrated, seeded, dropped afterwards), all eight services plus the in-process worker behind `backend/all-in-one` on :3100, main at `6e5c7c5`. Runs: `pnpm smoke --base http://127.0.0.1:3100 --run-tag money-1` and the same with `--destructive`.
Steps:
  1. POST /receipts with `tripId` set to the value the published contract example carries (`01a06d0b-bd31-7813-8e79-aa7c39f75385`). The call succeeds and creates RCPT-9006.
  2. In SQL: `select count(*) from trips where id='01a06d0b-bd31-7813-8e79-aa7c39f75385'` returns **0**. A LEFT JOIN of receipts to trips leaves `trip_no` NULL for RCPT-9006 and RCPT-9007, while every seeded RCPT-VAN-* and RCPT-9005/9008/9009 joins to a real trip.
  3. POST /receipts/deposit for that receipt, as owner and as manager.
Expected: either the receipt is refused at creation because its trip does not exist, or it can be banked like any other office receipt.
Actual: creation succeeds — `receivables.receipts.create` does not validate `tripId`, and receipts→trips is an upstream reference so there is no foreign key to catch it. The deposit is then refused **forever** with 409 `trip_cash_not_settled` ("was taken on a trip that is not settled yet; bank it after the trip's cash is handed over at Day-end"), because the trip it names can never settle. The guard is `receivables.service.ts:1006`, added by DOS-132 (`460269e`) and hardened by the money lane (`9c44458`) — both correct; the hole is the missing validation at creation. Money collected against a non-existent trip can never reach the bank.
Evidence: `deposit` never returned 200 on either smoke run; the harness classifies the 409 as EXPECTED, so the run table stayed green while the operation was never actually proven.

### DOS-176 — Proof of delivery fails with a 500: 'proof insert returned nothing'
Category: bug | Priority: P1 | Role: delivery crew | Platform: API (`pnpm smoke` on a fresh seeded database, main 6e5c7c5), both runs
User: delivery crew
Platform: API
Environment: as DOS-175.
Steps: POST /delivery/deliveries/{id}/pod with the published example body `{"evidence":{"kind":"geo","payload":{"distanceM":40},"lat":19.2437,"lng":73.1355},"id":"…"}`.
Expected: the proof is stored and the stop can be closed.
Actual: **500 Internal Server Error**, message "proof insert returned nothing". Stack: `DeliveriesService.writePod` (`libs/core/dist/modules/delivery/deliveries.service.js:404`) → `addPodInTx:199` → `runIdempotent`, thrown as INTERNAL_SERVER_ERROR. Reproduced on a fresh seeded database in both the non-destructive and the destructive run. A proof of delivery that 500s is a delivery the crew cannot close.

### DOS-177 — The driver's GPS consent fails with a 500, and no van can depart without it
Category: bug | Priority: P1 | Role: delivery crew | Platform: API (`pnpm smoke` on a fresh seeded database, main 6e5c7c5), both runs
User: delivery crew
Platform: API
Environment: as DOS-175.
Steps: POST /delivery/consents with the published example body `{"granted":true,"noticeVersion":"gps-notice-2026-09","locale":"en-IN"}`.
Expected: the consent is recorded and the trip can start.
Actual: **500** with no message at all. Consent is the gate a trip cannot depart without, so this blocks the whole day for that crew.

### DOS-178 — A late trip payment refused by the office can be DISCARDED on the phone, erasing the only record of cash a shop already paid
Category: bug | Priority: P1 (money, data loss) | Role: delivery crew | Platform: delivery app (architect review of the DOS-168..170 lane; code reading — the walk is owed)
User: delivery crew
Platform: delivery app, the unsent-changes tray
Environment: Found by the architect (Fable) reviewing the money lane's merge gate, 2026-09-19.
Steps:
  1. A crew takes cash at a door with no signal. The office settles that trip before the phone gets signal back.
  2. The phone syncs; the server refuses the receipt op with `trip_settled` (correct, founder answer A: the driver hands the money to the cashier instead).
  3. The refused op lands in the phone's unsent-changes tray, which offers **Discard**.
Expected: nothing a person entered is ever thrown away (founder answer A, 2026-09-14, and never-list #11). A refused payment should be kept and routed to the cashier, never offered for deletion.
Actual: Discard erases the phone's only record that the shop paid. The shop's money then exists nowhere — not on the phone, not in the books.

### DOS-179 — When the browser cannot keep an offline copy, only one screen says so, and the order button claims the opposite
Category: bug | Priority: P2 (honesty) | Role: salesperson (delivery and warehouse not yet walked) | Platform: web (Chromium, desk and phone widths), 3/3 FAIL
User: salesperson
Platform: sales app on web; the same question is unwalked on delivery and warehouse, on every platform
Environment: DOS-167 ruling-3 re-proof, main at `ce3dc8c`/`6e5c7c5`, fallback to a memory store induced by holding the expo-sqlite chunk past the 15 s deadline.
Steps: induce the memory fallback, then visit Beat, Shops, Orders, Me and Settings, and place an order.
Expected: a rep working all day on Shops and Orders is told the phone keeps nothing.
Actual: "This browser will not keep the offline copy" appears on the **beat screen only**. Shops, Orders, Me and Settings show the connection strip reading "Updated just now" and nothing about the store. Worse, the order screen says the opposite: its button reads "Save on this phone" and then "Saved on this phone" after the order is queued into a store that dies with the tab. The ruling-3 merge review asked for exactly this to be ruled out ("the app now looks healthy while keeping nothing"); it was executed and it fails.
Founder decision 2026-09-19: warn on every screen, and stop the button claiming it saved.

### DOS-180 — An order saved in a dead spot re-labels itself 'Order placed' the moment signal returns, though the office holds only a draft
Category: bug | Priority: P2 (honesty) | Role: salesperson | Platform: Android (sales app)
User: salesperson
Platform: sales app
Environment: DOS-167 ruling-3 Android re-proof, main at `6e5c7c5`.
Steps: place an order with no signal (the honest "queued" banner shows), stay on that screen, restore signal.
Expected: the banner keeps telling the truth about THIS order until the office actually confirms it.
Actual: `frontend/sales-app/app/orders/new.tsx:387-388` picks `title` and `meta` from `local.online` at render time, so the same banner re-renders as "Order placed — The office has it, with its number and its price" over an order the office still holds as a DRAFT with no number.
Founder decision 2026-09-19: the banner stays honest until the office confirms.

## Suspected by the architect's plan sign-offs — NOT TESTED

Raised by Fable while signing off batch-2 plans (sign-off run wf_b67cb996-95f, 2026-09-13), from code reading only. None of these is a
finding until a probe, a test or a walk proves it (Charter A.4). Verify the money and security ones first, then file each proven one as a
DOS block above with its evidence. Full wording: QA/evidence/batch2/verdicts/<verdict id>.md, section "New findings outside this fix".

| # | Raised in verdict | Suspected defect (file:line, as the architect wrote it) | Status |
|---|---|---|---|
| S-01 | DOS-116 | backend/libs/core/src/modules/claims/build.service.ts:481 — creditNoteCandidates filters on the note header reason === 'return_damaged', not the line's saleable (registers.service.ts:84 carries it), so damaged lines inside a mixed doorstep return (reason return_saleable, deliveries.service.ts:187-191) are never offered as brand damage-claim candidates. P2, claims. | NOT TESTED |
| S-02 | DOS-116 | backend/libs/core/src/modules/delivery/delivery.sync.ts:132 — an offline delivery op that omits returned_saleable defaults to true instead of isSaleableReturn(l.reason), so a queued damaged return without the flag becomes a durable sync rejection rather than a booking to the bin (the delivery app sends the flag today, deliver.tsx:269). P3, delivery. | NOT TESTED |
| S-03 | DOS-133 | backend/libs/core/src/modules/warehouse/load-sheets.service.ts:754-767 — onALiveSheet counts a CONFIRMED sheet for an order later return_undelivered → packed, so loadSheets.create refuses it for ever (409); only exit is trips.depart's dispatchIfPacked (trips.service.ts:844-858) with no sheet/challan. Every fresh seed: SO-0845/SO-0850; dos_qa SO-0857/SO-0899/SO-0904. P1. | CONFIRMED (probe + skeptic, API) — filed as DOS-172 (P1) |
| S-04 | DOS-133 | backend/libs/core/src/modules/orders/fulfilment.ts:105,178 — fulfilmentQueue orders/pages on desc(sales_orders.id); seeded orders carry hash ids (SO-0862 = a4e4ef5b…) above every live UUIDv7, so W2/W3's confirmed queue lists seeded orders above today's (DOS-023/133 class, unfiled). P1. | NOT TESTED |
| S-05 | DOS-133 | backend/libs/core/src/modules/warehouse/load-sheets.service.ts:227,233 — loadSheets.list still id-ordered with an id cursor; W7 LOAD SHEETS (limit 30) shows seeded sheets above today's; DOS-025 fixed only the manager side client-only. P2. | NOT TESTED |
| S-06 | DOS-133 | backend/libs/core/src/modules/warehouse/load-sheets.service.ts:758-765 (and the new predicate) — no index serves load_sheets.order_ids containment; every create/awaiting_load scans all non-cancelled sheets of the tenant, which accumulate for ever (docs/20 rule 3); GIN jsonb_path_ops later. P3. | NOT TESTED |
| S-07 | DOS-133 | backend/libs/core/src/modules/warehouse/packing.service.ts:335 — packedAt: new Date() is the Node clock while the column defaults to Postgres now(); skewed instances make W5's from/to (packed_at) disagree with list order (created_at). P3. | NOT TESTED |
| S-08 | DOS-132 | backend/libs/core/src/modules/receivables/receivables.service.ts:1744-1746 — reversing a collected trip cash receipt after its trip settled credits CASH_VAN, which settlement already emptied; should credit CASH once settled (P2). | NOT TESTED |
| S-09 | DOS-132 | backend/libs/core/src/modules/delivery/settlement.service.ts:348-360 (+ receivables.sync.ts:60-79, frontend/delivery-app/src/lib/queue.ts:226-270) — settlement counts collections rows only; an offline doorstep receipt uploaded through the receipts table has none, so its cash is never credited out of CASH_VAN and reads as cash over in CASH_SHORT, which can force owner approval (P1). | CONFIRMED (probe + skeptic, API; same defect as S-76) — filed as DOS-169 (P0) |
| S-10 | DOS-132 | backend/libs/core/src/modules/delivery/settlement.service.ts:217-238 vs receivables.service.ts:855 — after a settlement with expenses or a shortage CASH holds handed-over minus float, yet receipts bank at full value; the per-receipt deposit takes CASH below the drawer by expenses + short and cannot represent the bank slip (P2, design). | NOT TESTED |
| S-11 | DOS-132 | backend/libs/core/src/modules/receivables/receivables.service.ts:832-870 — deposit reads without FOR UPDATE and its UPDATE has no status = 'collected' guard; two desks can bank the same receipt twice (P2, reproduce first). | NOT TESTED |
| S-12 | DOS-132 | frontend/owner-app/app/money/receipts.tsx:305-311 — the O11 dialogs close on any refusal (.then(done, done)), so a server 409 is never shown to the owner (P2). | NOT TESTED |
| S-13 | DOS-132 | backend/libs/database/src/seed-demo/receivables.ts:664-669 — RCPT-VAN receipts attach to a settled trip with no collections row and seeded trip_settlements post no journal entry; CASH_VAN never nets and the owner's cash-in-transit tile reads 0 (P3, demo data). | NOT TESTED |
| S-14 | DOS-117 | backend/worker/src/jobs/reporting.ts:16-20, :83, :91 — reporting.rollup.tenant/schedule/finalize are 'standard' queues, so singletonKey is never enforced (pg-boss 12 indexes only short/singleton/stately/exclusive, plans.js:668-709); the header's 'never doubled up' is false — harmless by idempotency, wasteful at scale | NOT TESTED |
| S-15 | DOS-117 | backend/worker/src/jobs/reporting.ts:74-76 — the finalize loops rollupBehaviour over every tenant inside one job, O(all shops), against rollup.ts property 2 and the 900 s expiry | NOT TESTED |
| S-16 | DOS-117 | backend/worker/src/jobs/retention.ts + docs/20 partition addendum — ageing_snapshots (one row per shop per day after this fix) has no retention sweep and no partition plan | NOT TESTED |
| S-17 | DOS-117 | backend/libs/core/src/modules/reporting/rollup.ts:267 — the finalize's yesterday re-run calls refreshOwnerSummary with day = yesterday, so owner_summary.today_invoiced/collected show yesterday's figures until the next 15-minute tick | NOT TESTED |
| S-18 | DOS-117 | backend/libs/core/src/modules/receivables/receivables.service.ts:1441 — the owner's rebuildAgeing accepts any past asOf and re-dates every summary row backwards; the new catch-up heals it only at the next worker start | NOT TESTED |
| S-19 | DOS-126 | backend/libs/core/src/modules/pricing/bargains.service.ts:81-97 and :185-196 never write expires_at, so quote.service.ts:299-302 applies an API-filed standalone approved rate to every future order of that shop for that variant, forever (dos_qa: 13 approved standalone requests; every row with an expiry is the seed's). P2 business. | NOT TESTED |
| S-20 | DOS-126 | backend/libs/core/src/modules/orders/pricing-lines.ts:174 vs :192 against backend/libs/database/src/seed-demo/sales.ts:689-700, :865: two stored conventions for a bargained sales_order_line in one table; rate × qty − discount is not the taxable and every reader must use line_total − tax. One convention plus a backfill is a tech-debt item. P3. | NOT TESTED |
| S-21 | DOS-126 | dos_qa holds a confirmed sales_order with no order_no whose line is rate 4000 / tax 0 / line_total 0 (sales_order_lines, direct insert, not an API path): the QA database has taken a non-API write; the Q5 rebuild clears it. P3 hygiene. | NOT TESTED |
| S-22 | DOS-146 | frontend/retailer-app/app/pay.tsx:141-142 — value={amount ?? owed} / onChange={setAmount}: an emptied Pay amount snaps back to what is owed on every platform; fix with DOS-124/DOS-154 as one Pay slice, not with ?? 0 (Start would disable with the wrong reason, pay.tsx:104-109; retailer strings.ts:191 helper changes with it). | NOT TESTED |
| S-23 | DOS-146 | frontend/manager-app/app/inbound/documents.tsx:418 — value={totalPaise ?? header.totalPaise}: clearing the invoice total on the desk text field snaps back to the header value on blur; same class. | NOT TESTED |
| S-24 | DOS-146 | frontend/libs/ui/src/money.ts:162-164 + frontend/libs/ui/src/native/money.tsx:165-183 (both renderers) — a money pad opened on a PRE-FILLED whole amount appends the first digit (₹3,000 → '4' → ₹30,004); DOS-060 design for a reopened entry, but on a pre-filled field it is the crores hazard the finding names; replace-on-first-digit (⌫ and . still edit) is a kit-wide founder call, P2 UX, own id, touches retailer Pay too. | NOT TESTED |
| S-25 | DOS-131+DOS-137 | backend/libs/core/src/modules/delivery/trips.service.ts:209-219 — create's busy check covers input.driverId only; a helper already on another open trip that day is accepted as helper again. | NOT TESTED |
| S-26 | DOS-131+DOS-137 | backend/libs/core/src/modules/delivery/trips.service.ts:1034-1050 — assertMember selects tenancy's `memberships` table from the delivery module (module rule; should go through a tenancy export). | NOT TESTED |
| S-27 | DOS-131+DOS-137 | frontend/warehouse-app/app/load/index.tsx:149-151 — `reason: t('w7.challan')` on a default-state ListRow never renders (ui/src/web/list.tsx:185 and native/list.tsx:175 render reason only for needsAttention), so the challan number is invisible on W7's sheet rows. | NOT TESTED |
| S-28 | h4-stock merge review | frontend/delivery-app/app/stop/[id]/van-sale.tsx:193,217 — the van sale shows quote.totals.netPaise (pre-GST) under 'Sale total' above 'Take the money now', so the crew asks for the wrong amount at the door; DOS-096 now gives totals.totalPaise. P1 candidate. | CONFIRMED (probe + skeptic, API + screen source) — filed as DOS-171 (P1) |
| S-29 | h4-stock merge review | hsn_rates holds same-date duplicates (0406 at 500 and 1200, 2106, 2202) and loadGstBps orders by effectiveFrom only (quote.service.ts:402, billing.internals.ts:240), so which GST rate wins is heap order. P2. | NOT TESTED |
| S-30 | h4-stock merge review | price_list_items.inclusive_of_gst is ignored by the engine, the quote and the order while R7 now prints '+ GST'. P2. | NOT TESTED |
| S-31 | h4-stock merge review | frontend/owner-app/app/prices/index.tsx:347 — the owner what-if shows netPaise (pre-GST) under o8.quoteNet with no label. P2. | NOT TESTED |
| S-32 | h4-stock merge review | Order tax uses percentOf(net, rate) (quote.service.ts:126) while the invoice uses splitGst = 2 × percentOf(taxable, rate/2) (invoices.service.ts:1421, domain/gst.ts:23-24): one paisa difference per intra-state line (SO-0885 line 1: 25277 vs 25276). P3. | NOT TESTED |
| S-33 | h4-stock merge review | Two more copies of the godown rule: billing/invoices.service.ts:1276-1295 and delivery/settlement.service.ts:536-555, while inventory/index.ts:23-26 now says no module keeps its own copy. Tech-debt. | NOT TESTED |
| S-34 | h4-stock merge review | api-client/src/cache.ts:172-182 keeps the last good value when a refetch fails; sales-app and retailer-app stock hooks read only data, so an hour-old godown total stays on screen with no signal. P3. | NOT TESTED |
| S-35 | DOS-031 | frontend/manager-app/app/inbound/documents.tsx:277-280,293-294 (R1): after a successful submit, done() keeps sessionId, so Save / submit / Give it up and the editable Bill no. and Amount (l.413, l.420) stay on a Reviewed document and every write answers 409 'review session … is submitted' (review.service.ts:347-350); clear session and header edits on submit success as l.518-520 does. | NOT TESTED |
| S-36 | DOS-031 | frontend/manager-app/app/inbound/documents.tsx:486 + libs/api-client/src/react/index.tsx:379-386 (R2, code reading, not walked): Start again on the same document after Give it up re-sends the spent key; the server replays the first start's stored 200 (platform/idempotency.ts:61-74) and the panel holds an abandoned session; startReview.reset() when the session ends. | NOT TESTED |
| S-37 | DOS-031 | frontend/manager-app/app/inbound/documents.tsx:549-556 (R3): Reject the document is offered on a Failed document; documents.service.ts:509 fires 'reject' and failed is terminal (document.ts:76), so it always answers 409; gate with documentMachine.can(status, 'reject'). | NOT TESTED |
| S-38 | DOS-031 | frontend/manager-app/app/inbound/documents.tsx:537-548 (R4): Book it as a supplier bill is enabled on Extracted and Needs review documents with lines while the server refuses anything not reviewed (documents.service.ts:550-553); DOS-030 covers only the brand-DMS 501 at l.545-549. | NOT TESTED |
| S-39 | DOS-031 | frontend/manager-app/app/inbound/documents.tsx:62-72 (P3): DOC_FAMILY keys capturing, queued, in_review are not document_status values (contracts/src/docint.ts:159-169), so uploaded, verifying and extracted chips fall to neutral; `satisfies Partial<Record<DocumentStatus, StatusFamily>>` would have caught it. | NOT TESTED |
| S-40 | DOS-098 | orders.repeatLast (server) inflates every `inner` line: orders.service.ts:209-214 forwards enteredUnit into packSizeFor, which counts inner as a case (pricing-lines.ts:94-95); 382/382 seeded inner lines, e.g. 36 pc → 180 pc. | NOT TESTED |
| S-41 | DOS-098 | orders.repeatLast ignores the DOS-073 reach rule: a salesperson copies a colleague's order lines (orders.service.ts:180-190 has no salesperson_id scope while get/list do). | NOT TESTED |
| S-42 | DOS-098 | Sales app 'Repeat last order' sorts `_local_rev DESC` before `created_at` (frontend/sales-app/src/lib/local.ts:447, :471-477) so a re-synced older order wins, and keeps an `inner` count as the entered quantity under `piece` (app/orders/new.tsx:131-141). | NOT TESTED |
| S-43 | DOS-098 | R7 places basket lines it does not show for a hand-built basket too (frontend/retailer-app/app/order.tsx:149 vs :338-342); amendment (a) closes only the seed path. | NOT TESTED |
| S-44 | DOS-098 | orders.repeatLast still writes `note: 'Repeat of <uuid>'` for API callers (docs/23 §10 #10; orders.service.ts:206). | NOT TESTED |
| S-45 | DOS-037 | P3 product: permissions.ts:574/584/585 (billing.invoices.issueForPack/setEwayBill/requestIrn), :629 (warehouse.challans.recordEwb), :631 (warehouse.reservations.release, pinned at permissions.test.ts:500-503), :577 (importBrandDms), :831 (notifications.messages.resend) still let the accountant write on screens docs/23:205 calls read-only for her; founder's call whether issuing a bill is bookkeeping. | NOT TESTED |
| S-46 | DOS-037 | P3 cosmetic: backend/libs/core/src/platform/authz.ts:38 requireRole answers 'This action needs one of: owner, manager, system', naming the internal `system` pseudo-role to a client whenever the guard passes and the service refuses. | NOT TESTED |
| S-47 | DOS-044 | P1 stock-from-nothing through Move: transfer() (backend/libs/core/src/modules/inventory/stock.service.ts:271-318, BACK_OFFICE_OR_WAREHOUSE) posts transfer_out with no floor but the balance row's negative_allowed, and every damaged bin is negative_allowed=true (backend/libs/database/src/tenant-bootstrap.ts:196-201; dos_qa 47/36/7 rows all t) — a warehouse Move of N from the bin to the godown creates N sellable pieces and restocks damaged goods (cf. DOS-116). | NOT TESTED |
| S-48 | DOS-044 | P1 same class: inventory.locations.upsert (backend/libs/contracts/src/permissions.ts:477, BACK_OFFICE_OR_WAREHOUSE) accepts negativeAllowed from a warehouse token (contracts inventory.ts:48-55; stock.service.ts:131-164) — a loader can create a negative-allowed location and transfer out of it. | NOT TESTED |
| S-49 | DOS-044 | P2 no desk screen posts a cycle count: inventory.cycleCounts.post is unreachable from any app (frontend/manager-app/app/stock/ has only index.tsx; frontend/owner-app/app/stock/ has no counts route) while W8c (frontend/warehouse-app/app/stock/counts/index.tsx:4-7) tells the loader the desk posts it and docs/23 :876 names M16 and O15. | NOT TESTED |
| S-50 | DOS-044 | P3 test coupling: backend/libs/contracts/src/permissions.test.ts:1048 asserts the reporting fill-rate rows equal permissionFor('inventory.stock.adjust'), an accidental tie DOS-037 will break; compare to a literal role list. | NOT TESTED |
| S-51 | merge review h5-orders (qa/b2-h5-orders, 117717c..46fdd38: 89873d7 DOS-003, 46fdd38 DOS-004) | frontend/manager-app/app/orders/index.tsx:285-292 — manager credit sentence hides owes/limit for stop+breached and prints '₹0.00 over the limit' for an overdue-only or bill-count breach (headroom ≥ 0); rebuild from reasons[] exactly as owner approvals.tsx:186-204 now does | NOT TESTED |
| S-52 | merge review h5-orders (qa/b2-h5-orders, 117717c..46fdd38: 89873d7 DOS-003, 46fdd38 DOS-004) | backend/libs/core/src/modules/orders/pricing-lines.ts:70-75 and fulfilment.ts:6-10,161-173 — orders still joins product_variants/tenant_products/retailers tables directly (pre-existing cross-module table reads the amendment's rule forbids for new code); retire in a tech-debt item via tenant-catalog/retailers index exports | NOT TESTED |
| S-53 | merge review h5-orders (qa/b2-h5-orders, 117717c..46fdd38: 89873d7 DOS-003, 46fdd38 DOS-004) | frontend/sales-app/app/orders/[id].tsx:73 — line names come from the offline catalog index (byVariant), which can lack a delisted item; the offline sales_order_lines table carries no name, so DOS-098/offline work must decide whether the manifest carries variant_name or the index keeps delisted rows (design-level) | NOT TESTED |
| S-54 | merge review h1-money-slice1 (qa/b2-h1-money, commit c2c472f only; range 117717c..c2c472f) | backend/libs/core/src/modules/receivables/receivables.queries.ts:34,40 — raw NUL bytes inside the cursor-separator string literals make git treat the file as binary, so its diffs are invisible to every review (this slice's hunk included); write '\0' as an escape | NOT TESTED |
| S-55 | merge review h1-money-slice1 (qa/b2-h1-money, commit c2c472f only; range 117717c..c2c472f) | backend/libs/database/src/schema/warehouse.ts:89 — picklists_no_idx is (tenant_id, picklist_no) with no series/FY, so a new FY's PL-0001 is refused (contrast invoices_no_idx) | NOT TESTED |
| S-56 | merge review h1-money-slice1 (qa/b2-h1-money, commit c2c472f only; range 117717c..c2c472f) | backend/libs/core/src/modules/billing/invoices.service.ts:1569-1575 — a lagging INV counter still ends in a 409 with no product repair path (the plan's risk 3, now solved for receipts only); candidate to extend the docs/22 l.276 self-heal rule to invoices and credit notes | NOT TESTED |
| S-57 | merge review h1-money-slice1 (qa/b2-h1-money, commit c2c472f only; range 117717c..c2c472f) | No unique number index on grns, trips, claims, sales_orders or purchase orders (the plan's own out-of-scope note) — same class as DOS-032, not yet filed | NOT TESTED |
| S-58 | merge review h2-trips (qa/b2-h2-trips, 117717c..d20a584, DOS-043) | backend/tools/smoke-endpoints.mts:720 — draftLoadSheet() confirms the seed's TRIP-NEXT draft ahead of the run's own sheet, so smoke dispatches the demo's tomorrow load and orphans its own draft; part of the queued 'smoke leaves its calls behind in the pilot tenant' item. | NOT TESTED |
| S-59 | merge review h2-trips (qa/b2-h2-trips, 117717c..d20a584, DOS-043) | backend/libs/core/src/modules/delivery/delivery.internals.ts:193-200 — assertCrewOrDesk(trip, DOORSTEP) returns on desk.includes('delivery') before the crew check, so for depart/return the 'only the driver or the helper' branch is unreachable and crew scoping rests on trips_read RLS alone (schema/delivery.ts:205); correct today, but pass a desk tuple without 'delivery'. | NOT TESTED |
| S-60 | merge review h2-trips (qa/b2-h2-trips, 117717c..d20a584, DOS-043) | docs/22-source-of-truth.md:309 — 'may a trip depart before its planned date' is still an open founder question; the finding's 'two days early' symptom stays by design until answered. | NOT TESTED |
| S-61 | DOS-108 | backend/libs/database/src/seed-demo/platform-admin.ts:229-236 — the demo's live grant asks 72 h but is inserted approved for ACTIVE_GRANT_DAYS (29 days); after DOS-108 the owner reads 'Hours asked 72 h · Open until 12 Oct'. dos_support_grant_guard() accepts the INSERT, so 'never lengthened' is enforced on UPDATE only (check migrations 0034/0036). | NOT TESTED |
| S-62 | DOS-108 | frontend/libs/ui/src/web/controls.tsx:387 and native/controls.tsx:421 — <Segments> silently drops items past three; owner surfaces losing their fourth view: prices/index.tsx:233-246 ('quote'), staff/index.tsx:134-146 ('bounds'), stock/catalog.tsx:143-156 ('brands'), owner-app/src/lib/ui.tsx:219-242 RangeSegments ('This FY' on billing, credit-notes, money, books, receipts — the owner can never see an FY register). P2, arguably P1 for the money screens. | NOT TESTED |
| S-63 | DOS-108 | backend/libs/core/src/modules/tenancy/support.service.ts:168-203 — revoke on an already-expired approved grant records 'revoked' (audit before 'approved') instead of refusing; the honest status was 'expired'. | NOT TESTED |
| S-64 | DOS-107 | console.service.ts:160-185 — the comment promises disabling an already-disabled user is a no-op, but run() updates unconditionally and writes a second user.disabled audit row on every new-key call against a locked identity; guard on status='active' and audit iff a row returned (P3 audit hygiene). | NOT TESTED |
| S-65 | DOS-107 | platform-admin/internals.ts:88 + platform-admin.spec.ts:801,1341 — nothing in the product closes or reopens a console account (platform_admins.disabled_at is written only by spec fixtures): the same one-way, SQL-only shape as DOS-107 for the platform's own staff (P2 missing-feature: admin.admins.close/reopen, super only, audited). | NOT TESTED |
| S-66 | DOS-107 | auth.service.ts:173-191 — a locked identity presenting the RIGHT password gets FORBIDDEN while a wrong one gets the credentials error, so a locked account's password can still be confirmed from outside; answer the credentials error for a non-active user too (P3 security). | NOT TESTED |
| S-67 | DOS-080 | P1 · Sign-out never wipes the device store and the store is not keyed by user: SyncEngine.wipe() (frontend/libs/offline/src/engine.ts:187) has no caller; databaseName="dos-sales.db" (frontend/sales-app/app/_layout.tsx:299; delivery :299, warehouse :305); the identity check (engine.ts:330-332) compares schemaVersion, role and tenantId only. A second rep on the same phone inherits the first rep's rows and cursor and never receives their own beat's rows older than that cursor. docs/27 lines 68-69 and 178-179 promise the wipe. | NOT TESTED |
| S-68 | DOS-080 | P2 scale · The four global catalog tables are pulled unnarrowed by every device (backend/libs/core/src/modules/catalog/catalog.module.ts:30-33): the whole curated master rides every first sync; at lakhs of SKUs it dominates the snapshot (docs/20). Narrow to what tenant_products lists, or bound. | NOT TESTED |
| S-69 | DOS-080 | P3 dev-tooling/docs · docs/27 §2 line 36 says the dev server sends COOP/COEP; nothing in frontend/ sets the header (only the comment in frontend/libs/offline/src/store/open.web.ts), so every `expo start --web` walk runs on the memory store and re-snapshots on each reload — the 'per screen' half of this finding and of every web QA measurement. | NOT TESTED |
| S-70 | DOS-164 | P3 web/feedback.tsx:158-169 — useEscape is registered by BOTH the web Sheet and the web Dialog, so Esc in a confirm dialog over a side panel closes the panel too (manager orders decision dialog; owner-app/app/approvals.tsx:189-199 adds a third handler); the topmost overlay alone should take Esc. | NOT TESTED |
| S-71 | DOS-164 | P3 kit hazard (by code, not walked) frontend/libs/ui/src/native/money.tsx:415 — RupeeInput's pad <Modal> sits outside the overlay stack, so a Sheet or Dialog whose open flips while a PAGE-LEVEL pad is presented (an async open) is refused on iOS and sticks by the same mechanism; a layered pad chains correctly. Fold the pad into the stack when the kit next touches money.tsx. | NOT TESTED |
| S-72 | DOS-164 | Process: QA/evidence/batch2/lanes.md:322-325 (L12 depends-on L02/L05 and its Owns list) and L20's file-order wait on L12 are stale after this design; update lanes.md when the lane launches. | NOT TESTED |

Raised by the Fable merge reviews (run wf_a8e3a6c2-fdd, 2026-09-13), from code reading only. Full wording is in QA/evidence/batch2/merge-reviews/<slice>.md, section "Defects outside this slice". The priorities are provisional.

| id | raised in | suspected defect | status |
|---|---|---|---|
| S-73 | h7-syncdoor, h3-doorstep | P2 engine.ts:771/:793 + sync.service.ts:106-127: retry keeps the opId and the server replays the stored refusal, so "Send it again" can never succeed; docs/27 :71-72 and :168 contradict. Probably the same defect as DOS-046 (confirm, then fold). | NOT TESTED |
| S-74 | h5-orders-dos115 | P1 orders.sync.ts:107: an uploaded `source` is cast straight into the order_source enum. An unknown value raises 22P02, so the whole batch answers 5xx and the device retries forever; a device can also label a draft van_sale or brand_dms_import. | NOT TESTED |
| S-75 | h1-money-slice2 | P1 money: receivables.service.ts:1849 undoReceipt credits CASH_VAN when a trip cash receipt is reversed after its trip settled. :870 depositReceipts reads without FOR UPDATE and its UPDATE has no status='collected' guard, so two desks can bank the same receipt twice. | CONFIRMED (probe + skeptic, API) — part (b) filed as DOS-168 (P0), part (a) as DOS-170 (P1) |
| S-76 | h1-money-slice2 | P1 money: settlement.service.ts:236 + receivables.sync.ts:60-79: settlement credits CASH_VAN only for collections rows. An offline doorstep receipt uploaded through the receipts table never leaves CASH_VAN and reads as cash over in CASH_SHORT. | CONFIRMED (probe + skeptic, API) — filed as DOS-169 (P0) |
| S-77 | h1-money-slice2 | P2 worker jobs/notifications.ts:158-171: the 48 h stale guard publishes an old StatementRequested without sending it, so the owner who saw "queued" never learns. | NOT TESTED |
| S-78 | h1-money-slice2 | P2 scale: schema/receivables.ts:403-407 has no (tenant_id, as_of) index on retailer_outstanding_summary. Worker jobs/reporting.ts:151 runs the finalize over every tenant in one job (900 s expiry). :161/:169/:200 reporting.rollup.* queues are 'standard', so singletonKey is never enforced. | NOT TESTED |
| S-79 | h1-money-slice2 | P2 owner-app money/receipts.tsx:305-311: the O11 dialogs `.then(done, done)`, so the new 409 trip_cash_not_settled is never shown to the owner. | NOT TESTED |
| S-80 | h1-money-slice2 | P2 seed-demo/receivables.ts:664-669: RCPT-VAN receipts sit on a settled trip with no collections row, and the seeded trip_settlements post no journal, so the owner's cash-in-transit tile reads 0. docs/plans/integrations.md §7: the ImportCommitted / ExportReady notifications consumer was never built. | NOT TESTED |
| S-81 | h2-trips-slice2 | P1 orders/fulfilment.ts:193: queueRows orders by desc(sales_orders.id). Seeded hash ids beat live UUIDv7, so the W2/W3 queue and the planning board list seeded orders above today's (DOS-023/133 class). | NOT TESTED |
| S-82 | h2-trips-slice2 | P1 load-sheets.service.ts:795: onALiveSheet counts a confirmed sheet for an order later returned undelivered and back to packed. loadSheets.create then refuses that order forever (409) and awaiting_load hides it; every fresh seed has SO-0845/SO-0850. | NOT TESTED |
| S-83 | h2-trips-slice2 | P2 trips.service.ts:216-226: the busy check in create covers only driverId, so a helper already on another open trip that day is accepted. load-sheets.service.ts:227,233: loadSheets.list is id-ordered, so seeded sheets show above today's on W7. | NOT TESTED |
| S-84 | h2-trips-slice2 | P3 trips.service.ts:1174-1178: assertMember reads tenancy memberships from the delivery module (module boundary). packing.service.ts:360: packedAt comes from the Node clock while created_at is Postgres now(). schema/warehouse.ts:203-206: stale trip_id comment. warehouse-app load/index.tsx:211: the challan reason never renders. :156: "N selected · 0 pc". | NOT TESTED |
| S-85 | h3-doorstep | P1 delivery-app stop/[id]/collect.tsx:154 and van-sale.tsx:226 stay online-first with no recordOrSave fallback. A call that gets no answer before the engine knows the phone is offline is lost, the way D4's was. | NOT TESTED |
| S-86 | h3-doorstep | P2 sync.service.ts:217 filters on sync_errors.resolved_at, which nothing sets, so a mirrored refusal returns to every fresh device. ui files.native.ts:34 / files.web.ts:48: upload has no deadline. retailer-app pay.tsx:141-142 and manager-app inbound/documents.tsx:418: the same `value ?? plan` snap-back as DOS-146. | NOT TESTED |
| S-87 | h3-doorstep | P2 kit decision: ui money.ts:162-164 + both NumberPads: a pad opened on a pre-filled amount appends the first digit (₹3,000 → "4" → ₹30,004). This is DOS-060 by design, but it is the crores hazard DOS-146 names. | NOT TESTED |
| S-88 | h3-doorstep | P3 delivery-app queue.ts:156: the local apply never writes deliveries.outcome, so the stop still offers "Deliver this bill" after a save-on-phone. files.service.ts:84: pending file_objects rows and orphan objects are never swept. trip/start.tsx:357: shows "float ₹0.00" before the trip loads. | NOT TESTED |
| S-89 | h5-orders-slice3 | P2 orders.service.ts:216-221 + pricing-lines.ts:98-99: repeatLast forwards the old enteredUnit, and packSizeFor counts an inner as a case, so every seeded inner line comes back case-size times too large for API callers. | NOT TESTED |
| S-90 | h5-orders-slice3 | P3 orders.service.ts:195: repeatLast calls lastPlacedOrder without the rep's id, so a salesperson copies a colleague's order lines (DOS-073 scope). :213 still writes the note "Repeat of <uuid>" (docs/23 §10 #10). | NOT TESTED |
| S-91 | h5-orders-slice3 | P3 retailer-app order.tsx:344 vs :385-387: a hand-built basket places `priced` while the panel draws only `chosen`. sales-app src/lib/local.ts:448,:474: "Repeat last order" sorts _local_rev before created_at. orders/new.tsx keeps an inner count under piece. | NOT TESTED |
| S-92 | h9-desk | P3 seed-demo/stock.ts:152-156: positive warehouse cycle_count rows are written directly with ref_type 'manual', against the spirit of DOS-044 (only the desk adds stock). | NOT TESTED |
| S-93 | h9-desk | P3 platform/authz.ts:38: requireRole's refusal names the internal 'system' role to a client when the guard passes and the service refuses. | NOT TESTED |
| S-94 | h9-desk | P3 tech-debt backend/tools/smoke-endpoints.mts (about :2408, :2529): TS2322/TS2345 PermissionRole vs membership roles under tools/tsconfig.json; CI does not typecheck tools. | NOT TESTED |
| S-95 | h9-desk | P3 docs/23-app-screens-and-api-gaps.md:891 still labels dispute/cancel BACK_OFFICE after DOS-037 (doc miss). | NOT TESTED |
| S-96 | h8-billing | P3 credit-notes.service.ts:289-294: draft() returns an existing note by id whatever the new payload, so a create with a NEW idempotencyKey and a reused id answers 200 with the earlier note instead of 409 (a convention shared by other modules). | NOT TESTED |
| S-97 | h12-kit | P3 frontend/libs/ui/README.md:214: the "71 of them" token-test count is stale. (S-70 web useEscape and S-71 native RupeeInput pad are still open after DOS-164.) | NOT TESTED |
| S-98 | h11-syncpull | **P1, possibly P0 (verify first)** frontend/libs/offline/src/engine.ts:200: wipe() has no caller. Sign-out keeps the previous user's rows and cursor, and the store is named per app, not per user (sales-app/app/_layout.tsx:299, delivery :299, warehouse :305). A second user on the same phone therefore inherits the first user's local rows and cursor. If that user belongs to another distributor, this is cross-tenant data on the device. | CONFIRMED on web with a persistent store, filed as DOS-167 (P0) |
| S-99 | h11-syncpull | P2 scale catalog.module.ts:30-33: the four global catalog tables are pulled unnarrowed by every device; at lakhs of SKUs this dominates every first sync. | NOT TESTED |
| S-100 | h11-syncpull | P2 sync.service.ts:385: a mid-pass cut carries no overlap. A row stamped by an in-flight transaction before the cut and committed after the read is skipped unless the pass ends within 5 s of it (pre-existing watermark gap). | NOT TESTED |
| S-101 | h11-syncpull | P3 docs/27-offline-sync-client.md:36 says the dev server sends COOP/COEP, but nothing in frontend/ sets it, so every expo start --web walk runs the memory store and re-snapshots on reload. | NOT TESTED |
| S-102 | lean-kit-overlays | P4 frontend/libs/ui/src/native/list.tsx:170: a numeric `secondary` of 0 is dropped by the truthiness check (pre-existing; no register passes 0 today). | NOT TESTED |
| S-103 | lean-kit-overlays (DOS-152 builder) | P3 frontend/libs/ui/src/native/money.tsx: NumberPad's own `flex: 1` (meant for a full-screen pad) may inflate a Sheet when the pad sits inside the Sheet's ScrollView. | NOT TESTED |
| S-104 | lean-sales-entry | P3 frontend/libs/ui/src/web/controls.tsx:42 and native/controls.tsx:77: a kit Button is width 100% off desk with no flexShrink 0, so a non-wrapping Row that pairs it with text splits the row by flex basis (SuggestionRow dodges it with fullWidth={false}). | NOT TESTED |
| S-105 | lean-manager-billing | P4 frontend/libs/ui/src/platform/types.ts (PlatformDocuments.open): no JSDoc says the web implementation must run inside the tap; the rule lives only in documents.web.ts:11 and DOS-026 missed it. | NOT TESTED |
| S-106 | lean-manager-billing | P2 frontend/manager-app/app/billing/index.tsx:181: a packed order whose pack was parked is counted twice in 'left to bill' (the queue and the packs panel both hold it); pre-existing. | CONFIRMED (probe + skeptic, API + screen helpers) — filed as DOS-173 (P3) |
| S-107 | lean-manager-billing | P4 docs/23-app-screens-and-api-gaps.md:229: stale 'NO HTTP path' note for a parked pack; pre-existing. | NOT TESTED |
| S-108 | lean-manager-billing (DOS-026 verifier) | P3 frontend/manager-app/app/fulfilment/load-out.tsx: closing the load-sheet dialog does not bump challanToken, so a challan poll still in flight can open a PDF or show an error against a different sheet reopened within ~60 s (the merge-time fix 144bcfe reworked this flow; recheck). | RE-CONFIRMED by code reading on main (adversarial verifier, 2026-09-14 08:04): the close handler (load-out.tsx ~:357-361) resets challanUrl and the note but never bumps challanToken. So a poll still running for sheet A can show 'Challan ready' on sheet B, and B's Print then opens A's challan (the web Sheet is modal). Rated MAJOR by the verifier (wrong document). One-line fix: challanToken.current += 1 in onClose. Runtime NOT TESTED; fix offered to the founder, not applied. |
| S-109 | lean-manager-billing (DOS-134 verifier) | P4 frontend/manager-app/app/fulfilment/pack.tsx: reopening the pieces sheet shows a blank field instead of the pick already typed for that line. | NOT TESTED |
| S-110 | lean-retailer-shop | P4 backend/libs/database/src/seed-demo/sales.ts:1935: seeded delivered pieces leave out free goods, while core recordDelivered (fulfilment.ts:350) and seed-demo/delivery.ts:514 count them; the seeders disagree on what delivered_qty_pcs counts. | NOT TESTED |
| S-111 | lean-retailer-shop | P4 frontend/libs/ui/src/document-urls.test.ts:160: the identifier rule is file-level, so one `x = absoluteUrl(` in a file lets every other source={x} or documents.open(x) in it pass (pre-existing). | NOT TESTED |
| S-112 | lean-backend-platform | P2 backend/libs/core/src/modules/orders/orders.service.ts:497: cancelInTx expires the approval gates but never closes the bargain_requests they name (no OrderCancelled consumer in pricing), so a cancelled order's rate request stays 'requested' in Rate requests and in the shop's deals.tsx for ever. | NOT TESTED |
| S-113 | lean-backend-platform | P2 REGRESSION SUSPECT on main d1c7da0: DOS-112's PATH_FIELD_REDIRECTS (tenant.guard.ts:350-351) compares the settle path {id} with body.tripId, but the apps' compact OpenAPILink fills {id} from input.id (the new settlement id), so every manager Day-end settle may answer 400. This is a Fable review blocker that was not applied at integration. | CONFIRMED in process: the real client link sent POST /delivery/trips/<settlementId>/settle and the real guard answered 400. FIXED by 468a926 (path {tripId}, redirect removed). Live success NOT TESTED: dos_qa has no closing trip. Live path check PASS: the trip id was in the path and the reply was 409 already settled, not 400. |
| S-114 | lean-backend-platform | P3 backend/libs/core/src/modules/orders/orders.service.ts cancelInTx writes no audit_log row: a manager cancelling a shop's order is still untraced (DOS-028 covered decisions and confirm only). | NOT TESTED |
| S-115 | lean-backend-platform (DOS-160 verifier) | P4 backend/libs/core/src/platform/idempotency.ts: a cached reply counts as a hit only when it is !== undefined, so a future void-output idempotent procedure would replay as a 500. | NOT TESTED |
| S-116 | DOS-167 ruling 1 (out of scope) | S-row — no render test of the field layouts' Chrome wiring (steps = device.waiting/device.end, the sweep over otherIdentities(session), the running ref, the sheet state): frontend/sales-app/app/_layout.tsx:396-419, frontend/delivery-app/app/_layout.tsx:440-460, frontend/warehouse-app/app/_layout.tsx:405-425. A Node test would need expo-router and the kit mocked; the design's Step 8 platform proof (web V5A-D + OPFS listing, Android 1-6 / 1-3, iOS 1-5) is the check and stays mandatory before merge, plus the two cases in notes. | NOT TESTED |
| S-117 | DOS-167 ruling 1 (out of scope) | S-row — a rejected switchDistributor escapes as an unhandled promise rejection from the tap, 'Send now' and 'Switch anyway', with nothing shown: frontend/sales-app/app/_layout.tsx:415 (`void step().finally`), frontend/delivery-app/app/_layout.tsx:457, frontend/warehouse-app/app/_layout.tsx:422; the call is `useSession().switchDistributor` in frontend/libs/api-client/src/react/index.tsx. Pre-existing on main (`void switchDistributor(tenantId)`); not DOS-167's. Fix later: `run` catches and sets the sheet's `note`, or the switcher gets an error line. | NOT TESTED |
| S-118 | DOS-167 ruling 1 (out of scope) | S-row (from the design, restated) — the server-issued cursor carries no device identity: backend/libs/core/src/modules/sync/sync.service.ts:481-493; defence in depth only. | NOT TESTED |
| S-119 | DOS-167 ruling 1 (out of scope) | S-row (from the design, restated, P3) — identity files of people who never return are never swept; and the pre-DOS-167 unkeyed `dos.sales.draft.<retailerId>` keys are left at rest (frontend/sales-app/src/lib/draft.ts). | NOT TESTED |
| S-120 | DOS-167 ruling-1 verifier | P2 frontend/sales-app (useEnqueueOrder): The gate works per engine call, but `useEnqueueOrder` queues one order as several awaited enqueues: the header, then each line. If Sign out lands between two of them, part of the order is kept and part is refused. PROBE-3 (race.jsonl): the header resolved an opId, the line was refused with SyncEngineEndedError, and end({keepQueue:false}) returned {kept:true, pending:1}. The file for Rahul held only the header, and his next sign-in uploaded only 'o-split'. The server accepts a header-only draft (orders.sync.ts applyOrderSync). On screen, the rep sees the generic error, then the sign-in screen. leaveNow still runs forgetDraftsOf (keepQueue param false), so the typed lines are gone and the office holds an empty or partial draft. Nothing is silently deleted and no other person's data is involved; the draft still needs an online submit. Follow-up: queue an order atomically, or have leaveNow consult EndResult.kept before forgetting drafts. | NOT TESTED (sent to DOS-167 ruling 2) |
| S-121 | DOS-167 web proof | P3 The leave sheet and the strip count outbox operations, not changes a rep would recognise. One order with one line queued offline reads '2 changes have not reached the office' and '2 waiting to send', because the header and each line are separate outbox rows. Ruling (n) and the design's proof expected '1 change has not reached the office' for one queued order. A rep with a 10-line order would be told 11 changes. Evidence: v5d-03-queued-offline-desk.png (strip 'Offline since 12:13 am · 2 waiting to send'); v5d-04-leave-sheet-offline-desk.png (title '2 changes have not reached the office'); R.draft in results-v5b-v5c-v5d-v5e.json shows 1 line | CONFIRMED on web (proof run); sent to DOS-167 ruling 2 |
| S-122 | DOS-167 web proof | P3 A write refused because the phone is signing out reaches the sales order screen as the generic 'Something could not be completed. Try again.' instead of the engine's own sentence. Optional amendment (q) is not on main (no SyncEngineEndedError branch in frontend/libs/api-client/src/errors.ts). The guarantee holds (nothing saved, nothing sent), but the rep is told to try again on a phone that is signing out. Evidence: v5e-02-inside-stall-after-place-desk.txt line 18 'Something could not be completed. Try again.'; results-v5b-v5c-v5d-v5e.json V5E afterPlace {genericError:true, engineSentence:false} | CONFIRMED on web (proof run); sent to DOS-167 ruling 2 |
| S-123 | DOS-167 web proof | P1 REGRESSION FROM DOS-167 (tracked under DOS-167, not a new id): Regression from DOS-167 on web: the per-person store names never open on expo-sqlite web, so every web field app silently runs on a memory store, and the 'Sign out, keep here' promise loses the order. storeNameFor() (frontend/libs/offline/src/engine.ts:62) builds 'dos-sales__u-<uuid>__t-<uuid>.db', 92 characters (96 for dos-warehouse). wa-sqlite's AccessHandlePoolVFS in expo-sqlite web rejects any databasePath longer than 56 characters ('./' + name, so names of 54 characters or more) with 'Error: sqlite3_open_v2'. openStore() (frontend/libs/offline/src/store/open.web.ts) swallows the error and returns createMemoryStore(). Consequences on a cross-origin-isolated web build (the hosted configuration, the only one with a persistent store): (1) the offline copy is not persisted, although pre-fix 'dos-sales.db' was; (2) an order queued offline and kept with 'Sign out, keep here' is gone. The sheet said 'They stay on this phone for Rahul Deshmukh only and go the next time Rahul Deshmukh signs in here'; on the next sign-in nothing was uploaded and dos_qa has 0 rows for that order id. (3) The DOS-167 web proof on a persistent store (V5A–V5D file walk, the cold-start case) cannot run, so the no-leak results on web only show the memory fallback. The same prefix scheme is used by delivery-app and warehouse-app (STORE_PREFIX 'dos-delivery' / 'dos-warehouse' with openStore), not walked here. Rated P1 rather than P0: the loss needs web + offline + sign-out, and the beat screen does say 'This browser will not keep the offline copy after you close it'. It is still data loss against an explicit on-screen promise, so the judge may raise it. Suggested fix (product change, needs approval): a lossless name within 54 characters, for example the two UUIDs as 22-character base64url (~48 chars with a short prefix), or a per-identity directory; plus a test that opens every storeNameFor() output through the web adapter, and a logged fallback instead of a silent one. Evidence: QA/evidence/batch2/dos-167/web/ (diag-02-worker-channel.json (open './dos-sales.db' error null; open './dos-sales__u-8760e17e-…-0099.db' error at +3 ms); diag-03-sqlite-web-path-length.json and diag-04-sqlite-web-path-length-threshold.json (lengths 14–56 open, 57–70 and 94 fail with 'Error: sqlite3_open_v2'); results-v5a.json and results-v5b-v5c-v5d-v5e.json (every OPFS header empty after sign-in, beat screen text 'This browser will not keep the offline copy after you close it'); v5d-04-leave-sheet-offline-desk.png; V5D sqlOrder '(0 rows)' for 01a09c14-ab1b-75bf-b262-3889d0aac1cb; V5D d4Uploads 0; pre-fix comparison QA/evidence/batch2/s-098/results-v5a-v5b-v5c.json (notPersisted=false, expo-sqlite/ in use)) | CONFIRMED on web (proof run) — repair: DOS-167 ruling 2 |
| S-124 | DOS-167 ruling 2 (out of scope) | S-row (new, P3): the connection strip cannot say 'not kept in this browser' — the kit's ConnectionState (frontend/libs/ui/src/types.ts:353) has no persistence field and docs/27 §2 claims a sentence the strip does not render; a kit field and the strip copy are a UX-00 §6.11 item, not DOS-167's. | NOT TESTED |
| S-125 | DOS-167 ruling 2 (out of scope) | S-row (new, P3 copy): on a memory store the sales order screen still says 'Saved on this phone' (orders/new.tsx) although the beat screen says the browser will not keep it; reword from status.persistent when the strip item is done. | NOT TESTED |
| S-126 | DOS-167 Android proof (warehouse) | P2 frontend/warehouse-app (debug APK 2026-09-07, JS 199952b): native crash on the first cold start after `adb install -r` + `pm clear`, before any sign-in — SIGSEGV (SEGV_ACCERR) on mqt_v_js in libreactnative.so MountingCoordinator::pullTransaction ← FabricUIManagerBinding::schedule. The next launch was normal. Not attributed to DOS-167. Evidence: QA/evidence/batch2/dos-167/android/w1-crash-logcat.txt, w1-launch-diagnosis.txt. | SEEN ONCE — re-try in the ruling-2 re-proof |
| S-127 | DOS-167 Android proof (delivery) | P3 frontend/delivery-app home: after an offline relaunch with a completed pull (cursor and lastPulledAt set, all 10 stops local), it still says 'Still filling this phone from the office — this may not be the whole trip yet.', and during a first pull the strip says 'Updated just now' while the home says still filling. The cause is not proven either way; DOS-167 ruling 1 (o) is a candidate. Evidence: QA/evidence/batch2/dos-167/android/d5a-offline.txt, d5b-queue-stop.txt, d3-sachin-4.png. | CONFIRMED on Android — cause comparison in the ruling-2 re-proof (addendum x) UPDATE 2026-09-14 re-proof: delivery 3/3 on main bafb7b5 AND the same on pre-DOS-167 JS 144bcfe, so it PREDATES DOS-167; a warehouse variant was also seen (PICK-0078 sheet 'Still filling this phone from the server', not compared). Stays P3. |
| S-128 | DOS-167 Android proof (delivery) | P1 REGRESSION FROM DOS-167 (tracked under DOS-167, not a new id): the delivery app crashes natively right after 'Sign out, keep here', 2 of 2. SIGSEGV in libexpo-sqlite.so exsqlite3_reset ← NativeStatementBinding::sqlite3_reset ← AsyncFunctionComponent, most likely a store call racing the close. No data lost: the kept file is intact and the op was sent once at the owner's next sign-in. Evidence: QA/evidence/batch2/dos-167/android/d5c-crash-logcat.txt, d6b-repro-keep-crash.txt, d5c-after-keep-diagnosis.txt. | CONFIRMED 2/2 on Android — repair: addendum (x) in the ruling-2 build |
| S-129 | DOS-167 iOS proof | P3 frontend/libs/ui native Sheet (iOS): the account menu and the DOS-167 leave sheet each appear as ONE XCUIElementTypeOther covering the whole screen, with every row's text joined into one label and no child elements. 'Sign out', 'Sign out, keep here' and 'Cancel' cannot be targeted one by one (VoiceOver is likely affected too; not run). The same in the sales, delivery and warehouse menus. It predates DOS-167. Evidence: QA/evidence/batch2/dos-167/ios/2-rahul-menu-src.xml, 5d-amit-leave-sheet-src.xml, 2-rahul-signout.txt. | CONFIRMED in the XCUITest tree; VoiceOver NOT TESTED |
| S-130 | DOS-167 iOS proof | P0 ON THE DOS-167 PATH (tracked under DOS-167, not a new id): on iOS, tapping 'Sign out, keep here' crashes Expo Go natively, 2 of 2 — SIGSEGV EXC_BAD_ACCESS at 0x1000000bb on expo.module.sqlite.AsyncQueue in exsqlite3_reset, the same fault as S-128. The sign-out never completes: a relaunch is still signed in as that rep, so the next person on a shared phone is inside their session. No data was lost; both kept orders reached dos_qa once. Evidence: QA/evidence/batch2/dos-167/ios/5e-crash-expo-go-2026-09-14-021827.ips, 5r-crash-expo-go-2026-09-14-022331.ips, 5e-crash-summary.txt, 5r-crash-summary.txt, 5e-simulator-log-02-18.txt. | CONFIRMED 2/2 on iOS — repair: addenda (x) and (y) in the ruling-2 build |
| S-131 | DOS-167 ruling-2 merge review | P1 ON THE DOS-167 PATH (tracked under DOS-167): Settings > Sign out in frontend/delivery-app/app/settings.tsx:285 and frontend/warehouse-app/app/settings.tsx:165 calls useSession().signOut() directly and bypasses the leave flow: no sheet, no end(), no sibling sweep; the session is cleared only after the server revoke (up to 20 s); on a browser memory store it throws away queued deliveries and picks (against answer A and ruling t). Pre-existing since 2f8a5a5 / 335a11c. | Found by review (code reading) — repair: addendum (z1) in the ruling-2 lane |
| S-132 | DOS-167 ruling-2 merge review | P2 frontend/libs/ui/src/platform/storage.native.ts:36-45: setItemSync fires SecureStore writes without awaiting them, so a token refresh applied just before the tap may land after clearSession's delete; together with a crash inside end() before the revoke, a relaunch could be signed in. The fix is a per-key write chain (a kit change). Checked on device only by the ruling-2 re-proof walk (SecureStore ordering). | NOT TESTED |
| S-133 | DOS-167 ruling-2 merge review | P3 frontend/libs/offline: a forced sign-out (refresh answered 401) on a memory store drops the queue without a word; docs/27 §14 needs the row (and ruling 2's strip persistence field, S-124). | NOT TESTED |
| S-134 | DOS-167 ruling-2 integration gate | P3 PROCESS/REGRESSION: main was red since lean wave 1. The DOS-026 merge-time fix 144bcfe (manager load-out.tsx:236, :464) opened the challan URL through nextChallanPoll and state, so @dos/ui src/document-urls.test.ts could not see absoluteUrl() and failed. The lean integrator re-ran only the group's own test files. Behaviour was correct: the URL was already absolute. | CONFIRMED on main (guard test run 05:19) — FIXED on main 1f8e0c3 (inline absoluteUrl, guard green, manager-app typecheck, eslint and prettier clean); later lean integrators run the kit guards Follow-up 9532a67: the challan URL is absolutised once into readyUrl, with no raw fallback (replaces 1f8e0c3's absoluteUrl(x) ?? x); verifier UPHELD. |
| S-135 | DOS-167 re-proof (Android warehouse) | P2 NOT DOS-167: after an offline relaunch the warehouse picker cannot reach a picklist already on the phone. Home 'WAVES ON THE FLOOR' and the Pick tab's 'WAVES' / 'ORDERS WAITING' are online reads that show only 'No connection. This is not the current picture.'; PICK-0078 opened only by deep link and then worked offline. A picker whose phone restarts in a dead corner cannot continue a wave. Evidence: QA/evidence/batch2/dos-167/reproof/android/wk-a-dinesh-offline-pick.txt. | CONFIRMED on Android — to be filed as a DOS block at the next approval gate |
| S-136 | DOS-167 re-proof (Android sales, warehouse) | P3 debug builds: LogBox 'Console Error: Can't perform a React state update on a component that hasn't mounted yet' (ExpoRoot.js:135, ContextNavigator) on cold start and sign-in; its toast covers the tab bar and swallows taps. Also in the 199952b proof. Evidence: QA/evidence/batch2/dos-167/reproof/android/logbox-console-error-logcat.txt. | CONFIRMED on Android debug builds; release NOT TESTED |
| S-137 | DOS-167 re-proof (web) | P3 a sales-service call (e.g. GET /sync/manifest) answers 401 about 5-24 s after sign-in, then /auth/refresh succeeds and nothing breaks; the pre-DOS-167 S-098 probe shows the same (15 times). Cause not investigated. Evidence: QA/evidence/batch2/dos-167/reproof/web/results-vmem.json. | CONFIRMED on web; cause NOT TESTED |
| S-138 | DOS-167 re-proof (web) | P1 ON THE DOS-167 PATH (tracked under DOS-167): on web with a persistent OPFS store, the sales device database sometimes never opens when the expo-sqlite chunk and worker arrive late (the first load after a Metro start; 600 ms added latency): 'SQLiteError: not a database', the 6-file pool filled with orphan temp files and '/dos-sales.db-wal', no /sync call for 240 s, beat empty, the next person's open fails too. 4 of 4 slow loads failed; the control passed. The judge suspects a clean-up step DOS-167 added. Evidence: QA/evidence/batch2/dos-167/reproof/web/run3-v5a/, diag-01..03. | CONFIRMED on web (Metro dev server); production export NOT TESTED — repair: DOS-167 ruling 3 |
| S-139 | DOS-167 re-proof (web, Android) | P3 ON THE DOS-167 PATH: no field app passes onLog to OfflineProvider, so every 'offline:' line is silent — ruling (t)'s 'no persistent store; running in memory' on web and ruling (s)'s 'kept the store from before ruling 2' on Android. The on-screen line and storeNote work. | CONFIRMED on web and Android — repair: ruling 3 |
| S-140 | DOS-167 re-proof (web) | P3 ON THE DOS-167 PATH: on a persistent web store the beat screen shows 'This browser will not keep the offline copy after you close it' for 39-82 ms after every sign-in, because status().persistent reads false while the store is still opening (engine.ts:919). | CONFIRMED on web (50 ms DOM watch) — repair: ruling 3 |
| S-141 | DOS-168..170 merge reviews (money-1 §Defects outside the lane, money-2) | P2 MONEY, code reading only, NOT TESTED: `collections.list` still totals `collections` rows (backend/libs/core/src/modules/receivables/collections.service.ts:193-199), but a doorstep payment taken offline and synced writes a receipt without such a row. After DOS-168..170 merge, the accountant's collections screen can therefore under-state what the crew actually took. Both binding reviews ordered it filed as its own finding. | NOT TESTED — needs a probe, then a DOS block for approval |
| S-142 | DOS-168..170 merge reviews (money-1, money-2) | P2 SCALE/PRIVACY, code reading only, NOT TESTED: the delivery device pulls 90 days of WHOLE-TENANT receipts (backend/libs/core/src/modules/receivables/receivables.module.ts:48-54) rather than the crew's own, so every delivery phone syncs the whole distributor's collection history. docs/20 scale rules and the crew's need-to-know both argue against it. Both binding reviews ordered it filed. | NOT TESTED — needs a probe, then a DOS block for approval |
| S-143 | DOS-168..170 merge-gate ruling (Fable, 2026-09-19) | P1 MONEY/DATA LOSS, code reading only, NOT TESTED: the delivery phone's tray offers **Discard** on a receipt op the server refused with `trip_settled`. Discarding erases the phone's only record of cash a shop has already paid — the exact thing founder answer A (2026-09-14) forbids: nothing is thrown away. The refused op should be kept and routed to the cashier, never offered for deletion. | NOT TESTED — needs a probe, then a DOS block for approval |
| S-144 | DOS-168..170 merge-gate ruling (Fable, 2026-09-19) | P2 MONEY DISPLAY, code reading only, NOT TESTED: D8's settlement preview is fetched once per mount and never refetched when the outbox drains or the desk settles, so a stale 'money still on the phone' note can stand beside an ENABLED check-in button until the screen is re-opened. | NOT TESTED — needs a probe, then a DOS block for approval |
| S-145 | DOS-168..170 merge-gate ruling (Fable, 2026-09-19) | P2 MONEY ARITHMETIC, code reading only, NOT TESTED: the D8 'held money' figure is totals-minus-totals, so it mis-states what the phone holds when a crew runs two phones (driver plus helper) or when a desk receipt has not been pulled. Bounded today by the outbox-based check-in gate, which is why it is P2 and not P1. | NOT TESTED — needs a probe, then a DOS block for approval |
| S-146 | `pnpm smoke --run-tag money-1` on a fresh seeded database, main 6e5c7c5 (Fable ruling item 1) | **P1 MONEY, CONFIRMED BY EXECUTION.** A receipt can be created against a `tripId` that exists in NO row of `trips` — `receivables.receipts.create` does not validate it, and receipts→trips is an upstream reference so there is no FK to catch it. The settled-trip deposit guard (DOS-132 `460269e`, hardened by the money lane `9c44458`, receivables.service.ts:1006) then refuses that receipt FOREVER with `trip_cash_not_settled`, because the trip it names can never settle. Net effect: money can be collected against a non-existent trip and is then permanently un-bankable. Seen as RCPT-9006/9007: `select count(*) from trips where id='01a06d0b-…'` returns 0, and a LEFT JOIN leaves their trip_no NULL while every seeded RCPT-VAN-* joins. The published contract example is what supplied the bad id. | CONFIRMED on a fresh seeded database, both runs — needs a DOS block for approval |
| S-147 | same smoke run | P1 SERVER FAULT, CONFIRMED: `delivery.deliveries.addPod` answers **500** 'proof insert returned nothing' on a fresh seeded database, both runs. Stack: `DeliveriesService.writePod` (deliveries.service.js:404) → `addPodInTx:199` → `runIdempotent`, thrown as INTERNAL_SERVER_ERROR. Body was the published example: `{evidence:{kind:'geo',payload:{distanceM:40},lat:19.2437,lng:73.1355}, id:…}` to POST /delivery/deliveries/{id}/pod. A proof of delivery that 500s is a delivery the crew cannot close. | CONFIRMED on a fresh seeded database, both runs — needs a DOS block for approval |
| S-148 | same smoke run | P1 SERVER FAULT, CONFIRMED: `delivery.consents.grant` answers **500** with no message on a fresh seeded database, both runs. Body: `{granted:true, noticeVersion:'gps-notice-2026-09', locale:'en-IN'}` to POST /delivery/consents. The driver's GPS consent is the gate the trip cannot depart without. | CONFIRMED on a fresh seeded database, both runs — needs a DOS block for approval |
| S-149 | same smoke run | P3 DEMO DATA, CONFIRMED: `notifications.messages.markRead` answers 404 for the manager and the warehouse on a fresh seeded database, both runs — the published example names a message id the seed does not create. Fixture or example, not a product fault. | CONFIRMED on a fresh seeded database, both runs — fix the example or the seed |
| S-150 | same smoke run (harness) | P2 QA TOOLING, CONFIRMED: re-running `pnpm smoke --destructive` with the SAME `--run-tag` against a database the first run already mutated silently guts coverage — 328 of 1606 operations skipped (vs 23 on a clean pass), because `delivery.trips.create` skips on a missing fixture and cascades through start-loading, depart, stops, collections, settle and return. The totals still read healthy, so **collections and settle were never exercised at all** and nothing said so. The run should fail loudly when it skips a large fraction. | CONFIRMED — fix in backend/tools/smoke-endpoints.mts |
| S-151 | DOS-167 ruling-3 re-proof (web, judge 2026-09-19) | **P2 HONESTY, CONFIRMED 3/3 on web.** When the browser cannot keep an offline copy and the app falls back to memory, the sales app says so on the BEAT screen ONLY. Shops, Orders, Me and Settings show the connection strip reading 'Updated just now' and nothing about the store — and the order screen says the OPPOSITE, labelling its button 'Save on this phone' and then 'Saved on this phone' over an order queued into a store that dies with the tab. A rep who works on Shops and Orders all day is never told the phone keeps nothing. The ruling-3 merge review asked for exactly this to be ruled out ('the app now looks healthy while keeping nothing'); it was executed and it fails. Never walked at all on delivery or warehouse, on any platform. | CONFIRMED on web sales — needs a design and a DOS block for approval |
| S-152 | DOS-167 ruling-3 re-proof (Android, judge 2026-09-19) | **P2 HONESTY, CONFIRMED on Android.** The sales order screen's outcome banner is bound to the LIVE connection state, not to what happened to that order: `frontend/sales-app/app/orders/new.tsx:387-388` picks `title`/`meta` from `local.online` at render time. An order saved on the phone in a dead spot shows the honest 'queued' banner, and the moment the radio returns while the rep is still on that screen the SAME banner re-renders as 'Order placed — The office has it, with its number and its price' over an order the office still holds as a draft with no number. | CONFIRMED on Android sales — needs a DOS block for approval |
| S-153 | Q3 repair analysis on a copy of the founder's `dos` (2026-09-19) | **P1 FIXTURE, CONFIRMED BY QUERY.** The demo seed builds trip settlement figures from `collections`, but the live `settleTrip` reads `receipts` — so on **44 of 48 settled trips** the stored `trip_settlements` row disagrees with what the real code would compute (divergence: Kalyan ₹2,48,114.37, Sai ₹2,38,202.89, Tarsun ₹2,26,218.48), and **33 `collections` rows contradict the very receipt their `receipt_id` points at**. Any test or walk that judges settlement behaviour against seeded data is judging the wrong number. Not the founder's money — demo rows — but it makes `dos`, `dos_qa` and every template copy unsafe as a settlement fixture. | CONFIRMED on a copy of dos — belongs in the queued demo-data slice (docs/23 §10) |
| S-154 | Q3 repair analysis on a copy of the founder's `dos` (2026-09-19) | P2 FIXTURE, CONFIRMED BY QUERY: the seed writes journal rows directly instead of going through the posting service, so (a) `journal_entries.reversed_by_entry_id` is NULL on every reversed original (`stampReversed` never ran), (b) a bounce is posted as the mirror receipt's own `ref_type='receipt'` entry and there are ZERO `receipt_reversal` entries, and (c) 44 trip-tagged cash receipts post to office CASH rather than CASH_VAN because `receiptAccountCode()` never ran on them. The books still balance to the paisa, so this is fixture authorship, not a money fault — but a query keyed on `receipt_reversal` would report every bounce as unreversed. | CONFIRMED on a copy of dos — belongs in the queued demo-data slice |
| S-155 | DOS-167 amendment integration (2026-09-19) | **P1 QA TOOLING, CONFIRMED BY OBSERVATION.** The kit's cross-app guards (`document-urls.test.ts`, `parity.test.ts`, the new `dos-167-persistent-null` guards) read files OUTSIDE their own package — `frontend/*-app/app/**` and `src/**` — but turbo's input hash for `@dos/ui`'s `test` task does not cover those paths. So a turbo CACHE HIT reports the cross-app guards green without ever running them against the tree that changed. This is the same class of gap that let a wave-1 integration turn main red (S-134): the gate says pass, the guard never looked. Every integrator since has had to remember `--force`; the fix is to declare those paths as inputs of the task so the cache cannot lie. | CONFIRMED (9/9 tasks FULL TURBO on a tree whose app files had just changed) — fix in frontend/turbo.json |
