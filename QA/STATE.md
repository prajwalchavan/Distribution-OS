Stage: 1
Current phase: 2 — Cross-role end-to-end business flow: RUN (holds with findings, `QA/09-cross-role-workflows.md`); **its six P1s are ALL MERGED** (DOS-185, 186, 191, 195, 196, 197 + DOS-203/204). Next: the business simulation (`QA/24-simulation-design.md`) on a fresh seed of main. Batch 2 (Phase 4) merged: 155 of 158.
Last updated: 2026-09-21, 21:40 IST — day 1 of seven (live target Sat 27 Sep)

**This file is CURRENT STATE ONLY.** Every history — what was found, what was ruled, what was merged and why — lives in `QA/13-change-log.md`. Findings live in `QA/findings/12-batch2-new-findings.md`. Founder decisions live in `docs/22-source-of-truth.md` §8; **read docs/22, never a note in here, for what the founder has decided.** Twice on 2026-09-19 a stale note in this file sent work down a wrong path (eleven "open" questions docs/22 had already answered on 09-13, and a Q3 money repair on a database that holds no real money). If this file and docs/22 disagree, docs/22 wins and this file is wrong.

## RESUME HERE — exact state at 2026-09-21 17:40 IST (founder: "before the weekly budget ends, keep status recorded")

**Merged to main today (all pushed):** deploy plumbing `628caa7` · D8 display `6a23b39` · S-176/S-177 `7247d3f` (migrations 0060/0061) · **the one app `a75e13b`** (six apps → `frontend/dos-app`; extra roles on the wire) · DOS-185 free goods `3cbaa34`. Docs/22 §8 carries every ruling; the rendered page is republished.

**IN FLIGHT — read this first:**
1. ~~The undelivered merge~~ **DONE — `qa/p2-undelivered` merged `bee8a37` and pushed** (gates: libs build, READMEs identical, core typecheck 0, orders 58/58, free-goods 8/8, receivables 44/44, claims 20/20, dos-app typecheck 0 and 490/490). Main's last migration is now **0062**. Lane DB dropped, worktree removed.
2. ~~Stock lane~~ **DONE — `qa/p2-stock` merged `5032b31` and pushed** (migrations 0063/0064/0065; verifier pass, S-193 carried). **All six Phase 2 P1s are on main.** Worktree and lane DB to remove.
3. **BUSINESS SIMULATION RUNNING — `wf_48284851-891` (task `wf7oreja7`), launched 2026-09-21 20:20 IST** on a fresh seed of main (`dos_test_sim`, migrations to 0065), worktree `.claude/worktrees/sim`, branch `qa/simulation`, API :3300 + web export :5300 (`--clear`, S-192): Stage → seven day agents in sequence (each person in the one app, in the Browser pane, one action-log line per action with the numbers off the screen and the wire) → a blind auditor recomputing the books from SQL (`opening + receipts − sales − damage − returns = closing` per SKU per batch; `revenue = payments + outstanding`; any drift is P0). Evidence lands in `QA/evidence/simulation/` on `qa/simulation`; findings as DOS-2xx in its `findings.md`. **The architect (Fable, Tuesday) judges `audit.md`** and merges the evidence branch. If the run dies: resume with `Workflow({scriptPath: ".../workflows/scripts/business-simulation-wf_48284851-891.js", resumeFromRunId: "wf_48284851-891"})` (completed days replay from cache).
4. **Oracle A1 retry loop** (background task `b0ndejk31`, `scratchpad/a1-retry.sh`, log `scratchpad/a1-retry.log`): 39 attempts, all "Out of host capacity" (Mumbai). On success it writes `INSTANCE=` and `PUBLIC_IP=` to `scratchpad/oci-ids.env`. If this session dies the loop dies with it — restart it (`nohup scratchpad/a1-retry.sh &`) or re-create from STATE: network already built (VCN `dos-vcn`, subnet `dos-public`, IG, security list 22/80/443), image `Canonical-Ubuntu-24.04-aarch64-2026.08.25-0`, AD `qfvI:AP-MUMBAI-1-AD-1`, shape `VM.Standard.A1.Flex` 2 OCPU/12 GB (fallback 1/6), SSH key `~/.ssh/dos_oracle`.
5. **Website:** `frontend/scripts/pages-deploy.sh dos` publishes the one app to Cloudflare Pages (`dos`, `www.distributionos.in`), but not before the API is reachable at `api.distributionos.in` (needs the VM). Cloudflare token `~/.config/dos/cloudflare.token` (rotate after go-live). Owed by hand on go-live day (token lacks Zone Settings:Edit): SSL Full, Always Use HTTPS, min TLS 1.2.
6. **Docker build-and-run proof** still owed (never built anywhere; Mac RAM). Take it on the VM's first `docker compose up` or on the Mac when idle.
7. **Fable sittings left:** Tuesday (review the stock merge + launch the simulation) and Saturday (final audit + handover). Everything else runs on Opus.

**HOSTED TEST PREVIEW (S-192: the first publish shipped a cached bundle with `127.0.0.1:3210` inside — caches cleared, founder re-publishes), 2026-09-21 18:40 IST (founder: "share me urls logins to test hosted app"):** database `dos_test_hosted` (from the template, migrated to 0062, seeded); the all-in-one runs on this Mac at :3100 against it (background task `brmh5rxqm`, `NODE_ENV=development`, `WORKER_INLINE=1`, `CORS_ORIGINS=https://www.distributionos.in,https://distributionos.in,https://dos-7ij.pages.dev`); a Cloudflare **quick tunnel** (background task `bkk7kk0mi`, `cloudflared tunnel --url http://127.0.0.1:3100`) exposes it at `https://philosophy-administration-encouraging-want.trycloudflare.com` — `/health` 200 from the internet. **Both die with this session; the quick-tunnel hostname changes on every restart (then the web build must be redone).** **PUBLISHED by the founder from their terminal (2026-09-21 evening): Pages deployment `f567b3c9.dos-7ij.pages.dev` on branch `main` (the second publish, after S-192's fix — the bundle now carries the tunnel URL, verified by grep; the first, `52bb8857`, carried `127.0.0.1:3210`) → https://www.distributionos.in and https://distributionos.in serve the one app (Welcome screen verified in the Browser pane, no console errors).** The publish command lives in this session's reply of that evening: `DOMAIN=distributionos.in PAGES_PROJECT=dos PAGES_BRANCH=main EXPO_PUBLIC_API_URL=<tunnel> EXPO_PUBLIC_AUTH_URL=<tunnel>/auth ./scripts/pages-deploy.sh dos` with `CLOUDFLARE_API_TOKEN` from `~/.config/dos/cloudflare.token` and `CLOUDFLARE_ACCOUNT_ID=6730952ae1e2212f14c52d66a5339d35`; the tool's permission classifier refuses to run it from here (it exposes the founder's machine and publishes), so the founder runs it. Stable `api.distributionos.in` needs the API token to gain **Account → Cloudflare Tunnel: Edit** (the tunnel create returned code 10000); then: create tunnel `dos-mac` by API, CNAME `api` → `<id>.cfargotunnel.com`, ingress → :3100, rebuild the web export with the stable URL. Logins for the preview: the seed's (`sunil.tarsun` owner, `vikas.kadam` manager, `meena.joshi` accountant, `rahul.deshmukh` rep, `dinesh.patil` warehouse+delivery, `ganesh.more` delivery, `ramesh.gupta` shop; all `Dos@1234`).

**KEEP THE HOSTED APP RUNNING WITHOUT A CLAUDE SESSION (founder, 2026-09-21 evening):** `backend/infra/mac-host/serve-from-this-mac.sh` (committed) installs three user LaunchAgents — `in.distributionos.api` (all-in-one :3100, test DB, KeepAlive), `in.distributionos.tunnel` (cloudflared; NAMED tunnel if `~/.config/dos/tunnel.token` exists, else a QUICK tunnel whose address changes on restart), `in.distributionos.awake` (caffeinate) — then republishes the website against the tunnel address. The permission classifier refuses to write LaunchAgents from this session, so **the founder runs it once**: `cd ~/Desktop/Distribution\ OS && bash backend/infra/mac-host/serve-from-this-mac.sh`. This session's own API/tunnel background tasks (`brmh5rxqm`, `bkk7kk0mi`) are killed by the script (it frees :3100/:3101). **Stable `api.distributionos.in` — WIRED 2026-09-21 21:15 IST:** the founder added *Cloudflare Tunnel: Edit* to the token; named tunnel `dos-mac` id `123a733e-2310-425f-9cf9-25eb500891e0` created by API, its run token at `~/.config/dos/tunnel.token` (600), ingress `api.distributionos.in` → `http://127.0.0.1:3100`, CNAME `api` → `123a733e-…cfargotunnel.com` proxied. **RUNNING UNDER LAUNCHD since 2026-09-21 21:25 IST** (the founder ran the script): `in.distributionos.api` (pid 31398, :3100 200), `in.distributionos.tunnel` (named, 4 connections registered, `https://api.distributionos.in/health` 200), `in.distributionos.awake`; logs in `~/Library/Logs/dos/`. **The site is republished against `https://api.distributionos.in` — verified 21:40 IST by grep of the live entry bundle (2 × `api.distributionos.in`, no trycloudflare); www 200, api 200.** Simulation: staged; day 1 done-with-findings (27 actions, DOS-211…); day 2 running. This session's own API/tunnel tasks are gone. Nothing about the hosted preview depends on a Claude session any more; a reboot brings it all back at login (lid open, plugged in). (Original plan for reference: create the named tunnel by API (`POST /accounts/{id}/cfd_tunnel` name `dos-mac`, `config_src: cloudflare`), write its token to `~/.config/dos/tunnel.token` (600), set the ingress (`PUT …/cfd_tunnel/{id}/configurations`: `api.distributionos.in` → `http://127.0.0.1:3100`), CNAME `api` → `<id>.cfargotunnel.com` proxied, and rerun the script — one last republish, then the address never changes until the Oracle VM.

**Carried findings from today's merges:** S-185..S-191 (all P2/P3), S-189 (sign-out sweep, after go-live). **Founder still owes:** gated-or-public; the real data extract if it is coming; docs/22 §10's three cut-over items.

**The plan is now SEVEN days (founder, 2026-09-21 evening): role election and the one app land BEFORE go-live, website included, and the simulation runs on the merged app.** Live by Sat 27 Sep if Thursday's books balance. `QA/10-DAY-PLAN.md` top section; page https://claude.ai/artifact/5c7mWeutvCDtEEsiujS1ei. **The seven per-role web apps are retired at the merge** — founder confirmed.

**Tomorrow's first act (day 2):** read `wgar6wy16`'s branch as the architect, merge role election; read `docs/31-one-app-layout.md` and the skeptics' amendments, approve; launch the one-app merge lanes (one per group, then the root layout + strings, then guards and gates).

**Three sign-in decisions today, designed in `docs/29-sign-in-roles-and-one-store-app.md`, recorded in docs/22 §8:** (1) Welcome + landing — building now; (2) **role election at sign-in, downward only** (owner may act as any staff role, manager as the field roles, others as their own plus owner/manager-set *extra roles*; the token carries the elected role so nothing on the server changes) — **not before the simulation**, day 4 if free, else after go-live, Fable reviews it; (3) **one store app** that becomes the right app after sign-in — after go-live, after (2). The seven web apps stay for the browser.

**Hosting, decided on verified terms (`wf_e2039cff-1b2`, 2026-09-21):** free managed Postgres is IMPOSSIBLE for this schema (`CREATE ROLE app_worker BYPASSRLS` needs a superuser; Neon, Supabase and RDS withhold it) → Postgres self-hosts on **Oracle Cloud Always Free** (now 2 OCPU / 12 GB ARM, halved on 2026-06-15 without announcement; home region Mumbai, irreversible; idle reclamation is the real risk — mitigate by PAYG-inside-free-limits and off-Oracle backups). The seven apps on **Cloudflare Pages** (unlimited bandwidth, no card). Domain **`distributionos.in`** (checked available; ~₹690/yr at Porkbun, same at renewal). **There is no working path from the repo to a server today**: the Dockerfile has never been built and has a definite COPY bug, no compose/Caddy/migrations step/backups/secrets/static pipeline, Docker not installed on the Mac, and builds must NOT run on the free VM (OOM) — build on the Mac or CI, ship artifacts. Full findings in the run's output; the deployment plumbing is its own lane, next.

**Oracle, 2026-09-21 12:30 IST:** account `distributionos` live, API key working (`~/.oci/config` + `~/.oci/dos.pem`, both 600; `oci-cli` 3.93.0 via Homebrew). Network BUILT in Mumbai AD-1: VCN `dos-vcn` 10.0.0.0/16, internet gateway, default route 0.0.0.0/0, security list open on 22/80/443 + ICMP 3/4, public subnet `dos-public` 10.0.1.0/24; instance SSH key `~/.ssh/dos_oracle`. **The A1 ARM launch returned `Out of host capacity` (HTTP 500) — Mumbai's free ARM pool is empty right now.** This is a regional queue, not an account or billing problem, and it costs nothing. A retry loop is running (`scratchpad/a1-retry.sh`, background `b0ndejk31`): every 4 min, 2 OCPU/12 GB, falling back to 1 OCPU/6 GB on every fifth attempt, up to 200 attempts (~13 h); on success it appends `INSTANCE=` and `PUBLIC_IP=` to `scratchpad/oci-ids.env` and logs to `scratchpad/a1-retry.log`. Nothing in the seven-day plan blocks on it — Cloudflare Pages serves the website either way and the API can stay on the Mac until the VM lands. Ids: tenancy `ocid1.tenancy.oc1..aaaaaaaaxu3wqjnbiolvedt3b6dki322id7txg4tk5au4wz7h5hg55kome3q`, image `Canonical-Ubuntu-24.04-aarch64-2026.08.25-0`.

**Deploy plumbing MERGED, 2026-09-21 13:05 IST (`628caa7`, lane `qa/b2-deploy-plumbing`, 22 commits, architect merge):** `backend/infra/` now holds the Dockerfile (COPY collapse fixed, `--legacy` deploy, argon2 from source, non-root, HEALTHCHECK, `node:24-alpine`, `--platform linux/arm64`), `compose.prod.yml` (db, one-shot migrate, app, Caddy; only 80/443 published), `Caddyfile` (`api.<domain>`, Let's Encrypt, HSTS), `bootstrap.mjs` (production bootstrap, not the dev seed), `backup.sh`/`restore.sh` (nightly pg_dump to R2, restore drill run against `dos_test_b2_deploy` and proven to fail on `--no-acl`), `gen-secrets.sh` (writes `.env.prod`, refuses twice), `.env.prod.example` (52 variables); `frontend/scripts/pages-deploy.sh` (one Pages publish per app; `dos` exports the empty prefix); `.github/workflows/image.yml` (arm64 image to GHCR); `docs/30-deploy-runbook.md` (22 command blocks: 4 `[run here]`, each run verbatim; 18 `[not run here: …]`); docs/26 §9 as-built. Ten specs in `backend/all-in-one/src/deploy/` (47 pass, 3 opt-in skipped). Verifier: pass, three minors — two fixed at the merge (`ba16eb3`: the header guard for never-list #12, proven red; 55→52), the third already www. on main. **STILL OWED — the docker build-and-run proof:** never built on any machine; the Mac never had the headroom (0 of 13 checks in an hour, swap 83–97 %). `docker-image.proof.spec.ts` behind `DOS_DOCKER_PROOF=1` is the proof; the Dockerfile header says NOT YET BUILT and `dockerfile.spec.ts` now fails if that claim changes before docs/30 §12 records a date. Take it either on the Oracle VM's first `docker compose up` or on this Mac once the Android lanes are idle (colima 2 CPU / 3 GB).

**Walk-findings fixes, 2026-09-21 13:40 IST (`wf_bdb752e3-de1`):** (1) **D8 display lane MERGED** (`6a23b39`, lane `qa/b2-d8-display`, delivery-app only): S-168/S-169/S-183/S-184 fixed, verifier pass with five minors → S-169's default route recorded as an architect ruling in docs/22 §8 (D8 opens on the owed trip; the founder may reverse), the other four carried as **S-185..S-188** (S-185 is P2: the offline hand-over estimate ignores expenses booked online). (2) **S-176/S-177 lane FAILED verification** (`qa/b2-tax-and-leak`, 5c8d583): the fix itself is right — one live GST rate per HSN, sub-headings 22029920/22029930/04063000/21069099/2201, unique index, one resolver `pricing/hsn-rates.ts`; S-177 omits the four credit columns from the shop's sync copy — but **migration 0058 aborts `pnpm db:migrate` on the founder's own `dos`** (no 2201 rate row there; P0001 proven on a byte copy). **Repair launched `wf_bb85bd6e-0ab`** with rulings: merge main first; **renumber to 0060 (hand-written) / 0061 (generated)** because `qa/p2-stock` already holds 0058/0059 (gap allowed); 0060 guarantees every target rate row with guarded inserts using the seed's ids; `tier` joins the shop's omit; the pricing/rls hsn fixtures get the full run suffix and an afterAll delete (the unique index would otherwise turn a repeat into a hard failure). The verifier will read `dos` as a template only (`createdb -T dos …test…`), never write it. (3) Findings renumbered: S-166/S-167 collision resolved (money-web items → S-183/S-184; the smoke block's four rows pasted as S-164..S-167); table runs S-01..S-188. Note for the p2-stock integrator: if it reaches main second it regenerates its snapshot after merging main.

**One app, 2026-09-21 14:20 IST (`wf_b2061941-2d8` → NOT READY; repair `wf_05a488fe-0e1` running):** the merge is BUILT and GATED on `qa/one-app` (14 commits, head 5c309b2): kit `routeFor`/`useGo`/`homeHref`, `GROUP_OF` in the api-client, `frontend/dos-app` with the six groups as visible segments, the root with the Continue-as chooser and one client for six services, the six per-role apps DELETED and every guard re-pointed, frontend gates green, six sign-ins walked in a browser. The blind verifier FAILED it on one major: **a staff member's owner-granted extra role never reaches the device** (`MembershipSummary` has no `extraRoles`, so the chooser offers only the fixed table; the retired per-role app used to ask for the role itself). Architect ruling **B5**: `MembershipSummary.extraRoles`, required, filled by the auth service at login / `memberships.summary` / `switchTenant`; recorded in docs/22 §8. The repair also **ports the D8 fixes** that landed on main after the move (main deletes nothing; the branch deletes `frontend/delivery-app`, so the eleven D8 files are re-applied under `dos-app/app/delivery` and `src/groups/delivery`), and writes the docs/29 §3 paragraph the verifier asked for. The assemble lane's sign-out gap is **S-189, carried** (after go-live). After the merge: re-render docs/22 (the retire lane rewrote §2 as-built and CLAUDE.md), then the Pages deploy targets `dos-app` only.

**S-176 / S-177 MERGED, 2026-09-21 14:50 IST (`7247d3f`, `wf_bb85bd6e-0ab`):** second verification pass; the three majors acknowledged by the architect — (1) `migrations-journal.test.ts` now requires strictly increasing `idx` + monotonic `when`, not density (0057 → 0060 is a permanent gap; drizzle never reads idx); (2) **RULING for the Phase 2 lanes `qa/p2-stock` (0058/0059) and `qa/p2-undelivered` (0059, when 1789979416887): at their merge each takes the next free indices (0062+ …) with a `when` later than 0061's 1789979170001 — a lane merging second with an earlier `when` would be skipped by drizzle on every database that already applied 0061; main's journal test fails such a merge rather than letting it in silently;** (3) `pnpm db:seed` refuses the founder's `dos` ("seeded by an earlier demo seed … no demo.anchor_date") — a pre-existing guard from 472a5df, not this lane; migrate on a copy of `dos` is exit 0. Nine specs' global `hsn_rates` fixtures now carry the run suffix and delete in afterAll. S-29 closed by execution. **Founder's `dos`: the next `pnpm db:migrate` applies 0060/0061 cleanly (proven on a byte copy); `pnpm db:seed` on it has been refused since 472a5df regardless of this lane.**

**Phase 2 P1 fixes, 2026-09-21 15:30 IST (`wf_40acf6b9-c2b` → all three lanes BUILT, all three FAILED verification; repairs running):** `qa/p2-stock` (DOS-195/204/186) — blocker: a refused bill could never go out again (check-in counted its pieces van → godown while the new load-out takes only from the dock and silently clamped to zero, still issuing the challan); majors: the clamp itself, and the demo seed still writing the old model (69 of 103 undelivered-on-active-trip pairs short on the van). `qa/p2-free-goods` (DOS-185) — blocker: the reward line repeated the trigger's rule, so brand claims and the scheme-spend register counted every gift twice (proven 20 vs 10). `qa/p2-undelivered` (DOS-197/196/191/203) — major: the Undelivered register missed the `closing` state (the server frees a bill at check-in); the credit gate silently gave headroom back for an undelivered bill. **Architect rulings** (docs/22 §8, three rows; §4 W6 amended): check-in returns undelivered pieces to the dock; the load-out refuses (409) instead of short-loading; seed + back-fill migration for old-model databases; the reward line carries a marker, never a second `freeQty`; `orders.setLines` guarded; the credit gate counts undelivered bills. **Repairs:** undelivered `wf_8f09dea3-c22` (renumbers to 0062/0063), free-goods `wf_1dda8cce-cd9` (no migration), stock **`wf_98d50a61-f7b`** (its first run `wf_676ff035-56d` made all four fix commits — 3fe7888 check-in → dock, a83ff99 no short-load, bc10540 seed, 293900a back-fill as 0062 — then its agent died waiting on a background test without reporting; relaunched as a finish-and-report run with the same verify/integrate; its Integrate step returns `blocked` until the undelivered lane is on main; then resume it with `args: {attempt: 2}` so it renumbers to 0064–0066 and regenerates its snapshot on top of main's). **Merge order: undelivered → stock → free-goods (any time).** The business simulation starts only when all three are on main.

**ONE APP MERGED, 2026-09-21 16:10 IST (`a75e13b`, `wf_05a488fe-0e1` repair → verifier pass with four minors):** `frontend/` now holds `admin-app` and `dos-app` only; `pnpm --filter @dos/dos-app web` serves the one app (5173) with groups `/owner /manager /sales /warehouse /delivery /retailer`; `MembershipSummary.extraRoles` (and the dues summary rows) on the wire; the D8 fixes ported (check-in.ts identical to main's); docs/28 and CLAUDE.md rewritten by the retire lane; docs/29 §3 carries the retirement. Gates at the merge: backend lint/typecheck/build/docs:readme:check/format, auth-service + core auth 101/101; frontend lint/typecheck/test (dos-app 105 files / 483 tests), format, web export 3.4 MB entry. Carried: S-189; the field-global sampler hint (owner example shows `extraRoles: ["delivery"]`, cosmetic). Housekeeping: the six retired apps' leftover build dirs (android/ios/dist/node_modules, ~9 GB) deleted from the main checkout; eight merged worktrees removed → 3 left (the Phase 2 repairs); 42 GB free; frontend `pnpm install` clean. **Next for the website:** `frontend/scripts/pages-deploy.sh dos` publishes the one app to Pages, but it needs a reachable API (`EXPO_PUBLIC_API_URL` = `https://api.distributionos.in`), which needs the Oracle VM — still out of ARM capacity; do not publish a sign-in page against a dead API.

**DOS-185 MERGED, 2026-09-21 17:00 IST (`3cbaa34`, `wf_1dda8cce-cd9`):** verifier pass (two cosmetic minors → S-190, S-191); integrated on the one-app main — frontend gates green on dos-app (105 files / 483 tests), core 178/178 in the nine touched specs, rls 97/97. Remaining before the simulation: **undelivered** (`wf_8f09dea3-c22`, running) → **stock** (`wf_98d50a61-f7b`, running; resume with `args: {attempt: 2}` after undelivered merges).

**Go-live prerequisites, 2026-09-21 11:20 IST:** `distributionos.in` BOUGHT (Hostinger) and **ACTIVE on Cloudflare** (zone `46c6a92480ab7d13658390a48d9edbda`, nameservers `kay`/`lloyd.ns.cloudflare.com`, account `6730952ae1e2212f14c52d66a5339d35`). The Cloudflare API token is stored at `~/.config/dos/cloudflare.token` (mode 600, outside the repo, never committed; verified active) — it also sits in this session's chat transcript, so rotate it after go-live. Pages project `dos` with `www.distributionos.in` + apex attached (see the run log for what the token allowed). **Still owed by the founder:** the Oracle account (Mumbai — irreversible; a real credit card, PIN-debit/prepaid/virtual refused) + its API key config; gated-or-public (recommended gated); the data extract if it is coming.

**The simulation design is written: `QA/24-simulation-design.md` (Fable, the architect seat — the main session runs on Fable 5.1 since 08:20 today).** It is binding for days 2–3. The founder should read §4 (the seven days) and §7 (the blind auditor) — those are the two things that make it a test rather than a demo.

**The five-day plan** (`QA/10-DAY-PLAN.md`, page https://claude.ai/artifact/5c7mWeutvCDtEEsiujS1ei): day 1 chain · days 2–3 simulation · day 4 fix + aimed checks · day 5 Android basics, security slice, go-live, audit with the handover. Four days scheduled, one unknown; the founder hears on day 3 evening whether the five hold.

**A deployment finding from the deploy lane, covered by the one-app plan:** every app's `absoluteUrl()` absolutises document URLs against `API_URL` alone, with no `API_PREFIX`, so behind the all-in-one (where each service sits under `/owner`, `/sales`, …) tenant logos and invoice PDFs would 404. `docs/31` already replaces it with `absoluteUrl(group, url)` in the assemble lane; it must be walked at the gate (a logo and a PDF open from the deployed shape).

## The founder re-ordered this work on 2026-09-21 — read docs/22 §8, not this summary

Three sentences, and they change what "done" means for the rest of Stage 1:

- **"Smoke gate to be done for failed ones."** No all-or-nothing bar. `pnpm smoke` runs and every BROKEN is triaged on its own merits into one of three kinds — a real fault (a finding), no demo row that qualifies (a harness fault: it must say SKIPPED), or a wrong published example. Nothing waits for a perfect run. The old "0 BROKEN re-arms the main-health gate" framing is gone.
- **"IOS and andriod to be validated completely in the end / For now basics can be done."** One complete device pass, at the end, on both platforms. Until then a device walk is a short sanity check and **no finding stays open waiting for a phone** — it closes on its browser evidence and is listed for the end pass. This folds up the per-lane "platform proof follows" debt of 2026-09-14 and turns the standing iOS gap (open since 2026-09-06) into scheduled work instead of a running debt. NO iOS in the current run, deliberately.
- **"As working and user tests and all other are more imp that can be done on system it self."** What a browser on this Mac can exercise comes first. That is the cross-role end-to-end phase and the full regression, and they now outrank device time.

**The architect's ruling on list order, which the founder delegated ("go as per your (FABLE's) recommendation"):** one rule, not a rule plus an exception — *every list orders newest first on the same column its own from/to window filters: server time `created_at` for a queue of work, the document's own stamped date for a dated register, the row id only ever breaking a tie.* `QA/evidence/batch2/verdicts/DOS-009-list-order-ruling.md`. Its code cost is one stale comment and one client-side re-sort to delete in `frontend/delivery-app/app/trips.tsx` — a screen that silently re-sorts is exactly the second rule the ruling forbids.

## Queued next, and it is now the priority

1. **Full A.12 regression** — 7 apps in a browser, at desk and phone widths, against the running services.
2. **Phase 2 — the cross-role end-to-end business flow.** This is the "working and user tests" the founder means: one order carried by real people through every app, end to end.
3. **The complete device validation**, Android and iOS, once the product is otherwise right.

## Wave 3 is closed — all 21 lean groups on main, 153 of 158 findings merged

`lean-retailer-platform` was the last, merged `d211e21` after a repair, a device walk and a Fable
re-review. Its story is worth keeping: the previous repair had moved the retailer home screen's
memberships read onto the auth link, where `AUTH_RETRY_PATHS` did not list it — so a 401 on that call was
never retried and the screen could tell a shop that owes lakhs "You owe ₹0.00 across 3 distributors". The
fix replaced the four-name allow-list with a deny-list of the nine routes that authenticate through the
BODY (login, refresh, logout, switchTenant, forgotPassword, resetPassword, platformLogin, platformRefresh,
jwks), so every Bearer-carrying auth route heals like any other read **and a new one heals the day it
lands**; a test walks the auth contract's leaves and pins that exactly those nine are excluded. The screen
was fixed at the same cause: an unanswered read can no longer become a money figure, a green chip or
"No bills yet".

**The walk stage is the method from now on.** Three lanes walked with services running under the port rule
and produced measured evidence in `QA/evidence/batch2/walks/`: web desk 1280, web phone 390, the Pixel 7,
and — for `lean-warehouse-pick` and `lean-warehouse-rules` — the iOS simulator. One walk overturned its own
finding honestly ("CLAIM FALSE at HEAD — the call IS healed"). Every remaining "merged, proof owed"
finding is settled this way, not by another round of static reading.

**Machine limits that shaped these runs, and will shape the next.** At walk time the Mac had ~40 MB of RAM
free and 6.07 of 7.17 GB of swap in use; the retailer lane declined to boot an iOS simulator on top of that
rather than risk the other live lanes, and said so instead of pretending. The 25 lane worktrees held 44 GB
on a disk that was 94% full; they are removed now that every branch is merged and pushed (the branches
themselves are kept as the evidence trail). Size the next run to this machine: few lanes, one device queue.

The Mac is held awake two ways: the app's keep-awake hold, and `caffeinate -dimsu -t 86400` (pid 58695). Founder, 2026-09-19: the machine runs unattended around the clock; **the 70%-by-Wednesday stop rule is WITHDRAWN** — batch 2 runs to the end, P2 and P3 included, aiming to finish this week.

## The smoke gate, reshaped (Fable, 2026-09-19 — binding)

`pnpm smoke`'s "0 BROKEN" could not be reached on a fresh database for reasons no lane owns (S-149, S-156, S-157), so a lane was being held to a bar it could not clear. The architect's ruling `QA/evidence/batch2/verdicts/DOS-175-177-blocker2-ruling.md` replaces it, per lane, at the walk stage:
(a) the design's NAMED operations read **OK** — never EXPECTED, never a 409 counted as green — on a fresh seed AND on a replay;
(b) **no NEW BROKEN against main at the merge base** (same seed, same `--run-tag`, first run), and every BROKEN that is also on main is filed as an S-row in the same turn;
(c) nothing turns BROKEN on the replay.
Absolute 0 BROKEN survives as a **main-health** gate the integrator runs once after each merge of main — and it is **binding only after** S-149, S-156 and S-157 are fixed and one observed `pnpm smoke` on a fresh seed records 0 BROKEN in this file with its commit hash. **Until that line exists, nobody may be held to 0 BROKEN.**

## Queued, in order

0. **Fix S-149, S-156 and S-157, then observe 0 BROKEN once on main** and record it here with the commit hash and the seed's row counts — that is what re-arms the main-health gate. S-149: `examples.ts` `ownNotices` must be filtered to in_app/push with `recipient_user_id` set and no retailer BEFORE any limit (never "newest 500 of the tenant"), proven by `markRead` OK on the manager AND warehouse lanes on a fresh seed. S-156/S-157: the seed leaves one PARKED pack and one OPEN gate-count discrepancy, AND the harness reports an INVENTED path id as SKIPPED "no demo row qualifies", never BROKEN.
1. **Money + DOS-172 platform walks** — Fable's merge-gate ruling items 2–7 (`QA/evidence/batch2/verdicts/DOS-168-170-merge-gate-ruling.md`). Device-bound: they queue behind the DOS-167 walks. DOS-168/169/170 and DOS-172 are **MERGED, PROOF OWED, still OPEN** until these pass.
2. **Lean wave 3** — the last 16 groups, 84 items, runner `QA/tools/batch2/workflows/lean-wave3.js`, Fable in every architect seat. Nothing is held back: all sixteen `defer` lists are empty. Waits only for the device queue above to clear.
3. **The suspects still unprobed** — S-141, S-142, S-144, S-145, S-149, S-150, S-153, S-154, S-155. S-153 (the seed builds 44 of 48 trip settlements from the wrong table) and S-155 (a turbo cache hit reports the cross-app guards green without running them) are the two that make other tests untrustworthy; do those first.
3b. **The four product answers of 2026-09-20 (docs/22 §8) — a lane of their own, after wave 3.** Batch 1 fixed only the mechanical half of each; these are the product clauses the founder has now decided:
   - **DOS-075** — `priceOrder()` counts a line under an exclusive scheme toward a threshold ("bills over ₹X") while still giving it nothing from that scheme. `backend/libs/domain/src/pricing/schemes.ts` + its specs; every order/invoice line's `applied_rules` must still read true.
   - **DOS-023** — every list in every app orders by server time, newest first, record id only as a tie-break. Batch 1 (`bdd6055`) did picking sheets alone; this is the convention across all seven apps and the contracts behind them.
   - **DOS-043** — a trip may depart before its planned date; the trip carries the planned date and "departed early" beside it. Batch 1 (`d20a584`) settled who may depart, not when.
   - **DOS-057 / DOS-099** — NOTHING TO BUILD. A permanent public link to a shop's papers is refused; phone sharing stays. Recorded so nobody proposes it again.

4. **docs as-built** — docs/22 rows for what merged today, docs/23 §10 #6, docs/27 §14, `pnpm docs:readme` per merge.
5. **Full A.12 regression** — 7 apps × browser + Android, iOS sanity. The largest device block.
6. **Phase 2** — the cross-role chain.

## One new question for the founder (raised by a lane, not yet asked)

`lean-manager-order-lifecycle` hit the founder's own DOS-023 rule of 2026-09-20 — every list orders by
server time — and found one place it cannot apply cleanly. Orders now sort by `created_at`, exactly as he
said. **Bills and trips sort by `invoice_date` and `trip_date`**, the column each list's own window filters
on, because a bill dated 14 Aug that was typed today would otherwise jump to the top of a 1–31 Aug window.
The lane recorded that as the ONE stated exception in docs/22 rather than deciding it, which is right.
Ask him: pure server time everywhere (a follow-up moves both lists to `created_at` with new indexes and
nothing else changes), or keep the two date columns where the list is a dated register?

## Expected tomorrow (2026-09-20)

The founder hands over a **real data extract** ("okk share real data extract with you tomorrow"). Copy it before reading; never work in place. Then decide which job it is, and do not start either until that is clear:
- a **Distribution OS book** (a dump carrying rows the apps wrote) → the money repair re-runs against the copy, read-only first, founder signs off trip by trip;
- an **old-system export** (TradeEzee, Tally, Excel) → nothing in Distribution OS is wrong; it is an import through the generic importer (docs/17 §D7) with its own reconciliation.

## Merged into main, 2026-09-19 → 20

`c3b4ec1` S-108/DOS-174 challan poll · `d65034b` DOS-171 van sale · `ce3dc8c` DOS-167 ruling 3 (the web store opens under slow loads) · `6e5c7c5` DOS-168+169+170 money · `d25574e` DOS-172 loading · `609388b` DOS-167 Fable amendments A1–A5 · `ed3e1b7` DOS-178+179+180 honesty · `f144e29` DOS-175+176+177 money-delivery · plus two CI repairs, `d30ccf7` (backend lint: a package imported its own name) and `eb9a155` (CI seeds before `pnpm test`).

**Coverage: 153 of 158 batch-2 findings merged; seven of the eight MERGED-PROOF-OWED findings are now CLOSED on browser evidence (DOS-172 stays open on rule C).** P0 DOS-167 is OPEN on one clause (DOS-183). MERGED BUT STILL OPEN, platform walks owed: DOS-168, DOS-169, DOS-170, DOS-172, DOS-175, DOS-176, DOS-177. Filed and building: DOS-181, DOS-182, DOS-183.

## Databases

- `dos` — the founder's own. Backed up 2026-09-19 (2.7 MB dump in the session scratchpad) and migrated 43 → 48. **Nothing was changed in it and nothing will be without the founder's word.** It holds no real trade: every row came from `pnpm db:seed`.
- `dos_test_q3_repair` — a template copy of `dos`, kept for now; the read-only repair analysis ran on it.
- `dos_qa` — REBUILT clean on 2026-09-19 after a smoke run committed 15 destructive operations against it (dropped, recreated, 48 migrations, fresh seed; the renamed demo staff are correct again). The harness gap that allowed it is closed: `pnpm smoke` now refuses to run when the tenant the services return does not exist in the database the fixtures read.
- Lane databases `dos_test_b2_*` are copies of `dos_test_batch2b_template`; a lane may drop and recreate only its own.

## Standing rules for this programme

- Never touch `dos` or `dos_qa` from a lane; destructive work only on a database whose name carries `test`.
- A blocker in a merge review was raised against the branch AS REVIEWED: **a commit that already existed when the review was written is never its fix.**
- An integrator must run the kit's cross-app guards with `--force`; a turbo cache hit cannot prove them (S-155).
- Fable is the architect in every seat — design, ruling, merge review, judge. Opus builds and verifies; Sonnet for mechanical steps.
- Workflow scripts live in `QA/tools/batch2/workflows/`, in the repository, because a scratchpad wipe once made resume impossible.
- The auto-mode classifier sometimes refuses an agent's `git merge`. That is a tool permission, not a conflict: the main session completes the merge by hand and says so.
- Never report a command that was not run or an outcome that was not seen. "not-tested" is an honest answer.
