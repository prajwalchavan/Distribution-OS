# DOS-115 — architect verdict (Fable, 2026-09-13)
**Verdict:** APPROVE WITH AMENDMENTS

Refutation attempted and failed: the matrix rows (permissions.ts:518-523 = ANY_MEMBER), the guard path (tenant.guard.ts:136-179, route
`POST /orders/:id/cancel` via @orpc/nest `toNestPattern`), the handler tuple (orders.service.ts:101 ORDER_ROLES = STAFF + retailer, checked at
:142/:155/:174/:235/:394), `callerReaches` (orders.internals.ts:285-288, narrows only the rep) and the sync door (sync.upload is STAFF at
permissions.ts:451, SyncService.upload dispatches by table with no role check, sync.service.ts:108-128; orders.sync.ts:59/:85/:109 re-head, insert
and re-line drafts for any non-rep; sales_orders RLS is staff-wide). Nothing legitimate depends on the five procedures for these roles: van sales
use insertDraft/writeLines/submitInTx (vansales.service.ts:79-88), AI drafts use the same under DRAFT_TAKERS (ai.internals.ts:30-36), approvals
use cancelInTx (approvals.service.ts:118); warehouse-app calls only orders.get (pack/[orderId].tsx:64), delivery-app calls no orders.* write; no
warehouse/delivery nav entry is gated on one; the warehouse and delivery device manifests never carry sales_orders (database/src/sync-tables.ts:80-81
= SALES + SHOP). The plan's two-layer closure (matrix + sync handlers) is right and minimal for the two roles named.

## Amendments (binding)
(a) ORDER_PLACERS excludes the accountant (FOUNDER DEFAULT below): contracts tuple `['owner','manager','salesperson','retailer']`, core copy adds
    `'system'`. Every test in the plan that spells the placer tuple uses these four; the permissions.test.ts pin also asserts `isAllowed` is false
    for `accountant` on the five writes and true on orders.get/list. Blast radius verified nil: no spec creates or cancels an order as the accountant
    (billing.spec.ts `placeOrder` is always `rep`; the accountant actors in receivables/reporting/incentives/docint/config/tenancy/catalog specs never
    POST /orders), `pnpm smoke` signs in no accountant, and manager-app draws Cancel under `can('orders.confirm')` (app/orders/index.tsx:84), so the
    screen does not change. Keep ORDER_PLACERS a separate tuple from DRAFT_ORDER_TAKERS even though the two are equal today (they are declared apart
    on purpose and will drift); the existing narrowing loop (permissions.test.ts:1211-1222) still holds.
(b) orders.sync.ts: ONE exported-in-file helper `requirePlacer()` (currentTenant().actorRole ∉ ORDER_PLACERS → `SyncRejection('forbidden', …)` with the
    Hindi sentence) is the first statement of BOTH `applyOrderSync` and `applyLineSync`, before `putOnly`. Not two inline copies. Header comment
    (:9-17) gains one sentence: the device doors follow the same rule as the five procedures.
(c) Stale comment made false-by-omission: frontend/manager-app/app/orders/index.tsx:13-15 claims `orders.cancel` is "owner + manager in the matrix".
    Rewrite (comment only, no code) to: orders.cancel is ORDER_PLACERS (owner, manager, salesperson, retailer — never the accountant, DOS-115) and the
    queue draws Confirm and Cancel under `can('orders.confirm')` deliberately. Also correct permissions.ts:293-294 and :940-942 as the plan says.
(d) The orders.spec pin (test c) asserts, for ALL five paths, `permissionFor(p)` deep-equals `ORDER_PLACERS` minus `'system'` — one loop, not one path.
(e) Verify adds the accountant: `amol.vaidya` (seed-demo/people.ts:180) on manager-service :3002 — POST /orders/{B}/cancel → 403
    'the accountant role may not call POST /orders/:id/cancel'; GET /orders?limit=5 → 200. Manager app :5174 as amol.vaidya: queue loads, no Cancel.
(f) docs/22 clarification row (plan's wording) additionally says: "the accountant is also outside the five order writes, as the architect's default
    under the 2026-09-05 accountant-scope row; the founder may reverse it with one tuple change." docs/23: when removing the order items from
    §4.3 (:423-424) and §5.3 (:488-489), grep §2 (manager/accountant) for an order-write line and mark it the same way if present.

## Decisions
- technical: `orders.repeatLast` is "create" (plan option A). It inserts a draft for any shop through insertDraft + writeLines
  (orders.service.ts:173-221) and docs/23 §4.3 already lists it with the order writes; leaving it would keep "a loader creates orders" true.
- technical: the sync door is closed in the two orders handlers (as planned), not by a registry-level mechanism, so DOS-115 ships now and
  touches no other module. The device-upload refusal needs no founder decision: a PUT on sales_orders/sales_order_lines IS create/re-line.
- technical: reads stay (orders.get/list ANY_MEMBER; ORDER_ROLES at :541/:553 unchanged). Narrowing a driver's reads to its trip is Phase 3.
- technical: RLS untouched, no migration. Warehouse writes order state at pack through applyFulfilmentEvent; billing/delivery/reporting read all.
- technical: NEW FINDING for the QA lead, outside this lane — the same sync-door class exists for every other registered table: receipts and
  allocations (receivables.module.ts:32-33), trip_stops/deliveries/pod_evidence/collections/trip_expenses (delivery.module.ts:75-79), pick_lines
  (warehouse.module.ts:45), documents/document_pages (docint.module.ts:37-38). None of those handlers or the services they call check a role
  (grep: zero `requireRole`/`assertCrewOrDesk`/`actorRole` in delivery.sync.ts, warehouse.sync.ts, receivables.sync.ts, docint.sync.ts; `recordReceipt`
  at receivables.service.ts:588 has none), and `receipts` carries `staffWritePolicy` (receivables.ts:219), so a salesperson or warehouse token can
  record a receipt by upload although receivables.receipts.create is MONEY_COLLECTORS. Design for that finding: `SyncRegistry.register(table,
  handler, { standsFor: '<contract key>' })`, checked ONCE in SyncService.upload via `isAllowed(PERMISSIONS[standsFor], role)` before `vetoIfStale`,
  and `manifest().writable` reading the same; the DOS-115 handler check then becomes defence in depth, like requireRole behind the guard.
- FOUNDER DEFAULT: the accountant loses orders.create/setLines/repeatLast/submit/cancel with the godown and the crew. Chosen because docs/22 §8
  2026-09-05 (:248) fixes the accountant's WRITE scope to the money desk, permissions.ts itself already says "The accountant, the godown and the crew
  take no orders" (:293-294, :942), and the manager app already hides the button. Reversible by re-adding one role to ORDER_PLACERS.

## What the verifier must prove
- FAIL FIRST on the unfixed tree: the permissions pin, the three orders.spec tests and both service-spec tests fail for the stated reason
  (ANY_MEMBER; 200s; upload accepted 2; in-process `OrdersService.cancel` under a warehouse TenantContext succeeds). Controls pass before and after.
- After: `pnpm --filter @dos/contracts test`, `@dos/core test -- src/modules/orders`, `-- src/modules/billing`, `-- src/modules/sync`, both service
  specs (their describePermissionMatrix suites now expect the gate refusal for warehouse/delivery/accountant on the five routes), docs/readme.test.
- Live, per plan steps 1-9 plus amendment (e): 403 bodies carry the exact Nest-pattern messages; /sync/upload answers 200, accepted 0, code
  'forbidden', and `sync_errors` holds rows for qa-dos115-w and qa-dos115-d; order_state_transitions, outbox_events (OrderCancelled), the held
  reservation and B's state/total are unchanged; GET :3004/orders/{A} and :3005/orders?limit=5 are 200; van sale on :5177 and on the Pixel 7 is 200
  with source van_sale + INV number; warehouse Pack detail loads; scoped smoke (`--service warehouse|delivery --run-tag dos115`) ends 0 BROKEN with the
  five rows EXPECTED.
- OpenAPI x-roles on :3004 and :3002 for the five routes list neither warehouse, delivery nor accountant; GET /orders still lists all three.
- `pnpm docs:readme:check` clean after ONE regeneration on the merged tree; no hand edits in the 12 READMEs.
- The pin in (d) passes and `DRAFT_ORDER_TAKERS ⊆ ORDER_PLACERS` still holds (permissions.test.ts:1211-1222).
- docs/22 §7 wording + §8 clarification row + §11 row present, HTML re-rendered, artifact republished; QA/13 row in its own commit (CHARTER A.14).

## Risks for the builder
- Land after DOS-003, DOS-004, DOS-043, DOS-074+097, DOS-106 and DOS-096; permissions.ts/.test.ts, orders.internals.ts (:188-207 region is theirs),
  orders.spec.ts (append LAST) and warehouse-service/src/service.spec.ts are all shared. Regenerate READMEs once, at the end.
- orders.spec.ts phones: `${run}1-5` are taken (:115-119, :118 comment names 4); use 6 and 7 and re-check after the held lanes merge.
- billing.spec.ts:907/:935: create the two van_sale orders as `manager` (OrderCreateInput.source is a plain enum with no role rule, so the body is
  unchanged); `driver` still issues at :919/:945 — issueVanSale checks DOORSTEP only.
- In the sync path an ORPCError FORBIDDEN would also map to 'forbidden' (sync.service.ts, `String(code).toLowerCase()`); throw SyncRejection directly
  anyway so the Hindi sentence is yours and no 4xx can leak. `vetoIfStale` runs before the handler: an op with a stale baseUpdatedAt answers 'stale'
  first — harmless (both roles may read orders) but the spec must send none.
- Do not reuse DRAFT_ORDER_TAKERS for the five rows; do not touch ORDER_ROLES for get/list; do not add a policy; do not filter docs examples.
