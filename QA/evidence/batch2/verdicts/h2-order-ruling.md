# Lane h2 order — architect ruling (Fable, 2026-09-13)
**Order:** DOS-043 → DOS-133 → DOS-131+DOS-137. DOS-133 verdict amendment (e) stands. Superseded: the DOS-131+137 verdict's first "Risks for the builder" line ("DOS-043 → this → DOS-133") and its plan's notes "Order in lane h2"; also the DOS-133 plan's W7 entry "Build on the DOS-131+137 version…" and its verify "lane at DOS-131+137" / "(and the trip, per DOS-131+137)" — the base is the DOS-043 file, vehicle picker as today.

## Why
- Smallest rework. DOS-133's W7 edit is the query (:55-58), the panel meta/actions (:179), the row `secondary` (:191-193), the :55 comment and three strings. DOS-131+137 rewrites the vehicle panel, the create body, the row `state`, the header and adds two queries to the same file. The small backend-anchored change lands on an untouched file; the large UI change rebases once onto a stable data source. Reversed, the M fix rebases onto an L rewrite AND the W7 rows DOS-131+137 just verified must be re-walked, because DOS-133 changes which rows exist under its disabled-row claims.
- Each red test stays meaningful. DOS-133's red is a core spec plus `GET /warehouse/packs?status=awaiting_load` answering an unfiltered id-ordered page — independent of any trip panel. DOS-131+137's W7 proof ("a pack of a shop not on the trip is disabled", "choose FREE1..FREE3 → Build") assumes the rows are the set `loadSheets.create` accepts (its plan: "whose data source is DOS-133's"; its own dependsOn lists DOS-133). On today's list a lane-made pack (`01a0…`) sorts below 1 461 seeded hash ids and never reaches page 1, so that proof would pass or fail by hash luck.
- Nothing in between leaves W7 worse. After DOS-133, W7 is today's screen with a correct list, pack times and the vehicle picker. After DOS-131+137 alone, the founder default ("a sheet is always for one trip") routes the godown's only path through a list of delivered 4-Sep orders, some enabled because their shop is on the trip, whose Build answers 409 — worse than today.

## Amendment changes (binding, supersede the verdicts where they differ)
- DOS-133 (a)–(g): unchanged; (e) confirmed as written.
- DOS-131+137 (a), W7 sentence, replace with: "the W7 not-on-trip rows use `state="disabled"` and PREPEND the sentence to DOS-133's `secondary`: `${t('w7.notOnTrip', { trip })} · ` + (order no · packages · `Packed …`), so the sentence survives truncation at 375 px and the pack time is not lost. The e2e asserts the sentence is visible; the verifier also proves enabled rows still read 'Packed …'."
- DOS-131+137 (c), first sentence, replace with: "`packOnTrip(pack, trip, unplannedInvoiceIds)` runs over the rows of DOS-133's `awaiting_load` query (`packed.data?.items`), which is already exactly the set `loadSheets.create` accepts; it only narrows that set to the trip, never widens it, and `packs.list` gets no trip, stop or invoice parameter — the narrowing is client-side on `retailerId` and `invoiceId`. List membership and order are DOS-133's, and its oracle must still hold after this merge." Rest of (c) unchanged.
- DOS-131+137 "Risks for the builder" line 1, replace with: "Lane h2 order: DOS-043 → DOS-133 → this. Build W7 on the DOS-133 version of load/index.tsx and strings.ts; keep its query, meta, 'Show older packs' and `packedAt` text."
- DOS-131+137 "What the verifier must prove", W7 bullet gains: "PACKED ORDERS lists exactly DOS-133's oracle rows in its order; the trip only disables rows."

## What the first fix must leave for the second
- `const packed = useQuery(['packs', 'awaitingLoad', packedLimit], () => api.api.warehouse.packs.list({ status: 'awaiting_load', limit: packedLimit }), { enabled: signedIn })` — the name `packed`, the `['packs']` prefix (both fixes invalidate it), `status` as the ONLY new input; `PackListStatusSchema = z.enum(['awaiting_load'])`, `PacksListInput.status` optional.
- `PackListItemSchema` unchanged: `retailerId`, `invoiceId`, `orderId`, `packedAt` are what `packOnTrip(pack: Pick<PackListItem, 'retailerId' | 'invoiceId'>, …)` and the row key on.
- The per-pack `ListRow` keeps `key={pack.id}`, `testID={`w7-packed-${pack.orderId}`}`, `state={chosen.includes(pack.orderId) ? 'selected' : 'default'}` (DOS-131+137 adds the `'disabled'` arm), `onPress={() => toggle(pack.orderId)}`, and ONE `secondary` template string that DOS-131+137 prefixes.
- Untouched by DOS-133, replaced by DOS-131+137: `chosen`/`toggle`, the `vehicles` query and `vehicleId` panel, the `create` mutation and its `newId` import (→ `tripId`, `loadSheetInput`, `meta.id`), the file header (:1-11).
- Strings: `w7.packedAt`, `w7.packedMore`, `w7.packedCapped` inserted after `w7.packedEmpty` (:310); the W10 block is DOS-043's, then DOS-131+137's. Backend names fixed: `OrdersService.orderInState`, private `onLiveLoadSheet` in packing.service.ts, `pack_confirmations_created_idx`, cursor still a pack id.

## Corrected dependsOn
- `plans/DOS-133.json`: `["DOS-043", "DOS-032+DOS-059"]` — DOS-131+DOS-137 dropped; the second entry is migration numbering only (DOS-133 (f)), not a build blocker.
- `plans/DOS-131+DOS-137.json`: `["DOS-043", "DOS-133", "DOS-115", "DOS-056"]` — unchanged; DOS-133 was already there and is now honoured.
