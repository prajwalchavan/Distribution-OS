# Walk-debt judgement (Fable, architect, 2026-09-21) — judged against docs/22 §8 of 2026-09-21

Read: all eight walk reports in `QA/evidence/batch2/walks/`, their SQL/log artefacts, the merge-gate ruling, the DOS-175..177 blocker-2 ruling, the findings file, the lane branch `qa/b2-smoke-failures` against main. Terms: a finding whose only remaining debt is a phone CLOSES on its browser evidence and is LISTED for the end pass; a claim read out of the journal is proof, the same claim read off a screen is not; the smoke gate is a list of failures, each classified. Nothing was built or run.

## Finding by finding

| Finding | Ruling | Why (the measured evidence, strongest first) | End pass must see |
|---|---|---|---|
| **DOS-168** | **CLOSED** | SQL: 10 API pairs + 2 browser pairs (108 ms apart, both widths) → ONE `deposit_ref`, ONE deposit entry, ONE `ChequeDeposited` row each, loser 409 "is deposited, not collected", 0 unbalanced entries, trial balance 0 (`money-web-sql/item2-*`). | Nothing — a desk action. |
| **DOS-169** | **CLOSED** | SQL: TRIP-0008 `collections`=0, receipt via `/sync/upload` only, settle = float + X, variance 0, CASH_VAN nets 0; TRIP-0009 float alone → 409 `settlement_needs_owner`, one approval; cockpit TRIP-0010/0011 read the same rupees (`item3-*`). | The receipt must come from a real phone's SQLite outbox through airplane mode (the walk pushed the op by hand), then the cockpit counts it. Mac residual for the regression: the owner-accepts-variance branch (TRIP-0009 left `closing`). |
| **DOS-170** | **CLOSED** | SQL: six journal lines for TRIP-0010 net AR 0 / CASH 0 / CASH_VAN 0; reversal lines are `AR +X, CASH −X`, no CASH_VAN line; INV/9022 back to 154 400; settlement row byte-identical; trial balance 0 (`item4-web-ledger.txt`). Screen and API agree. | Nothing. |
| **DOS-172** | **OPEN on Rule C** | Proven both widths with rows: returned bills re-offered (SO-0845/0850), on-road bill a real disabled button + 409 `bill_on_road`, second load-out W10→W7→M7→blind count→confirm, DC-0083 written, transition `packed→dispatched` reason NULL. NOT proven: `trips.depart` refusing 409 `bill_not_loaded` on an uncounted bill — the walk hit the state machine first. That clause is the one that stops the title's "carries it off with no count", and it is one API probe on this Mac (mirror `delivery.spec.ts` "no trip departs with a bill nobody counted out"). E-way threshold case also unbuilt (lesser). | W7 blind-count keypad and the check-out dialog under a real touch target. |
| **DOS-174** | **CLOSED** | Timed request counts at both widths: after close exactly one already-scheduled GET (reply discarded by the token check), none for the next 16–18 s, `window.open` 0, the next panel clean. Counterfactual rests on the source guard test, accepted. | M7 Print against the phone's `documents.open`. |
| **DOS-175** | **CLOSED** | API 404 `trip_not_found` / 409 `trip_not_on_road`, 0 rows written; office receipts RCPT-9005/9006 bank with ONE `deposit_ref`, ONE Dr BANK, entries sum 0, tenant has 0 unbalanced entries; the browser's "Cash to bank" fell by exactly ₹55.00. money-web 5(B) proved the sync door for `trip_settled` (server sentence in the tray, no journal line, bill still open). Mac residual for the regression: `trip_not_found`/`trip_not_on_road` through `/sync/upload` as a named `SyncRejection`; UPI accepted on a settled trip (answer A). | The refused-money tray card on a phone. |
| **DOS-176** | **CLOSED** | API 200 + `pod_evidence` row; browser both widths: 2 rows (geo + photo), photo object `uploaded`, credit-shop photo gate held. | The CAMERA path (platform capture, ≤300 KB squeeze) — web used a file chooser. |
| **DOS-177** | **CLOSED** | API 200, row written, prior consent withdrawn in the same transaction; browser both widths withdraw → grant 200/200, "You agreed on 21 Sep". | D2 under the real OS location prompt. |

DOS-178 (S-143): money-web 5(B) measured the tray on web — "Handed to the cashier", shop and amount named, no Discard. Closed on web; the same card on a phone joins the end pass.

## The wave-3 lanes' owed walks

- **Done, closed:** `lean-retailer-platform` (web both widths + Pixel 7; the ₹0.00 claim is FALSE at HEAD by counterfactual; DOS-102 on the native store), `lean-warehouse-rules` (web + Pixel 7 + iOS boot; the blank-setting blocker measured `""` → reader 30), `lean-warehouse-pick` (all four platforms). Sixteen wave-3 items PASS on web with wire and SQL in `wave3-web-walks.md` (DOS-013/014/016/035/036/038/081/083/084/086/088/092/122/138/141/145, W5 put-back); the retailer app carries no credit figure (9/9 responses, 0 credit keys).
- **DOS-118 residual, measured on every platform:** "Too many pieces" is legible everywhere, but the sentence with the figure is never co-visible with the button, and the Pixel 7 also clips the ask. Acceptance met on its "or the button" branch; the stronger reading fails. Founder's one-string call: put the figure in the label ("Only 220 pc asked").
- **OPEN — DOS-079 REOPENS as S-176, P1 money, proven by execution:** `hsn_rates` holds three live rows for HSN 2202; the order path prices Campa at 12 %/0 while the invoice path bills 28 % + 12 % — a freshly placed order quoted ₹1,360.00 for a ₹1,699.49 bill. The lane's seed-only fix must not merge alone; the fix is the rate lookup.
- **OPEN — `lean-delivery-collect`: never walked** (D3/D4/D5/D7/D9/D11/D12). Browser-walkable; it is the last lane with no evidence at all.
- **OPEN — order-lifecycle blocker 1** (open wave + rep cancel + no hook): both neighbouring states proven safe, the guard itself never executed. Needs a rep-owned confirmed order made through `orders.create`+`submit`, not a seeded one.
- **OPEN — DOS-006** credit-release sentence: no pending `credit_limit` approval in the DB; run the review's delete + re-seed. Not reached: DOS-011, DOS-078/087/090, DOS-086's offline straddle.
- **New open doors from the walks, each needing a DOS block:** S-177 (retailer `sync.pull retailers` serves the shop its own credit policy — an `omit`), S-175 (every document URL 404s behind the all-in-one, the deployment shape; POD survives on an inline fallback, challan print has none — covered by docs/31's `absoluteUrl(group,url)`, must be walked at that gate), S-183/S-168/S-169 (D8 on a memory store: the held-money note never renders, the figure blanks to "—" beside an enabled button, D8 re-points to another trip after the desk settles — 3 of 5 pass conditions of ruling item 5 fail; none is a ledger fault, no revert trigger met).

## The smoke gate, worked failure by failure

Three runs on a fresh copy: fresh 0 BROKEN, replay 1, destructive 1 — ONE distinct failure, `retailer/notifications.inbound.create` 500. Classified honestly and correctly as BOTH a real fault (a 23505 on `inbound_messages_pkey` escaped untranslated; every sibling create answers CONFLICT) AND a wrong example (the one create absent from the `freeSlots` registry, so its id was spent after run 1); not a missing demo row. Both fixed with a spec that fails without the guard; wf-4..wf-7 0 BROKEN including the replay. Two more findings filed, not fixed: the S-149 census goes red after any smoke run that creates staff (P3, scope it to seeded staff), and both API docs pages hard-code `/docs/openapi.json` so every service's docs are empty in all-in-one mode (P2, needs a prefix-aware URL). **Housekeeping that blocks nothing but must happen:** the lane (3 files, `merge-tree` clean, main untouched on them) is unmerged and unpushed and has not had `pnpm lint` or the full `pnpm test`; its four rows are unregistered AND its S-166/S-167 collide with money-web's — **resolved on 2026-09-21 the other way round**: the collision was repaired by renumbering the LATER-filed money-web rows (**S-166 → S-183**, **S-167 → S-184**), and the lane's four rows were pasted into the findings file unchanged as **S-164..S-167**. Do not paste them again, and S-183..S-186 is no longer reserved for them (S-183/S-184 are money-web's). P3: audit the whole `freeSlots` registry, because the existing spec cannot see a missing registration (proved by stashing the fix).

## The honest state of the batch

**Proven:** the money ledger — every DOS-168/169/170 claim read out of `journal_lines` with the trial balance at 0 after every stage; DOS-174..177 on web and API with rows; DOS-172's re-load path with its challan; the smoke failure list and both its causes; sixteen wave-3 items with wire and SQL; no credit figure on the shop's screen.
**Asserted, not proven:** every phone claim since 19 Sep — the installed builds are dated 09-12..09-19, older than every fix judged here, the retailer app is not installed at all, and this Mac (8 GB; 20–226 MB free; swap 85–90 % of a store the OS grew twice; 28 s for one idle screencap) could not boot a device walk in this run. Also DOS-172 Rule C, blocker 1, the delivery-collect lane, and S-167's blast radius.
**Found wrong today:** S-176 (P1 money, on main), S-175 (P2, the deployment shape), S-177 (P2 security door), S-183/168/169 (P2 D8 money display; S-183 was filed as S-166 — see the numbering repair below).

**The single largest thing still unproven, named plainly: the van's money on a real phone.** Nothing merged since 19 Sep has run on Android or iOS. The delivery crew's offline cash path — D5 cash with no signal → D8 → check-in → the desk settles — is the P0/P1 core of this batch, and it has been seen only on a browser memory store, where its D8 screen failed three of five pass conditions; a phone with the SQLite store and a cached preview may behave differently in either direction. The end pass runs that path first, on binaries built from main after the S-16x/S-17x fixes, before anything else on a device.

Fable, architect — 2026-09-21. Binding.
