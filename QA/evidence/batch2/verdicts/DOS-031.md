# DOS-031 — architect verdict (Fable, 2026-09-13)
**Verdict:** APPROVE WITH AMENDMENTS

Root cause re-traced on main (117717c) and holds in both halves. (a) The overlay is gone: `documents.tsx:486-488` now ends in `.then(…, stayOpen)` and `<Refusal>` at l.475-479 prints the 409 (DOS-029); `mutateAsync` must keep rethrowing (`api-client/src/react/index.tsx:398-401`) or every refused dialog would close again — the finding's "catch in the hook" is rightly dropped. (b) Still live: the Start branch at l.480-491 reads only `mayReview` (l.101) and `sessionId` (l.104), never `doc.status`; `onSelect` (l.313-318) resets `sessionId`, so every Reviewed or Failed row in the worklist (`QueueStatusSchema`, contracts `docint.ts:259-264`) opens on a Start reviewing the server refuses at `review.service.ts:91-94`. Those two accepted statuses are exactly the `start_review` rows of `documentMachine` (`domain/src/state-machines/document.ts:58,65`); `reviewed` (l.73) has none and `failed` (l.76) is terminal. dos_qa read 2026-09-13: uploaded 7, extracted 2, needs_review 5, committed 2, failed 1; the four walker ids carry the statuses the plan names. No second cause: no trigger, RLS, sync or offline path; no other app calls `review.start`. Design is minimal and at the right layer: a pure rule on the domain machine (the `tripMachine.can` precedent, `delivery-app/src/lib/trip-choice.ts:62`), one JSX branch, a source-reading guard because the screen cannot be imported under vitest.

## Amendments (binding)
(a) **Pin the server to the same table.** `review.service.ts:91` becomes `if (!documentMachine.can(doc.status, 'start_review'))` (import from `@dos/domain`, as `reviewSessionMachine` already is; keep the message literal and the early position — it must run before the stale-session update at l.96). Add one case to `backend/libs/core/src/modules/docint/docint.spec.ts` directly after `expect(doc.body.item.status).toBe('reviewed')` (l.846): `POST /docint/documents/${docId}/review` with a fresh `sessionId` and idempotencyKey answers 409 and a message containing `can be reviewed`. Client rule and server guard then read one transition table; the client's literal table lives only in the test.
(b) **The screen guard survives `pnpm format`.** Match on comment-stripped source with whitespace-tolerant regexes (`\s*`); assert `testID="docint-start"` occurs exactly once in the file, that the import of `mayStartReview` from `'../../src/lib/review-desk'` is present, and that exactly one `<Button` lies between the last `mayStartReview(doc.status)` and that testID. Never match on line breaks or indentation.
(c) **Absent, not disabled.** The Start button renders `null` outside the gate; it is never rendered with `disabled` (UX-00 §6.1 requires a reason line on every disabled control, and "Start reviewing — already reviewed" beneath a Reviewed chip claims a review is still owed). The status chip at l.345-347 is what says the state.
(d) **Nothing else in the panel moves.** The in-session trio (l.493-524), Match the items again (l.526-536), Book it (l.537-548), Reject (l.549-556), `commit()` (l.275-295) and the header inputs keyed on `sessionId` (l.413, l.420) stay byte-for-byte. R1-R4 below are their own ids.
(e) **Build order inside the file.** Domain must be built before the app spec runs (`review-desk.ts` imports `documentMachine` at runtime through the `link:` to `backend/libs/domain/dist`); a module-not-found there is an environment failure, not a red test, and the builder says so in the log.

## Decisions
- technical: hide, not disable (amendment c); gate Start reviewing only; no contract, permission, schema, kit, string, sync or docs/22 change; `pnpm docs:readme` not needed; one QA/13 row after merge.
- technical: the rule is `documentMachine.can(status, 'start_review')`, never a literal list in app code; the exhaustive `satisfies Record<DocumentStatus, boolean>` table is test-only and is enforced by `pnpm typecheck` (manager-app tsconfig includes `src`).
- technical: lane — its OWN lane `b2-h7-desk`, branched from main at or after 04c193e, first in a sequential chain DOS-031 → DOS-037 → DOS-030 (all three edit `documents.tsx:473-556`; building them in one lane avoids textual conflicts). It shares no file with h1-h6 (checked `git diff --name-only main` on each worktree: 0 hits on `modules/docint`, `manager-app/src/lib`, `manager-app/app/inbound`). `dependsOn: []` stands; DOS-164 (Sheet/Dialog call sites) is a textual neighbour only.
- FOUNDER DEFAULT: none — no product rule changes.

## What the verifier must prove
- Red: stub `mayStartReview` returning `true`, `documents.tsx` untouched → the three DOS-031 cases fail on assertions (not on a missing module). With the guard at `review.service.ts:91-94` commented out, the new docint.spec case fails (the machine throws at `transition`, l.148, after the session insert). Green: all four pass; restore the guard.
- Drift proof: delete one row of the typed table → `pnpm --filter @dos/manager-app typecheck` fails; restore. Then `pnpm lint && pnpm typecheck && pnpm test && pnpm format:check` in `frontend/`, and `pnpm --filter @dos/core lint`, `typecheck`, `build` and `test -- src/modules/docint` in `backend/` (DB-backed, `DATABASE_URL` from `backend/.env`).
- Web A/B/C/D exactly as the plan writes them (BEFORE from a Metro on the pre-fix tree, AFTER from one restarted on the fixed tree — a Metro under CI=1 never reloads). B reloads the page before the BEFORE press (same-input hooks keep their key, `react/index.tsx:379-386`; the server replays the stored 200, `idempotency.ts:61-74`). B's writes on the lane DB only. Match 409 sentences by substring (`can be reviewed`, `is reviewing this document`); Metro log has no `Web ERROR [ApiError`.
- Web 390×844, Android Pixel_7_API_36 (`-memory 3072`, openjdk@21; uiautomator dump before any press, DOS-158) and iOS Expo Go via `ios-drive.mjs has` — walk A on all three; B on web only (DOS-164).
- DB after the AFTER run: the reviewed document has one `submitted` session and no `open` row; no `POST …/review` in the manager-service log.

## Risks for the builder
- The nested ternary in JSX is prettier-reflowed; only amendment (b)'s regexes keep the guard green.
- `documentMachine` under vitest resolves to `backend/libs/domain/dist` — build domain (and contracts) in `backend/` first, as every lane does.
- The server change (a) alters no HTTP behaviour, so the walks do not depend on a rebuilt manager-service; rebuild core anyway so the lane's service carries it.
- Lane DB ids differ from dos_qa; pick rows by chip (Failed, Extracted, locked Needs review), never by the ids in the plan.
- Do not press Start twice on one row without a reload (R2) and do not "fix" it here.

## New findings outside this fix
- `frontend/manager-app/app/inbound/documents.tsx:277-280,293-294` (R1): after a successful "This reading is right", `done` keeps `sessionId`, so Save / submit / Give it up and the editable Bill no. and Amount (l.413, l.420) stay on a Reviewed document and every write answers 409 `review session … is submitted` (`review.service.ts:347-350`); clear the session and the header edits on submit success as l.518-520 does.
- `frontend/manager-app/app/inbound/documents.tsx:486` + `libs/api-client/src/react/index.tsx:379-386` (R2, code reading, not walked): Start again on the same document after Give it up re-sends the spent key; the server replays the first start's stored 200 (`platform/idempotency.ts:61-74`) and the panel holds an abandoned session; `startReview.reset()` when the session ends.
- `frontend/manager-app/app/inbound/documents.tsx:549-556` (R3): Reject the document is offered on a Failed document; `documents.service.ts:509` fires `reject` and `failed` is terminal (`document.ts:76`), so it always answers 409 — same class, gate with `documentMachine.can(status, 'reject')`.
- `frontend/manager-app/app/inbound/documents.tsx:537-548` (R4): Book it as a supplier bill is enabled on Extracted and Needs review documents with lines, and the server refuses anything not `reviewed` (`documents.service.ts:550-553`); DOS-030 covers only the brand-DMS 501 at l.545-549.
- `frontend/manager-app/app/inbound/documents.tsx:62-72` (P3): `DOC_FAMILY` keys `capturing`, `queued`, `in_review` are not `document_status` values (`contracts/src/docint.ts:159-169`), so `uploaded`, `verifying` and `extracted` chips fall to `neutral`; `satisfies Partial<Record<DocumentStatus, StatusFamily>>` would have caught it.
