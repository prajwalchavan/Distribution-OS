# DOS-037 — architect verdict (Fable, 2026-09-13)
**Verdict:** APPROVE WITH AMENDMENTS

Root cause holds and is complete: the 2026-09-05 narrowing was applied row by row and missed 24 rows (5 inventory, 7 procurement, 12 docint; recounted from permissions.ts:477-728), and the module `requireRole` sites mirror the same groups (stock.service.ts:89, cycle-counts.service.ts:207, supplier-invoice.service.ts:116/131/337/419/496/526, grn.service.ts:128/305, docint DESK at docint.internals.ts:30). No second path: inventory registers pulls only (inventory.module.ts:40-42), the worker runs docint as `system`, and the accountant's legitimate stock side effects (settlement.service.ts:178/193, credit-notes.service.ts:480) go through `InventoryService.post` (inventory.service.ts:174, no role check), not `StockService`. Design is minimal: matrix + mirrored role checks, no schema, no RLS, no migration, no screen code (manager-app gates at stock/index.tsx:68, inbound/index.tsx:107, inbound/documents.tsx:101-102 follow the matrix on web and native). Tests are at the right layers (matrix unit, HTTP 403 at the guard, FORBIDDEN at the service under `tenantStorage.run`) and are red today because the guard admits the accountant and the services answer 200/409.

## Amendments (binding)
(a) **Tripwire for the accountant's write surface.** Add to `backend/libs/contracts/src/permissions.test.ts` one test, "DOS-037 pins every non-GET procedure the accountant may call", that computes `allProcedures().filter(r => r.method !== 'GET' && isAllowed(r.permission, 'accountant')).map(r => r.path).sort()` and `toEqual`s a literal sorted list derived from the rebased matrix, grouped by source in comments (MONEY_DESK 2026-09-05; BILLING_ISSUERS; CREDIT_NOTE_RAISERS; claims coordination §6; docint CAP; the M2/M6/M7/M11 leftovers named in the new finding below). None of the 24 may appear. This closes the actual root cause: the next BACK_OFFICE write added by hand fails CI instead of reaching the accountant.
(b) **Seed attribution.** `backend/libs/database/src/seed-demo/stock.ts:265` (grn ledger `actorId`), `:308` (`approvedBy`), `:321` (`postedBy`) and `seed-demo/docint.ts:653` (`reviewerId`) become `people.manager.id`, so the dos_qa rebuild ordered by the 2026-09-12 receipt-number row shows no accountant-posted GRN, accountant-approved bill or accountant review. Edit the matching lines of `QA/tools/seed/REALISTIC-SEED-SPEC.md` if any name the accountant as poster/approver/reviewer (doc only).
(c) **No new role check on the internal ledger entry point.** `InventoryService.post` (inventory.service.ts:174) and the `withSystem` paths stay role-free; the verifier proves delivery.spec.ts:1522 ("the accountant settles") and the billing credit-note specs are green after the change.
(d) **Name the tuples by what they are.** In stock.service.ts the exported read list at :79-86 (owner/manager/warehouse/delivery/accountant/system) is named `STOCK_KEEPERS`, the same name permissions.ts:52 and cycle-counts.service.ts:57 use for owner/manager/warehouse. Rename it `STOCK_VIEWERS` (uses: :199, :322; inventory.spec.ts:341 comment) and write `STOCK_WRITERS` as `['owner', 'manager', 'warehouse', 'system']` with the comment "ROLE_GROUPS.STOCK_KEEPERS plus system". Otherwise the DOS-044 builder, editing `adjust`/`upsertLot` next, reads the wrong population.
(e) Everything else in the plan as written (Option A, all 24; `DESK_WRITERS = MANAGEMENT` in docint.internals.ts; MANAGEMENT in cycle-counts, supplier-invoice and grn services; the four existing assertions flipped as tightening; docint.spec.ts:870/:919 actor → `managerA`; header comments in docint.ts:29-34/:55 and procurement.ts:17-22; READMEs regenerated; docs/22 §8 + §11 row and republish; docs/23 §2.3/§8.18/§8.20).

## Decisions
- technical: Option A in full, not A-minus. `purchaseOrders.upsert`, `lots.upsert` (a lot carries MRP and case size) and `locations.upsert` (where stock lives) are M4/M16 writes; docs/23:205 makes those screens read-only for her and the finding's Expected says "no stock writes, no procurement commits".
- technical: docint CAP (create/pageUploadUrl/addPage/verifyQr/submit) stays with the accountant. It books nothing, the CAP tuple is one population (desk + gate, permissions.test.ts:678), and no finding asks for its removal.
- technical: RLS unchanged, no migration; 0045 stays free. Consistent with every MANAGEMENT narrowing (permissions.ts:9-10); the guard and the service check are the enforcement, the ledger and the specs prove it.
- technical: the service constants mirror the matrix plus `system` (MANAGEMENT already includes it, authz.ts:24), so the worker's docint steps and `createInTx` from `documents.approve` (documents.service.ts:627) keep working.
- technical: lane h4-stock, second in its chain, after DOS-074+097 (real overlap: permissions.ts inventory block and stock.service.ts) and before DOS-096; rebase on main for DOS-115 (orders rows, permissions.test.ts), DOS-043 (permissions.ts:662, permissions.test.ts:589-610) and DOS-106 before merging; merge before DOS-044 is planned. Shared files: permissions.ts, permissions.test.ts (h2, h5), stock.service.ts (h4, later DOS-044), docs/22 §8/§11 (every lane, append-only), seed-demo/stock.ts (h1 only if its seed change reaches it).
- FOUNDER DEFAULT: Option A stands on the 2026-09-13 approval of DOS-037 as written. If Tarsun's CA must in practice book supplier bills (Option B), that is a new §8 row before building, not something this lane assumes.

## What the verifier must prove
- On the lane DB, never dos_qa: the four planned tests fail on main at the named first assertion and pass after; the tripwire (a) is green and its literal contains none of the 24.
- `pnpm --filter @dos/manager-service test` (matrix gate: accountant 403 on all 24), owner-service and warehouse-service green; backend CI order green including `docs:readme:check` after `pnpm docs:readme`.
- `diff` of :3002 `/docs/openapi.json` before/after: exactly the 24 POST routes lose `accountant` in `x-roles`; `/inventory/adjustments` = owner, manager, warehouse; `/docint/documents/{id}/approve` = owner, manager; no GET row changes.
- Web as meena.joshi on :5174 at 1280x800 and 390x844: Stock rows inert, Balances/Ledger/Held load; Inbound and Documents (REL/26-27/00495) show no write button and the `app.accountantRead` sentence. API with her token: 403 "the accountant role may not call POST /inventory/adjustments" and the other six in the plan; `select count(*) from stock_ledger where note = 'DOS-037 verify'` = 0.
- Controls: vikas.kadam adjusts −1/+1 with notes and sees every Inbound/Documents action; dinesh.patil adjusts on :5176 and check-in `stock.transfer` still works; amendment (c) specs green; `grep -n requireRole backend/libs/core/src/modules/inventory/inventory.service.ts` still shows none inside `post`.
- Android (Pixel_7_API_36, −memory 3072) and iOS (Appium headless, never the Simulator panel) as meena: a Stock row tap opens nothing; the Documents panel shows the read-only line.
- `pnpm smoke --service manager` and `--service owner` end 0 BROKEN.
- After `pnpm db:seed` on the lane DB: `select count(*) from grns g join memberships m on m.user_id = g.posted_by and m.tenant_id = g.tenant_id where m.role = 'accountant'` = 0; the same for `supplier_invoices.approved_by` and `review_sessions.reviewer_id`.
- docs/22 §8 and §11 rows present, HTML rendered, artifact republished by the main session; docs/23 §2.3, §8.18, §8.20 edited as planned.

## Risks for the builder
- Rebase order matters: take DOS-074+097's inventory block first, then main for the three test blocks; re-run the whole permissions.test.ts, not one describe.
- Lint: `BACK_OFFICE` becomes unused in stock.service.ts:37 and cycle-counts.service.ts:32; `MANAGEMENT` must be added at supplier-invoice.service.ts:34-43 and docint.internals.ts:16.
- The tripwire (a) is a deliberate merge hotspot: DOS-115 (ORDER_PLACERS) may change the accountant's orders rows; whoever merges second re-derives the literal.
- docint.spec.ts: only :870 and :919 use the accountant; the review chain in that test already runs as other actors, so nothing else moves.
- Stale device bundles keep drawing the forms until Metro reloads; the server refuses and DOS-029/135 show the sentence. Restart :3001/:3002/:3004 and the :5174 Metro after rebuilding contracts and core.
- A review lock an accountant holds at deploy time can only be released by the owner or by expiry (review.service.ts:228-231); dos_qa has none open (1 submitted).
- DOS-044 (next on stock.service.ts) must express any desk-only rule with MANAGEMENT, never BACK_OFFICE.

## New findings outside this fix
- P3, product: permissions.ts:574/584/585 (billing.invoices.issueForPack/setEwayBill/requestIrn), :629 (warehouse.challans.recordEwb), :631 (warehouse.reservations.release, pinned at permissions.test.ts:500-503), :577 (importBrandDms), :831 (notifications.messages.resend) still let the accountant write on screens docs/23:205 calls read-only for her; founder's call whether issuing a bill is bookkeeping.
- P3, cosmetic: authz.ts:38 `requireRole` answers "This action needs one of: owner, manager, system", naming the internal `system` pseudo-role to a client whenever the guard passes and the service refuses.
