# DOS-044 — architect verdict (Fable, 2026-09-13)
**Verdict:** APPROVE WITH AMENDMENTS

Root cause holds on main: `StockService.adjust()` (stock.service.ts:247-269) runs only `requireRole(STOCK_WRITERS)` (:89, back office + warehouse) and the single floor is the balance CHECK `on_hand >= 0 OR negative_allowed` (schema/inventory.ts:172 → 400 at inventory.service.ts:740-745); the matrix row (permissions.ts:480) is procedure-level by design. dos_qa confirms: ledger 01a094e8… is +100000 `opening`/`adjustment` by dinesh.patil (warehouse) and it is the only positive warehouse `adjustment` row. The controller (inventory.controller.ts:46-50) is the ONLY caller of `adjust()`; billing's `invoice_cancel` and the desk's cycle-count post go through `InventoryService.post`, no sync upload handler exists (inventory.module.ts:39-41), so a handler-level narrowing closes the door and needs no migration or trigger. Contributing cause, also in scope: W8 offers "Opening stock" and a signed field whose iOS `decimal-pad` (native/controls.tsx:148) has no minus, so an iOS loader can ONLY add. The shape is minimal and right: no procedure, input, output, matrix row or schema change; `STOCK_ADDERS` + `mayPostAdjustment()` in permissions.ts beside `ADMIN_LEVELS` (the DOS-106 (b) precedent; the `AdjustmentReason` import is type-only and permissions.ts already reaches inventory.ts through contract.js); one predicate read by the server, the /docs example builder, smoke and W8; 403 FORBIDDEN with `data.code` (delivery.spec.ts:912 pattern), and api-client shows the sentence because `serviceMessage()` (errors.ts:108) only reads `data.body.message`. Money/quantities untouched (integer pieces), the ledger stays append-only, nothing is written before the refusal so no idempotency row is stored.

## Amendments (binding)
(a) Copy must not promise a desk step that has no screen. `inventory.cycleCounts.post` (permissions.ts:488, BACK_OFFICE) is called by NO manager-app or owner-app screen (manager `app/stock/` holds only index.tsx; owner has no counts route), so "the desk posts the difference" is a dead end today (new finding 3). Server sentence: "Only the owner or a manager can add stock or post opening stock. If the rack holds more than the books show, record a count and tell the desk; goods arriving come in on a GRN." `w8.addIsDesk`: "More on the rack than the books show? Record it under Counts and tell the desk — only the owner or a manager can add stock. Goods arriving come in on a GRN." The spec's `/owner or a manager/` and the no-UUID assertion stand; the e2e asserts the W8 line verbatim.
(b) Smoke: `planFor(op, ctx, chain, target)` (smoke-endpoints.mts:1257) carries the role on `target.role` (ServiceTarget :96), not `ctx.role`. Pin `qtyDelta: mayPostAdjustment(target.role, 'adjustment', 1) ? 1 : -1`.
(c) `mayPostAdjustment(role, reason, qtyDelta)` JSDoc states it guards the HTTP door only: internal modules post through `InventoryService.post` and never through `StockService.adjust()`, and `system` is deliberately NOT an adder. permissions.test.ts asserts `mayPostAdjustment('system', 'opening', 1) === false` and `mayPostAdjustment('system', 'damage', -1) === true` alongside the membership roles.
(d) W8: `REASONS` becomes a `useMemo` on `session?.role` INSIDE the component (the module-level const cannot see the session); `reason` state stays `AdjustmentReason`, default `'adjustment'`; `keyboard="decimal"` stays (the kit has no `numeric`); `w8.qtyNeeded` untouched (Move sheet :382).
(e) inventory.spec DOS-044: after the +100000 probe and its replay, also assert `select count(*)::int from idempotency_keys where tenant_id = … and key = <probe key>` = 0. That pins the ordering (refusal before `idempotent()`, stock.service.ts:250-251), which the ledger count alone does not.
(f) Lane and order: h4-stock, directly after DOS-074+097 (same files: contracts inventory.ts header, stock.service.ts `availability()`, the inventory rows of permissions.ts), before DOS-096. Drop DOS-043 and DOS-106 from dependsOn (106 is merged at 04c193e; 043 shares only the W10 block of warehouse strings.ts and the depart row of permissions.ts — different regions, merge order suffices). DOS-037 follows DOS-044 in h4: same matrix row, and permissions.test.ts:1048 equates the fill-rate rows to `permissionFor('inventory.stock.adjust')`, which DOS-037 changes in that lane.
(g) NOTES (examples.ts:4842) receives ctx only, as the plan says; the reworded note names both signs in one sentence. Do not widen the NOTES signature.

## Decisions
- technical: rule lives in permissions.ts (`STOCK_ADDERS = ['owner','manager']`, `mayPostAdjustment`), not inventory.ts and not a DB trigger — the HTTP handler is the sole door, `ref_type` is free text, and the cycle-count post / orders retailer-source narrowings set the handler-level precedent.
- technical: 403 FORBIDDEN, code `stock_add_desk_only`, checked after `requireRole(STOCK_WRITERS)` and before `requireDb`/`withTenant`/`idempotent`; a replayed pre-fix key is refused the same way (only QA's probe exists in dos_qa).
- technical: outside STOCK_ADDERS a row is legal iff `qtyDelta < 0 && reason !== 'opening'` — negative `adjustment`, `damage`, `expiry_writeoff`, `cycle_count` stay with the godown; `opening` is desk-only in both signs.
- technical: W8 takes "pieces going out" (positive), negates on send, four reasons, no Dialog in the Sheet (DOS-164); M16/O15 unchanged; `/docs` example is −1 on warehouse-service, +1 where the service serves an adder; smoke pins the same predicate.
- FOUNDER DEFAULT: Q1 (a) — refuse every positive and every `opening` adjustment from roles outside owner and manager; extra stock is recorded as a count and added by the desk, arrivals come in on a GRN. No cap (a cap adds in bites) and no approval queue (size L, new approval kind).
- FOUNDER DEFAULT: Q2 — owner and manager only; the accountant is not an adder (docs/22 §8 2026-09-05: accountant = money desk + reads). Until DOS-037 lands the M16 dialog still opens for the accountant and +N shows this sentence through `<Refusal>`; −N still posts. Reversible by adding `'accountant'` to STOCK_ADDERS.
- FOUNDER DEFAULT: Q3 — no confirmation for large reductions in this fix (the CHECK bounds them; a Dialog in W8's Sheet is broken on iOS until DOS-164); a later P2 once DOS-164 lands.

## What the verifier must prove
- BEFORE (lane at DOS-074+097, on `dos_test_b2_044`): POST :3004/inventory/adjustments +100000 `opening` as dinesh.patil → 200; `pnpm --filter @dos/contracts test -- src/permissions.test.ts` fails at import; `src/docs/examples.spec.ts` fails on warehouse's +1; the inventory.spec DOS-044 case fails at the first probe with 200.
- AFTER, API: +100000 `opening`, +5 `adjustment`, +5 `cycle_count`, −1 `opening` as warehouse → 403, `data.code = stock_add_desk_only`, message matches the (a) sentence, no UUID; replay of the +100000 key → 403; `stock_ledger` and `idempotency_keys` hold 0 rows for those keys; on_hand unchanged; −1 `damage` → 200 and on_hand −1. vikas.kadam +1 `adjustment` on :3002 → 200; sunil.tarsun +1 `opening` on :3001 → 200; meena.joshi +1 on :3002 → 403 with the sentence, −1 → 200 (Q2 transitional).
- Docs: `GET :3004/docs/openapi.json?fresh=1` example `qtyDelta: -1`, `reason: 'adjustment'`; :3001 and :3002 show +1; Execute on :3004 returns 200; `pnpm docs:readme:check` green; the summary text names the narrowing.
- Suites: `pnpm --filter @dos/core test -- src/modules/inventory src/docs/examples.spec.ts` green; `pnpm smoke --service warehouse --run-tag dos044`, then manager and owner, each 0 BROKEN; frontend `pnpm lint && pnpm typecheck` after `pnpm --filter @dos/contracts build`.
- W8 web at 1280×800 and 390×844 as dinesh.patil: four reasons, no `w8-reason-opening`, label "Pieces going out", "0" and "-5" keep `w8-adjust-do` disabled with "Enter the pieces going out", the (a) line is present, Move helper still "Enter a number of pieces", 2 Damaged → toast, ledger `damage` −2 by dinesh.patil, on-hand −2, no horizontal scroll. Android (Pixel_7_API_36, `-memory 3072`) and iOS (`xcrun simctl`, panel never opened): post 1 Damaged → a −1 row; screenshots.
- DB after the walk: the plan's step-8 query (positive or `opening` `adjustment` rows by a warehouse actor since the walk start) = 0. docs/22 §8 row + §5 note + §11 row written, rendered and republished; docs/23 :401 amended.

## Risks for the builder
- Rebase on DOS-074+097 first (inventory.ts header, `availability()` in stock.service.ts), then `pnpm docs:readme`; never merge README hunks by hand.
- W8 imports a runtime symbol from `@dos/contracts` (precedent: delivery-app `_layout.tsx:32`, `deliver.tsx:41`): build contracts before frontend typecheck and restart Metro with `--clear` if `mayPostAdjustment` is undefined at runtime.
- `import type { AdjustmentReason } from './inventory.js'` in permissions.ts must stay type-only; a value import would create a real cycle through contract.js.
- `addsStock(options)` must treat `roles` undefined/empty as +1 (documents built outside a service), matching `servesBackOffice` (examples.ts:2891); for sales/delivery/retailer documents the −1 example is unreachable by matrix and harmless.
- `Number.parseInt` on the decimal pad turns "2.5" into 2 (pre-existing, Move does the same) — out of scope, do not "fix" it here.
- The seed's positive `cycle_count` rows with the warehouse actor (seed-demo/stock.ts:152-156, `ref_type = 'manual'`) contradict the rule in spirit; leave them, the step-8 query keys on `ref_type = 'adjustment'`.

## New findings outside this fix
- P1 stock-from-nothing through Move: `transfer()` (stock.service.ts:271-318, BACK_OFFICE_OR_WAREHOUSE) posts `transfer_out` on the source with no floor but the balance row's `negative_allowed`, and every damaged bin is `negative_allowed = true` (tenant-bootstrap.ts:196-201; dos_qa 47/36/7 damaged-bin rows, all `t`) — a warehouse Move of N from the bin to the godown makes N sellable pieces and restocks damaged goods (cf. DOS-116). Fix shape: transfer refuses a source below zero unless back office/system.
- P1 same class: `inventory.locations.upsert` (permissions.ts:477, BACK_OFFICE_OR_WAREHOUSE) accepts `negativeAllowed` from a warehouse token (contracts inventory.ts:48-55; stock.service.ts:131-164) — a loader can create a negative-allowed location and transfer out of it. `negativeAllowed` should be desk-only.
- P2 no desk screen posts a cycle count: `inventory.cycleCounts.post` is unreachable from any app (manager-app/app/stock/index.tsx only; owner-app/app/stock/ has no counts route) while W8c (warehouse-app/app/stock/counts/index.tsx:4-7) tells the loader the desk posts it and docs/23 :876 names M16 and O15.
- P3 test coupling: permissions.test.ts:1048 asserts the reporting fill-rate rows equal `permissionFor('inventory.stock.adjust')` — an accidental tie that DOS-037 will break; compare to a literal role list instead.
