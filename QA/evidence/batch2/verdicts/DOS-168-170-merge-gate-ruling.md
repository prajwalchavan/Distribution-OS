# DOS-168 · DOS-169 · DOS-170 — merge-gate ruling (Fable, architect, 2026-09-19)

**Decision: MERGE NOW.** The walks are owed, not a gate. Read: the charter, STATE, money-1 and money-2, the design, the lane (`main..qa/b2-money`, 8 commits, 13 files), docs/22 §6/§8 (founder answer A of 2026-09-14), QA/13, the DOS-172 lane and design, and the code on the lane. Nothing was built, run or written outside this file.

Why. (1) The programme's convention is "Platform proof follows": every batch-2 P1 and DOS-167 itself merged on green automated gates and were walked after, and the founder-approved build plan of 2026-09-14 08:12 says in terms "Platform proofs run in a later run, once the devices are free". (2) The reviews do not contradict it: money-2's own decision line reads "then merge first as the design orders", and both "Walks" sections are lists of what is OWED (the design's §Platform proof, verbatim); the integration verifier read an owed list as a merge condition, and that reading is wrong for this lane. (3) Main today carries two confirmed P0 ledger faults this lane removes (a receipt banked twice; offline cash missed at day-end); holding a proven ledger fix for a screen walk that needs devices the DOS-167 re-proof is holding inverts the risk. (4) Merged is not closed: DOS-168/169/170 stay OPEN in STATE until every item below passes and I judge; no Q3 repair on the founder's `dos`, no docs/22 as-built row, no closure before that.

Pre-condition. The lane last merged main at 6f2614b; DOS-171 (d65034b) and ruling 3 (ce3dc8c) landed since. `git merge-tree` is clean, but the automated gate ran on the old tree, so it is re-run on the merged tree before the merge commit (item 0).

## Proof set — in this order; evidence under QA/evidence/batch2/dos-168-170/{api,web,android,ios}/

0. **Integration gate, pre-merge, no device.** Merge main into `qa/b2-money`; on that tree run backend `pnpm format:check && pnpm lint && pnpm typecheck && pnpm build && pnpm docs:readme:check && pnpm db:migrate && pnpm test` on `dos_test_b2_money`, frontend `pnpm lint && pnpm typecheck && pnpm test && pnpm build`, plus the kit cross-app guards (`@dos/ui` test, api-client test, examples.spec, the delivery/manager/owner permission matrices). Pass: every command exit 0. Then `git merge --no-ff` into main and push, message "Platform proof follows"; STATE: DOS-168/169/170 "MERGED, proof owed, OPEN".
1. **`pnpm smoke`, no device; first after the merge and before DOS-172 rebases.** Merged main's dist, eight services + worker on a fresh copy (`createdb -T dos_test_batch2b_template dos_proof_money`; `DATABASE_URL=… QA/tools/start-services.sh`, on the standard ports once the DOS-167 re-proof releases them). `pnpm smoke --run-tag money-1`, then the same with `--destructive`. Pass: 0 BROKEN on both; `delivery.collections.record`, `delivery.trips.settle` and every `receivables.receipts.*` / deposit operation OK; no new SKIPPED reason.
2. **Web — two desks bank one receipt (DOS-168); browser only, 1280×800 plus one 390×844 capture.** Two contexts (manager, accountant) on :5174 Day-end with one `collected` office cheque or cash receipt; both press Bank in the same second (`Promise.all`). Pass: exactly one 2xx; the loser sees "is deposited, not collected" (a batch: 409 `receipt_moved`); `receipts.status = deposited` with ONE deposit ref; ONE `ChequeDeposited` outbox row; ONE Dr BANK line for the amount; the trial balance balances.
3. **Web — the offline receipt settles (DOS-169); browser + API probe, 1280×800.** A trip loaded out per design Step 5, returned (`closing`), whose only cash X is a `receipts` op uploaded as the driver through `/sync/upload` (the S-76 skeptic shape; `collections` rows for the trip = 0). Pass: the cockpit shows X in "Cash taken" and expected = float + X; settle handing float + X → `settled`, variance 0, Cr CASH_VAN = X, the trip's CASH_VAN nets 0. A second such trip, float alone → 409 `settlement_needs_owner`, variance −X, one pending approval.
4. **Web — undo after settlement (DOS-170); browser, 1280×800.** The accountant undoes the item-3 receipt. Pass: the reversal credits CASH, never CASH_VAN; the trial balance (owner :5173 / accountant) shows CASH −X and CASH_VAN unchanged; the bill reopens for X; the settlement row is untouched. Before/after screenshots plus the `receipt_reversal` journal lines.
5. **Web — D8 with one receipt held; both widths in full (1280×800 and 390×844), delivery :5177; a browser is enough for this leg.** (A) `context.setOffline(true)`, D5 collect cash X, D8: `d8-uncounted` names X with "have not reached the office yet", the button is disabled with the `d8.pendingBlocks` sentence (count 1), hand-over = expected + X. Online → strip Updated, button enabled; RE-OPEN D8 → note gone, hand-over the same rupee; the server holds the receipt once. (B) money-2's named case: the phone offline holding X, the desk returns the trip for the driver and settles it while D8 is open; phone online → the op is refused `trip_settled` into the tray with the server's sentence; D8 re-opened shows hand-over = the settled `expectedCashPaise` (never more) and `d8.uncountedSettled` naming X; no journal line for X; the bill stays open until the cashier records it. A note that survives a re-open FAILS the item.
6. **Android — REAL DEVICE REQUIRED** (the SQLite outbox, airplane mode, background push and the tray on a phone exist only here). Pixel_7_API_36 `-memory 3072`, openjdk@21, delivery dev build, the driver from docs/18. Airplane on → D5 cash → D8 disabled with the pending sentence and the note; airplane off → Updated → re-open → note gone, enabled → check in (`trips.return` 200) → the desk settles green with float + X on web. Then leg 5(B) on a second trip. `adb exec-out screencap` per step. Pass: as stated, the op sent ONCE, no ANR.
7. **iOS — sanity of the same renderer**, `xcrun simctl` + QA/tools/ios-drive.mjs, never the panel. Stop delivery-service :3005, queue one receipt: `has "have not reached the office"` exits 0 and the button is disabled; restart → after sync and a re-open the gate clears. Three `simctl io booted screenshot`s. A failure here that Android does not reproduce is P3 and never blocks.

Items 1–5 need no device and run while the emulator is busy; 6 is the one that needs the Pixel 7; 7 is sanity. Then I judge per platform from the evidence (the DOS-167 pattern), the main session writes SUMMARY.md, and the three findings close only on "closed".

## (c) A walk fails after the merge
Default: a NEW finding on top — the walk itself confirms it, so it is filed as a DOS block tracked under DOS-168..170 (as S-138 was under DOS-167) and repaired forward on `b2-money-r1` (implement → adversarial verify → my review → integrate with the full gate → re-proof of the failed leg only → judge). The merge stands: a screen fault over a right ledger beats a wrong ledger under a right screen.
Revert (`git revert -m 1` of the merge, pushed, QA/13 entry) if and only if a walk shows the merged tree writing money WRONG where main did not — a journal that does not balance, a receipt banked or counted twice, CASH_VAN not at 0 after any of the four traced sequences, a settle decided against founder answer A — AND the forward fix is not on main within one working day. Screen-level failures (a sentence, the button, a stale note, a strand the desk can clear) never revert. On any failure DOS-172 does not rebase and wave 3 does not launch until the repair or the revert is on main; the founder's `dos` is not touched.

## (d) Ordering against DOS-172
Nothing in money waits for DOS-172; DOS-172 must not merge before money. The approved order money → DOS-172 stands: money's specs already depart through a confirmed sheet or carry no bill (settlement-money.spec.ts:94-95, :392-436; money-locks.spec.ts:293-336), so DOS-172's 5c changes none of them and only records its verdicts on the tree that holds them; DOS-172 rebases once, resolving `trips.service.ts` (money's import :64, constructor :181, `tripMoney` dep :1219) and the `contracts/delivery.ts` JSDoc hunks against money, never the reverse; money's smoke (item 1) runs on today's resolver, which DOS-172's 5d replaces. Items 0 and 1 precede DOS-172's rebase; items 2–7 may run while it integrates, since they edit nothing. At the rebase, fold DOS-172's `core/src/testing/load-out.ts` and money's `loadedOutTrip` into one helper (P3). The docs/22 §4 D5/D8/T1 edit stays one pass after both merge (amendment n) — the main session's, not a merge condition.

## (e) Risks nobody raised
1. **The D8 snapshot goes stale silently.** `useQuery` (api-client react/index.tsx:276-318) fetches once per mount and again only on invalidation; day.tsx invalidates `['settlement']` only after `trips.return`. Nothing refetches when the outbox drains or the desk settles, while `status.pending` and the local receipts are live — so "holds ₹X … not reached the office yet" can stand beside an ENABLED button until the driver re-opens D8, and the settled figure appears only on a re-mount. The rupee is right in every state (old expected + X = new expected + 0). Code reading; items 5 and 6 re-open D8 on purpose. If seen: P2 ux, fixed forward — invalidate `['settlement', tripId]` when `status.pending` reaches 0 and when `lastPulledAt` changes (two lines; group 14 owns the file).
2. **4b is totals minus totals, and that is my design.** Held money = Σ local `receipts` − Σ the server counted; exact only while this phone's table is a superset of what the server counted. A trip has a driver AND a helper (`isCrew`, delivery.internals.ts:236); a helper's landed receipt, or a desk receipt on the trip, not yet pulled masks this phone's own unsent one of the same amount — the note vanishes and, if both are cash, the hand-over drops by it; `useLocalTripReceipts` also caps at 200 rows. Bounded: the check-in gate reads the OUTBOX, so no van checks in over it, and the pull that runs with the push closes the window. The honest form reads held money from `useOutbox` (queued/sending `receipts` ops of this trip). Group 14, not this lane; item 6 walks one phone, accepted.
3. **Discard on a `trip_settled` cash op.** The tray discards refused ops only (ruling 2), and `trip_settled` IS a refusal — one tap removes the phone's only record of cash a shop already paid, and the office learns of it only from the driver: the DOS-169 honesty hole reopened at the width of one tap for the late case. A money op in the tray must show shop and amount, and its Discard must read "handed to the cashier"; one sentence to group 14 (attention.tsx). Not a merge blocker; founder answer A already relies on the cashier.

Fable, architect — 2026-09-19. Binding.
