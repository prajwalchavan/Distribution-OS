# Lane h4-stock (DOS-074+097, DOS-096) — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Lane `qa/b2-h4-stock` = `eb587b7` (074+097) then `7f7fc8b` (096) on `117717c`, 25 files, read through git objects only. Both fixes are the amended designs; the money path moves nothing for an order (proven by construction and by spec), the new read leaks nothing and pages honestly, and every cross-module call goes through `index.ts`. READMEs are not regenerated (12 files will change, listed below) — by the batch rule that is done in the lane right before merging and is not a blocker.

## Blockers / majors (must be fixed before merge)
none.

## Minors (may follow)
- **Two more copies of the godown rule survive**: `billing/invoices.service.ts:1276-1295` (private `warehouseLocation`, credit-note return to stock) and `delivery/settlement.service.ts:536-555` (same body). Amendment (a) named orders and warehouse only, so the lane is not at fault, but `inventory/index.ts:23-26` now says "no module keeps a copy of its own" and that is not yet true. One-line delegation each to `reservableLocationId(tx)` (both modules are downstream of inventory and already import its `index.js`); drop their `asc`/`locations` imports if unused. Do it in the DOS-131/133 warehouse-billing lane or as a tidy-up on main.
- **A failed refetch keeps a stale hint on screen.** `api-client/src/cache.ts:172-182` keeps the last good value and sets `error` when a revalidation fails; both hooks (`sales-app/src/lib/stock.ts:47-57`, `retailer-app/src/lib/stock.ts:43-53`) read only `stock.data`, so a device that read the godown once and then lost signal for an hour still shows that hour-old total. Same as before the lane and it is a real godown number, but the S3 comment promises "no signal → no line". Two-line fix when convenient: return unknown when `stock.error !== undefined` (or show the age). Not the truncated-page wrongness the findings were about.
- **Order tax and invoice tax can differ by one paisa per intra-state line** (pre-existing, now visible on R7): the quote/order use `percentOf(net, rate)` (`quote.service.ts:126`); the invoice uses `splitGst` = `2 × percentOf(taxable, rate/2)` (`invoices.service.ts:1421`, `domain/gst.ts:23-24`). SO-0885 line 1: 140425 × 18% = 25276.5 → order 25277, invoice 2 × 12638 = 25276. The rupee rounding absorbs it in the total except at an exact .50 boundary. Amendment (a)'s "bill equals the quote" therefore holds for the rounding rule and the payable, not for every GST paisa. File as P3; the clean fix is in `splitGst` (tax = percentOf(taxable, rate); cgst = floor(tax/2); sgst = tax − cgst) so both halves derive from the one figure, then the quote can never disagree with the bill.
- **Unfiled money hazards the DOS-096 review surfaced, none introduced here — file them**: (i) delivery van sale `stop/[id]/van-sale.tsx:193,217` shows `quote.totals.netPaise` under "Sale total" above "Take the money now": pre-GST money at the door, P1 candidate — the lane already gives it `totals.totalPaise`, a one-token change plus a strings check; (ii) `hsn_rates` has same-date duplicates (0406 at 500 and 1200, 2106, 2202) and `loadGstBps` orders by `effectiveFrom` only, so first-wins is heap order — add `desc(id)` as the tie-break in `quote.service.ts:402` and `billing.internals.ts:240`, and fix the data (a 4-digit HSN cannot carry two rates); (iii) `price_list_items.inclusive_of_gst` is ignored by engine, quote and order while R7 now prints "+ GST"; (iv) owner what-if `prices/index.tsx:347` still shows `netPaise` under `o8.quoteNet` (pre-GST, unlabelled).
- `quote.service.ts:122-125` throws INTERNAL_SERVER_ERROR "quote lost the GST rate" — unreachable (the engine already refused an unknown variant) but the right status for a defensive branch; leave it.
- After landing: docs/18 row; docs/22 needs no register entry beyond the batch-2 approval already in §8, but the one product-visible rule worth a line there is "the shop's payable = GST-inclusive, rupee-rounded; the stock hint counts only the godown orders reserve from".

## Amendments — satisfied? evidence
- **074+097 (a) satisfied.** `inventory/reservable-location.ts` (new): `findReservableLocationId` (nullable) + `reservableLocationId` (400 when none), first active `kind='warehouse'` by id; exported at `inventory/index.ts:26`; `orders.internals.ts:155-157` and `warehouse.internals.ts:226-228` are one-line delegations (their `asc`/`locations` imports dropped; `ORPCError` still used elsewhere). `availability.spec.ts` test 4 pins `reservableLocationId(tx) === godown` with a second active warehouse present, and a never-bootstrapped tenant gets `{items: [], nextCursor: null}` rather than a 400 from a read.
- **(b) satisfied.** `contracts/inventory.ts:138-153` `{variantId, available}` pieces only, `limit 1..500 default 500`, `cursor` = last variantId; route :369-377; `permissions.ts:479` ANY_MEMBER, comment :474-476 reworded. `stock.service.ts:214-237`: `sum(available)` from `sellable_stock` at exactly `location_id = godown`, `group by variant_id order by variant_id limit n+1` — a van, the damaged bin, in-transit, quarantine (if ever added) and a second warehouse are excluded by construction, and only positive rows exist in the view (0003:174). RLS: security_invoker view over `stock_balances_read`/`stock_lots_read` and `locations_read` = `tenantReadPolicy` (any member, schema/inventory.ts:66); the godown's id is never returned. Spec test 2: 64 pieces everywhere vs 38 at the godown net of 12 reserved; test 3 as a retailer: keys exactly `['available','variantId']`, pages 2+1 with the cursor = last variant, rep and shop read the same rows; test 1: 505 lots of one item overflow one `sellable` page yet give one row of 1,010.
- **(c) satisfied.** Both hooks key on `['inventory','availability', tenantId]`, staleTime 120 s (sales) / 60 s (retailer), the search text is not in the key, `cache.fetch` (cache.ts:143) returns the in-flight promise so S3 and S11 share one read, `availableOf` is a Map lookup. `readEveryPage` (DOS-095's helper, already exported — no `index.ts` change needed) with `maxPages: 20`: rejects on any failed page, `complete: false` on the cap or a stuck cursor; absent → 0 only when `complete`. `pages.test.ts:80-135` proves all three; the screens' "0 only after a complete read" lives in the two hooks and is proven by the walk (no app test runner).
- **(d) satisfied.** The rep/shop hint is godown-only; the raw `sellable` view is untouched, so DOS-140's view half stays its own finding.
- **096 (a) satisfied.** `loadGstBps` moved verbatim to `quote.service.ts:381-412` (same select, `lte effectiveFrom`, `effectiveTo` null-or-gte, `desc(effectiveFrom)` first-wins, same 400 text and `data: {hsnCodes, on}`); `orders/pricing-lines.ts` lost it and `hsnCode` entirely and takes `q.gstBps / q.taxPaise / q.lineTotalPaise` (:138-162). Arithmetic identical: `percentOf(paise(lineNet), gstBps)`, `lineTotal = lineNet + tax`, dated to `result.pricingDate` (the same date the old code passed). Header: pricing-lines still rounds `subtotal − discount + tax` (:171) and the quote rounds `net + tax` (:150) — equal because the engine's `lineNet = gross − discount − bargain` and `net = Σ lineNet` (domain schemes.ts:334, :342), both with billing's `roundToRupee` (invoices.service.ts:1506). `orders.spec.ts:1020-1067` places a retailer order from a quote and asserts every line's gstBps/taxPaise/lineTotalPaise and the header (4980 / +20 / 46500) equal the quote; the pre-existing 26_900 and 40_300 pins are untouched. Existing orders are not re-priced (writeLines runs only on create/setLines/submit/repeatLast/sync/van sale/AI draft). **DOS-160 not widened**: no mutation's output changed (`OrderDetail` lines already carried the three fields), `pricing.quote` is not `MutationBase` and is never wrapped in `idempotent()` so no reply is stored, and the new read is a GET.
- **(b) satisfied.** `pricing.spec.ts:602-634`: 400, message contains the HSN, `data` equals `{hsnCodes: [hsn], on}`, and the rated item still prices. R7 `order.tsx:644-659` `unratedHsnCodes()` reads `error.status === 400` and `data.hsnCodes` (`ApiError.data` passes `ORPCError.data` through, errors.ts:134-147); the sentence renders where the `Group` was (`testID r7-no-gst-rate`) and for a refused basket quote (`r7-order-no-gst-rate`), replacing "Prices could not be loaded" and the false "unpriced" list. The wording is the screen's own plain sentence naming the codes (`r7.noGstRate`), not the server string verbatim — meets the intent.
- **(c) satisfied.** `strings.ts`: `r7.askRate` "The rate you want, per piece, before GST", `r7.askListRate` "… + GST", `r7.perPiece` "… + GST"; `r8.lineGst` "+ GST {amount}" on R8 lines; R7 rows show `lineTotalPaise`, the panel Before GST · GST · Rounding (only when non-zero) · You pay = `totalPaise`, footer `totalPaise`.
- Architecture: contracts additive only; orders/warehouse reach inventory through `index.js`; pricing reads the global `hsn_rates` directly as billing, warehouse and docint already do; screens import only `@dos/ui` and read through `@dos/api-client`; one universal file per screen; docs/23 S3/S11/R7 updated, D6 keeps `sellable`.

## Instructions for DOS-126 and other later fixes that build on this lane
- `priceOrderLines(tx, quotes, args)` in `orders/pricing-lines.ts` is the ONLY place an order line gets money, and GST now arrives inside the `QuotedLine` (`gstBps`, `taxPaise`, `lineTotalPaise`). Never re-add an HSN lookup in orders; a re-priced line's tax is whatever the quote says for its new net at the same dated rate. `VariantPack` no longer carries `hsnCode`.
- `QuoteService.quote()` opens its own `withTenant` transaction (quote.service.ts:83), so a re-price inside `confirmInTx` cannot see the bargain approved by the same decision. Add a `quoteInTx(tx, input)` and make `quote()` call it — and move the post-engine block (`loadGstBps` → per-line tax → `roundToRupee`, :113-152) into that shared function, so both paths keep the one GST implementation. Price on the draft's stored pricing date and pass `orderId` (order-bound bargains, :286-312), so only the approved bargain moves; leave `priceLocked` lines alone; recompute the header exactly as pricing-lines does. Spec: after the last approval, `rate_paise = approved_rate_paise`, `tax_paise = percentOf(newNet, gstBps)`, header re-rounded, and the existing DOS-096 equality still holds.
- An unrated HSN at confirm surfaces as the quote's 400 naming the codes; keep it a 400, never a 500 — the sync path already turns it into a `SyncRejection` (orders.sync.ts:13-15).
- Van sales (`delivery/vansales.service.ts:87`) and AI drafts (`ai/drafts.service.ts:157`) also go through `writeLines`; any signature change to `priceOrderLines` reaches them.
- DOS-023/DOS-095-class first-page bugs: reuse `readEveryPage` with an explicit `maxPages`; do not add a second helper.

## Merge conflicts to expect and how to resolve
- `permissions.ts` vs main (h6 merged): h4's hunk is :471-482 (inventory block, one new row + comment); h6 is :1 (import) and :992+ (tail block) → auto-merge. Later DOS-115 (`ORDER_PLACERS` in `ROLE_GROUPS`, order rows mid-file) also clears it.
- `orders/orders.spec.ts` vs h5 `89873d7` (DOS-003): h4 adds `import type { Quote }` at :3 and one test before the `countRows` helper (~:1020-1067); h5 edits the import list at :37-45 and appends at EOF (:1618+) → auto-merge; if git stumbles on the import region, keep both. h5's coming DOS-004/DOS-115 (`orders.sync.ts`, approvals, `contracts/orders.ts`) touch nothing h4 changed; DOS-003's `variantName` on `OrderLineSchema` is the DOS-160 exposure, not this lane's.
- `warehouse/warehouse.internals.ts` vs h2 (DOS-043, not yet committed): h4 changed :2 (`asc` dropped from the drizzle import), :19 (new import) and :225-228 (`activeWarehouseLocation` body). If h2 edits the `VehicleLocation` block just above or uses `asc`, keep h4's one-line body and re-add `asc` to the import.
- `api-client/src/errors.ts` vs h3 (not yet committed): h4 does not edit it but `order.tsx:644-659` relies on `ApiError.status: number` and `.data: unknown` surviving; if h3 renames either, fix `unratedHsnCodes` (typecheck will say so). `pages.ts`/`pages.test.ts`: h4 only.
- h1 (receivables, numbering, migrations, seed): disjoint files. `orders.internals.ts`, `pricing-lines.ts`, `quote.service.ts`, the inventory module and the four app files: h4 only among current lanes.
- Build order after merge: `pnpm --filter @dos/contracts build && pnpm --filter @dos/core build` before the frontend typecheck; never build libs in the main checkout while a walk runs (services `node --watch` the dist).

## READMEs that will change
Regenerate in the lane right before merging (`pnpm --filter @dos/contracts build && pnpm docs:readme && pnpm docs:readme:check`), never hand-edit. Service READMEs (new `GET /inventory/availability` row + the reworded `sellable` summary): `backend/{owner,manager,sales,warehouse,delivery,retailer}-service/README.md`; of these, owner, manager, sales, delivery and retailer also change their `/pricing/quote` response sample (three line fields, three totals); warehouse-service mounts inventory but not pricing. App READMEs (endpoint tables): `frontend/{owner,manager,sales,warehouse,delivery,retailer}-app/README.md`. Unchanged: auth-service, admin-service, admin-app. Run `pnpm docs:readme:check` once more on main after the last batch-2 merge.

## Regression walk, API probes and SQL
- Ground truth first (stock moves with other lanes): the godown query from the plan (`sellable_stock` joined to the tarsun tenant, `location_id = (select l.id from locations … kind='warehouse' and active order by id limit 1)`), for Campa Cola 200 ml / 750 ml, Campa Orange 1 L, Godavari Cheese Slices 200 g (last read: 480 / 9,709 / 249 / 7 pc) and `count(distinct variant_id)` (166); `select count(*) from locations where tenant_id=<tarsun> and kind='warehouse' and active` = 1 (the multi-godown rule is spec-only: mark NOT TESTED in product).
- Sales web :5175 (rahul.deshmukh, 1280×800 and 390×844): S3 Take order → search "Campa": chips equal the SQL in cases (was 154/0/1 → 404/10/10); the draft line "N cs available" under the stepper matches; Network shows ONE `GET /inventory/availability?limit=500` (166 items, nextCursor null), no `/inventory/sellable`, no request per keystroke; open S11 within 2 min → no new request; S11 "In stock" lists only godown-stocked items, "All" shows the brick "0 cs available" on the listed items with no godown stock (count from SQL). DevTools offline + fresh load: no chip anywhere, no "0 cs".
- Retailer web :5178 (ramesh.gupta, Tarsun): R7 Godavari Cheese Slices 200 g "Only 7 pc left"; zero-godown items "Out of stock"; page search "Stock not known" = 0 while online; rows "₹7.54 per piece + GST"; basket Bourbon ×2 cs + Ghee 200 ml ×1 cs + UHT 500 ml ×1 cs → lines ₹1,657.02 / ₹3,399.95 / ₹797.76, panel Before offers ₹5,474.88 · Offers −₹237.20 · Before GST ₹5,237.68 · GST ₹617.05 · Rounding ₹0.27 · You pay ₹5,855.00, footer ₹5,855.00; Ask sheet "Today's rate is ₹7.54 + GST" / "The rate you want, per piece, before GST"; Place → R8 "You pay ₹5,855.00", line 1 "192 pc · ₹7.54 · −₹43.43 · + GST ₹252.77" beside ₹1,657.02; cancel it. Pre-place footer = R8 total = DB total if a scheme window has moved the figures.
- Android (Pixel_7_API_36, `-memory 3072`, openjdk@21): retailer Bourbon 1 cs → GST ₹130.29 · Rounding −₹0.13 · You pay ₹854.00, placed ₹854.00, cancel; retailer rows as web; sales Repeat last → draft-line hint (suggestion rows only after DOS-077). iOS (Expo Go, headless `ios-login.mjs` 5178 then 5175 after a keychain reset, `xcrun simctl io booted screenshot`): the Cheese row and the totals panel at iPhone width, the sales draft-line hint; never open the simulator panel.
- Owner :5173 (sunil.tarsun) Prices → What-if: still answers (the quote now fills six more fields); it shows the pre-GST net under `o8.quoteNet` — unchanged, file per minors. Delivery :5177 (a driver with vehicle stock on an active trip) van sale: the screen still lists vehicle stock through `sellable` and its quote loads; "Sale total" is still pre-GST — unchanged by this lane, file as the P1 candidate above; do not walk it as a pass.
- API (GET, tokens from DevTools): `:3003` rep and `:3006` shop `GET /inventory/availability?limit=500` → `items|length` 166, `nextCursor` null, `items[0]|keys` = ["available","variantId"], identical rows on both; `?limit=50` → 4 pages, cursor = last variantId, strictly increasing, union 166 distinct; `?variantId=<Campa Orange 1 L>` → one row 249; an item with damaged-bin or van stock (SQL in the plan) equals the godown-only sum, not the all-locations sum; `:3001` owner token → 200 (ANY_MEMBER); a shop token on `:3003` → 403 (service role gate, unchanged). `POST :3006/pricing/quote` with the three lines → `totals.taxPaise 61705, roundOffPaise 27, totalPaise 585500`, lines 1800/25277/165702 · 1200/36428/339995 · 0/0/79776. The unrated-HSN 400 cannot be shown on dos_qa (0 unrated listed items): prove it on a throwaway template copy by deleting one listed HSN's `hsn_rates` row → R7 shows `r7-no-gst-rate` naming it, or mark spec-only.
- SQL after the web order: `select order_no, subtotal_paise, discount_paise, tax_paise, round_off_paise, total_paise from sales_orders where source='retailer_app' order by created_at desc limit 1` → 547488 / 23720 / 61705 / 27 / 585500; its lines `gst_bps, tax_paise, line_total_paise` → 1800/25277/165702, 1200/36428/339995, 0/0/79776. Existing orders unchanged: `select count(*) from sales_orders where updated_at > '<merge time>' and state <> 'draft'` = only the walk's own.
