# Lane h10-console — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE
Range 43824b0..388987a (4201823 DOS-109, 388987a DOS-107), 21 files, read through git objects against main 7119dbc. Both verifier verdicts pass; nothing here overturns them. The change is right to put on main: the unlock is the lock's mirror behind the same door (PERMISSIONS `PLATFORM` + ADMIN_LEVELS `CONSOLE_SUPER` + `requireActiveAdminLevel` as the first statement inside `withSystem`, before the id-ordered `FOR UPDATE` of both rows and before `platformIdempotent`), the trail names people through the one sanctioned lookup, and no product rule moved.

## Blockers / majors (must be fixed before merge)
none.

## Minors (may follow)
- backend/libs/core/src/modules/platform-admin/platform-admin.spec.ts:296-304 — not Prettier-clean (the `.insert(memberships).values({…})` chain); `pnpm format` in backend/ on the lane during the merge step (h1-money precedent) or CI's first backend step fails.
- frontend/admin-app/src/lib/audit.ts:96-97 — `auditChange` has `case 'user.disabled'` but no `user.enabled`; unlock rows show "—" under Change (Why still shows). Fix: `case 'user.disabled': case 'user.enabled': return text(after.username)` plus one fixture in audit.test.ts.
- frontend/admin-app/app/users.tsx:368-370 — `ErrorState` from a failed unlock is shown again when the dialog is reopened (no `reset()`, by amendment (a)); cosmetic, the server no-op makes any retry safe. Clear `enable`'s error on open if the kit exposes it, else leave.
- backend/admin-service/src/service.spec.ts:65 — comment says "all fifteen procedures"; sixteen now. Comment only.
- backend/libs/contracts/src/admin.ts:521-541 — the vocabulary is compile-time only (`platform_audit.action` is free text, by design so old rows never 500): a future raw-SQL writer that omits `satisfies PlatformAuditAction` drifts silently. The core case asserts only its own fixture's rows. Acceptable now; when `admin.admins.close/reopen` (P2 below) lands, add its actions the same way.

## Amendments — satisfied? evidence
DOS-109 (a) subscriptions.service.ts:195-201 `status: statusToWire(before.status)`; core case asserts `from.status: 'trialing'` (spec ~:912); audit.test.ts has both `'trial'` and `'trialing'` fixtures reading "On trial". (b) audit.ts:84-90 right-hand side is always `word('revoked')`; dedicated test `{was:'requested', now:'rejected'}` → "Waiting for their owner → Handed back". (c) internals.ts `platformAdminNames` returns only what `dos_support_requester_names()` answers; mapper `names.get(id) ?? null` (console.service.ts:398); `auditWho` falls back to `word(actorRole)`; no directory, no `users.list` call in audit.tsx. (d) all SIX production writers typed: internals.ts:73 `writePlatformAudit`, tenants.service.ts:352 (via `setStatus.audit.action`) and :392/:398 `insertPlatformAuditUnderSystem`, subscriptions.service.ts:187, support.service.ts:88/:188, console.service.ts:185/:263 (`satisfies`), support-audit.interceptor.ts:72 (`satisfies`); verifier's `user.zapped` probe failed typecheck TS2322. (e) both raw inserts still `gen_random_uuid()::text`; cursor untouched.
DOS-107 (a) users.tsx:138-148 `idempotencyKey: uuidv7()` per call, no `reset()`, Dialog `busy` while pending. (b) smoke-endpoints.mts:1437-1443 `{ pathParams: { id }, pinned: { id } }` from the cached `disposableIdentity` (:818-823, no status filter); both ops in `STATE_TOGGLE_OPS` (:82-87). (c) `PLATFORM_AUDIT_ACTIONS` gains `'user.enabled'`; insert typed; `word.user.enabled` 'Login unlocked' (strings.ts:428). (d) console.service.ts ~:253-266: UPDATE `where id = :id and status = 'disabled' returning`, insert only `if (row)`; spec proves `updated_at` unchanged and `count(user.enabled) = 1` after a same-key replay and a new-key repeat, for a member and for a console account. (e) unlock Dialog is a sibling of the lock Dialog beside the Sheet (users.tsx:345-380), same kit component; DOS-164 (146eed2) is on main now, so the iOS walk is no longer blocked after merge. (f) examples.ts:786-791 drops the `active` filter and orders `(status = 'disabled') desc, created_at desc`; disable note keeps DESTRUCTIVE; enable override + note added.
Security focus, checked in code: owner/membership tokens are refused twice — admin-service's `TenantGuard` role gate (403 before any handler) and `permissionFor('admin.users.enable') = ['platform_admin']` with `isAllowed(...)` false for every `ALL_ROLES` entry (permissions.test.ts DOS-107 case); support/billing levels refused by `requireActiveAdminLevel` with the login still locked (spec case 3), and the DOS-106 locked-token sweep now carries the enable row. **The 5-failure lock:** `login` checks `locked_until` (auth.service.ts:169) BEFORE `status` (:189) and a success resets both counters (:229-232); a person unlocked while a `locked_until` is still running gets 423 with the minutes left, at most `LOCK_MINUTES = 15`, then signs in — never stuck, and the People panel shows that chip separately (users.tsx:266-268). Not a blocker; leaving it alone is right (it is brute-force protection on somebody else's account). Append-only holds: 0034 `platform_audit_append_only` (UPDATE/DELETE → `dos_reject_mutation`) and `platform_audit_guard` on INSERT; enable only inserts, under `withSystem`, with `admin_user_id` = the acting super. Staff names: `admin.audit.list` and `admin.tenants.get` are every-level reads, so super, support AND billing see colleagues' display names — the 0037 function names platform administrators only (the `platform_admins` join is inside it and DO-block guarded), never a distributor's person; acceptable. Filters and paging (docs/20): `platform_audit_tenant_idx`, `_admin_idx`, `_action_idx` are each `(filter, created_at)`, so every filter the screen sends is an index range with the IST window; page ≤ limit+1, screen asks 200 inside a ≤ 92-day window; the lookups are one deduped PK call per page, awaited sequentially on `tx`. Idempotency: `MutationBase`, `platformIdempotent` under the first membership's tenant with a request hash (a reused key with a different body → 409); a console account has no row and relies on the two-row lock + the `status = 'disabled'` guard, which the spec proves for both keys.

## Merge conflicts with main and how to resolve
Legacy `git merge-tree 43824b0 7119dbc qa/b2-h10-console`: **0 conflict markers**. Five files changed on both sides merge automatically (disjoint hunks): backend/libs/contracts/src/permissions.ts (main: `STOCK_ADDERS`/`mayPostAdjustment` + inventory rows; lane: two `admin.users.enable` rows), permissions.test.ts (main: DOS-037 list; lane: 15→16 pins + DOS-107 case), backend/libs/core/src/docs/examples.ts (main: inventory adjust examples; lane: admin override/note + disposable pick), backend/tools/smoke-endpoints.mts (main: `mayPostAdjustment` use; lane: `STATE_TOGGLE_OPS`, `disposableIdentity`, `planFor` case), docs/23 (main rows near 281/406/880/907; lane rows 13/14). Nothing else on main since the fork (h12-kit, h8-billing, h11-syncpull) touches a lane file.
`git merge-tree 43824b0 qa/b2-h10-console qa/b2-h13-owner-support`: **0 conflict markers**. The only shared file is frontend/pnpm-lock.yaml — admin-app importer (@~321) vs owner-app importer (@~1081), identical `vitest 4.1.11(...)` resolution, no `packages:`/`snapshots:` change (vitest is already resolved for other apps). Order: h10 first, then h13 does `git merge main`, `pnpm install --frozen-lockfile` in frontend/ (must be a no-op), regenerates READMEs (none for h13), merges.
Recipe (QA/tools/batch2/merge-set-a.sh): in the lane `git merge main` → backend `pnpm install --frozen-lockfile` → `turbo run build --filter=@dos/contracts --filter=@dos/core` → `pnpm format` (the spec drift) → `pnpm docs:readme && pnpm docs:readme:check` → commit READMEs + the formatted spec → `git merge --no-ff qa/b2-h10-console` into main → push. Then the main session's owed rows (below).

## What the later fixes must build on
- docs/22 (main session, owed): §8 dated precision to the 2026-09-12 row — "A super administrator can unlock a login (QA DOS-107): the reason is mandatory, 1-500 characters, audited as `user.enabled`; unlocking a login that is not locked answers 200 with no update and no audit row; an unlock restores `users.status` only — the sessions the lock ended stay ended, and memberships, `platform_admins.disabled_at` and the 5-failure password lock are untouched" — plus a §11 row; `python3 docs/tools/render-source-of-truth.py`; republish. docs/18 rows for DOS-109 and DOS-107; docs/23 §10 rows 13/14 are closed in-lane.
- `PLATFORM_AUDIT_ACTIONS` (contracts/admin.ts) is THE list: any new `platform_audit` writer (DOS-111's interceptor row, `admin.admins.close/reopen`) adds its action there, types it, and adds `word.<action>` in admin-app strings.ts.
- `platformAdminNames(tx, ids)` in platform-admin/internals.ts is the only name lookup in module 13; the tenancy copy stays separate (boundary). Every `toSupportGrant` call passes the looked-up name.
- DOS-112's path-vs-body sweep must include `admin.users.enable`. DOS-114's audit read inherits the longer query key (prefix `['admin','audit']` still invalidates).
- QA/tools/e2e/dos-107-unlock-login.mjs (plan §tests) is QA's to write; the same-reason lock→unlock→lock→unlock replay step is its proof of amendment (a).

## READMEs that will change
backend/admin-service/README.md (GET /admin/audit sample gains `actorName`/`tenantName`; new POST /admin/users/{id}/enable section, example, note and matrix row) and frontend/admin-app/README.md (endpoint table gains the enable row). `docs:readme:check` fails on the lane until regenerated in the merge step. No other README embeds the admin contract.

## Regression walk, API probes and SQL
After merge, libs rebuilt, admin-service :3007 and auth :3000 restarted, admin Metro :5179, on a dos_test_* copy (never dos_qa for the lock steps). Sign-ins dos.admin (super), dos.support, `Dos@1234`; tokens via QA/tools/tok.sh.
1. Seed two actors' rows: as dos.support ask for a read-only window on Tarsun ("DOS-109 walk: ask"); as dos.admin suspend then reactivate Kalyan Agencies with reasons; change Sai Distributors' plan and back.
2. Web desk 1280 → Audit trail: Who reads "Rohit Nair (Distribution OS)" / "Anita Rao (Distribution OS support)", no row reads "Distribution OS staff"; Distributorship shows the legal name; suspend row Change "Active → Suspended", Why as typed; plan rows "Standard · Active · ₹… → Pro · Active · ₹…"; support.read rows "GET /… · Answered". Tap the "Distributorship suspended" chip → network `GET /admin/audit?…&action=tenant.suspended`, rows narrow; Sheet → "Only this person" → `actorId=<dos.support id>`, chip shows her name; "Only this distributorship" → `tenantId=`; both combine; empty combination shows "Nothing matches these filters in this window"; Sheet shows Why, Change and "Everything recorded".
3. Web phone 390: chips wrap, Who chip shows the name (may truncate; Sheet in full). Android Pixel_7_API_36 (`-memory 3072`, `QA/tools/android-login.sh admin 5179 dos.admin ""`): chips scroll sideways, Sheet Why/Change, "Only this person" narrows.
4. Lock/unlock: People → fatima.shaikh → "Lock this login": dialog lists both distributorships under "It locks them out of", no "Nothing in this console puts it back"; confirm → chip "Locked out", panel offers "Unlock this login"; Locked-out scope → Unlock: dialog lists the same, confirm inert with empty reason, confirm with "DOS-107 walk" → she leaves the list; All → chip active. Audit trail top row "Login unlocked" (Change "—" until the minor above lands). Repeat lock → unlock with the SAME reason: network shows a different `idempotencyKey`, status active again.
5. API: as dos.support `POST :3007/admin/users/<id>/enable {idempotencyKey,id,reason}` → 403 "a support console account may not call admin.users.enable; ask a super administrator", status still disabled; an owner token (sunil.tarsun) on the same route → 403 before business logic; as dos.admin enable an ACTIVE login → 200 and no new row; `POST :3000/auth/login` for fatima → 200 after the unlock.
6. SQL: `select status, failed_login_count, locked_until from users where username='fatima.shaikh'` → active, counters unchanged; `select admin_user_id, tenant_id, payload from platform_audit where action='user.enabled' and payload->>'userId'='<id>'` → one row per unlock by dos.admin with the reason; `select count(*) from auth_sessions where user_id='<id>' and revoked_reason='platform_disabled' and revoked_at is null` → 0; `select status from memberships where user_id='<id>'` → active, active; plan §verify 7's cross-check (who / legal_name / reason match the screen 1:1); a new `subscription.updated` row's `payload->'from'->>'status'` is a wire word.
7. `pnpm smoke --service admin` → 0 BROKEN (enable OK or skipped); `--destructive` → disable then enable both OK, body id = path id in the verbose log; frontend `pnpm install --frozen-lockfile`, `pnpm test` (admin-app 6 specs), `pnpm build`; backend `pnpm format:check && pnpm docs:readme:check`.

## Defects outside this lane
- backend/libs/core/src/modules/platform-admin/console.service.ts:171-190 — `disableUser.run()` updates unconditionally and writes a second `user.disabled` row on every new-key call against an already-locked identity (the comment at :164-167 promises a no-op); guard on `status = 'active'` and audit iff a row returned, as enable does. P3.
- console.service.ts:373 — keyset cursor `id < cursor` under `order by created_at desc, id desc`, with v4 ids minted at :185 and tenants.service.ts:398; page 2 can skip/repeat. Compare on `(created_at, id)`, mint `uuidv7()`. P3 (verdict finding, untouched by amendment (e)).
- console.service.ts:409-413 — `entityType`/`entityId` filtered after LIMIT: a short page with a non-null `nextCursor`. P3.
- backend/libs/database/src/schema/platform-admin.ts:318-320 — no `(created_at, id)` index: the UNFILTERED default view (window only, newest first) is a seq scan + top-N sort at lakhs of rows. Add in the next expand migration. P3.
- backend/libs/core/src/modules/auth/auth.service.ts:173-191 — a disabled login with the RIGHT password gets FORBIDDEN, a wrong one the credentials error: a locked account's password can be confirmed from outside. P3 security (verdict finding).
- backend/libs/core/src/modules/platform-admin/internals.ts:144 — `platform_admins.disabled_at` has no product writer or undo (spec fixtures only): the same one-way, SQL-only shape DOS-107 just fixed, for the platform's own staff. P2 missing feature `admin.admins.close/reopen`, super only, audited; copy `enableUser`'s shape.
- backend/libs/core/src/modules/tenancy/support.service.ts:221-236 — second copy of the 0037 lookup; one helper beside `withTenant` in @dos/db would serve both without crossing the boundary. P3.
- frontend/admin-app/src/strings.ts:418,424,426 — `word.tenant.created`, `word.support.revoked`, `word.support.approve` name actions nothing writes to `platform_audit`. P3 copy hygiene.
- backend/tools/smoke-endpoints.mts:2437,2558 — two pre-existing `PermissionRole` vs `MembershipRole` errors under `tsc -p tools/tsconfig.json` (blame 8ea449a1); the tools tsconfig is outside CI's turbo typecheck. P3.
