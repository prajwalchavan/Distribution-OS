# Lane h13-owner-support — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Range 43824b0..038c09c, two commits (e10bab1 red specs against a stub; 038c09c fix). Eight files, all under `frontend/owner-app` plus one 3-line hunk of `frontend/pnpm-lock.yaml`. No backend, contract, permission, migration or README change. The fix is the right shape: the kit's `<Segments>` draws three (web/controls.tsx:387, native/controls.tsx:421), so the fourth view is now a single-select `<Chips>` in the Screen body; the decisions moved off a desk-only Register column onto page-level Panels; the fixed `hours: 4` became the owner's pick from `supportDecision().hours`; `.then(done, done)` became `.then(done, stayOpen)` with `<Refusal>` printing the service's sentence. The rules live in a pure lib (`src/lib/support.ts`) with `now` passed in, so the four specs are deterministic and red on assertions before the fix (verified by the independent verifier with two mutation probes).

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
- `QA/evidence/batch2/lanes.md` L28 and L33 lack the two transfer records the verdict required (the lane may not edit QA/): L28 imports `frontend/owner-app/src/lib/refusal.tsx` instead of copying `Refusal` again; L33 (owns `owner-app/package.json`, `strings.ts`) rebases on the `test` script + `vitest` hunk and the owner-app importer hunk of the lockfile. The orchestrator writes both at merge time.
- `app/settings/index.tsx:325-333` — for a grant that stopped waiting after its card was pressed, the approve sentence still prints "until <requestedAt + requestedHours>" (possibly past) before the 409 lands. Accepted deviation; a later polish may drop the sentence when `dialogDecision.kind !== 'waiting'`.
- A lapsed ask sits in history as an ochre "Requested" with no card and no explanation until DOS-110 gives it a server status; the console already says "Lapsed".
- h10-console's `admin-app/package.json` test script is `vitest run`; owner, sales and manager use `vitest run --dir src`. Harmless; align when L33 touches the file.

## Amendments — satisfied? evidence
- (a) YES — `src/lib/refusal.tsx` is new (58 lines, the manager copy with its DOS-029 comment under a provenance header); `index.tsx:36` imports `{ Refusal, stayOpen }` from it; `git diff 43824b0..038c09c --stat` lists no `owner-app/src/lib/ui.tsx`.
- (b) YES — `package.json` gains exactly `"test": "vitest run --dir src"` and `"vitest": "catalog:"`; the lockfile hunk is the owner-app importer's three lines, version `4.1.11(@types/node@26.4.1)(vite@8.2.2(…))`, identical to every other app importer.
- (c) YES — `support.ts:108-124`: `open` only if `active && expiresAt !== null && Date.parse(expiresAt) > now`; `waiting` only if `status === 'requested' && !askLapsed(grant, now)`; the fall-through is `{ kind: 'closed', actions: [] }` with no enumerated statuses. Spec 1 pins the lapse instant (waiting at −1 ms, closed at 0), a stale `active` read at its `expiresAt`, a null `expiresAt`, and an unknown `'lapsed'` status.
- (d) YES — `index.tsx:325-333` finds `dialogGrant` by `grantId` in `supportItems`, computes `dialogHours` once, and the sentence (`windowClosesAt(grant.requestedAt, dialogHours)`), `confirmLabel` (`o24.approveFor { count: dialogHours }`) and the body sent (`{ id: grantId, hours: dialogHours }`) all read it.
- (e) YES — `<Async state={[grants]} rows={5}>` with no `empty`; `cards.length === 0` renders `o24.noneWaiting` (testID `support-none-waiting`); the `settings-support` Register always renders and draws the kit's own empty state.
- (f) YES in code — the open card prints `decision.closesAt`, which is the row's `expiresAt`; nothing adds 72 h. Spec 1's LIVE_WINDOW asserts `closesAt === '2026-10-12T07:10:29.663Z'` (the template row dc9b8792 read from psql on the lane's private copy). The on-screen comparison against psql on the merged tree is still owed — regression walk step 1.

## Merge conflicts with main and how to resolve
- Against main as it is now (7119dbc): NONE. Since 43824b0 main touched, among lane-adjacent paths, only `frontend/owner-app/README.md` (regenerated by h9-desk; this lane does not touch it) and `frontend/pnpm-lock.yaml` not at all — main's lockfile is byte-identical to the base. `backend/libs/contracts/src/permissions.ts` changed on main (h9-desk) but `tenancy.support.*` stay OWNER_ONLY at :441-443.
- Against h10-console (merged first): ONE shared file, `frontend/pnpm-lock.yaml`. h10 adds the `vitest` entry to the `admin-app` importer (~line 321); h13 adds the same three lines to the `owner-app` importer (~line 1081), same resolved version. A three-way `git merge-file` of base/h10/h13 in the scratchpad gives 0 conflicts, so `git merge qa/b2-h13-owner-support` auto-resolves. Proof after merge: `cd frontend && pnpm install --frozen-lockfile` succeeds and `git status` shows no lockfile change.
- Platform-admin support files (`backend/libs/core/src/modules/platform-admin/support.service.ts`, `platform-admin.spec.ts`, `admin.ts`, `smoke-endpoints.mts`, `docs/23`): h13 touches none of them. No conflict. Owner and admin strings are different files.
- Semantic, not textual: h12-kit (on main) rewrote `frontend/libs/ui/src/native/feedback.tsx` and added the overlay host; `types.ts` (Dialog/Chips/Button props) is unchanged, so the page-level Dialog here compiles as before, but the Android and iOS dialog must be walked on the MERGED tree, not the lane's.

## What the later fixes must build on
- DOS-110 (server `lapsed`): add it to `SupportGrantStatusSchema`, derive it in both `statusOf()`s, exclude it from `openOnly` and `status=requested`, regenerate READMEs; then delete `askLapsed` from `owner-app/src/lib/support.ts:84-92` and `admin-app/src/lib/ui.tsx:365-372`. `supportDecision` needs no change (closed by default; the spec's `'lapsed' as unknown as …` cast becomes a plain literal). Owner history should then say "ask them to raise it again".
- A Settings badge for a waiting ask (decision 1) is one `tenancy.support.list({ openOnly: true })` read in `_layout.tsx` — only after DOS-110, or it counts lapsed asks.
- DOS-111 (L32) hangs support reads under `support-card-<id>`; L28 imports `refusal.tsx`; L33 rebases on the package.json hunk.
- The kit's silent `slice(0, 3)` wants a type-level fix (2/3-tuple `items` or a `segments()` helper), then the four owner surfaces listed below.

## READMEs that will change
None: no contract change, and an app README's generated table derives from the contract only. `pnpm docs:readme:check` stays green on the merged tree.

## Regression walk, API probes and SQL
Run on the merged tree against dos_qa (the lane's `dos_test_b2_h13` is not main). Sign-ins: owner `sunil.tarsun`, console `dos.admin`, a manager login from docs/18, all `Dos@1234`.
0. `cd frontend && pnpm install --frozen-lockfile` (no lockfile diff); `pnpm --filter @dos/owner-app test` → 4 passed; then the frontend-wide `pnpm lint && pnpm typecheck && pnpm build` when the Mac is free.
1. Owner web 1280×800 → Settings: `[data-testid="settings-view"] button` = 4; press Support access. Card of the live seeded grant: "Rohit Nair (Distribution OS)", Read only, the reason word for word, "Open until" = the row's `expires_at` from `psql -h 127.0.0.1 -p 5439 -U dos -d dos_qa -c "select id, requested_hours, requested_at, approved_at, expires_at, revoked_at from support_grants where tenant_id=(select id from tenants where slug='tarsun') order by requested_at desc"` — NOT requested_at + 72 h. Revoke → POST `/tenancy/support-grants/{id}/revoke` 200, `revoked_at` set, `audit_log` has `support.revoke`.
2. Console :5179 → Tarsun → Ask for support access, 2 h read-only. Owner card: "Hours asked 2 h", chips exactly "1 h" and "2 h", "Counted from when they asked"; pick 1 h → "Open until" = requested_at + 1 h; Approve for 1 h → POST body `hours: 1`, 200; psql `expires_at = requested_at + 1 h`; console Support › Open now shows the window closing at that same instant; audit `support.approve` with after.status `approved`.
3. API probes as owner (curl, fresh 2 h ask): approve `{hours: 4}` → 400 `window_too_long` "shortened, never lengthened"; approve twice → 409 `already_approved`; approve a refused one → 409 `grant_closed`; approve 7232298a (lapsed) → 409 `request_expired`; the same POST as manager → 403; as `dos.admin` → 403. No 500 and no `window_too_long` anywhere in the owner app's own network log.
4. Owner web 390×844: the chip row wraps to show all four; a fresh 4 h ask → Refuse → confirm → `revoked_at` set, `approved_at` null, history chip "Rejected", console shows it refused.
5. Two owner tabs on one ask: approve in A; in B press Approve → the dialog stays open and `settings-support-refusal` reads "this support window is already open"; close and reopen clears it.
6. Android (Pixel_7_API_36, `-memory 3072`): `node QA/tools/proxy8081.mjs 5173`, `QA/tools/android-login.sh owner 5173 sunil.tarsun Owner`; the chip row scrolls to Support access; hour chips, Approve for N h (Dialog opens on the h12 overlay host) and Refuse work.
7. iOS: `node QA/tools/ios-login.mjs 5173 sunil.tarsun owner Owner`, then `QA/tools/ios-drive.mjs` for the same walk; page-level Dialog, no Sheet, so DOS-164 is not involved. If the simulator drive fails, record it rather than skip.

## Defects outside this lane
- `backend/libs/database/src/seed-demo/platform-admin.ts:236` — the demo's live grant asks 72 h but is inserted approved to +29 days; after this lane the owner reads "Hours asked 72 h · Open until 12 Oct". The guard allows it on INSERT (0034 rule (b), ≤ 30 days); "never lengthened" is enforced on UPDATE only (0036:168-177).
- `frontend/libs/ui/src/web/controls.tsx:387`, `native/controls.tsx:421` — `<Segments>` drops items past three silently. Still broken in the owner app: `app/prices/index.tsx` ('quote'), `app/staff/index.tsx` ('bounds'), `app/stock/catalog.tsx` ('brands'), `src/lib/ui.tsx:230-240` `RangeSegments` ('fy' → "This FY" unreachable on billing, credit-notes, money, books, receipts). P2; P1 for the money screens.
- `backend/libs/core/src/modules/tenancy/support.service.ts:168-203` — `revoke` on an already-expired approved grant records `revoked` (audit before `approved`) instead of refusing; the row's honest status was `expired`.
