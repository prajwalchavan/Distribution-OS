# Slice h5-orders — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE
Range 117717c..46fdd38 (89873d7 DOS-003, 46fdd38 DOS-004); 11 files, +463/−32; read through git objects only, nothing run.

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
- `frontend/owner-app/app/index.tsx:118-120`, `frontend/manager-app/app/orders/index.tsx:313-315` (and :322): the type guard `(p): p is string => p !== null` keeps `undefined`, so a row hydrated from a PRE-fix persisted cache paints " · " until the mount refetch lands (sub-second, deploy window only). Use `typeof p === 'string'` so it falls back to `word(row.kind)`.
- `frontend/owner-app/app/approvals.tsx:189` `creditLine()`: `credit.data === undefined` prints "Credit not checked" while loading AND on error; a loading word (or `credit.error` → o3.creditUnknown only) is more honest. Cosmetic.
- `trip_settlement` approvals (no order) still list as the kind word with "—"; name the trip in a P3 follow-up (delivery/settlement.service.ts writes `settlementId` in the payload).
- `frontend/retailer-app/app/orders/[id].tsx:183` still names lines through `useItemNames()` (listedOnly, 500 cap → "item unknown" for a delisted item); switch to `line.variantName` — the field now exists on the retailer service too (spec proves the shop GET is named).
- DOS-160 (open P2) stays: stored idempotent replies are re-validated against the new contract. Acceptable now (local, pre-deploy, 24 h retention with the worker up). Every `pnpm smoke` on the same IST day as a pre-fix run must pass a fresh `--run-tag` (smoke-endpoints.mts:56, :206 keys `orders.approvals.decide` and the other non-run-scoped mutations by IST date).

## Amendments — satisfied? evidence
- DOS-003 (a) `variantNames(tx, ids)` exported by tenant-catalog, alias first then global name, ONE query, LEFT JOIN on the listing: `tenant-catalog/import.ts:310-328`, exported at `index.ts:24`; orders only calls it (`orders.mappers.ts:5,135-138`) and joins no catalog table. Tenant comes from `currentTenant()`, safe because `OrdersService.detail` (orders.service.ts:588) already requires the context; every `loadDetail` caller is a guarded request (orders.service, approvals.decide, vansales.create, ai.drafts.confirm via submit); no worker/withSystem caller. No snapshot column; `OrderLineSchema.variantName` output-only (`contracts/orders.ts:74-79`), `OrderLineInput` untouched. Spec `orders.spec.ts:1791` asserts create reply + owner GET + retailer GET after `listed=false` — red before (field undefined), green after; placed last so no other test sees the delisting.
- DOS-003 (b) DOS-160 logged (QA/findings/10:997, 11:93); `--run-tag` in the smoke instruction above. Satisfied.
- DOS-004: `ApprovalQueueItemSchema = ApprovalSchema.extend({orderNo, orderTotalPaise, retailerId, retailerName}, all nullable)` replaces the item type of `orders.approvals.list` only (`contracts/orders.ts:161-167, 283`); `ApprovalSchema`, `OrderDetail.approvals`, `decide` untouched. The list LEFT JOINs the module's own `sales_orders` on (tenant_id, id) and names the shop through `retailerRefs` from `retailers/index.ts:41` (beat-reads.ts:95, tenant-scoped, one `IN` query) — no cross-module table join, 2 queries per page, no N+1 (`approvals.service.ts:61-88`). Built on merged batch-1 main (DOS-005/DOS-020 header comments present in approvals.tsx / manager orders). Three specs at `orders.spec.ts:449,484,517` cover product payload, seed-shaped payload (names come from `order_id`, not the payload) and a no-order approval (four nulls). Satisfied.
- Business rules: money stays integer paise (`salesOrders.totalPaise`, `PaiseSchema.nullable()`), no state or ledger writes, mutations untouched; nothing cost-bearing reaches salesperson/delivery/retailer — `variantNames` selects id/name/local_alias only (product_variants: global read; tenant_products: `tenantReadPolicy`, any member); the queue's four fields sit on a BACK_OFFICE procedure; `retailerRefs` returns name + beatId and only the name is emitted. `receivables.creditCheck` is CREDIT_CHECKERS (owner included), one read per selected credit_limit row, sentence built from `reasons[]` (approvals.tsx:186-204) so overdue-only never reads "₹0.00 over" and a stop still shows figures. Screens import `@dos/ui`, `@dos/api-client/react`, app libs and `expo-router` (allowed: not in `frontend/libs/config/eslint/app.js:33-56`; five owner screens already do). Web/native: same universal code; `router.push('/orders?q=…')` is honoured by `useLocalSearchParams` in owner orders/index.tsx:77-79.

## Merge conflicts with main and how to resolve
- `git merge-tree --write-tree main 46fdd38` → exit 0, clean tree 35197a8d. Only shared file: `backend/libs/core/src/modules/orders/orders.spec.ts`; git places h4's DOS-096 test at ~:1133 between the DOS-004 block (:434-530) and the DOS-003 test (last). No fixture interaction: DOS-004 leaves one pending credit_limit approval on Shop B; DOS-096 quotes Shop A with variantA/B; DOS-003 adds its own variants. `orders.internals.ts`, `pricing-lines.ts`, `permissions.ts` untouched by the lane → h4's `reservableLocationId`/GST changes merge untouched.
- READMEs: h4 already regenerated owner/manager/retailer/sales/warehouse on main; the lane never touched them → clean. Order: in the lane `git merge main`, then `pnpm --filter @dos/contracts build && pnpm --filter @dos/core build && pnpm docs:readme && pnpm docs:readme:check`, commit, merge into main, push. Never hand-resolve a README hunk. Rebuild the main checkout's libs only when no live probe or walk runs (services `node --watch` dist).

## What the later fixes must build on
- DOS-115 (requirePlacer in orders.sync.ts): untouched here; sync handlers never build an OrderDetail, so `variantNames` (needs `currentTenant()`) is never on the upload path — keep it that way.
- DOS-098 (Order again): the reply goes through `loadDetail`, so lines are named for free; `variantName` is OUTPUT only — never copy it into `OrderLineInput`; repeat from the last PLACED order's `sales_order_lines`, and a delisted item still names (LEFT JOIN).
- DOS-126 (confirm re-prices at the approved rate): the queue reads `orderNo/orderTotalPaise` LIVE from `sales_orders`, and the owner panel asks `creditCheck` with that total, so a re-priced total shows without touching `submitInTx`'s `payload {orderNo,totalPaise,flag}` (orders.service.ts:305, now informational only). `approvals.decide` still returns `OrderDetail` → named lines.
- DOS-127 (filed, P2): retailer-cancelled orders leave pending approvals the owner cannot clear; after this slice they list NAMED in the queue — fix `cancelInTx`'s expiry UPDATE (orders.service.ts:~421) under a context that sees the rows.

## READMEs that will change
backend/owner-service, manager-service, sales-service, warehouse-service, delivery-service, retailer-service README.md — every order-line sample gains `variantName`; the `GET /approvals` sample gains `orderNo/orderTotalPaise/retailerId/retailerName`. App READMEs: none (endpoint table only; no route, summary or role change). auth/admin: none.

## Regression walk, API probes and SQL
- Services :3001–:3006 restarted on the new dist; reload each app once (persisted cache repaints, then revalidates).
- API: `curl -s :3001/docs/openapi.json` → `/orders/{id}` line schema lists `variantName`; `GET :3001/orders/<id>` (owner bearer) and `GET :3006/orders/<id>` (shop bearer, own order) → every `item.lines[].variantName` non-empty; `GET :3001/approvals?status=pending&limit=20` → every order approval carries the four fields, bargain/trip rows carry four nulls; `GET :3001/receivables/credit-check?retailerId=<r>&orderTotalPaise=<t>` (owner) — `reasons[]` matches the panel sentence. As salesperson: `GET :3003/approvals` → 403.
- SQL truth (read-only): `select so.order_no, l.line_no, coalesce(tp.local_alias, pv.name) expected_name, l.free_qty_pcs from sales_orders so join sales_order_lines l on l.order_id=so.id join product_variants pv on pv.id=l.variant_id left join tenant_products tp on tp.tenant_id=so.tenant_id and tp.variant_id=l.variant_id where so.order_no in (…) order by 1,2;` and `select a.id,a.kind,o.order_no,o.total_paise,r.name from approvals a left join sales_orders o on o.id=a.order_id left join retailers r on r.id=o.retailer_id where a.status='pending' order by a.id desc;` — rows must equal the queue.
- Owner web (:5173 sunil.tarsun, desk 1280 + phone 390): Orders → SO-0854 (or any delivered order) panel names 6 lines with "10 pc free" on the Campa Cola 750 ml line, equal to INV/0824; an unbilled submitted order (SO-0885) is named; Android SO-0689 names the delisted Chamak Glass Cleaner 500 ml. Approvals (nitin.bhoir Kalyan): rows "Kailash Traders · SO-0178 · ₹4,158.00"; sheet title "Kailash Traders · SO-0178", Order field + Money, Reason, Credit "Owes … · limit … · Shop is on credit stop · Oldest bill N days past due · terms 14 days" and NOT "over the limit"; Sai Krupa → "Within terms"; "Open the order" lands on /orders?q=SO-0178; Approve dialog names the same (cancel it). Today "Needs you": "Kailash Traders · SO-0178 ₹4,158.00". Tarsun SO-0879 shows "takes it ₹17,873.00 over the limit" + overdue; SO-0882 overdue only.
- Manager web (:5174 ashok.kulkarni; accountant swati.naik read-only): cards "Kailash Traders · SO-0178 / Over credit limit / ₹4,158.00"; order panel lines named with "1 cs · 24 pc · 1 pc free" (SO-0867), "inner" wording. Android: owner approvals + sheet, owner order panel, manager SO-0870 panel. iOS: same three screens (universal code; not walked by the gate).
- If dos_qa was rebuilt from the fixed seed, pick equivalents by the SQL above rather than the order numbers.

## Defects outside this slice
- `frontend/manager-app/app/orders/index.tsx:285-292`: the manager credit sentence hides owes/limit for stop+breached and prints "₹0.00 over the limit" for an overdue-only or bill-count breach (headroom ≥ 0); rebuild it from `reasons[]` exactly as approvals.tsx:186-204 now does. Unfiled.
- `backend/libs/core/src/modules/orders/pricing-lines.ts:70-75` and `fulfilment.ts:6-10,161-173`: orders still joins `product_variants`/`tenant_products`/`retailers` tables directly (pre-existing); the amendment's rule (index exports only) should retire these two ad-hoc joins in a tech-debt item. Unfiled.
- `frontend/sales-app/app/orders/[id].tsx:73`: line names come from the offline catalog index (`byVariant`), which can lack a delisted item; the offline `sales_order_lines` table carries no name, so DOS-098/offline work should decide whether the manifest carries `variant_name` or the index keeps delisted rows. Unfiled, design-level.
