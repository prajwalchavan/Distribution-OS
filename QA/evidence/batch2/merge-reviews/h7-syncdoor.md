# Slice h7-syncdoor — architect merge review (Fable, 2026-09-13)
Range 5499c86..eb33170 (25a3329 upload door checks PERMISSIONS per synced table; eb33170 receipts INSERT policy = money collectors, migrations 0045/0046). Read through git objects only; no build, no test run, no worktree touched.

**Decision:** MERGE

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
1. `backend/libs/core/src/modules/sync/sync.service.ts:163` — `isPrivilegeViolation` is SQLSTATE 42501 only (pg-errors.ts:19), which is right for a policy or a guard trigger, but a missing GRANT on a new synced table also raises 42501 ("permission denied for table …"). That is a deployment bug, not this actor's refusal, and it would be recorded as a durable `not_permitted` on every device. Narrow: `isPrivilegeViolation(error) && !/^permission denied for/.test(pgMessage(error))` (the RLS message is "new row violates row-level security policy"); a one-line spec beside (e).
2. `backend/libs/contracts/src/sync.ts:47` — `not_permitted` is a string literal in the service while docs/27 §6 now names it beside `role_not_allowed`; add `notPermitted: 'not_permitted'` to `SYNC_REJECTION_CODES` so the device can switch on it the day it wants to.
3. `sync.service.ts:167` — the Hindi sentence of a `not_permitted` rejection is the Postgres message. A fixed Hindi line ("डेटाबेस ने यह बदलाव अस्वीकार किया") reads better in the tray; the English keeps the trigger's sentence.
4. Three tuples now say "money collectors": contracts `MONEY_COLLECTORS` (permissions.ts:105), core's copy (+`system`, receivables.service.ts:160) and `MONEY_COLLECTOR_ROLES` (columns.ts:73). Pin them equal in a core spec the way orders.spec.ts pins ORDER_PLACERS, so a founder reversal moves all three.
5. `columns.ts:249-256` — `receipts_write_update/delete` stay staff-wide by design; no application path lets a salesperson UPDATE a receipt today, but the database still would. Candidate for the h1 money lane: narrow update to MONEY_DESK + delivery + system, delete to nobody.
6. sync.coverage.spec.ts:713 — test (a)'s warehouse half was never seen red (the loop stops at the rep's assertion); split into `it.each(['rep','store'])` so each half reports alone. Coverage note only: (e) and probe 03 prove the 500 path.
7. Owed to the main session, not the lane: docs/22 §8 row, §9 never-list #2 wording ("— database policy plus the upload door"), §11 row, HTML re-render + artifact republish; QA/13 row in its own commit; `pnpm db:migrate` on dos_qa and on the founder's `dos` before any probe.

## Amendments — satisfied? evidence
- Design table (12 tables → procedure → roles): satisfied. delivery.module.ts:78-95, docint.module.ts:39-44, orders.module.ts:36-41, receivables.module.ts:35-42, warehouse.module.ts:46-48 name exactly the tabled paths; every path is a PERMISSIONS key (permissions.ts:442-710); `git grep registry.register(` on the tip = 14 = 12 + the two spec fakes (`retailers.visits.record`). Test (c) pins all 13 and equates `mayUploadTable` with `isAllowed` over ALL_ROLES.
- (1) contracts `roleNotAllowed` + doc sentence: sync.ts:37-47; no route or shape moved (`code: z.string()` at :83 unchanged; lane's docs:readme:check exit 0).
- (2) registry: `register(table, handler, spec: UploadSpec)` required (:124); empty list throws (:127), undeclared path throws (:129-133) — fail closed at boot; `mayUploadTable` (:145-151) = unregistered → false, `system` → true, non-membership (curator/support/platform_admin) → false, else EVERY path allowed; `uploadTables()` (:154); manifest `writable = mayUpload && handlers.has && mayUploadTable` (:202).
- (3) service: the branch sits after `unknown_table` and before `try { tx.transaction(...) }` (:123-135) — after the `sync_ops` replay lookup (needed) and before the savepoint, `vetoIfStale` and any `nextDocumentNumber` draw; falls through to `sync_errors` + `sync_ops` (:174-190); HTTP stays 200. Test (a): rep and store → 200, accepted 0, `role_not_allowed`, tray row, `sync_ops` ok:false, receipts/journal_entries/journal_lines/allocations/ReceiptRecorded counts unchanged, replay 1.
- (4) five module files only; `*.sync.ts` untouched (stat). (5) no per-handler check added. Client: no frontend file in the stat; engine.ts:737 special-cases only `stale`, every other code is stored and shown.
- Warehouse 500 → `not_permitted`: sync.service.ts:163-168 using `isPrivilegeViolation`/`pgMessage`; test (e) fakes `cause.code = '42501'` through the real loop → 200, row, replay. The only runtime 42501 raisers on this schema are RLS policies and the settlement-approval trigger (0015) — per-actor refusals; see minor 1 for the GRANT edge.
- Database: `MONEY_COLLECTOR_ROLES` (columns.ts:73-79), `staffWritePolicy(name, { insert })` narrowing the INSERT check alone (:243-250), schema/receivables.ts:235; 0045 = one `ALTER POLICY receipts_write_insert … IN ('owner','manager','accountant','delivery','system')` (same name, expand-only, in place); 0046 = DO block asserting FORCE RLS, no FOR ALL, an INSERT policy whose WITH CHECK names neither salesperson nor warehouse AND is an allow-list (names 'delivery', no `<>`) — the extra clause is what makes a regression to `<> 'retailer'` fail, a correct deviation. Snapshot chain: 0043 d5efecec → 0044 71fb2489 → 0045 a64316ab (prevId 71fb2489); journal 44/45/46 `when` 1789273155181 < 1789285591648 < 1789285591649; 0046 carries no snapshot. Every INSERT path checked: `createReceipt` requireRole(MONEY_COLLECTORS) :717 → `recordReceipt` :592 → `insertReceiptIfNumberFree` :1614; collections.service.ts:84/171/217 MONEY_COLLECTORS; the sync handler now crew-and-desk only; the reversal/bounce path :1814 is MONEY_DESK; the worker only UPDATEs `pdf_object_key` as `system` (update policy untouched); the seed writes with the owner `dos` connection (BYPASSRLS); `payments.initiate` creates no receipt. Test (f) refuses salesperson and warehouse, accepts delivery and accountant in rolled-back transactions.
- Manifest per role (my own reading of sync-tables.ts:80-107 + pullRolesFor): delivery = receipts/trip_stops/deliveries writable (unchanged); salesperson = sales_orders/lines writable (unchanged; ORDER_PLACERS keeps it after DOS-115); warehouse = pick_lines writable (unchanged); accountant loses trip_stops, deliveries, pick_lines now and sales_orders/lines after DOS-115; owner/manager unchanged; documents/document_pages have no pull, so no manifest. Only the accountant's `schemaVersion` moves (schemaHash hashes `writable`, sync.service.ts:376), and no desk app mounts `<OfflineProvider>`. A field device whose hash ever moves flushes its pending ops first, then drops and re-snapshots (engine.ts:328-350) — nothing is lost.
- Founder defaults 1-3: as designed (allocations = MONEY_DESK; INSERT list = owner, manager, accountant, delivery, system; nothing else moved).
- Scale (docs/20): the check is one Map lookup and ≤ 3 array reads per op, no query; the manifest gains no query.

## Merge conflicts with main and how to resolve
- h7 → main 722ae91: clean (only QA docs since 5499c86).
- h5 2ec9583 (next): no file overlap. BEHAVIOURAL, git will not flag it: orders.spec.ts 'DOS-115: a warehouse or delivery device upload cannot draft or re-line an order — rejected forbidden' (h5 diff :268-335) will FAIL on merged main — the registry answers `role_not_allowed` before `requirePlacer` runs, because `orders.create/setLines` become ORDER_PLACERS. Change the expected code to `role_not_allowed` (keep `requirePlacer` as defence in depth; a direct unit assertion of its `forbidden` may stay), and fix the comment at orders.sync.ts:19-21.
- h2 f92c47c: `0045_pack_confirmations_created_idx.sql`, `meta/0045_snapshot.json` (prevId 71fb2489 — the same parent as h7's) and its `_journal.json` idx 45 collide. After `git merge main`, delete those three from h2, run `pnpm db:generate` on the merged tree → `0047_pack_confirmations_created_idx.sql` + `0047_snapshot.json` (prevId must be a64316ab-2f7b-474e-a010-3c6e8ab768ba) + journal idx 47 with a fresh `when`; confirm the SQL is exactly the one CREATE INDEX from schema/warehouse.ts. Recreate the lane DB from the template. permissions.ts: h2 only adds `delivery.trips.planning`; none of h7's standsFor paths is renamed, so the boot check passes.
- h1 460269e: `delivery.module.ts` — h1's hunk (:69-79, `receivables` ctor dep + `registerTripSettled` above `if (!this.registry) return`) carries the OLD register lines as context → textual conflict. Keep h1's two lines above the early return, then h7's five `standsFor` registrations. Nothing else overlaps.
- h3 d0100ce: `sync.service.ts` — h3 adds `MAX_SYNC_OP_BYTES`/`refuseOversizeOp` near :48-70 and one line as the first statement inside the savepoint; h7's role branch ends right above `} else { try {` and its 42501 branch sits in the catch → adjacent hunks, expect a conflict. Final order: unknown_table → role_not_allowed → try { savepoint { refuseOversizeOp → vetoIfStale → handler } } catch { SyncRejection → ORPCError 4xx → isPrivilegeViolation → rethrow }. `docs/27`: h3 edits :92-99 and :204+, h7 :103-105 — keep both bullets.

## What the later fixes must build on
- DOS-115 (h5): the upload door's answer for a non-placer is `role_not_allowed` from the registry; `requirePlacer` is the second line, never the first. After both merge the accountant's manifest stops advertising `sales_orders`/`sales_order_lines` writable — consistent with the online door; test (d) is unaffected.
- DOS-056/156 (h3) and DOS-080: keep the role branch outside the savepoint and `writable` role-aware in `manifest()` (sync.registry.ts:202); DOS-080 edits the pull half only.
- Any new synced table: `register(table, handler, { standsFor })` is a compile error to omit; add its row to the pin in test (c). Never add a role list in the registry or a handler.
- h1 DOS-132 (settlement): the crew's offline receipt now reaches `recordReceipt` only as `delivery`; the policy admits it. S-09 (settlement ignores upload receipts) stands.

## READMEs that will change
None from this slice: the contract change is a constant, no route or shape moved (lane's `docs:readme:check` exit 0). The main session's regenerate run is a no-op for h7; the orders rows move with h5 and `delivery.trips.planning` with h2.

## Regression walk, API probes and SQL
1. `pnpm db:migrate` on dos_qa (47 migrations) and later on the founder's `dos`; `SELECT policyname, cmd, with_check FROM pg_policies WHERE tablename='receipts'` → insert = the five-role allow-list, select/update/delete unchanged.
2. probe.mjs 01–06 on dos_qa with fresh device ids → `QA/evidence/batch2/sync-role-probe/after/`: 01 delivery → 200 accepted 1, receipt + balanced journal + allocation + ReceiptRecorded + PDF; 02 salesperson → 200 accepted 0 `role_not_allowed`, `sync_errors` row (rahul.deshmukh, receipts), `sync_ops` ok:false, receipts/journal_lines/allocations/outbox counts and `numbering_series` RCPT `next_no` unchanged; 03 warehouse → 200 (not 500) `role_not_allowed`, same proof, and a `sync_ops` row now exists; 04 retailer → 403; 05 salesperson `POST /receipts` → 403; 06 salesperson trip_expenses → `role_not_allowed`. Re-send 02's batch → `replayed 1`.
3. `GET /sync/manifest` as ganesh.more → receipts/trip_stops/deliveries writable; as amol.vaidya → receipts writable, trip_stops not (sales_orders writable until DOS-115 merges, then not); as rahul.deshmukh → sales_orders/lines writable and `schemaVersion` equal to before the merge; as dinesh.patil → pick_lines writable, hash unchanged.
4. Apps (web :5175/:5176/:5177 and the Pixel 7): delivery app records a doorstep receipt offline → synced accepted; sales app drafts an order offline → accepted; warehouse app pick → accepted. Then `pnpm smoke` → 0 BROKEN (a `role_not_allowed` rejection is still a 200).
5. psql on dos_qa: `BEGIN; SET LOCAL ROLE app_rw; SELECT set_config('app.tenant_id','<tarsun>',true), set_config('app.actor_id','<rahul>',true), set_config('app.actor_role','salesperson',true); INSERT INTO receipts (id, tenant_id, receipt_no, series_code, fy, retailer_id, mode, amount_paise, received_at, received_by, idempotency_key) VALUES (…);` → `42501 new row violates row-level security policy`; same as `delivery` → INSERT 0 1; `ROLLBACK`.
6. Every service spec's `describePermissionMatrix` green on merged main (the `sync.upload` row is unchanged; the real registrations boot under the new check).

## Defects outside this slice
- `frontend/libs/offline/src/engine.ts:771` + `backend/libs/core/src/modules/sync/sync.service.ts:106` — `retry(opId)` "after the user fixed what was wrong" keeps the opId, and the server replays the stored rejection for any known `(device, opId)`, so a retried rejection can never succeed (docs/27 lines 71-72 and 168 contradict each other). Either retry mints a new opId and discards the old, or the server re-runs a stored REJECTION when the payload hash differs. By code reading; reproduce with a `stale` op on the delivery app's tray.
