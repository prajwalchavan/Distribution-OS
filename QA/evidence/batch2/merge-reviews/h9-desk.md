# Lane h9-desk — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Range e5f29ae..dd722a5, three commits (8798c18 DOS-044, d59c8fe DOS-037, dd722a5 DOS-031), 29 files. Read through git objects only; no build, test, service or git write. Merge simulated read-only with `git merge-tree e5f29ae main qa/b2-h9-desk`: zero conflict hunks, six files auto-merge, ONE semantic conflict (the DOS-037 tripwire vs DOS-115) that the merge commit must carry — named below. Nothing in the lane itself needs fixing first.

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
- `backend/libs/core/src/modules/inventory/stock.service.ts:106` `STOCK_WRITERS` and `modules/docint/docint.internals.ts:35` `DESK_WRITERS` are hand copies of the matrix tuples (plus `system`). They are pinned BEHAVIOURALLY — the DOS-037 specs call every narrowed service directly under an accountant context and expect FORBIDDEN, and the DOS-044/037 controls prove manager and warehouse still pass — but not by equality. Add the DOS-115 pattern (orders.spec.ts pins `ORDER_PLACERS`): `expect([...STOCK_WRITERS]).toEqual([...ROLE_GROUPS.STOCK_KEEPERS, 'system'])`, `expect(DESK_WRITERS).toEqual([...permissionFor('docint.review.start'), 'system'])`.
- `permissions.test.ts:1720` (DOS-044): also assert `expect([...STOCK_ADDERS]).toEqual(permissionFor('inventory.cycleCounts.post'))` — the two ways to put pieces into the books by hand (an adjustment, a posted count) are one population; today `STOCK_ADDERS` (permissions.ts:92) and `MANAGEMENT` (:181) are two literals that agree by convention and by the verdict's stated design (reversible by adding `accountant` to STOCK_ADDERS alone).
- `docs/23-app-screens-and-api-gaps.md:891` still says `supplierInvoices.dispute` is BACK_OFFICE (lane's own doc miss; §8.18/§8.20 were done).
- Main-session follow-ups the lane rule forbade: docs/22 §8 rows + stock-section note + §11 rows for DOS-044 and DOS-037 (DOS-031 needs none), `render-source-of-truth.py`, republish; QA/13 rows; `QA/tools/e2e/dos-044-warehouse-adjust.mjs` (plan CHANGE 13, carries amendment (a)'s verbatim W8-line assertion) is unwritten; `QA/tools/seed/REALISTIC-SEED-SPEC.md` names the accountant only in role tables (:448, :462-463, :903), never as poster/approver/reviewer — no edit needed.
- `seed-demo/stock.ts:152-156` still writes positive `cycle_count` rows with the warehouse actor (`ref_type='manual'`); unreachable by API, contradicts the rule in spirit — switch the actor to the manager at the next seed pass.

## Amendments — satisfied? evidence
- DOS-044 (a) YES: sentence verbatim at `stock.service.ts:303-307`; `w8.addIsDesk` verbatim in `warehouse-app/src/strings.ts:394-395`, rendered `stock/index.tsx:355` (`w8-add-is-desk`); inventory.spec asserts `/owner or a manager/` and no UUID on all four probes. e2e verbatim assertion: QA-side (above). (b) YES: `smoke-endpoints.mts:1478` `mayPostAdjustment(target.role, 'adjustment', 1) ? 1 : -1`, `target` is the `ServiceTarget` (role of the login used on that port); classify() untouched. (c) YES: JSDoc `permissions.ts:94-104` (HTTP door only, `system` not an adder); `permissions.test.ts` asserts `system opening +1 → false`, `system damage −1 → true`, plus null/undefined. (d) YES: `stock/index.tsx:64-68` `useMemo` on `session?.role`, `useState<AdjustmentReason>('adjustment')` :76, `keyboard="decimal"` :331, `w8.qtyNeeded` untouched (Move helper). (e) YES: `idempotency_keys` = 0 for the +100000 key after replay, ledger 0 for all four keys, on_hand 10, positive control (damage key = 1). (f) YES: DOS-074+097 already on the branch, only the type import / STOCK_ADDERS block / row comment touched. (g) YES: NOTES stays `() =>` (`examples.ts:4965`), one sentence naming both signs; sign comes from OVERRIDES `(ctx, options)` :3589 via `addsStock` :2907 (undefined/empty roles → +1).
- DOS-037 (a) YES: `permissions.test.ts:849` computes the accountant's non-GET set and toEquals a 73-row literal grouped by source; none of the 24 appear (asserted). (b) YES: `seed-demo/stock.ts:265,308,321`, `seed-demo/docint.ts:653` → `people.manager.id`; verifier seeded a scratch DB: 212 GRNs / 212 bills / 4 reviews, 0 by an accountant. (c) YES: `inventory.service.ts` not in the diff, no `requireRole` anywhere in it; delivery.spec 25/25 ("the accountant settles"), billing.spec 21/21 per verifier. (d) YES: `STOCK_VIEWERS` (:85-97, used at balances :249 and ledger :381), `STOCK_WRITERS = ['owner','manager','warehouse','system']` commented "ROLE_GROUPS.STOCK_KEEPERS plus system"; BACK_OFFICE import removed. (e) YES: exactly 24 rows change (4 → `ROLE_GROUPS.STOCK_KEEPERS`, 20 → `MANAGEMENT`), 25 `requireRole` sites mirror it (`DESK_WRITERS = MANAGEMENT` ×12, MANAGEMENT in cycle-counts post, supplier-invoice ×6, grn ×2); docint.spec approve/replay actor → `managerA`; headers in `docint.ts:29-34,52`, `procurement.ts:17-23`; docs/23 §2.3, §8.18, §8.20.
- DOS-031 (a) YES: `review.service.ts:92` `if (!documentMachine.can(doc.status, 'start_review'))`, message literal kept, still before the stale-session update; docint.spec "DOS-031: review.start refuses a reviewed document with 409 before it takes a lock" (409, `can be reviewed`, 0 `review_sessions` rows for the refused id); verifier proved it red with the guard commented out. (b) YES: `review-desk.test.ts` strips comments, every regex `\s*`, `docint-start` exactly once, one `<Button\b` between the last `mayStartReview(doc.status)` and the testID. (c) YES: `documents.tsx:483-495` renders `null`, no `disabled`. (d) YES: three hunks only (comment, import, Start branch). (e) n/a in code; `manager-app/package.json:24` links `@dos/domain`.

## Business correctness
- Stock from nothing via `inventory.stock.adjust`: closed for every role outside owner/manager — `qtyDelta > 0` any reason, and `opening` in both signs. The refusal (`stock.service.ts:303`) runs after `requireRole` and BEFORE `requireDb`/`withTenant`/`idempotent`, so no ledger row, no `idempotency_keys` row, and a replayed key is refused identically (spec-proven). `inventory.controller.ts:55` is the only caller of `adjust()`; internal posts (GRN, sale, return, count, settlement) go through `InventoryService.post` (no role check, unchanged). Ledger append-only: no schema, trigger or migration touched.
- Sibling paths (the verdict's two P1s) are NOT closed by this lane and stay open on main after the merge — already filed as S-47/S-48/S-49 in QA/findings/12: `transfer()` (`stock.service.ts:330`) has no floor but the CHECK (`inventory.service.ts:741`), the damaged bin is bootstrapped `negativeAllowed: true` (`tenant-bootstrap.ts:204`), and W8's Move is offered on every balance row including the bin's (`stock/index.tsx:281`) — a warehouse Move of N bin → godown still creates N sellable pieces; `upsertLocation` (`stock.service.ts:147,157`; `contracts/inventory.ts:55`) still takes `negativeAllowed` from a warehouse token. Cycle counts: no hole (count is STOCK_KEEPERS, post is MANAGEMENT, and no desk screen posts yet — S-49). The accountant's settle still posts van `cycle_count` rows via `InventoryService.post` with the variance gated to the owner (`settlement.service.ts:168`) — by design, unchanged.
- Accountant = Option A exactly: keeps CAP (create/pageUploadUrl/addPage/verifyQr/submit, also the DOS-166 sync doors `standsFor` those rows), every read, and loses the 24 writes at the guard AND in the services. Manager app draws no write control for her: stock rows inert (`stock/index.tsx:68,249-257,302`, transfer dialog only reachable from inside), Inbound open/dispute/post hidden (`inbound/index.tsx:107,435,512`), Documents review/rematch/approve/reject hidden behind `mayReview`/`mayApprove` (`documents.tsx:103-104,475,541`) with `app.accountantRead` (:564). All gates are `useCan → isAllowed(permissionFor(path))` (`src/lib/ui.tsx:600-603`): no second list. Web and native are the same universal screens.
- DOS-031: client (`review-desk.ts:19`) and server (`review.service.ts:92`) read the same `documentMachine.can(status, 'start_review')`; the typed exhaustive table is test-only and `satisfies Record<DocumentStatus, boolean>` fails typecheck on a new status. Server 409 remains the backstop for a stale cached status.
- Architecture: PERMISSIONS stays the single matrix (no row duplicated anywhere); `STOCK_ADDERS` + `mayPostAdjustment` live in permissions.ts and are the ONE predicate read by the server (:303), the /docs builder (:2907), smoke (:1478) and W8 (:66); pinned by `permissions.test.ts:1720` (exact tuple, every adder reaches the row; `ROLE_GROUPS.STOCK_KEEPERS ⊇ STOCK_ADDERS`). `AdjustmentReason` is a type-only import (no cycle). Wire shapes unchanged (one summary string). Screens import `@dos/ui` plus runtime helpers from `@dos/contracts`/`@dos/domain` (delivery-app precedent); core imports a contracts value like `isSaleableReturn`.

## Merge conflicts with main and how to resolve
1. **SEMANTIC, must be in the merge commit or main is red:** `permissions.test.ts:849` "DOS-037 pins every non-GET procedure the accountant may call" lists `orders.cancel`, `orders.create`, `orders.repeatLast`, `orders.setLines`, `orders.submit` (:868-872) under "every member or every staff member: the order pad …" (:864). On main DOS-115 made those five `ORDER_PLACERS` (owner, manager, salesperson, retailer). Delete the five rows and the words "the order pad," from the comment (73 → 68 rows); re-run the WHOLE `permissions.test.ts`. Nothing else on main changes the accountant's non-GET set: the only rows added since e5f29ae are the two GETs `orders.lastPlaced` and `delivery.trips.planning`.
2. Textual, auto-merged (verified, 0 markers): `permissions.ts` (main: ORDER_PLACERS block, orders rows, `trips.planning`; lane: import :5, STOCK_ADDERS :86-113, inventory/procurement :506-552, docint :730-770); `permissions.test.ts` (main: DOS-115/DOS-131 tests, `orders.lastPlaced` in a read list; lane: docint test, two DOS-037 tests, fillRate :1273, DOS-044 :1720); `examples.ts` (main: `orders.lastPlaced`/`trips.planning` overrides + QUERY_FILL; lane: `addsStock`, the adjust override, NOTES); `smoke-endpoints.mts` (main: `case 'orders.lastPlaced'` ~:1386; lane: import block + inventory case); `warehouse-app/src/strings.ts` (main: W7/W10 keys; lane: W8 keys); `docs/23` (main: W7 :401, §4.3, §5, §10 rows; lane: §2.3, W8 :407, §8.18, §8.20). `stock.service.ts`, `docint/*`, `procurement/*`, manager-app screens: main untouched. `docint.module.ts` (DOS-166 `standsFor`) does not touch DESK_WRITERS.
3. The `pricing.spec.ts` prettier drift the lane saw is already formatted on main (main changed the file since the base; the lane did not), so `format:check` is clean after the merge.
4. After the merge: `pnpm --filter @dos/contracts build && pnpm --filter @dos/core build`, `pnpm docs:readme`, restart :3001/:3002/:3004 and the :5174/:5176 Metros (`--clear` if `mayPostAdjustment` is undefined at runtime).

## What the later fixes must build on
- Any desk-only stock rule uses MANAGEMENT/STOCK_ADDERS, never BACK_OFFICE (DOS-037 would reopen at the handler); `StockService.adjust()` is the sole HTTP door and `InventoryService.post` stays role-free.
- S-47/S-48 (next on `stock.service.ts`): express the transfer floor and `negativeAllowed` as one predicate in permissions.ts beside `mayPostAdjustment`, read by W8 Move, smoke and the /docs transfer example (godown → van, unaffected today). DOS-116 (h8-billing) touches the damaged-bin restock — same theme.
- The tripwire literal is a deliberate hotspot: h10-console DOS-109/107, h13 DOS-108 and any new non-GET row admitting the accountant must re-derive it and carry a docs/22 §8 row. h11 DOS-080 (sync pull) does not touch it.
- DOS-164 (h12-kit): W8's Sheet gained a `Txt`, no Dialog; DOS-031 added none. DOS-030 edits `documents.tsx:537-548` right below the DOS-031 gate — textual neighbour only.
- S-49: a desk cycle-count post screen (M16/O15) is now what the W8 line and the server sentence point the loader to.

## READMEs that will change
All twelve (`backend/{owner,manager,sales,warehouse,delivery,retailer}-service/README.md`, `frontend/{owner,manager,sales,warehouse,delivery,retailer}-app/README.md`): the `inventory.stock.adjust` summary text changed and 24 rows lose `accountant`. `pnpm docs:readme` after the merge; `docs:readme:check` is red until then. Never by hand.

## Regression walk, API probes and SQL
Not run by the implementer, the verifier or this review (lane forbids servers; this review is read-only) — the gate must run, on a copy of `dos_test_batch2_template`, never dos_qa:
- W8 (dinesh.patil, :5176) web 1280×800 and 390×844, Android Pixel_7_API_36 (`-memory 3072`, openjdk@21), iOS via `xcrun simctl`: Adjust sheet shows four reasons (no `w8-reason-opening`), label "Pieces going out", `0` and `-5` keep `w8-adjust-do` disabled with "Enter the pieces going out", the `w8-add-is-desk` line verbatim, Move helper still "Enter a number of pieces"; 2 Damaged → toast, ledger `damage` −2 by dinesh.patil, on-hand −2, no horizontal scroll.
- M16 and Inbound/Documents as meena.joshi (:5174) web + Android: Stock rows inert, Balances/Ledger/Held load; no open/dispute/post; on REL/26-27/00495 no Start reviewing / Match again / Accept / Book it / Reject, the `app.accountantRead` line shown; capture still works if the manager app exposes it (it does not; CAP proven by the matrix spec). Controls: vikas.kadam adjusts −1/+1 and sees every action; dinesh.patil still Moves.
- Documents as vikas.kadam: a Failed row and a Reviewed row (start → This reading is right → RELOAD) show no `docint-start`; an Extracted row shows it and Start → 200; a locked Needs-review row → 409 "is reviewing this document" above the button, no overlay, Metro log has no `Web ERROR [ApiError`.
- API: warehouse token POST :3004/inventory/adjustments `{qtyDelta:100000, reason:'opening'}` → 403 `data.code=stock_add_desk_only`, message without UUID; also +5 adjustment, +5 cycle_count, −1 opening → 403; replay of the +100000 key → 403; −1 damage → 200. meena token POST :3002/inventory/adjustments −1 damage → 403 "the accountant role may not call"; same for /inventory/transfers, /procurement/grns/{id}/post, /procurement/supplier-invoices, /procurement/purchase-orders, /docint/documents/{id}/approve and /review; GET /inventory/balances, /procurement/supplier-invoices, /docint/queue → 200. vikas +1 adjustment :3002 → 200; sunil +1 opening :3001 → 200. POST /docint/documents/{reviewed id}/review as vikas → 409 `can be reviewed`. `GET :3004/docs/openapi.json?fresh=1` adjust example `qtyDelta:-1, reason:'adjustment'`; :3001/:3002 `+1`; none of the 24 POST routes lists `accountant` in `x-roles`; Execute on :3004 → 200.
- `pnpm smoke --service warehouse|manager|owner --run-tag h9`: each 0 BROKEN.
- SQL after the walk: `select count(*) from stock_ledger l join memberships m on m.user_id::text=l.actor_id::text and m.tenant_id=l.tenant_id where m.role='warehouse' and l.ref_type='adjustment' and (l.qty_delta>0 or l.reason='opening') and l.occurred_at > '<walk start>'` = 0; `select count(*) from stock_ledger where note='DOS-037 verify'` = 0; `select count(*) from idempotency_keys where key like 'dos044-huge%'` = 0; for the reviewed document: one `submitted` review_session, no `open` row; after `pnpm db:seed` on the copy: GRNs `posted_by`, `supplier_invoices.approved_by`, `review_sessions.reviewer_id` joined to memberships `role='accountant'` all = 0.

## Defects outside this lane
- S-47 (P1, confirmed by reading): `stock.service.ts:330` transfer has no source floor beyond `inventory.service.ts:741`; `tenant-bootstrap.ts:204` bin `negativeAllowed: true`; `warehouse-app/app/stock/index.tsx:281` Move offered on bin rows → stock from nothing through Move remains open after this merge.
- S-48 (P1): `stock.service.ts:157` / `contracts/src/inventory.ts:55` — warehouse token sets `negativeAllowed`.
- S-49 (P2): no desk screen calls `inventory.cycleCounts.post` (`manager-app/app/stock/` has only index.tsx; owner-app has no counts route).
- `docs/23-app-screens-and-api-gaps.md:891` — dispute/cancel still labelled BACK_OFFICE.
- `backend/libs/core/src/platform/authz.ts:38` — `requireRole` message names the internal `system` role to a client when the guard passes and the service refuses (P3, DOS-037 verdict).
- `backend/tools/smoke-endpoints.mts:2408,2529` (lane numbering) — TS2322/TS2345 `PermissionRole` vs membership roles under `tools/tsconfig.json`; CI does not typecheck tools.
- `backend/libs/database/src/seed-demo/stock.ts:152-156` — positive warehouse `cycle_count` rows written by SQL with `ref_type='manual'`.
