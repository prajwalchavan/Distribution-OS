# Slice h1-money-slice1 — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Range 117717c..c2c472f, one commit. Read through git objects only; no build, test, git write or service mutation. Two read-only `psql` counts on `dos` and `dos_qa` (duplicate groups, counter vs register) are cited below.

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
- `receivables.service.ts:1578,1594` — the audit row's `before.nextNo` is read AFTER `nextDocumentNumber` bumped the counter, so it is the post-draw value (drawn+1), not the counter the operator would recognise. Read the series before the draw or name the field `nextNoAfterDraw`. Cosmetic; `after.nextNo = advancedTo + 1` is right.
- Merge to-do, not the lane's: QA/13 has the design line (l.128) but not amendment (e) — add "sync manifest `schemaVersion` changes for delivery and retailer; each device re-snapshots once (pending ops flushed first)"; build-log row; docs/22 l.276 already carries the rule.
- `seed-demo/util.ts:15` `FY = '2026-27'` is a literal while the app draws under `numberingYear()`; after 1 April 2027 the seed's counters and receipts sit in the 2026-27 register and the app opens 2027-28 at RCPT-0001 (no collision, but `reconcileSeries`/`bumpSeries` only touch the literal FY row). Pre-existing; pin `DEMO_TODAY` or derive FY from `TODAY` later.
- Demo convention after the reseed: the next receipt is RCPT-9005 (the INV/9007 convention), not the RCPT-0700 the finding expected — say so in the change log so QA does not re-file it.

## Amendments — satisfied? evidence
- (a) self-heal, not 409: YES. `receivables.service.ts:1569-1642` `insertNumberedReceipt`: insert with `ON CONFLICT (tenant_id, series_code, fy, receipt_no) WHERE receipt_no IS NOT NULL DO NOTHING … RETURNING` (no savepoint, no aborted txn; pkey/idempotency/paper-book collisions still raise), then `readDocumentSeries` → `highestReceiptNumber` (own prefix + digits only, this FY) → `advanceDocumentSeries` GREATEST(next_no, max+1) → `nextDocumentNumber` → ONE retry → `writeAudit('numbering.heal')` with collided and healed numbers, receiptId, deviceId. Lock: `nextDocumentNumber`'s `ON CONFLICT DO UPDATE` holds the series row lock for the transaction (`numbering.ts:48-56`), so a second recorder waits at its own draw and sees the advanced counter; nothing else issues RCPT numbers (`git grep`: only :601 and :1697 insert receipts, both routed through the helper; sync `receivables.sync.ts:60` and door `collections.service.ts:124,127` call `recordReceipt`). 409 only after the retry fails; no client-supplied `receipt_no` path exists (paper book = `client_receipt_no`). Both roles can heal: `numbering_series` is `tenantPolicy`, the 0013 guard blocks only backward moves under an app actor (0013:142), `audit_log_insert` admits any `app_rw` actor under its own `actor_id` (platform.ts:421-428), and staff read the whole register (`tenantOrOwnRetailerPolicy`, columns.ts:113-121). Specs: desk (accountant, `receivables.spec.ts` DOS-032: healed number, taken count 1, AR −200, one journal entry, counter = highest+2, audit row, both `fy`), door (driver, `delivery.spec.ts` DOS-059: one receipt + one collection, audit row), offline (`receivables.spec.ts` DOS-059 sync: 200, accepted 1, rejected [], no `sync_errors`, audit row with device_id).
- (b) FY IST everywhere: YES. `numbering.ts:23-25` `numberingYear` = `@dos/domain financialYear` (calendar.ts:31-35, `businessDate`), `nextDocumentNumber:45` uses it, `@dos/db financialYear` (tenant-bootstrap.ts:25-27) now delegates too, so a new tenant's series, the counter and the stored `fy` share one key; invoices (`invoices.service.ts:550,1533`) already stamp the domain FY, so the receipt/invoice registers agree. Boundary spec: `receivables.spec.ts` "00:00–05:30 IST on 1 April" under `TZ=UTC`, three instants (18:29Z→2025-26, 18:30Z and 23:59Z→2026-27), asserts which counter moved AND the stored `fy`.
- (c) migrations: YES. 0043 generated expand (series_code DEFAULT 'RCPT' NOT NULL, fy nullable); 0044 hand-written (0015 method): fail-loud DO block grouped by (tenant, IST FY of received_at, receipt_no) naming the tenant SLUG, up to 20 numbers, ERRCODE unique_violation, and the full repair SQL (keep first, renumber later past the register, move RCPT counters) in the message; backfill `fy` with the touch trigger held off; SET NOT NULL; `receipts_no_idx` as drizzle generated it; closing assertion: FORCE RLS, no FOR ALL policy, fy NOT NULL, index UNIQUE on exactly those columns and partial. Expand-only, no new table/grant. Journal idx 43/44 are the next free on main (main's journal ends at 42; `when` 1789273070199 and 1789273155181 > 1788648239041, strictly increasing); snapshot chain 0042 `5a67d062…` → 0043 `d5efecec…` → 0044 `71fb2489…` verified; 0044 snapshot carries fy notNull + the index, 0043's does not.
- (d) seed ends past its register: YES. `sales.ts:2919-2929,3046` counts the numbered originals (residuals included) and bumps to `rcptNumbered + 1`; `billing.ts:1258` `reconcileSeries(RCPT)` (receipts/receipt_no, filtered by series_code + fy, digits-suffix) so counters end 9005 > RCPT-9004; every seed write stamps `fy: FY` (sales ×4, billing, receivables `addReceipt` Omit widened). `seed-demo.test.ts` DOS-032/059: per tenant `next_no > highest` for every RCPT series that issued, and zero duplicate (slug, series_code, fy, receipt_no) groups, on a fresh database.
- (e) manifest: YES, verifiable. `tablePull(receipts)` publishes columns (`sync.registry.ts:152-162`) and `schemaHash` hashes `tables` (`sync.service.ts:353-357`), so `schemaVersion` changes for the delivery and retailer roles only; sales/warehouse do not pull receipts. Change-log note is the merge's job (minors).
- DB guarantee: `rls.test.ts` DOS-032/059 rejects the second row with `/receipts_no_idx/` only (fresh id + key) and accepts the same number in 2027-28.

## Merge conflicts with main and how to resolve
None. `comm` of the slice's 19 paths against `117717c..main` is empty; main added no migration, no journal entry, no change to numbering.ts, receivables.*, the receipts schema, the seed or the four specs. Fast-forward merge of c2c472f is clean. (The `receivables.queries.ts` hunk shows as binary — see defects — it only adds `seriesCode`/`fy` to `receiptFromRaw`.)

## What the later fixes must build on
- Every receipts INSERT goes through `ReceivablesService.insertNumberedReceipt` (private). DOS-007/117/132 must not add a raw `tx.insert(receipts)`; a bounce/reversal goes through `undoReceipt`. `numberingYear`, `readDocumentSeries`, `advanceDocumentSeries` are the platform API for any module that stores `fy` beside a number.
- DOS-166 (h7): migrations 0045/0046, snapshot `prevId` = `71fb2489-2081-4833-b5db-b2607fc2c7da`, journal `when` > 1789273155181, re-run `db:generate` on top of 0044's snapshot. Tightening the receipts INSERT policy is safe; do NOT narrow a staff role's SELECT on receipts — `highestReceiptNumber` runs under the actor's read policy, and an under-counted max turns the heal into a 409 at the door.
- Q5 rebuild of `dos_qa` right after this merges (the current seed on it wrote the duplicates); the seed then writes RCPT-9005 next.
- Founder's `dos` (read-only check today): 0 duplicate groups, 43 migrations (through 0042), but every RCPT counter is at `next_no = 1` with receipts up to RCPT-9002 (the 8 Sep seed). Tell the founder: `pnpm db:migrate` applies 0043+0044 cleanly and backfills `fy`; run it BEFORE recording any receipt on `dos` (a receipt recorded first is RCPT-0001, a duplicate, and 0044 then refuses and prints the repair SQL). After migrating, the first receipt in each tenant heals its counter to RCPT-9003 with one `numbering.heal` audit row — expected, not a bug. Or rebuild `dos` from the new seed (`dropdb --force dos && createdb dos && pnpm db:migrate && pnpm db:seed`), which the new seed requires anyway.

## READMEs that will change
None expected: no contract change (`ReceiptSchema` has no seriesCode/fy), the 409 uses an existing code, the manifest is runtime. Run `pnpm docs:readme:check` in the lane before merging to confirm a clean check.

## Regression walk, API probes and SQL
1. Fail-loud first, on the UNREBUILT `dos_qa`: services and worker stopped, `DATABASE_URL=…/dos_qa pnpm db:migrate` → 0044 refuses naming `tarsun: RCPT-0696 … RCPT-0699 (2026-27, 2 receipts)` with the repair SQL; `select count(*) from drizzle.__drizzle_migrations` still 43. Then Q5: `dropdb --force dos_qa && createdb dos_qa && pnpm db:migrate && pnpm db:seed`, `QA/tools/seed/verify-seed.sh dos_qa` 206/206, `\d receipts` shows `fy NOT NULL` and `receipts_no_idx UNIQUE … WHERE (receipt_no IS NOT NULL)`, 45 migrations, counter query per tenant: next_no 9005 > highest 9004 ×3.
2. Desk, web: manager :5174 `vikas.kadam` → Money → Record a payment → Patil General Store → Cash → Record → RCPT-9005, listed once; accountant `meena.joshi` → RCPT-9006. Door, web :5177 `ganesh.more` → stop → Take money → Cash → Record → next number (reach the trip from the list if Today misroutes, DOS-061). Android Pixel 7 (`-memory 3072`): UPI + UTR → next; airplane mode → record → reconnect → `POST /sync/upload` 200, accepted 1, rejected []; the ConnectionStrip shows one re-snapshot at first sign-in (manifest changed) on delivery and retailer; retailer :5178 `ramesh.gupta` → Receipts: no repeated number.
3. Heal probe: as `dos`, `update numbering_series set next_no = next_no - 1 where series_code='RCPT' and tenant_id=(select id from tenants where slug='tarsun')`; then desk POST /receipts → 200 with the next free number (not the taken one); door (web) and offline (Android) the same → 200/accepted, no `sync_errors` row; `select actor_role, before, after from audit_log where action='numbering.heal' order by occurred_at desc limit 3` → three rows (accountant/manager, delivery, delivery with device_id).
4. SQL after all of it: `select t.slug, r.series_code, r.fy, r.receipt_no, count(*) from receipts r join tenants t on t.id=r.tenant_id where receipt_no is not null group by 1,2,3,4 having count(*)>1` → 0 rows; `select count(*) from receipts where fy is null` → 0; per-tenant `next_no > highest` holds.

## Defects outside this slice
- `backend/libs/core/src/modules/receivables/receivables.queries.ts:34,40` — raw NUL bytes inside the cursor-separator string literals; git treats the file as binary, so its diffs are invisible to every review (this slice's hunk included). Write `'\0'` as an escape.
- `backend/libs/database/src/schema/warehouse.ts:89` — `picklists_no_idx` is (tenant_id, picklist_no) with no series/FY, so a new FY's PL-0001 is refused (contrast `invoices_no_idx`).
- `backend/libs/core/src/modules/billing/invoices.service.ts:1569-1575` — a lagging INV counter still ends in a 409 with no product repair path (the plan's risk 3, now solved for receipts only); the docs/22 l.276 rule is a candidate to extend to invoices and credit notes.
- No unique number index on grns, trips, claims, sales_orders or purchase orders (plan's own note); same class as DOS-032, not yet filed.
