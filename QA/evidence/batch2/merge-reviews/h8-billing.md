# Lane h8-billing — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Range 492da02..1ea7cfe, one commit (DOS-116), five files. Read through git objects only; no build, test, git write or service mutation. Three read-only `psql` counts on `dos_qa` (rebuilt main 3d7cb36, 48 migrations) are cited below.

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
- `credit-notes.service.ts:337-338` — `isSaleableCreditNoteReason(input.reason)` is evaluated twice per line inside the loop; hoist one `const reasonSaleable` above `for (const line of input.lines)`. Cosmetic.
- `credit-notes.service.ts:340` — the message hard-codes "damaged goods"; when a second unsaleable reason (`return_expired`) is ever added, word it from the reason. Cosmetic until then.
- Manager M8 has no per-line saleable control (`credit-notes.tsx:172` derives it per note), so a desk return mixing damaged and good pieces from one bill is two notes. Residual, not this finding; record in QA/13 lean groups (UI, P3).
- Merge to-dos, not the lane's: docs/18 build-log row for DOS-116; QA/STATE; docs/22 needs nothing (§8 l.280 and §11 l.342 already carry the rule); one change-log sentence that `/docs/openapi.json` no longer carries `default: true` on `lines[].saleable` (runtime only, no checked-in file).
- Idempotency edge (verdict): a pre-fix key replayed with the flag omitted now hashes differently (`idempotency.ts:55`) and answers 409. Nothing is deployed and the manager app always sends the flag; no data action.

## Amendments — satisfied? evidence
- (a) one list in the contract: YES. `contracts/src/billing.ts:118-132` exports `UNSALEABLE_CREDIT_NOTE_REASONS = ['return_damaged'] as const satisfies readonly CreditNoteReason[]` and `isSaleableCreditNoteReason()`, doc-commented as the credit-note side of `UNSALEABLE_RETURN_REASONS` (delivery.ts:194); exported via `index.ts` (`export * from './billing.js'`). `credit-notes.service.ts:19` value-imports the predicate; `git grep` finds no billing-local set and the only `'return_damaged'` literals left are `RESTOCKING_REASONS` membership (:102), the screen's option list (tsx :52) and spec request literals. New `contracts/src/billing.test.ts` mirrors delivery.test.ts: `return_damaged` false, every other `CreditNoteReasonSchema.options` entry true, list and filtered options both equal `['return_damaged']`; red (`is not a function`) before, 1/1 after.
- (b) screen reads the same list: YES. `credit-notes.tsx:21-25` value import (`isAllowed`/`isSaleableReturn` precedent in `_layout.tsx:33`, delivery `deliver.tsx:42`); `:172` `saleable: isSaleableCreditNoteReason(input.reason)` — the same boolean as the old `!== 'return_damaged'` for all seven reasons, so the request body is byte-identical. The screen still imports only `@dos/ui` (via `src/lib/ui`) plus contracts/api-client/domain; manager-app typecheck, lint and prettier exit 0 on the rebuilt contracts dist.
- (c) derivation in `draft()`: YES. `:337` `const saleable = line.saleable ?? isSaleableCreditNoteReason(input.reason)` after the rate ceiling (:330-334); `:338-342` refuses `saleable && !isSaleableCreditNoteReason(reason)` with `ORPCError('BAD_REQUEST', { message: '<description>: damaged goods go to the damaged bin, not back on sale', data: { code: 'return_not_saleable', invoiceLineId: source.id, reason } })`, DOS-058's shape (deliveries.service.ts:347-350); `:361` stores the derived value; `:154` `raiseForDelivery` passes `l.saleable` through (no `?? true`); `:283` line type `saleable?: boolean | undefined` (exactOptionalPropertyTypes).
- (d) everything else stands: YES. `CreditNoteLineInput.saleable: z.boolean().optional()` (:620) with the field doc; one sentence on `CreateCreditNoteInput` (:629); `CreditNoteLineSchema` output unchanged; no enum, migration, trigger, permissions (`permissions.ts:610-614` untouched) or `issue()`/`restock()` change; no inverse rule (`return_saleable` lines may still be `false`, which the doorstep's mixed return needs, deliveries.service.ts:187-191). Three billing.spec.ts tests as planned (fresh 12-pc bill per test via `freshBill`, posted as `manager`, tags `dos116-*`, error body typed as delivery.spec.ts): red at :932 (`expected true to be false`) and :995 (`expected 200 to be 400`), guard green both runs; 24/24 after; delivery.spec.ts 25/25 unedited (DOS-058 and the doorstep bin test); claims 20/20; three permission specs green.

## Stock correctness, refusal ordering, claims
- Damaged bin: `restock()` (:473-509, unchanged) routes on the STORED line flag — `line.saleable ? saleableLocation : damagedLocation`, `locationOfKind('damaged')` (:524-526), ledger reason `sale_return_damaged`, `inventory.post` keyed `credit-note:<note>:<line>` (append-only `stock_ledger`, idempotent). With the fix every stored `return_damaged` line is false (derived, or explicit false; explicit true never reaches an insert), so no saleable stock is created and `stock_balances` of the godown never moves for that reason. `sellable_stock` still lists the damaged bin (DOS-140, below), so the bin is invisible to reps only once that lands.
- Refusal before any insert, replay-consistent: the throw at :338 sits in the line loop, before the `credit_notes` insert (:371) and the `credit_note_lines` insert (:399), inside `withTenant` (one transaction, client.ts:66) and `idempotent` (the key row is inserted in that same tx, idempotency.ts:56-58), so the key row rolls back with the refusal. Test 2 proves it: the same key, note id AND line id resent with `saleable:false` answer 200 for both `autoIssue` values; SQL counts of `credit_notes` and `credit_note_lines` are 0 and `ledgerFor` is empty for the refused id. A successful call replays through the stored response (same hash) and, with a new key, through `draft()`'s `existing` return (:289-294) — a note that already passed the rule.
- Doorstep unchanged: `deliveries.service.ts:187-207` picks `return_damaged` only when every line is `!returnedSaleable` and always passes an explicit boolean, so `draft()` sees `false && …` and never throws; the DOS-058 gate at :347 still refuses `returnedSaleable:true` on `damaged`/`expired` first. The `RaiseForDeliveryInput` doc (:116-119) states the new default honestly.
- Claims: `claims/build.service.ts:481` filters candidates on the HEADER reason. After this fix every new `return_damaged` line is unsaleable, so desk notes no longer put pieces on sale and claim them at once; the polluted CN/9005 is gone (`dos_qa` today: 0 issued `return_damaged` lines with `saleable=t`, 10 with `f`). Damaged lines inside a mixed doorstep `return_saleable` note are still not candidates — the verdict's P2, filed below, not this lane's.
- Contract doc + READMEs: `sample.ts` renders `optional` through its inner type (boolean → `true`) exactly as `default` renders `defaultValue` (`true`), and `fields()` marks both not-required, so the six service READMEs (`"saleable": true` at owner/manager :17191, :17236, :17397; delivery :15022…) do not move; the verifier's `pnpm docs:readme:check` exited 0. No app README mentions the field.
- billing.spec.ts vs DOS-126 (now on main): main's only change to the file since 492da02 is the DOS-126 test at @@ -1207 (tail, before `reservedFor`); the lane's single hunk is @@ -899 (after the financial-note test, before "the other two sources of a bill"). Every fixture the lane uses exists on main unchanged: `placeOrder(actor, retailerId, lines, tag)` :151, `issueFor(orderId, tag)` :194, `ledgerFor` :217, `godown`/`damaged` :145-146/:399-400, `run` :111, `rep`/`shopMh`/`variantA`, `CreditNoteDetailBody.lines[].saleable` :103; tags `dos116-*` ≠ `dos126`; 5 000 opening pcs cover three more 12-pc bills.
- credit-notes.service.ts / invoices.service.ts vs h5 slice 3: main's billing changes since base are `invoices.service.ts` (+55) and `index.ts` (+2) only; `credit-notes.service.ts` is untouched on main, the lane does not touch `invoices.service.ts`, and `ANY_MEMBER` (invoices.service.ts:186) that the lane imports still exists.

## Merge conflicts with main and how to resolve
None. Against main 5150c3f: `contracts/src/billing.ts`, `credit-notes.service.ts` and `manager-app/app/billing/credit-notes.tsx` are unchanged on main since 492da02; `contracts/src/billing.test.ts` does not exist on main; `billing.spec.ts` hunks (@@ -899 lane, @@ -1207 main) are disjoint — git merges it clean, no textual work. Against h9-desk (`e5f29ae..qa/b2-h9-desk`): none of the five files is in its 29-file stat; its `permissions.ts` rows are inventory/procurement/docint (creditNotes rows :610-614 untouched) and its `examples.ts` change has no credit-note example. Also checked: h10-console, h11-syncpull, h13-owner-support, h12-kit, h4-stock, h6-admin, h7-syncdoor — no overlap on the five files. After merging run `pnpm --filter @dos/contracts build && pnpm --filter @dos/core build`, then `pnpm docs:readme:check` (expect clean), then restart the eight services and the worker on dos_qa.

## What the later fixes must build on
- `UNSALEABLE_CREDIT_NOTE_REASONS` / `isSaleableCreditNoteReason` in `@dos/contracts` is THE list for credit notes, as `UNSALEABLE_RETURN_REASONS` / `isSaleableReturn` is for delivery lines. A `return_expired` reason later = pg enum migration + this list + PDF label (`documents/templates/credit-note.ts:18`) + claims split; never a local set or a `!== 'return_damaged'` comparison in a service or screen.
- `CreditNotesService.draft()` is the single writer of `credit_note_lines` and the single place the default is decided; `raiseForDelivery` passes the flag through untouched. Do not re-add a `?? true` at any caller; do not add a re-check at `issue()` (drafts: 0 on dos_qa today; step 0 below re-checks).
- Claims P2 (below) should filter on the LINE's `saleable` — `registers.service.ts:84,:505` already returns it — so both desk and mixed doorstep damage is offered to the brand.
- DOS-140 (`sellable_stock` lists damaged-bin lots; 84 rows on dos_qa today) matters more after this: every desk damaged return now lands in the bin.

## READMEs that will change
None expected (sampler reasoning above; verifier's `docs:readme:check` clean). Only the runtime `/docs/openapi.json` of owner/manager/delivery loses `default: true` on `lines[].saleable`.

## Regression walk, API probes and SQL
0. Done today, read-only, on dos_qa: `select count(*) … c.state='draft' and c.reason='return_damaged' and l.saleable` = 0; drafts of any reason = 0; issued `return_damaged` lines: 10 × `saleable=f`, 0 × `t`; CN/9005 absent (Q5 rebuild); 48 migrations. Fixture from the plan's query: INV/0838, line `14100f11-9a62-7202-8b1b-c45e4a25a245` "Konkan Masala Khakhra 180 g", lot `10109c2d-7c6e-7de9-86e9-fcf3ea8bac2e`, 330 pcs left; tarsun has one `warehouse` (Godown) and one `damaged` ("Damaged / expiry bin"). Take stock before: `select l.name,l.kind,sb.on_hand,sb.reserved from stock_balances sb join locations l on l.id=sb.location_id where sb.lot_id='<lot>' order by l.kind`.
1. `pnpm smoke --service manager` → 0 BROKEN (its creditNotes.create pins `short_delivery`, smoke-endpoints.mts:1560-1573).
2. As vikas.kadam: POST :3002/credit-notes `{idempotencyKey,id,invoiceId,reason:'return_damaged',autoIssue:true,lines:[{id,invoiceLineId,qtyPcs:2}]}` (flag omitted) → 200, `item.lines[0].saleable=false`, state issued. SQL: `select sl.reason,l.name,l.kind,sl.qty_delta from stock_ledger sl join locations l on l.id=sl.location_id where sl.ref_type='credit_note' and sl.ref_id='<note>'` → exactly `sale_return_damaged | Damaged / expiry bin | damaged | 2`; Godown `on_hand` for the lot unchanged, bin +2; `select saleable from credit_note_lines where credit_note_id='<note>'` → f.
3. Same body with `"saleable": true`, once `autoIssue:true` and once `false` (new ids/keys) → 400 `{message:'Konkan Masala Khakhra 180 g: damaged goods go to the damaged bin, not back on sale', data:{code:'return_not_saleable', invoiceLineId, reason:'return_damaged'}}`; `select count(*) from credit_notes where id in ('<a>','<b>')` = 0; no `stock_ledger` row with those ref_ids; then resend ONE refused key+id with `saleable:false` → 200 (idempotency row rolled back). Left-pcs on the line drops only by the pieces issued in 2, 3's resend and 4.
4. Controls: explicit `saleable:false` → bin (as before); `reason:'return_saleable'`, flag omitted, qtyPcs 1 → 200, `saleable=t`, `sale_return_saleable` at Godown.
5. Doorstep unchanged: as ganesh.more, `deliveries.record` with a `damaged` line `returnedSaleable:false` → one `return_damaged` note, ledger row at the bin; the same line with `returnedSaleable:true` → 400 `return_not_saleable` (DOS-058).
6. Check 09c(i) in `QA/evidence/batch1/regression/reconcile/09-returns-credit-notes.sql` → 0 rows created by verification.
7. Manager web :5174 at 1280 and 390, vikas.kadam then meena.joshi: Billing → Credit notes → INV/0838 → "Return, damaged" → 1 pc → Draft → open → Issue; the captured POST /credit-notes body carries `saleable:false`, the issue answers 200, the ledger row lands at the bin (regression 'wrong-day-4b'). Android manager credit-notes: record NOT RE-WALKED — logic-only, request body unchanged, proven by the web capture (verdict (b), docs/22:279).
8. Claims: build a damage claim for the period as owner → the note from step 2 appears as a `credit_note` candidate at the policy's basis.

## Defects outside this lane
- `backend/libs/core/src/modules/claims/build.service.ts:481` — `creditNoteCandidates` filters on the note header `reason === 'return_damaged'`, not the line's `saleable` (`registers.service.ts:84,:505` carry it), so damaged lines inside a mixed doorstep `return_saleable` note are never offered to the brand. P2 (verdict's finding).
- `backend/libs/core/src/modules/delivery/delivery.sync.ts:151` — `returnedSaleable: l.returned_saleable ?? true`: an offline delivery op that omits the flag on a `damaged`/`expired` line becomes a durable sync rejection instead of a booking to the bin; default through `isSaleableReturn(l.reason)`. P3 (verdict cited :132; the line is :151; the app sends the flag, deliver.tsx:275).
- `backend/libs/database` view `sellable_stock` — lists damaged-bin lots (84 rows on dos_qa today); already filed as DOS-140, weight rises after this merge.
- `backend/libs/core/src/modules/billing/credit-notes.service.ts:289-294` — `draft()` returns an existing note by `id` whatever the new payload, so a create with a NEW idempotencyKey and a reused `id` answers 200 with the earlier note instead of 409 on a mismatch (pre-existing convention, same in other modules). P3.
