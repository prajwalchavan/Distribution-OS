# Merge review — lean group founder-answers (cross): DOS-075, DOS-043, DOS-023 — branch `qa/b2-founder-answers`

**First review:** no earlier file existed for this lane; this is the first written review, made after the repair (781841b, 5ca3e7b, deb93b5) and the verifier's red/green pass (all three proven). Read-only: branch diff, docs/22 §8 rows of 2026-09-20, the runner's lane definition, the six owned files and the app screens that consume the changed contract.

**Decision:** MERGE AFTER FIXES

## Blockers

1. **DOS-075 is applied to one of the two threshold paths in the file the lane owns.** `backend/libs/domain/src/pricing/schemes.ts:368-371` (step 5, `cash_discount_pct`) still measures its trigger over `lines.filter(l => l.schemesAllowed && qtyPcs > 0 && inScope)`, so a line on a FINAL RETAILER OVERRIDE counts toward "2% on bills over ₹25,000" (step 3b, `measured`, :294) but not toward a cash-discount scheme of the same threshold on the same bill — verified by reading both filters; the verifier's probe shows `orderRules [s-order-25k]` beside `cashDiscountPaise 0`. The seed carries cash-discount schemes (`seed-demo/pricing.ts:362, :574`), so the pilot tenant sees two thresholds disagree. **Fix (test-first, same file):** replace that filter with the same two-step shape as 3b — `const measured = lines.filter(l => l.input.qtyPcs > 0 && inScope(scheme, l.input))` drives `evaluateTrigger`; `const scope = measured.filter(l => l.schemesAllowed)` stays the base of `amount` (the base's treatment of locked lines is pre-existing and goes to the founder, below — do not change it here). Pin: the ₹26,000 final-override bill of the second DOS-075 test fires the cash-discount threshold; the override line adds nothing to `cashDiscountPaise`; the ₹2,000 line alone does not fire it.

## Minors

- **Benefit SIZE now follows the whole bill for fixed rewards and the earning lines for percentages** (`schemes.ts:300` `reward.multiples` from `measured`; `orderReward` :764-768). "₹500 off per ₹10,000" on a ₹26,000 bill whose ₹24,000 is exclusive pays ₹1,000 onto the single ₹2,000 line (50% off it); "2% over ₹25,000" pays 2% of ₹2,000. Both are a literal reading of the founder's row; the asymmetry and the 50% case are a product call, not a defect — put to the founder as a docs/22 §10 question, not re-decided here. Same for `free_qty` mix schemes (free goods anchored on the largest earning line).
- Pre-existing, exposed by this work: the cash-discount BASE (`:373`, `sum(netOf over scope)`) includes lines locked by an exclusive SCHEME, so such a line still earns the cash discount. Founder question: is cash discount a payment term on the whole bill (today) or a scheme the exclusive line earns nothing from (the 2026-09-20 wording)?
- `departedEarly()` lives in `backend/libs/database/src/schema/delivery.ts:231` and is the first schema file to import `@dos/domain` — a business rule (IST day compare) inside the SQL-schema package, and `drizzle-kit` (`db:generate`, schema `./src/schema/index.ts`) now needs the domain `dist` present. Move it to `delivery.internals.ts` (or `@dos/domain/calendar`) in a later touch; not worth a re-verification now.
- `TripSchema.departedEarly` is REQUIRED (`contracts/src/delivery.ts:408`) and `toTrip` is the only producer (verified: no other literal in backend, tools or frontend). `@dos/contracts` links into the frontend and the verifier did not run `cd frontend && pnpm typecheck`; run it at merge (expected green — no frontend file builds a `Trip` literal).
- `trip_stops` has no `(tenant_id, created_at, id)` index (`schema/delivery.ts:280-282`). Practical exposure is small: the ONLY caller of `delivery.stops.list` is the retailer app, narrowed by `retailerId` (`retailer-app/app/index.tsx:123`, `orders/[id].tsx:75`), which walks `trip_stops_retailer_idx` and sorts one shop's stops. Owed as a drizzle expand migration + `_journal.json` before any desk screen lists stops unnarrowed (docs/20).
- Lane premise: nothing ever refused an early departure (no date gate in `depart`, `startLoading`, the machine or any screen; batch 1 `d20a584` settled WHO, not WHEN). The repair disclosed it and pinned both halves; docs/22's as-built row must say "recorded, never refused" rather than "refusal removed".

## Conflicts

- `main` has not moved since the merge-base `d806012`; the branch applies clean.
- The other live lane `qa/b2-fix-seed-harness` touches `core/src/docs/examples.ts`, `examples.spec.ts`, `tools/smoke-endpoints.mts` — no file overlap. Its `examples.ts` change can shift the generated service READMEs this branch also regenerated: whichever merges second re-runs `pnpm docs:readme` once and commits, rather than resolving README hunks by hand.
- `delivery.mappers.ts` +2 lines outside the lane's list: correct place (only `Trip` producer), no other branch touches it.

## Walks

1. **DOS-043 has a record and no reader.** `grep departedEarly frontend` is empty; every trip list shows `tripDate` only (`manager-app/app/fulfilment/trips.tsx:197`, `owner-app/app/orders/trips.tsx:81`, `warehouse-app/app/load/index.tsx:261` and `load/trips.tsx:518`, `delivery-app/app/trips.tsx:126`). Frontend lane: a "left early" chip beside the planned date on those five lists and the trip detail; the delivery app's offline `trips` pull carries `trip_date` and `started_at`, so the device derives it the same way. Walk: manager M7 and owner O18 in a real browser at desk and phone widths, warehouse load board on web, delivery trips on the Pixel 7 — depart a trip planned for tomorrow and read the chip on all four, then a trip departed on its day shows none. The seed should plant one early-departed trip so the walk and the READMEs have a real row.
2. **DOS-023 "the rest" was never possible in this lane**: the runner's `ownsFiles` held six files, and exactly one id-ordered list lives in them. 39 lists remain (the repair's survey is honest; sampled `orders/fulfilment.ts:90-105,193` — filed P1 as S-04/S-81, feeding the wave queue, W7 packs and the planning board — `warehouse/load-sheets.service.ts:294`, `pricing/bargains.service.ts:219`). Needs its OWN lane owning those modules' files, in three parts: the 25 `desc(id)` feeds + `fulfilment.ts` (mechanical, each with a two-row test as here and an index where the page is not narrowed); the 8 `asc(id)` enumerations and `docint/queue.service.ts:54` (a product call — shops A–Z, a FIFO work queue — before flipping); the two `supportGrants` cursor/order mismatches. Walk after it: manager wave queue and planning board on web, seeded orders no longer above today's.
3. **Cash-discount fix (blocker 1):** re-run `@dos/domain` `schemes.test.ts` and `@dos/core` `pricing.spec.ts`, then one quote through the sales app (web) on a shop with a final override and a cash-discount scheme, reading the reported CD figure.
4. Merge gate as usual: `cd frontend && pnpm typecheck && pnpm build`; `pnpm docs:readme:check` after the README re-run.

## Defects outside

- docs/22 §8 needs the as-built row for the three 2026-09-20 answers (recorded not refused; stops list moved; the convention's remaining 39 lists as a named lane) and §10 the two founder questions above (fixed-benefit size over the whole bill; cash-discount base on exclusive lines). Main session.
- `frontend/delivery-app/app/trips.tsx:52` comment still says `trips.list` "pages by id DESC" — stale since lean-manager-order-lifecycle moved it to `trip_date`; doc rot, not behaviour.
- Bills/trips date-ordering exception (docs/22, 2026-09-20 "one stated exception") is still with the founder; nothing here touches it.
