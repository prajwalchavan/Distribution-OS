# lean-retailer-platform — merge review, second pass (Fable, 2026-09-21)

**First review:** approved the lane's design conformance; the verifier then proved the repair had moved the shop's cross-distributor read onto an auth link that never healed a 401 (₹0.00 to a shop owing lakhs) and left the DOS-102 phone walk owed. Since then: a1c1d63 (deny-list predicate + `acrossTotal()` on the screen) and 01cd7de (restart guard runs the shipped `storage.native.ts`) landed, the re-verification passed both red and green, and the settle stage walked web desk/phone AND the Pixel 7 with measured numbers (`QA/evidence/batch2/walks/lean-retailer-platform.md`, 37 screenshots). Branch `qa/b2-lean-retailer-platform` @ 01cd7de, 13 commits, main has moved 115 commits since the merge-base 8b030e5.

**Decision: MERGE AFTER FIXES**

## Verdict on the two proven defects

1. **Closed at the cause, not moved.** `client.ts:46-62` replaces the four-name `path[0]` allow-list with `BODY_AUTHENTICATED_AUTH_PATHS` matched on the whole dotted path; `client.ts:340` feeds the same predicate that gates both `refreshBeforeExpiry()` and the 401 replay. I checked the nine denied leaves against `authContract` (auth.ts:376-505): exactly the body-authenticated ones (`switchTenant` carries `refreshToken`, auth.ts:213); `me`, `memberships.summary`, `platformMe`, `supportPass`, `sessions`, `revokeSession`, `changePassword` all heal. `auth-retry.test.ts` walks the contract so a nested procedure cannot fall outside again. The screen half is fixed independently: `across.ts` names known/reading/unread, `index.tsx:147-238` renders all three, `:277-281` gives a card with no figures a neutral chip and a null Money. The walk measured it live: 401 → refresh → replay → `You owe ₹91,494.00 across 3 distributors`; with every summary refused, `₹0.00 anywhere on the page: false`. Nothing the first review approved is weakened — the lane's earlier widenings (`auth.memberships.summary: authenticated`, `inbound.list` + retailer) are unchanged and still narrowed by RLS; `MembershipsSummaryOutput` still carries no credit figure (ADR 0006).
2. **The owed Pixel 7 walk is now DONE** (walk §Debt 2): sign in as ramesh.gupta → Sai → `am force-stop` (pidof empty) → reopen lands in Sai with the real total; stronger, sign out + sign in lands in Sai again, which only `dos.lastTenantId` in the native store can explain. Read from `uiautomator dump`, not a picture.

## Blockers

- **B1 — migration numbers collide with main, and drizzle would SKIP them silently.** Branch adds `backend/libs/database/migrations/0048_inbound_reports_expand.sql`, `0049_inbound_reports_guarantees.sql`, `meta/0048_snapshot.json`, `_journal.json` idx 48/49 `when` 1789915099506/7. Main now holds 0048_order_cess … 0054_grns_supplier_guarantees (`when` up to 1789941326945) and its own `meta/0048_snapshot.json`. Drizzle applies a folder only when `lastDbMigration.created_at < folderMillis` (`node_modules/drizzle-orm/pg-core/dialect.js:62`), so on any database already at 0054 — dos_qa, every lane template, the founder's `dos` — the inbound columns and the policy swap never land, `inbound.create` 500s on a missing column and the shop's `inbound.list` hits the old staff-only FOR ALL policy. Fix, after merging main into the branch: delete the branch's two SQL files and its `meta/0048_snapshot.json`, take main's `_journal.json`; run `pnpm db:generate --name inbound_reports_expand` → `0055_inbound_reports_expand.sql` + `meta/0055_snapshot.json` (confirm the body equals the old 0048: four columns, FK, index, `inbound_messages_staff` dropped, the four policies); re-add the guarantees file as `0056_inbound_reports_guarantees.sql` with journal idx 56, `when` = 0055's + 1, header "companion to 0055"; update the comment "migration 0049" at `notifications.mappers.ts:180`; prove it by `pnpm db:migrate` on a database already at 0054 showing both applied, then `pnpm --filter @dos/db test` (rls) green.
- **B2 — the desk cannot see what the shop filed (founder decision DOS-103, docs/22 §8 2026-09-13: "lands … with its kind and the bill or delivery it names; the desk triages it").** Design item I (manager M18 'Kind' column + the reference, display-only) was not built: `frontend/manager-app/app/messages/index.tsx:142-147` still renders shop / text / channel, so a return request against a bill is a row reading "In app" with free text; `kind`, `refType`, `refId` are on the wire (`InboundMessageSchema`, walk §DOS-103) and invisible. Fix (~15 lines + 4 strings, no contract change): add `textColumn('kind', t('m18.kind'), row => row.kind === null ? null : t(`m18.kind.${row.kind}`))` and a reference column that, for `refType === 'invoice'`, links to the manager's bill route (`app/billing/`) by `refId`; strings `m18.kind`, `m18.kind.return_request/complaint/question`, `m18.ref`. Walk M18 once on web desk with the report the Pixel 7 filed.

## Minors

- `frontend/libs/api-client/src/platform-client.ts:38-42,181` keeps the identical `path[0]` allow-list. Not live today (all five console Bearer routes are flat) but the same trap; import `isBearerAuthPath` from `client.ts` or mirror the deny-list. Admin lane's file — flag, do not reach in.
- Design DOS-103 A asked the DEMO seed to set `branding.phone` for the three pilot tenants (`seed-demo/tenants.ts`); it does not, so the walk set it through `POST /owner/tenancy/settings`. Add the three rows or the founder's dos_qa shows no Call button until Settings is visited.
- docs/23 R2/R7 notes owed by the design (`docs/23:550` still says the summary "would make R2 one call"); no docs changed on the branch. One-line notes: `auth.memberships.summary` built, `pricing.rates` ✓, `inbound.create` ✓.
- `sign-in.tsx:35-48`: a `switchDistributor(open)` rejection (membership disabled between login and switch) falls into the sign-in `.catch` and prints as a sign-in error while the session is already open — design (e) says never a 403 to the shop. Wrap the switch in its own try/catch that falls back to `rememberDistributor(session.tenant.id)`.
- `index.tsx:226-235` counts `session.memberships.length` (all statuses) in "across {count} distributors" while the total excludes disabled ones; pre-existing card list, cosmetic.
- `dos-102-across-total.guard.test.ts` second case pins source spelling, not a render (react-native does not resolve outside Metro); the pure `acrossTotal` cases are real. Accepted.
- `<ConnectionStrip>` is fed by `inbox` (`_layout.tsx`), so it can read "online" while the panel says the summary could not be read; nothing false is claimed. Polish, with the missing retry affordance on the distributor panel.

## Conflicts (22 files touched on both sides since 8b030e5)

Hard: `_journal.json` (both append after idx 47) and `meta/0048_snapshot.json` (add/add) — resolved by B1. Generated: seven `backend/*-service/README.md` + six app READMEs — take either side, then `pnpm docs:readme`. Likely textual: `contracts/pricing.ts` (branch inserts after `QuoteOutput` :363, main edits :353-372), `pricing.spec.ts` (main's last hunk @ :682-787 sits beside the branch's DOS-104 test @ :760), `contracts/tenancy.ts` (:158 vs :170), `tenant-bootstrap.ts` (:123 vs :129). Clean by position: `quote.service.ts`, owner `settings/index.tsx`, owner and retailer `strings.ts`. After resolving: backend `pnpm lint && pnpm typecheck && pnpm build && pnpm docs:readme:check`, then `@dos/core` pricing/auth/notifications specs and `@dos/db` rls against a fresh migrate.

## Walks

Done (settle stage, measured): web desk 1280 / web phone 390 / Pixel 7 for DOS-102 (total, cards, van line, last-used landing, force-stop), DOS-103 (Call → dialler shows +91 98331 12255; report filed → manager queue → "Seen by"), DOS-104 (one 18 063 B `/pricing/rates`, zero `/pricing/quote` until `+`), DOS-125 (QR decoded by jsQR to the exact `upi://pay?…`, wraps at 390, Copy round-trips on localhost, no Copy on the phone). Still owed: (1) **iOS** — no build product exists, memory-bound; the programme's one unproven target; (2) owner Settings `settings-phone` and manager M18 on their own screens (after B2); (3) `pnpm db:migrate` on a DB already at 0054 (after B1); (4) `pnpm smoke --service retailer` per the reshaped gate — named ops `inbound.create/list`, `pricing.rates`, `tenancy.branding.get`, `auth.memberships.summary` OK on a fresh seed and a replay, no new BROKEN vs main; (5) a real UPI app scanning the tile.

## Defects outside this lane

- **DOS-100 is NOT on this branch** (6ab3ef7 took the half-built hold screen out; grep for `order_on_hold`/`r8-hold` is clean). The founder's 2026-09-13 decision stands undelivered; it belongs with the lane that owns `orders.internals.ts`. Record it as open, not merged.
- `frontend/retailer-app/src/config.ts` `absoluteUrl()` drops the service prefix in all-in-one mode (logo 404 at `:3100/storage/…`), walk §noticed.
- Phone shell: the sticky bottom bar covers the last card's `r2-switch-*`/`-call` buttons until scrolled (pre-existing).
- `platform-client.ts` allow-list (above), admin lane.
