# Slice h2-trips — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Range 117717c..d20a584, one commit, 11 files, no migration. `git merge-tree --write-tree main d20a584` resolves clean (tree e9308c8). The permission move is exactly DOORSTEP, the gate is the amended one (trip_id OR an order on the trip's stops), tenant-scoped and served by `load_sheets_status_idx`, both moves are audited inside the same transaction, and W10 no longer carries a depart path. The seeded TRIP-NEXT being refused at D2 is intended: docs/22 §8 already holds the 2026-09-12 decision, and DOS-039 (fixed, batch 1) lets W7 count the sheet out to release it.

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
1. **`pnpm smoke` can now lose its doorstep coverage by luck** — `backend/tools/smoke-endpoints.mts:1650-1672` builds a draft sheet from `loadableOrder()` (the run's own freshly packed order in a fresh seed), `loadSheets.confirm` then picks `draftLoadSheet()` (:720, `sheet_date desc, id desc`), which is the SEEDED today-dated draft as often as the run's own (both ids are sha256-shaped). When the seed's sheet wins, the run's sheet stays draft holding order X, `freeInvoice()` (:497) plans X's bill on the day trip, and `depart` now answers 409 → EXPECTED, as do the ~15 doorstep ops after it. 0 BROKEN still holds, so the tally is the only tell. Fix (harness only): in `freeInvoice()` add `and not exists (select 1 from load_sheets ls, jsonb_array_elements_text(ls.order_ids) x where ls.tenant_id = o.tenant_id and ls.status::text = 'draft' and x.value = o.id)`, and make `draftLoadSheet()` prefer the run's own sheet id when it exists. Do it in the lane with the README regeneration if convenient; otherwise right after the merge, and compare the OK count with the last recorded run.
2. `frontend/warehouse-app/src/strings.ts:420-421` — `w10.startFailed` and `w10.loading` were already unused and stay unused; drop them in DOS-131+137, which rewrites this block anyway.
3. `frontend/delivery-app/app/trip/start.tsx` — D2 learns of the draft sheet only from the 409 after its confirm dialog (TripDetail carries confirmed sheets only). Acceptable now; a pre-check needs a contract change and belongs with DOS-061.

## Amendments — satisfied? evidence
- **(a) gate on trip_id OR an order on the trip's stops; no sheet still departs** — YES. `load-sheets.service.ts:650-674` `draftsForTrip(tx, tripId, orderIds)`: `tenant_id = ctx`, `status = 'draft'`, `trip_id = tripId OR exists(jsonb_array_elements_text(order_ids) ∈ orderIds)`, `order by id`; delivery passes the order ids of its own planned deliveries (`trips.service.ts:387-397`), so neither module reads the other's tables. Spec `delivery.spec.ts:1971-2116` builds a linked van-stock sheet and an UNLINKED sheet carrying the stop's bill (`orderIds: [bill.orderId]`, no `tripId`), asserts 409 `load_sheet_not_confirmed` with both ids, then with `[unlinked]` alone, then 200 once both are cancelled, with the bill `dispatched` and outbox exactly Planned/Loading/Departed. trip2 (`:1437-1490`, no sheet) still departs.
- **(b) depart → DOORSTEP, warehouse keeps create/startLoading/stops.add** — YES. `permissions.ts:663`; core follows at `trips.service.ts:373` (`requireRole(DOORSTEP)`) and `:379` (`assertCrewOrDesk(trip, DOORSTEP)`); `permissions.test.ts:570-584` pins the tuple equal to `trips.return`'s and warehouse still allowed on the three planning calls; `warehouse-service/src/service.spec.ts:91-118` proves the gate 403 `may not call POST /delivery/trips/:id/depart` and that start-loading passes the gate.
- **(c) W10 header sentence** — YES. `trips.tsx:1-13` now reads "the godown may create a trip, add its stops and put it into loading"; the wrong sentence lived only in that header comment, nothing user-visible said it.
- **Audit rows** — `trips.service.ts:346-354` (`trip.start_loading`) and `:419-432` (`trip.depart`, ISO `startedAt`, odometer, opening cash), both after `updateTrip` and before the outbox emit; replays return at the short-circuit first; a 409 throw rolls back the idempotency row (`idempotency.ts:56-64` inserts it in the same tx). Spec asserts exactly one row each with actor id and role.
- **W10** — depart mutation and button gone; `Dialog` (contract `types.ts:383`, so web and native) with `w10-dialog`; `asking` cleared on success AND error; `w10-driver-departs-<id>` text for a `loading` trip; imports stay `@dos/ui`, `@dos/ui/platform`, `@dos/api-client/react`, `react`; the tab's nav permission is `delivery.trips.list` (`nav.ts:58`), so the shell keeps W10 for the godown.
- **Docs in the slice** — `delivery.ts` header, DepartTripInput JSDoc and route summary; `examples.ts:5053` note; smoke sweep comment (the sweep already cancels `planned` and `loading`, `:2667`, and the machine allows `loading → cancel`).

## Merge conflicts with main and how to resolve
None: `permissions.ts` main hunks (DOS-106 `ADMIN_LEVELS` tail, DOS-074/097 inventory row) and `permissions.test.ts` (new tests elsewhere) do not touch :633-664 / :570-625; the other nine files are untouched on main since 117717c. In the lane before merging: `pnpm --filter @dos/contracts build`, `pnpm docs:readme`, `pnpm format`, then `git merge --no-ff qa/b2-h2-trips`.

## What the later fixes must build on
- **DOS-133 (next):** touches only the W7 block of `warehouse-app/src/strings.ts` (:303-339) and `load/index.tsx`; the W10 block (:413-426) is now DOS-043's. h2-order-ruling.md stands; nothing here changes its "what the first fix must leave for the second".
- **DOS-131+137:** build W10 on d20a584's `trips.tsx` — keep the `asking` state and the start-loading `Dialog`, the `w10.driverDeparts` row for a `loading` trip, and never reintroduce a depart button; its header rewrite must keep the DOS-043 sentence that the crew departs from D2. `permissions.ts`: add `delivery.trips.planning` next to :663 and one bullet under the same block comment (keep the DOORSTEP bullet). `permissions.test.ts`: `warehouseMay` gains `planning`; `depart` stays out. `delivery.spec.ts`: its two `it`s go after DOS-043's (now the last in describeDb); DOS-043's trip3 is `driverId` on today+1, so today+5 as planned. `trips.service.ts`: `planning()` and `insertStop` are away from `depart` (:356-434); `draftsForTrip` must stay the only draft-sheet read delivery makes. `contracts/delivery.ts` header now names `draftsForTrip` — keep it.
- **DOS-115, DOS-044, DOS-037, DOS-098, DOS-107 (other lanes):** additive `permissions.ts` rows elsewhere in the file; DOS-115's warehouse-service `it` appends after DOS-043's; DOS-044 edits only the W8 strings block. **DOS-132:** appends to `delivery.spec.ts`. **DOS-166:** no shared file.

## READMEs that will change
`backend/{owner,manager,warehouse,delivery,retailer}-service/README.md` (depart row roles → owner, manager, delivery; new summary; warehouse-service detail gains the 403 note) and `frontend/{owner,manager,warehouse,delivery,retailer}-app/README.md` (the retailer app README also carries the row — the plan listed four). Regenerate in the lane, never by hand.

## Regression walk, API probes and SQL
On a fresh copy of the template, services pointed at it, ids by name (`trips.trip_no = 'TRIP-NEXT'` joined on `tenants.slug`).
1. **W10 web (1280 and 390 px) as bharat.jadhav:** Load → Trips → TRIP-NEXT planned → "Start loading" opens `w10-dialog` naming the trip, vehicle, date, stops; Cancel → SQL `planned`; Confirm → `loading`, row shows `w10-driver-departs-<id>`, no `w10-depart-*` anywhere. Same on Pixel_7_API_36 (`-memory 3072`) and Expo Go on iOS via `QA/tools/ios-login.mjs 5176 bharat.jadhav warehouse` + `xcrun simctl io booted screenshot`.
2. **Gate:** `curl -X POST :3004/delivery/trips/<id>/depart -H 'authorization: Bearer <bharat>' -d '{"idempotencyKey":"v43"}'` → 403 "the warehouse role may not call POST /delivery/trips/{id}/depart"; state still `loading`. Same call on :3002 with a manager token is not refused at the gate.
3. **D2 as sachin.dalvi (web :5177, Android, iOS):** `/trip/start?tripId=<id>` → odometer 41200 → Start the trip → confirm → `d2-error` reads the load-sheet sentence; SQL: trip `loading`, its orders `packed`, no `TripDeparted` in `outbox_events`. Then W7 as bharat counts the sheet out (DOS-039 is fixed) → `confirmed`, orders `dispatched` → D2 again → 200 `active`, `start_odometer_km = 41200`. Also build a sheet on W7 for the vehicle WITHOUT a trip holding one of TRIP-NEXT's bills → D2 409 names that sheet id.
4. **SQL:** `select action, actor_role, actor_id, after from audit_log where entity_type='trip' and entity_id='<id>' order by occurred_at` → `trip.start_loading` (warehouse, bharat) then `trip.depart` (delivery, sachin, `after->>'startOdometerKm' = '41200'`); repeat a depart with the same key → still one row. `explain (analyze) select id from load_sheets where tenant_id=$1 and status='draft' and (trip_id=$2 or exists (select 1 from jsonb_array_elements_text(order_ids) o where o.value in ($3)))` → Index Scan on `load_sheets_status_idx`.
5. **Chain:** contracts, core `src/modules/delivery`, warehouse-service, `docs:readme:check`, frontend lint/typecheck/build, then `pnpm smoke` — 0 BROKEN, and compare the OK count with the last run (minor 1).

## Defects outside this slice
- `backend/tools/smoke-endpoints.mts:720` — `draftLoadSheet()` confirms the seed's TRIP-NEXT draft ahead of the run's own sheet, so smoke dispatches the demo's tomorrow load and orphans its own draft; part of the queued "smoke leaves its calls behind" item.
- `backend/libs/core/src/modules/delivery/delivery.internals.ts:193-200` — `assertCrewOrDesk(trip, DOORSTEP)` returns on `desk.includes('delivery')` before the crew check, so for `depart`/`return` the "only the driver or the helper" branch is unreachable and crew scoping rests on `trips_read` RLS alone (`schema/delivery.ts:205`). Correct today; pass a desk tuple without `delivery`.
- `docs/22-source-of-truth.md:309` — "may a trip depart before its planned date" is still open; the finding's "two days early" symptom stays by design until the founder answers.
