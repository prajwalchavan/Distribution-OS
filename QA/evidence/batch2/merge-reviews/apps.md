# Merge review: lane apps (DOS-181, DOS-182), branch qa/b2-apps

Fable (architect), 2026-09-20. Read-only review of 1b3510b and 64fa490 (base f23a9a7) against the two findings, the Pixel 7 evidence under `QA/evidence/batch2/dos-167/amendments/` (and-run.log, wh-run.log, the wh-32/wh-33 and d4-record-* screenshots), the kit and engine sources on main. I ran the lane's own tests in its worktree (delivery 10/10, warehouse 6/6) and `git merge-tree main qa/b2-apps` (two conflicts). No device, no dev server.

**Decision:** MERGE AFTER FIXES

**DOS-181 is at the cause and its test is real.** The finding's "onPress never fires" is wrong; the implementer's reading is right and the evidence proves it: the D3 dump (and-run.log:625) shows Jain Kirana Mart "Credit", dos_qa holds it `POST_FULFILLMENT` under `delivery.pod_required = credit_only`, the bill balanced 444/444 as `delivered`, so `podRequired` was true, the press reached `commit()` and died at `podRequired && proof === null` with its one sentence off-screen. Without 1b3510b the first test's `footer.disabled` is `false` (main's `disabled=` at deliver.tsx:475 never names the photo). The write path is untouched, `loading` still refuses a second press, a refused press writes nothing: nothing is sent twice or lost. "Goes first" is not on any path this lane touches; DOS-183's walk remains its proof.

**DOS-182 is NOT at the measured cause, and the change moves a founder-visible boundary (DOS-040) on no evidence.** The measured phone HELD the sheet row: wh-run.log:245 prints `PICK-0052 | picking` (the chip is drawn only when `sheet !== null`), so `locked` was `false` on main. wh-32-cold-start-offline-sheet.png shows why `w5-pick-*` was absent: two standing sentences — the offline note and "Still filling this phone from the server" — pushed the first card's Picked/Short under the sticky bar; wh-33 is the same card at the same scroll position with both sentences gone and the buttons in view. The harness reads only the first viewport and never scrolls, so "5 min 48 s" is the time until the sentences left, not a lock. `pickGate` changes nothing on that path: the merged tree renders the same fold, and the walk fails again.

## Blockers

1. **Never-list #12 regression: `doorstepFooter` names a keep claim by key.** `frontend/delivery-app/src/lib/doorstep.ts:224` `label: online ? t('d4.record') : t('d4.recordOffline')`. Main's deliver.tsx:470 already routes that label through `keepKey('recordDelivery', status.persistent)` (DOS-179, 8c12de6) — the lane branched before it. On the merged tree a browser whose store is the memory fallback would again promise "Save on this phone", and `frontend/libs/offline/src/dos-179-keep-claims.guard.test.ts` (walks `app/` and `src/`, lists `d4.recordOffline` at :130) goes red. **Fix:** `doorstepFooter` takes `persistent: boolean | null` and returns `label: online ? t('d4.record') : t(keepKey('recordDelivery', persistent))` (import `./keep`); the screen passes `status.persistent`; add the case `online:false, persistent:false → t('d4.recordOfflineTab')` to dos-181-record-press.test.ts. Resolve the rebase conflicts: deliver.tsx (merge-tree :514-517, main's `label=` line vs the lane's removal → drop the line, the footer carries it) and pick/[id].tsx (:51-55 → keep both imports). Then `pnpm format`.
2. **DOS-182: fix the measured cause, keep DOS-040's rule.** (a) `frontend/warehouse-app/src/lib/pick-gate.ts:677` `return input.linesOnDevice > 0 ? 'pickable' : 'waiting'` → `return 'waiting'`. A completed pass always delivers the header with its lines (sync.service.ts:321-406: one instant-ordered page set, header `updated_at` ≥ its lines'), so lines-without-header exist only MID-PASS — exactly when the sheet may be incomplete and `left === 0` (pick/[id].tsx:253) would enable "Take it to packing" over lines not yet on the phone. Remove `unconfirmed`, `w5-unconfirmed` and `w5.unconfirmed`; invert the first test (absence → `waiting`). Keep the extraction and the empty bar while `waiting` — that part is right (main offers an ENABLED Confirm over an unknown sheet with no rows). (b) Nothing standing above the first row: move `w5-offline` (main pick/[id].tsx:285-287) under the progress figure in the bottom bar or drop it (the strip already reads "Offline since …"); in `src/lib/ui.tsx:326-332` print `local-filling` AFTER `{children}`. Source guard: no `Txt` between the Segments and `<LocalAsync` in W5's body, and LocalAsyncBody's order. (c) Correct the DOS-182 text in QA/findings/12 and QA/13-change-log.md:868 (mechanism: the fold, not `locked`), as blocker-free minor 1 does for DOS-181.

## Minors

1. DOS-181's finding text and QA/13-change-log.md:867 say "`onPress` never fires" and cite `deliver.tsx:283/:482` (no such path). Correct the mechanism on record: an accepted press refused off-screen. and-run.log:625 (d5-record on the same stop took its press) disproves the dead-press reading.
2. `doorstep.ts` now imports `@dos/ui` (`wordFor`, `Translator`); lean-delivery-collect's verdict holds it up as the "pure, no @dos/ui" pattern for `dues.ts`/`pod.ts`. It still runs under vitest; say so in the file header, and note that DOS-071 renames `d4.podRequired → d4.podRequiredCredit` and adds `podState` — `doorstepRefusal('photo')` must follow.
3. `doorstepFooter.onPress`'s refuse branch and `commit()`'s "belt" cannot be reached through the kit (native/controls.tsx:96 `disabled={off}`, web/controls.tsx:49) and read the same render's `gate`. Harmless; the comments overstate. Test 4 exercises a path the UI has no route to.
4. `d4-error` in the bottom bar is right; on native the bar is a fixed View (native/layout.tsx:459-473), so a long server message grows the bar. Acceptable; watch it on the walk.

## Conflicts

- **main:** two content conflicts (above). Rebase, resolve, `pnpm format`; the lane's guards ran before DOS-179 merged, so the `@dos/offline` guard has never seen this code.
- **Wave 3:** lean-delivery-door (deliver.tsx, delivery strings: DOS-064/070/148/149/163) and lean-delivery-collect (DOS-071 `podState`, the `d4.podRequired` rename, DOS-066 `dues.ts`) rebase onto `doorstepFooter`; lean-warehouse-pick (pick/[id].tsx, warehouse strings, ui.tsx: DOS-119 "hide Scan/confirm while the status is unknown" is now `waiting`; DOS-120's Start-reply precedence is `startedHere ?? deviceStatus` in `pickGate` — keep it there); lean-manager-order-lifecycle and lean-warehouse-rules touch pick/[id].tsx later; lean-libs-offline-boot owns engine.ts for defect 1 below.

## Walks (none run; Pixel_7_API_36 -memory 3072, openjdk@21; dos_qa; also web 390×844)

1. **DOS-181** SAI/0429 at Jain Kirana Mart: `d4-record` disabled with "This shop is on credit — a photo is required…" printed under it in the first viewport; photo → enabled; press online → "Delivery recorded"; press with the office cut → "1 waiting to send" and the button reads the `keepKey` word; a memory-store browser reads "Hold until there is a signal"; an ON_DELIVERY shop records with no photo; a server refusal appears in the bar without scrolling. Measure from the a11y tree, not eyeballed.
2. **DOS-182** full device, `am force-stop`, office cut, cold start, deep-link `dos-warehouse://pick/<id>`: the FIRST viewport's XML holds `w5-pick-*` for the first todo row with the offline sentence present; then cut `adb reverse` between pull pages of a fresh sign-in: rows drawn, no Picked/Short, bar empty, Confirm never enabled.
3. Merged-tree gates: frontend `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm format:check`, kit and offline guards with `--force` (S-155).

## Defects outside the lane (by code reading and the lane's own evidence, NOT TESTED)

1. **P2** `frontend/warehouse-app/src/lib/local.ts:344-345`, `frontend/delivery-app/src/lib/local.ts:205`: `useHydrated` requires `lastError === null`, so a cold start with no office prints "Still filling this phone…" over a device that completed its pass minutes earlier (wh-32; d4-record-after-press.png shows the delivery twin at the door). Fix in the engine: write `lastPassAt` when `pullLoop` returns with `hasMore === false` (engine.ts:1233-1265), restore it at open beside `lastPulledAt` (:610), expose it on `SyncStatus`; both hooks latch on `ready && lastPassAt !== null`. That also makes blocker 2(a) provable: a device past `lastPassAt` can never hold lines without their header.
2. **P3** `frontend/warehouse-app/src/lib/pick-gate.ts:660` (pre-existing at main pick/[id].tsx:103): a sheet held as `packed`/`cancelled` is `pickable`; picks queue and come back `picklist_closed`. It wants a fourth state (rows, no actions, no Confirm).
3. **P3** `QA/evidence/batch2/dos-167/amendments/wh-probe3.mjs`, `wh-step5.mjs`: the provers assert on `resource-id="w5-pick-…"` in the first viewport and never scroll. Any sheet whose first row falls under the bar reads as "locked". Scroll, or read the whole tree, before filing a lock.
