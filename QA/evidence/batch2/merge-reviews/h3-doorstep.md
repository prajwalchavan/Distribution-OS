# Slice h3-doorstep — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE AFTER FIXES — the code is right; the two fixes are edits the merge commit itself must carry (the DOS-056 spec's trip-number literal and the tail conflict in `delivery.spec.ts`), plus the lane DB rebuild. Nothing in the design needs to change.

## Blockers / majors (must be fixed before merge)
1. `backend/libs/core/src/modules/delivery/delivery.spec.ts:1980` (slice) `expect(planned.body.item.tripNo).toBe('TRIP-0003')` — false on merged main: DOS-043 (already on main, 5499c86) plans a third trip, h2 slice 2 (DOS-131, planned ahead of h3) plans more, so this trip is TRIP-0004+ and the spec fails on the merged tree while passing on the lane. Fix in the merge: `toMatch(/^TRIP-\d{4}$/)` (DOS-131 already reads `tripNo` dynamically). Same edit: rename the describe-scope `trip3` / `stopA3` (:1971-1972) to `tripOffline` / `stopOffline` — DOS-043 declares a local `trip3` inside its own `it`, and h2 adds `tripP`, `planDay`, `p1`, `p2` at describe scope in the same tail; shadowing is legal but the next reader will misread which trip departs. Busy rule holds (h3 plans `today` with `driverId`; DOS-043 is tomorrow; DOS-131 uses today+6..+9), and DOS-166's `standsFor` rows admit the driver on `trip_stops`, `deliveries`, `trip_expenses`, so the three DOS-056 tests stay green once renumbered.
2. Lane DB: after `git merge main` the schema is behind (0043/0044, h7's 0045/0046, h2's renumbered index) — `dropdb --force dos_test_b2_h3 && createdb -T dos_test_batch2b_template dos_test_b2_h3 && pnpm db:migrate`, then `pnpm --filter @dos/core test -- src/modules/delivery src/modules/sync` and `pnpm --filter @dos/delivery-service test` (a real `createServiceApp` boot with the new onRoute hook).

## Minors (may follow)
- No committed test pins the 8 MiB route limit: DOS-056 test 3 sends ~7 MiB through a harness whose global limit is 26 MiB, so it never exercises the hook. Add six lines to `backend/delivery-service/src/service.spec.ts` (real boot): 9 MiB `POST /sync/upload` → 413; 7.5 MiB → 401; 9 MiB `PUT /storage/x` → 403.
- A single op over 8 MiB still gets 413 (`claim()` sends it alone) and the uploader retries it for ever. Acceptable: D4's `proofTooBig` caps inline base64 at 690 000 chars (~0.7 MB op), every other table's ops are a few KB, and the server's 1 MiB check covers everything below the route limit. Belt-and-braces follow-up in the same upload half: when a ONE-op batch answers 413, mark that op `rejected` locally with code `row_too_large` instead of releasing it.
- `row_too_large` is a protocol-level code but lives as a literal in `sync.service.ts:64`; after h7 lands add `rowTooLarge: 'row_too_large'` to `SYNC_REJECTION_CODES` (`contracts/src/sync.ts`) and reference it; docs/27 §6's rejection bullet (the one h7 extends) should name it: keep the row, tray, never retry.
- Orphan object: `storeInline` (`delivery.internals.ts:431-479`) PUTs the object BEFORE the `file_objects` insert, and `writePod` runs after the trip/stop/invoice/line/POD-policy checks but before `orders.recordDelivered`, the credit note and `walkStop` (`deliveries.service.ts:146-213`). A refusal there rolls the row back and leaves one ≤ 300 KB object at a deterministic key (`tenant/…/pod/<deliveryId>/<podId>.jpg`). No duplicate is possible: a retry of the same `opId` is a `sync_ops` replay (no second PUT); a 5xx rollback re-PUTs the same key. Follow-up: an orphan sweep in the retention job (objects with no `file_objects` row older than a day) — the same sweep `files.service.ts:84`'s never-completed `pending` rows need.
- Stale prose the main session owns: `contracts/src/delivery.ts:100-106` (PROOF OF DELIVERY: "nothing else binary ever passes"), `delivery.internals.ts:429` (same sentence), docs/20 rule 15 (append "one bounded exception: the no-signal proof, docs/27 §15"), docs/22 §8 rows for DOS-056 (the founder-approved exception; ≤ 300 KB JPEG, 8 MiB route, `row_too_large`), DOS-146 (an emptied float is ₹0 and is written at depart) and DOS-156 (folded into DOS-056), §11 change log, re-render, republish.
- Expense proofs (`app/expenses.tsx:274` calls `captureProof`) are now squeezed to 300 KB too — beyond the amendment's wording but the right outcome (UX-00 §8.2 budgets ~200 KB); say so in docs/23 D7.
- Every platform walk is still owed (lane forbade servers/emulators): see the walk section.

## Amendments — satisfied? evidence
- (a) Inline in the op, files platform on the server, key only in the row — YES. `podFromDevice` maps `inline` (`delivery.sync.ts:247-263`); `applyDeliverySync → recordInTx → writePod → storeInline` inside the op's `withTenant` savepoint; `pod_evidence.object_key` holds the key, `payload` null. Spec test 1 asserts exactly one photo row, key matches `tenant/…/pod/<deliveryId>`, `TINY_PNG` absent from the row JSON, `file_objects.status = uploaded`, both `sync_ops` outcomes `{ok:true}`. Base64 lands nowhere else: `sync_ops` stores only `outcome`, `sync_errors` only code + sentences built from Zod issue messages (paths, never values), the outbox event carries ids, and Nest/Fastify log no bodies. `PodEvidenceInput.id` is `IdSchema` and `objectKey()` asserts the key shape, so the device cannot steer the key.
- (b) ≤ 300 KB JPEG, 8 MiB route, `row_too_large` never 413 — YES. `jpeg-budget.ts` ladder (quality first, then scale) walked by both cameras (`camera.web.ts` canvas, `camera.native.ts` expo-image-manipulator); `captureProof` passes `PROOF_MAX_BYTES = 300_000`; warehouse capture calls `photograph()` without `maxBytes` and is unchanged. `registerSyncUploadBodyLimit` sets `route.bodyLimit` on `POST /sync/upload` only, in `createServiceApp` and the spec harness; verifier's probe: 9 MiB → 413, 7.9 MiB → 401, storage PUT unchanged. `refuseOversizeOp` (1 MiB) is the first line in the savepoint → 2xx, `sync_errors` + `sync_ops` rows (test 3: four refusals, the expense in the same batch lands). Device side, `claim()` bounds a batch to 4 MiB FIFO, so 50 photo ops (~20 MB) never produce a 413 (engine test). Never-list #8 holds for everything the apps can send.
- (c) Expo `FetchError` → `network`, DOS-156 folded with a red→green test — YES. `isNativeFetchFailure` (message prefix, never the class) sits after the abort check; `errors.test.ts` (refused + aborted messages) and `client.test.ts` DOS-156 (real client, `warehouse.picklists.create`, refused connection and a 40 ms deadline abort) both fail before and pass after; manager-app prints `app.writeNoConnection` for kind `network`. Web is untouched: the browser's `TypeError('fetch failed')` was already `network`.
- (d) docs/27 §15 rewritten in the same commit — YES (b4d0daa); §6 uploader line carries the 4 MiB bound; "photo not uploaded yet" is gone from docs, frontend and backend.
- (e) Lost reply vetoed `stale` — YES, no code; `save` passes `existing: row` so the op carries `baseUpdatedAt`, and `vetoIfStale` runs before the handler.
- DOS-146 (a)–(f) — YES. Runtime import of `@dos/ui` proven in the delivery-app vitest (no alias, no `@dos/ui/web`); guard test 5 survives `pnpm format` (mutants M1–M5); literals 300000/475600; tri-state comment at `start.tsx:76-82`; confirm body reads through `openingCashShown` so Clear-only prints "float ₹0.00" (verifier's probe); the depart builder keeps `input.cashPaise === null` (a null check, so 0 is sent); server applies `??` (`trips.service.ts:388`), contract allows 0. Amendment (c) is a walk-sequencing rule: since DOS-043 is already on main, run the dos_qa depart write AFTER h1's step-4 rebuild of dos_qa (no draft sheet then); a 409 before that is DOS-043 working.
- Architecture: screens import only `@dos/ui` and read through `@dos/api-client` / `@dos/offline`; `doorstep.ts` imports only `toApiError`; `jpeg-budget.ts` is platform-neutral and not exported from the barrels; no contract, permission, schema or state change; money and pieces untouched; idempotency by `opId` on the sync path and by input on the online mutation. Dependency: `expo-image-manipulator 57.0.16` pinned in the frontend catalog (SDK 57 family, same version line as expo-image-picker), in delivery-app deps, optional peer + devDep of `@dos/ui`, lockfile committed; Expo Go bundles it on iOS and Android, a dev client needs one `expo run:*` rebuild, web never loads it (canvas path); the lazy `require` in try/catch protects the six other apps' builds. DOS-166 placement: h7's `role_not_allowed` sits between `unknown_table` and the savepoint, h3's size check inside it — after the merge the loop must read replay → unknown_table → role_not_allowed → savepoint { refuseOversizeOp → vetoIfStale → handler }, which is the right order (no read before the role answer, no handler before the size answer).

## Merge conflicts with main and how to resolve
- `backend/libs/core/src/modules/delivery/delivery.spec.ts` — CERTAIN, the tail: main's DOS-043 (2051-2204), h2 slice 2's DOS-131/133 + describe-scope helpers (2217-2500) and h3's DOS-056 block all append after the 'stranger' test. Keep all three in that order (DOS-043 → DOS-131/133 → DOS-056 last), apply blocker 1, keep h1's/main's import line (`sql, type SQL`; `financialYear, uuidv7` — h3 touches no import).
- `backend/libs/core/src/modules/sync/sync.service.ts` — expected to auto-merge: h7 adds imports, the `role_not_allowed` else-if and the 42501 catch branch; h3 adds constants after `PULL_OVERLAP_MS` and one line inside the savepoint, three unchanged lines below h7's `} else {`. If git does conflict, the answer is both, in the order named above. Rebuild core, run `sync.spec`, `sync.coverage.spec`, `delivery.spec`.
- `docs/27-offline-sync-client.md` — h7 edits the §6 rejection bullet (~line 104), h3 the §6 uploader bullet (95) and §15: auto-merge; then add `row_too_large` to h7's bullet.
- `frontend/pnpm-lock.yaml` — h2 adds vitest under the warehouse-app importer; h3 adds expo-image-manipulator (catalogs, delivery-app and ui importers, packages, snapshots): auto-merge, then `pnpm install --frozen-lockfile` in `frontend/` must pass (else `pnpm install` and commit the lockfile). `pnpm-workspace.yaml`: only h3 touches it.
- No conflict: `delivery.sync.ts`, `bootstrap.ts`, `testing/app.ts` (nobody else touches them; h1/h7 touch `delivery.module.ts` and `delivery.internals.ts`, which h3 does not); `frontend/delivery-app/*` (DOS-131+137 is manager-app + warehouse-app; main changed only the generated delivery-app README); `api-client` (main changed session.ts/pages.ts, not errors.ts/client.test.ts); `offline/engine.ts`, `ui/platform/*` (no lane ahead). After the merge run once: delivery-app `opening-cash`, `trip-choice`, `doorstep` specs, api-client, offline, ui `jpeg-budget`; `pnpm lint && pnpm typecheck` in `frontend/` (the start.tsx text guard).

## What the later fixes must build on
- DOS-080 (h11, pull half of `engine.ts` + S-67 wipe): `claim()` is byte-bounded now (`uploadBatchBytes`, `wireBytes`, `utf8Length`); write its brief against merged main and keep it out of the upload half.
- DOS-068: native failures are `network` now, so `reachable` flips on phones after a failed call; only the NetInfo radio hint is left.
- Collect (`app/stop/[id]/collect.tsx:154`) and van sale (`van-sale.tsx:226`) keep the online-first shape: when their no-answer gaps are filed, wrap them in `recordOrSave`, never a second classifier.
- Any new sync handler refuses in the rule's own sentence (`parsed()` pattern); protocol-level codes go in `SYNC_REJECTION_CODES`.
- DOS-164 (kit modal host) does not touch D2: the fix lives in `openingCashEntered`, not in when the pad mounts. DOS-131+137's plan-a-trip float field holds plain state, never `x ?? plan`. `retailer-app/app/pay.tsx:141` and `manager-app/app/inbound/documents.tsx:418` are the same class — fix Pay with DOS-124/154, not with `?? 0`.

## READMEs that will change
None from this slice: no contract, route, permission or example change (the onRoute body limit is not README content). `pnpm docs:readme:check` on the merged tree should be a no-op; regenerate anyway per procedure.

## Regression walk, API probes and SQL
- Android Pixel 7 (`-memory 3072`, ganesh.more): a-41..a-44 — reverses for :3000/:3005 removed, force-stop, relaunch; strip reads offline; TRIP-NEXT stop 1 → Deliver → Photograph → Done → "Save on this phone" → toast, Attention shows the delivery waiting; restore reverses, ≤ 60 s → Waiting 0, Refused 0. Fallback branch: reverses in place, photo attached, remove :3005, tap Record → "Saved on this phone" within ~1 s. Online regression: `files.uploadUrl` → PUT → record; D5/D6/D7/D8 unchanged; with the API cut they read "No connection", never "unknown".
- Web delivery app at 390 (and 1280): DevTools offline → save → online → accepted; dead-spot block of `/files/upload-url`, `/storage/*`, `/delivery/deliveries` only (never `/sync/*`) → saved on phone; two-tab + reload → tray shows "This phone no longer holds this write…" and no "Send it again".
- iOS Expo Go (`node QA/tools/ios-login.mjs 5177 ganesh.more delivery Delivery`): one capture proves expo-image-manipulator in Expo Go and a ≤ 300 KB proof; D2 read-only steps.
- Manager app Android (vikas.kadam, DOS-156): Waves → Make a picking sheet → API cut → dialog says "No connection. Check the signal, then press again."; restore → 409 sentence.
- D2 (sachin.dalvi) Android + iOS: Clear → ₹0 / "0 rupees"; 4756 → ₹4,756; ⌫×8 → ₹0; Done → ₹0.00; remount → ₹3,000.00; dialog controls ₹3,000.00 / ₹0.00 / ₹4,756.00; web select-all+delete+blur → 0.00. Depart write on dos_qa after h1's rebuild: `select state, opening_cash_paise from trips … TRIP-NEXT` → active, 475600.
- API: running delivery-service — `POST /sync/upload` 9 MiB → 413, 7.5 MiB unauthenticated → 401 (hook live); a 2 MiB op inside a signed batch → 200 with `row_too_large` and the small op beside it accepted.
- SQL (dos_qa): `select kind, object_key, payload from pod_evidence where delivery_id = …` → key, null; `select count(*) from pod_evidence where object_key is null and kind in ('photo','signature')` → 0; `select count(*) from sync_ops where length(outcome::text) > 300` → 0 and `select count(*) from sync_errors where message_en like '[%' or length(message_en) > 500` → 0 (no base64, no Zod JSON); `select status, bytes from file_objects where domain='pod' order by uploaded_at desc limit 5` → uploaded, ≤ 300000.

## Defects outside this slice
- `frontend/libs/offline/src/engine.ts:793` `retry()` re-sends the same `opId` and `sync.service.ts:113-127` replays the stored refusal — "Send it again" can never succeed for a held op in any tray (protocol decision, all three field apps).
- `backend/libs/core/src/modules/sync/sync.service.ts:217` filters `sync_errors.resolved_at`, which nothing ever sets; a mirrored refusal returns to every fresh device until thrown away there.
- `frontend/delivery-app/src/lib/queue.ts:156` the local apply never writes `deliveries.outcome`, so the stop screen still offers "Deliver this bill" after a save-on-phone; the second op is refused, no data harm.
- `frontend/libs/ui/src/platform/files.native.ts:34` / `files.web.ts:48` `upload` has no deadline: a dead spot that accepts TCP and never answers waits for the OS socket timeout, not 20 s.
- `backend/libs/core/src/modules/files/files.service.ts:84` `pending` `file_objects` rows (URL minted, PUT never came — the fallback path mints one) and storage objects without a row are never swept.
- `frontend/delivery-app/app/trip/start.tsx:357` confirm body prints "float ₹0.00" when the trip query has no data yet while depart leaves the key out (pre-existing `?? 0`).
- `frontend/retailer-app/app/pay.tsx:141-142`, `frontend/manager-app/app/inbound/documents.tsx:418` — same `value ?? plan` snap-back as DOS-146.
- `frontend/libs/ui/src/money.ts:162-164` + both `NumberPad`s: a pad opened on a PRE-FILLED amount appends the first digit (₹3,000 → "4" → ₹30,004) — DOS-060 by design, but the crores hazard the finding names; founder kit decision, own id.
