# Merge review: lean group lean-delivery-collect (DOS-062, DOS-065, DOS-067, DOS-066, DOS-071), branch qa/b2-lean-delivery-collect

Fable, 2026-09-20. Read-only review of c7b1a88 (8 commits over base 4d57a04) against `verdicts/lean-lean-delivery-collect.md`, the group note (lean-groups.md §14), the five findings in `04-walkthrough-delivery.md`, docs/22 §8 rows :304-305 and the build report. Nothing was run; every claim below is from the diff, the branch's sources and main's.

**Decision:** MERGE AFTER FIXES

The fixes match the designs and the founder's rules. DOS-066: `dues.ts` is pure, only `stop` earns the chip, no limit or days on the screen, nothing gated (`index.tsx:121-125,213-224,358-375`); docs/22:304 holds the answer. DOS-071: the rule lives once in `recordExpenseInTx` BELOW the replay short-circuit (collections.service.ts:243-264), `applyExpenseSync` calls the same method (delivery.sync.ts:253) and the spec proves the 2xx rejection; `int(…, min 0)` keeps 0 legal (delivery.internals.ts:510-513); `podState` mirrors `assertPodPolicy`'s failed-first line (deliveries.service.ts:394). DOS-062: the tag is a real `strategy: 'explicit'` split capped at the OFFICE's open balance; I checked the ground the fix stands on — `getOutstanding` returns every open bill unpaged (receivables.service.ts:1275-1296), `allocations_read` lets any staff role see allocation rows so the crew's `openPaise` is true, not face value (schema/columns.ts:210-219), `assertRepServes` returns for the crew (:1729), `planAllocations` accepts a split under the receipt and leaves the rest on account (:1962-1972), `cache.invalidate` is prefix-matched (cache.ts:196-201) and `PaiseSchema`/`OpenBillSchema` carry `id`+`openPaise`. DOS-065: `receipts.list` honours `tripId` (receivables.queries.ts:120) and `messages.list` honours `mine`/`refType`/`refId` (messages.service.ts:85-91). DOS-067: register divides by `stopsWithEta`, `ratio(0,0)=0`, the list runs to today+7 and the score to today. The tests are not weakened: the only existing tests touched (dos-181 key rename, dos-062 fixtures gaining a required field) keep every assertion; the reds the verifier reproduced stand. No leak: the three files outside `ownsFiles` are the ones the design authorises (collections.service.ts, delivery.internals.ts, tenant-bootstrap.ts, all additive); the rest are new pure modules and tests inside the group's own app, or generated READMEs.

## Blockers

1. **After an untagged receipt the bill in the driver's hand is never named as still open.** `frontend/delivery-app/src/lib/allocate.ts:196-229` (`appliedLines`) prints `d5.leftOpen` only for `result.invoices`, and that list is `settled` = the bills the receipt TOUCHED (receivables.service.ts:836-865, `SettledInvoiceSchema` doc "the bills a mutation touched"). Cash ₹7,856 untagged → FIFO pays INV/0099 exactly → D3 says "This paid INV/0099 ₹7,856.00" and nothing about INV/0825, which is the finding's own acceptance sentence ("this pays INV/0099 of 26 Jun; today's bill stays open") and what the function's comment claims ("the bills it left open"). Fix: add `doorBills: readonly DoorBill[]` to the `result` argument; after the touched loop, for each door bill with `TAKES_MONEY.has(state)`, not in `result.invoices`, and `owedOn(bill) > 0`, push `t('d5.leftOpen', { no: bill.invoiceNo ?? bill.invoiceId.slice(0, 8), amount: result.money(owedOn(bill)) })`; pass `doorBills` from `collect.tsx:257` `onSuccess`; one case in `dos-062-allocate.test.ts` ("a FIFO reply that touched only INV/0099 still names INV/0825 as open"). Fix the comment.
2. **Binding amendment (h) / item 4g not done.** `docs/23-app-screens-and-api-gaps.md:979` — append row 16 to the §10 table (4 columns): `| 16 | owner-app Settings (`frontend/owner-app/app/settings/index.tsx:537-550`) | the owner has no control for `delivery.expense_proof_min_paise`; only `settings.set` moves it and the ₹200 default holds | lean-retailer-platform (19): a Money field beside `delivery.pod_required`, 0 = every expense (DOS-071) |`. The run's own rules forbade docs/22, docs/18, CLAUDE.md and QA/, not docs/23.
3. **Gate on the merged tree** (the lane ran tests, tsc, eslint and prettier on its own tree only): backend `pnpm format:check && pnpm lint && pnpm typecheck && pnpm build && pnpm docs:readme:check && pnpm db:migrate && pnpm test` (at least `src/modules/delivery` and `src/modules/reporting`); frontend `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm format:check`, kit cross-app guards with `--force` (S-155); then `pnpm db:seed` on dos_qa (group note) so the ETA seed and the ₹200 row are there for the walk.

## Minors

- The second DOS-066 case (`dues.test.ts:140-145`, "tell, never block") passes on main too — a forward guard for the founder's rule, not proof of the fix (verifier). Fine to keep; do not count it as the fix's evidence.
- Offline, `owedHerePaise` sums FACE values (`allocate.ts:106-108` with `openPaise` null) under "Owed on the bills here" — the finding's third bullet survives offline for a part-paid bill. Say "as billed" or fall back to `dues.outstanding_paise` when `openByInvoice === null`.
- `d5-paid-*` says "Paid" for any non-open state (`collect.tsx:432-437`): a `cancelled` or `written_off` bill on the door would read "Paid". Label from `bill.state`.
- `whereTheMoneyGoes` counts `dues?.open_bills` (device summary) while the row figures come from the office answer one line above — `officeBills.data?.openBills` is the consistent source.
- A bill tagged, then dropped by a refreshed office answer, keeps `state="selected"` and silently sends no line (`tagAllocations` filters it out, `splitSent` false → FIFO). Clear it from `tagged` when it leaves `taggable`.
- delivery.spec.ts:1379-1399 sets the tenant's threshold to 0 and restores it only on the happy path; an assertion failing between leaves 0 for every later case. try/finally.
- Seed cosmetics: for the every-fourth late stop, the next stop's `startedAt` (eta−25) precedes the previous `completedAt` (eta+25) (`seed-demo/delivery.ts:407-421,466-476`). `key={line}` on `d3-applied-*` collides on two identical lines. The PART_PAID fixture comment cites 102000/16000, which the lane database no longer holds (verifier).
- The owner's rule is now two numbers on the wire (`TripPolicy.expenseProofMinPaise`) that no app can set; until blocker 2's row is built the desk sets it through `settings.set`.

## Conflicts

- **main 8b030e5** (lean-admin-support merged after the branch was cut): `git merge-tree --write-tree` is clean (tree a3d8cbab). All six `waitsFor` are on main (door b8b116d, offline-boot 5f710dc, retailer-shop b17e23f, owner-desk 1d993b4, warehouse-stock 4d57a04, backend-platform d1c7da0).
- **Branches still ahead of main** (kit-polish 5, manager-money 6, sales-rep 18, warehouse-pick 7 commits): zero file overlap with this diff, merge-tree clean against each.
- **Groups still to merge:** 17 lean-owner-money-approvals owns `registers.service.ts` and `reporting.spec.ts` (both changed here) and waits on this group — fine. 18 lean-manager-order-lifecycle owns `delivery.spec.ts` and does NOT wait on this group: its edit must rebase over the +121 lines here (a new `policy` field at :87-93 and two cases at :1335-1445; additive, disjoint). 19 and 20 both own `tenant-bootstrap.ts`: four additive hunks beside the `delivery.*` entries, commit ffebdc4 names the key. `pnpm docs:readme` on the merged tree regenerates the five service READMEs.

## Walks

None run (the run forbade servers and emulators), so every screen claim above is source-read. Owed on dos_qa after `pnpm db:seed`, as ganesh.more, web 1280×800 and 360 px, then Pixel 7 (`-memory 3072`), text from the DOM/accessibility tree: D3 stop 4 Vaibhav `d3-overdue` "Overdue ₹52,176.00 · oldest due 10 Jul", `d3-overdue-amount`, `d3-days-late`; stop 9 Khan `d3-credit-stopped` + `d3-stopped-line` and `d3-deliver` still opens D4; the chip row wraps at 360 px with no horizontal scroll. D5: rows inert and `d5-goes` reads "…has not come from the office" until `outstanding.get` answers; a part-paid row shows the open figure with "part paid"; tag one bill → `d5-tagged`, Record → D3 `d3-applied` names the bills (blocker 1's line included); a paid bill shows `d5-paid` and is out of `d5-expected`; offline the sentence is `d5.appliedOffline`. D4: credit shop, no photo → `d4-record` disabled with the credit sentence; after the photo the panel meta reads "Photo attached — it goes with the delivery"; a cash shop under `always` gets the office sentence; a failed outcome asks for none. D7: ₹500 diesel without a photo → disabled, ₹200 sentence, `d7-photo` primary; ₹50 records; ₹500 with a photo records. D9 from a stop: "Taken on this trip" + this bill's messages, `d9-older` opens the history; by direct link: "Not looked up". D11: "On time" with a non-zero rate on the seed, TRIP-NEXT listed. D12 inbox: only rows addressed to the crew. iOS: the group note asks for web + Android only; keep iOS for the A.12 regression.

## Defects outside

1. **Two readers of the on-time rate now disagree.** `backend/libs/core/src/modules/delivery/performance.ts:137-172` (`deliveryStopsByDay`) still writes `on_time_stops` with no `stops_with_eta`, and `reporting.service.ts:162-167` and `:780-785` divide it by delivered+partial, so the owner's delivery trend keeps the 0%-style distortion the register just lost (verifier's minor, confirmed). Needs `daily_tenant_stats.stops_with_eta` + migration; `schema/reporting.ts` is owned by lean-owner-money-approvals (17) — hand it there with this note.
2. **Seed payment states never move for the road's receipts.** `seed-demo/delivery-road.ts:113` picks `issued`/`partially_paid` bills and writes allocations after `seed-demo/receivables.ts:880` has recomputed states, so Tc803a3a5/1 sits `issued` with ₹1,020 against it (the lane's own measurement). That is why the device's `state` column could not be trusted and why the verifier's cheaper options were wrong; recompute after delivery-road.ts, or the demo data lies to every screen that reads `invoices.state`.
3. `TripsListInput` still has no `orderBy` (docs/23 §10 #7): D11 sorts client-side and a 51st trip in the 37-day window would fall off the page unseen — pre-existing, now one week wider.
