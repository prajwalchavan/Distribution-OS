# Merge review: lean group lean-delivery-collect (DOS-062, DOS-065, DOS-067, DOS-066, DOS-071), branch qa/b2-lean-delivery-collect

**First review (2026-09-20, c7b1a88): MERGE AFTER FIXES** — blockers 1 (untouched door bill never named open), 2 (docs/23 row 16) and 3 (gate + seed + walks), eight minors. Since then: the merge of main (de9fb07), the blocker-1/2 fixes and six of the minors (d8c444c…153d8c7), the verifier's seed regression and its repair 032c4e4; the gate half of blocker 3 is green on the merged tree (verifier). Re-reviewed 2026-09-21 at 032c4e4 (17 commits over base 8b030e5), read-only, nothing run.

**Decision:** MERGE

Both proven defects are answered at the cause, not moved. (1) `seed-demo/delivery.ts:488` derives `doorFinishedAt` for a failed stop from `stopCompleted`, `:504-511` derives `arrived_at` from it, `:534` stamps the failed delivery with the same clock — one clock, so the 15-minute inversion cannot recur; I re-did the slip algebra (18/−1/−8/−8, `stopStarted` = max(eta−25, prevDone+2)) and the tightest door (seq%4==1 after a late one) still arrives 2 min after it started; `occurred()` (util.ts:109) is monotone so today's clamp can collapse a gap to 0, never flip it; the verifier's fresh-seed red/green (64/64 inverted → 0/1484, +7.0 min everywhere, cross-stop and trip-window invariants 0) is the proof. (2) The walks are structurally impossible in a lane that may not start a server or touch dos_qa; under STATE.md's corrected rule (a major is carried, logged and owed; `finish-wave3.js` lifts the server ban for the walk stage) they are OWED, not a merge blocker — listed below with the numbers corrected. Nothing the first review approved is broken: blocker 1 is closed exactly as prescribed (`allocate.ts:249-304` names every untouched door bill that still takes money, `collect.tsx:276-281` passes `doorBills`, the FIFO case is in `dos-062-allocate.test.ts:284-311`); blocker 2's row 16 is a 4-column row at docs/23:980; the minors landed as pure helpers (`owedHereIsAsBilled`, `settledBillChip` via `wordFor`, `liveTags`) with unit cases, the try/finally restores the ₹200 threshold and deletes only what was recorded, `whereTheMoneyGoes` reads the office's count, the applied-line key carries the index. No test weakened; DOS-149's receipt number still reaches the stop (`collect.tsx:289`); the design's file list and docs/22 §8:304-305 hold; DOS-171's founder row (§8:318, a van-sale bill counts in what is owed at the stop) holds through the van sale's own `deliveries` row (vansales.service.ts:146-156) and D5's `staleTime: 0` re-read of the office's balances.

## Blockers

None.

## Minors (fix in the walk session or the next slice; none changes a screen the walk depends on)

- `seed-demo/delivery.ts:534` — `deliveredAt: occurred(stopCompleted(day, sequence))` should read `occurred(doorFinishedAt ?? stopCompleted(day, sequence))` so a future caller that passes a `completedAt` for a failed stop cannot split the clocks again.
- `seed-demo/delivery.ts:478-480` asserts `completed_at` NULL on a failed stop as design; the service writes it (`trips.service.ts:967-968`, `fail` sets `completedAt`). Pre-existing shape (main :793), demo only, excluded from the DOS-067 rate by state — but the comment must not cite it as intended: either pass `stopCompleted(day, i+1)` for failed stops at `:847` (then `doorFinishedAt` collapses to `completedAt`) or say "diverges from the service" in the comment.
- `allocate.ts:293-301` — the "still open" line uses `owedOn(bill)`, a FACE value when `openPaise === null` (office not yet answered, online). Either say "as billed" on that line or skip it until `openByInvoice` is in, the same rule the figure over the pad now follows.
- Walk-script numbers (verifier): D11 shows ganesh.more's OWN last-30-day window (`trips.tsx:44-45,75-80`, `registers.service.ts:348`) ≈ 80% (70/87 on a fresh seed), never the cross-tenant 78.7% the repair report quotes; the first door is promised 09:30 IST and one every 30 min (tomorrow's three road trips sit on delivery-road.ts:147's 10:00+40 grid, not a defect); failed seq 1 arrived 09:22 / stamped 09:29.
- The seed still stamps `started_at` on PENDING stops (`:503`, every state; pre-existing on main). Harmless to every register; a pending door with a start time is a shape `walkStop` never writes.

## Conflicts

- **main (40f4682):** merge base 8b030e5; main's three commits since touch no file of this diff; `git merge-tree --write-tree main qa/b2-lean-delivery-collect` clean (tree 784531c5).
- **Branches ahead of main:** merge-tree clean against all five that share a file — manager-order-lifecycle (delivery.spec.ts), owner-money-approvals (registers.service.ts, reporting.spec.ts, READMEs), retailer-platform and warehouse-rules (tenant-bootstrap.ts, READMEs), sales-rep (docs/23, no row-16 collision). kit-polish, manager-money, warehouse-pick: no overlap. `pnpm docs:readme` on the merged tree regenerates the five service READMEs.
- **Instruction conflict, reconciled:** dos171-1.md:47 told this group to "keep reading `planned_collection_paise`" on D5; this branch replaced it (first review, accepted) with the open balance of the bills riding on the door. The founder's rule survives because the van sale writes a `deliveries` row on the stop that D5 lists like any bill; the walk must prove it (below).

## Walks (none run here; OWED, on web + Android as the group note asks; iOS stays with the A.12 regression)

Seed first into an EMPTY database — a re-seed over existing rows keeps the old clocks (`insertMany` is do-nothing on conflict; the repair measured it): recreate dos_qa only with the founder's say-so (dropping it is destructive), else a fresh `dos_qa_wave3` in `DATABASE_URL` under the :3100 port rule; `pnpm db:migrate && pnpm db:seed`; services + `pnpm --filter @dos/delivery-app web`; ganesh.more, text from the DOM/accessibility tree at 1280×800 and 360 px, then Pixel_7_API_36 (`-memory 3072`). Measure FIRST: "Owed on the bills here, as billed" beside the Money figure at 360 px in the bottom bar (`collect.tsx:395-399`) and in `RupeeInput.expectedLabel` (`:541`) — no clipping, no horizontal scroll. Then: D3 stop 4 Vaibhav `d3-overdue` "Overdue ₹52,176.00 · oldest due 10 Jul", `d3-overdue-amount`, `d3-days-late`; stop 9 Khan `d3-credit-stopped` + `d3-stopped-line`, `d3-deliver` still opens D4; chip row wraps at 360 px. D5: rows inert and `d5-goes` "…has not come from the office" until `outstanding.get` answers, then the office's count; a part-paid row shows the open figure; untagged cash ₹7,856 → D3 `d3-applied` reads "This paid INV/0099 …" AND "INV/0825 is still open — …" (blocker 1); a paid bill's chip "Paid" (moss), a cancelled one "Cancelled" (neutral); tag, then let the office drop the bill → the row loses "Tagged" and the sentence returns to the oldest-first rule; offline the label says "as billed" and the sentence is `d5.appliedOffline`; after a van sale on D6, D5 lists the new bill and `d5-expected` includes it (DOS-171). D4: credit shop no photo → `d4-record` disabled with the credit sentence, after the photo "Photo attached — it goes with the delivery"; cash shop under `always` → the office sentence; failed outcome asks none. D7: ₹500 diesel no photo → disabled, ₹200 sentence, `d7-photo` primary; ₹50 records; ₹500 with photo records. D9 from a stop: "Taken on this trip" + this bill's messages, `d9-older` opens history; direct link: "Not looked up". D11: "On time" non-zero, roughly 80% for ganesh.more over the last 30 days, TRIP-NEXT listed; day screen ETAs on the 09:30 half-hour grid; a failed door on a settled day shows arrived 09:22 before its 09:29 stamp. D12: only rows addressed to the crew. Record the network log: no 4xx except the two designed 400s.

## Defects outside (carried; nothing new from this round)

1. `performance.ts:137-172` `deliveryStopsByDay` still writes `on_time_stops` without `stops_with_eta`, so the owner's trend (`reporting.service.ts:162-167, 780-785`) keeps the distortion the register lost — `daily_tenant_stats.stops_with_eta` + migration, owned by lean-owner-money-approvals (17).
2. `seed-demo/delivery-road.ts:113` allocates after `receivables.ts:880` recomputed states: the road's bills stay `issued` with money against them (why D5 asks the office). Recompute after delivery-road.ts.
3. `TripsListInput` has no `orderBy` (docs/23 §10 #7): D11 sorts client-side; a 51st trip in the 37-day window falls off unseen.
4. dos171-1.md's items 3 and 4 for this group: D5 offline still shows face values (now labelled "as billed"; the online half is answered by the office's open balance); "Still to collect" on D1 dropping a door before money is taken is a product question for the founder.
