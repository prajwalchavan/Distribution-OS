# Slice h1-money-slice2 — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE

Range c2c472f..460269e (b18cc68 DOS-007, ed9ee4d DOS-117, 460269e DOS-132), 37 files, read through git objects. Money stays integer paise; the only posting change is the deposit credit (CASH, never CASH_VAN a second time — delivery.spec (2) pins Dr BANK / Cr CASH and CASH_VAN net 0 over receipts + settlement + deposit); every mutation keeps `withTenant` + `idempotent()`, and the 409 rolls the idempotency row back so a retry after settlement re-runs. No schema, migration, permission or state-machine change. Boundaries hold: receivables never names `trips` (delivery hands `tripSettledSql` in through `registerTripSettled` via `receivables/index.ts`, default `sql\`false\`` fails closed); `ageing-rebuild.ts` and `loadStatementSummary` reach the worker only through `receivables/worker.ts` + the `./receivables` export; notifications takes every figure as an argument. Screens import only `@dos/ui`; Day-end and Today share inputs and keys (`['receipts','collected',<mode>,'office']`). Contract change is additive (`receipts.list` `withCrew` tri-state, `receipts.get` `withCrew: boolean|null`); `StatementChannelSchema` is `whatsapp|pdf`, so no channel is accepted-then-dropped any more.

## Blockers / majors (must be fixed before merge)
none

## Minors (may follow)
- receivables.service.ts:912 — `data.receiptIds` (and the message) order by a string compare of `receiptNo`; past RCPT-9999 the order is not numeric. Error payload only; sort by the numeric tail or by id later.
- receivables.service.ts:915-918 — plural wording "receipts X, Y were taken … bank them …" is a grammatical extension of amendment (c); the single-receipt form is exact and asserted in delivery.spec. Accepted.
- reporting.spec.ts:1364-1371 is not prettier-clean (verifier): run `pnpm format` in backend on the merged tree before committing, or CI `format:check` fails.
- schema/receivables.ts:403-407 — `retailer_outstanding_summary` has no (tenant_id, as_of) index, so the start-up probe (`ageingNeedsRebuild`) walks every summary row of a tenant that is already current; bounded by the 2 000-tenant cap and only at start, but O(shops) per tenant. Add the index in a later expand migration.
- outstanding.ts:384 `writeAgeingSnapshot` (via `rebuildAgeingPage`) writes a snapshot and a summary row for every retailer, zero-dues shops included, every night; growth is the verdict's retention/partition finding — not this slice's.
- Merge-time docs (main session): docs/18 + QA/13 rows for DOS-007/117/132 incl. amendment DOS-117 (f) (docs/plans/receivables.md §7's 00:30 `receivables.ageing_snapshot` is realised as the 00:20 finalize's per-tenant `receivables.ageing.rebuild` fan-out, stately key `receivables-ageing:<tenantId>:<day>`, expire 3 600 s, plus `receivables.ageing.catchup` on start, run as each tenant's system actor); docs/23:196 → "statements sent as a WhatsApp/SMS summary; PDF pending DOS-057/099"; docs/plans/receivables.md:241 superseded. docs/22 already carries DOS-132 (§6:189 cash AND cheques; §8:279, :284 item 3) — no edit.
- dos_qa is rebuilt right after this merge: the four platform templates (`statement`, `statement_no_upi` × whatsapp/sms) arrive with `pnpm db:seed`; run `QA/tools/seed/verify-seed.sh` after (it asserts no template counts).

## Amendments — satisfied? evidence
- DOS-007 (a) two keys, never an empty variable: notifications/events.ts `queueStatement` picks `statement_no_upi` and omits `upiLink` when `upiPayLink` is '' (no VPA, dues ≤ 0, `includeUpiQr=false`); four seed templates, none with a dangling "Pay by UPI:"; both keys in `PLATFORM_TEMPLATE_KEYS`; notifications.spec second DOS-007 test asserts no empty variable. (b) idempotency key `Statement:${eventId}` (events.ts), `sendStatement` passes `e.id`; summary and message both under `asTenantSystem`; pay link = `max(0, outstanding − unallocatedCredit)` today, receivables.spec proves = AR balance for a past window. Replay → 'replayed', one row (messages unique on tenant_id + idempotency_key). (c) `sendStatements` throws NOT_IMPLEMENTED `statement_pdf_not_rendered` before any write; spec asserts 501 + zero outbox rows. (d) worker/src/jobs/outbox-coverage.test.ts: AST scan of every `insert(outboxEvents)` + every `register*Jobs` registrar; AUDIT_ONLY list reviewed — receivables/orders/warehouse/delivery/billing/docint/claims/ai/incentives entries are correct as audit rows; `ImportCommitted`/`ExportReady` honestly marked as an unbuilt consumer (file a P3 for the integrations notifications). RED on the reversed tree named exactly `StatementRequested`.
- DOS-117 (a) reporting.ts:176-182 stately / retryLimit 3 / retryDelay 300 / retryBackoff / expireInSeconds 3600; catch-up :189-194 stately without expiry override. pg-boss `job_i3` (plans.js:675) indexes (name, state, coalesce(key,'')) — one created + one active per tenant-day, one keyless catch-up per state. (b) ageing-rebuild.ts: one `tenantStorage.run + withTenant` per 500-shop page; `select retailer_id … order by retailer_id for update` on the summary rows inside that transaction, nothing else locked; totals added after commit. (c) worker.ts appended (one line), package.json untouched, main.ts:80-82 log only. (d) reporting.test.ts goes through `registerReportingJobs` and the stored handlers; fake boss throws on an uncreated queue; `vi.mock('@dos/core/reporting')` intercepts (RED logged tenants: 1). (e) rollup.ts:206-212 is exactly isToday → live; else existing `daily_tenant_stats` → keep; else snapshot rows > 0; else 0; `refreshOwnerSummary` untouched; comment + property 4 rewritten. (f) deferred to merge (lane may not edit docs/18, QA/) — listed above. Run-time `businessDate()` in `runTenantAgeingJob` (test 3). `boss.start()` precedes registration in main.ts, so the start-up `boss.send` is valid.
- DOS-132 (a) `TripSettledPredicate = (tripId, tenantId)`; `tripSettledSql` carries `t.tenant_id = ${tenantId}` and the IN list from `TRIP_CASH_HANDED_OVER` (`cancelled` excluded; the machine allows cancel only from planned/loading, so a trip with a collection cannot be cancelled). No `trips` in receivables SQL. (b) wrapper passes `MONEY_DESK.includes(role) ? predicate : null`; queries.ts applies the filter only with a predicate, in the shared `where` (page and totals); receivables.spec asserts the crew's `withCrew=false` list equals its unfiltered list, GET → null. (c) mode-neutral: one VALUES-list query over distinct trip ids (`sql.join`, never `= any`), refused ids only, `data.code trip_cash_not_settled`; spec refuses [office cash, crew cash, crew cheque] whole and asserts only the two crew ids; JSDoc names cash and cheques; deposit credit `receiptAccountCode(mode, null)`. (d) specs sit at the anchors (`// check-in` :1549, settle test :1553, `// the other crew's trip` :1659 on main HEAD). (e) owner change = the `disabled` guard + one string; O11 dialogs untouched; manager `Refusal` unchanged; no new imports.
- Refusal cannot strand money: the desk returns (`trips.return` = owner/manager/delivery) and settles (`trips.settle` = MONEY_DESK) the trip from Day-end, then the receipts appear in the register; an orphan/cancelled trip id is repaired by reverse + re-record (verdict). The accountant alone cannot return a trip — the existing DOS-043 model, not new.

## Merge conflicts with main and how to resolve
(main will be 722ae91 + h7 + h5 + h2 when this slice's turn comes; the lane merges main into itself first.)
- backend/libs/core/src/modules/delivery/delivery.module.ts — h7 (DOS-166) rewrites the five `registry.register` lines after `if (!this.registry) return` with `standsFor`; h1 adds the `ReceivablesService` import + constructor param and `this.receivables.registerTripSettled(tripSettledSql)` BEFORE that line. Adjacent hunks: if git conflicts, keep h1's import/constructor/first statement, then `if (!this.registry) return`, then h7's `standsFor` registrations.
- backend/libs/core/src/modules/delivery/delivery.spec.ts — main (DOS-043) appends at :2051, h2 edits `SettlementBody` (:115) and appends, h3 edits :915 and appends; h1 widens line 1 to `import { sql, type SQL }` and inserts at :1545 (before `// check-in`) and :1655 (after the settle test). Expect a clean auto-merge for h1's hunks; the three end-of-file appends may conflict with EACH OTHER — keep all blocks in merge order. Re-run delivery.spec.ts once on the merged tree.
- backend/libs/core/src/modules/delivery/delivery.internals.ts — h2 hunks at :16, :64, :317, :338; h1 adds `TRIP_CASH_HANDED_OVER` + `tripSettledSql` after `TRIP_ON_THE_ROAD` (:124-146). Clean (`sql`, `SQL` already imported at :2).
- frontend/manager-app/src/strings.ts — h2 at :398, h1 at :517. Clean. frontend/owner-app/src/strings.ts — main at :126, h1 at :202/:305/:320/:753. Clean.
- receivables.service.ts, receivables.queries.ts, receivables.spec.ts, notifications/*, reporting/rollup.ts, worker/src/*, seed-demo/notifications.ts, contracts receivables.ts/notifications.ts, core package.json: h1 is the only lane touching them — no conflict. rls.test.ts, schema/receivables.ts (h7 receipts policy), permissions.ts (main, h5, h2), migrations 0045/0046 and `_journal.json`: not touched by h1 — nothing to resolve. worker/src/main.ts: only h1; the guard's REGISTRARS pins main.ts's registrar list, and no lane ahead adds a `register*Jobs` call.
- READMEs: main already regenerated manager-app/owner-app READMEs (DOS-003/004); regenerate once on the merged tree (below).

## What the later fixes must build on
- DOS-080 (sync pull storm): the nightly re-date moves every summary row's `updated_at`, so every field device pulls the whole dues table each morning (tie completion 2 000 vs page 500); bound the pull cursor there, do not touch the rebuild.
- DOS-016 edits the same rollup.ts dues block: keep precedence (e) — today live, past day keeps its stored closing, snapshot only when never rolled up.
- DOS-164 / DOS-136 rebase the one-line `disabled` guard on the two Bank it panels; DOS-029's error surface should replace the owner toasts' `error.message` and fix the O11 `.then(done, done)`.
- Any new outbox event type needs a worker handler or an AUDIT_ONLY entry; any new `register*Jobs` in main.ts goes into REGISTRARS in outbox-coverage.test.ts.
- The rebuild is idempotent by upsert: a start-up catch-up and the 00:20 finalize may both run one tenant-day; keep the page lock exactly as amendment (b), never widen it. `receipts.deposit` now assumes settlement credited CASH_VAN for every trip cash receipt — the offline `receipts`-table doorstep receipt (no `collections` row, verdict P1) and `undoReceipt` after settlement (verdict P2) are the two paths that still drift CASH_VAN.
- Statements are a text summary by founder default (QA/13 Q3); the PDF stays 501 until DOS-057/099.

## READMEs that will change
`pnpm docs:readme` on the merged tree (receipts.list input `withCrew`, receipts.get output `withCrew`, DepositReceiptsInput doc): backend/{owner,manager,delivery,retailer,sales}-service/README.md and frontend/{owner,manager,delivery,retailer,sales}-app/README.md. Unchanged: auth, warehouse, admin services, warehouse/admin apps (PLATFORM_TEMPLATE_KEYS is a const, not a procedure).

## Regression walk, API probes and SQL
- Worker: stop the old worker (`pgrep -fl "@dos/worker"`), start against dos_qa. `select name, policy, retry_limit, expire_seconds from pgboss.queue where name like 'receivables.ageing.%'` → 2 stately rows, rebuild 3600. `select name, state, singleton_key from pgboss.job where name like 'receivables.ageing.%' order by created_on` → one completed catchup, one completed rebuild per stale tenant. `select tenant_id, min(as_of), max(as_of) from retailer_outstanding_summary group by 1` → all today (IST); `select tenant_id, count(*) from ageing_snapshots where as_of=(now() at time zone 'Asia/Kolkata')::date group by 1` = each tenant's retailer count; `08-followup-stale-shops.sql` / `08-followup-ageing-freshness.sql` → zero drift; tarsun `daily_tenant_stats` today = sums of the summary. Restart again the same day → a catchup completes, no new rebuild, `max(updated_at)` unchanged.
- Owner web :5173 (sunil.tarsun): Shops → Prerna → Send statement → `find shop-toast` "Statement queued for sending" (4 s auto-dismiss); within one relay tick `select published_at, last_error from outbox_events where event_type='StatementRequested' order by created_at desc limit 1` → published; `select channel, status, template_key, idempotency_key, payload->>'body' from messages where template_key in ('statement','statement_no_upi') order by created_at desc limit 1` → whatsapp, key `Statement:<eventId>`, no empty "Pay by UPI:"; Settings → Messages lists it. Money O10 → Send statements → money-toast "N statements queued". API: POST :3001/receivables/statements channel 'pdf' → 501 `statement_pdf_not_rendered`, StatementRequested count unchanged; Today overdue tile = live; Reports → Outstanding last point = today's `daily_tenant_stats`.
- Manager + accountant :5174 at 1280×800 and 390×844 (vikas.kadam, meena.joshi): Day-end lists no receipt of an unsettled trip; `GET :3002/receipts?status=collected&mode=cash&withCrew=false&limit=5` and `limit=200` give one `totals.countedPaise` = `select sum(r.amount_paise) from receipts r left join trips t on t.id=r.trip_id where r.tenant_id=$t and r.status='collected' and r.mode='cash' and (r.trip_id is null or t.state in ('settled','settled_with_variance'))`; Today tile equals it. Receipt panel of an unsettled-trip receipt: Bank it disabled "Still with the delivery crew…"; as ganesh.more `GET :3005/receipts/{id}` → withCrew null; as a retailer `GET :3006/receipts?withCrew=false` = without it. Disposable copy: POST /receipts/deposit with it → 409 `trip_cash_not_settled`, receipt still collected, no `deposit` entry; then return + no-variance settle → the receipt appears in Day-end → Bank this batch → entry {BANK +sum, CASH −sum}, no CASH_VAN line; CASH_VAN over the trip's receipts + `trip_settlement` + deposit = 0. Never bank RCPT-VAN-*.
- Android Pixel_7_API_36 (-memory 3072): manager Day-end and receipt panel (disabled Bank it), owner Today tile. iOS via Appium if it taps; else NOT TESTED.
- Gates: `pnpm smoke` 0 BROKEN (the deposit example has no tripId); `pnpm docs:readme:check` after ONE regeneration; `pnpm format:check`; `pnpm --filter @dos/core test -- src/modules/delivery src/modules/receivables src/modules/reporting src/modules/notifications`; `pnpm --filter @dos/worker test`.

## Defects outside this slice
- backend/worker/src/jobs/notifications.ts:158-171 (+ the new handler): the 48 h stale guard publishes an old `StatementRequested` without sending and the owner who saw "queued" never learns — a Failed `messages` row would be honest (all translators share it).
- backend/libs/database/src/schema/receivables.ts:403-407: no (tenant_id, as_of) index on `retailer_outstanding_summary`; the start-up probe scans a current tenant's rows.
- backend/worker/src/jobs/reporting.ts:151: the finalize still loops `rollupBehaviour` over every tenant inside one job (O(all shops), 900 s expiry); :161/:169/:200 the `reporting.rollup.*` queues are `standard`, so their `singletonKey` is never enforced.
- backend/libs/core/src/modules/receivables/receivables.service.ts:1849 `undoReceipt` credits CASH_VAN after the trip settled; :870 `depositReceipts` reads without FOR UPDATE and its UPDATE has no `status='collected'` guard (double bank by two desks).
- backend/libs/core/src/modules/delivery/settlement.service.ts:236 with receivables.sync.ts: settlement credits CASH_VAN for `collections` rows only; an offline doorstep receipt uploaded via the `receipts` table never leaves CASH_VAN and reads as cash over.
- frontend/owner-app/app/money/receipts.tsx:305-311: the O11 dialogs `.then(done, done)`, so the new 409 is never shown to the owner.
- backend/libs/database/src/seed-demo/receivables.ts:664-669: RCPT-VAN receipts sit on a settled trip with no collections row and seeded settlements post no journal entry (cash-in-transit tile reads 0).
- docs/plans/integrations.md §7: the notifications consumer for `ImportCommitted` / `ExportReady` was never built (now declared audit-only by the guard).
