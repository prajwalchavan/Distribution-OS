# DOS-167 — architect merge review, loss and platform lens (Fable, 2026-09-13)
**Decision:** MERGE AFTER FIXES

Branch `qa/b2-dos167`, seven commits afc8b6e → 10e864e over main 0f5a9b5, 34 files, frontend + docs/27 only. Read in full: engine.ts on the branch (start/stop/end/settled/waiting/claimIdentity/wipeAll/sweep/enqueue/retry/discard/recordGpsPoint/requireStore/inHand, sync, applyManifest, pullLoop, flushInternal, refreshCounts, the read paths), react.tsx, shared/state/types, both adapters, api-client session/react/errors, the three layouts, leave.ts, leave-sheet.tsx, strings.ts, draft.ts, queue.ts, identity.test.ts (the (m)/(o)/(p) tests), the docs/27 diff, the design, ruling 1, the finding and s-098 SUMMARY. Nothing built or run.

**Founder answer A holds, in the engine.** `end()` (engine.ts:378-399) sets `ended` before its first await, waits for the open, the pull in flight, the flush chain and every write in hand (`settled()`), re-counts the file and keeps it (`kept`) whenever anything waits, whatever was asked; every write is refused at `requireStore()` (:1342) with `SyncEngineEndedError` from the tap on. A refused write is never saved, a write in hand lands whole and is kept, an acked-but-lost batch reverts to `queued` and re-sends the SAME opId/idempotencyKey (release :1043, the sending→queued revert at start :263), the kept file's `_outbox` keeps `seq` so FIFO stands, and the same person's next `start()` flushes BEFORE the re-snapshot (stale: no schemaVersion) — 'DOS-167 a write in hand…' proves manifest → upload → pull with the original opId, and Amit's start on that file uploads nothing. A forced sign-out (401 on refresh) only `stop()`s: file, queue and cursor stay for that person (react.tsx:145). No duplicate path found: the server replays `sync_ops` by opId. Sign-out cannot hang past one request deadline (pullLoop :782 and flushInternal :980 exit on `!started`), and it cannot crash: leave.ts:153-175 swallows `end`/`sweep`/`forgetDrafts` errors and still calls `signOut()`; the read hooks answer `[]` once `store` is null.

## Blockers (fix at merge time; each is a one-line edit)
- `docs/27-offline-sync-client.md:208-209` — "the file keeps only that queue and its refusals … and they go out the next time that person signs in" contradicts ruling (n) (refused ops never go by themselves). Replace with: "the file keeps only that queue and its refusals, for that person only; the queued ones go out the next time that person signs in on this phone, before the re-snapshot, and the refused ones wait in Needs attention for that person to fix or discard." Same sentence at `:77`: end it "…the queued ones go out the next time that person signs in there; refused ones wait in Needs attention (§12)."

## Minors (may follow; none loses data)
- `frontend/libs/offline/src/react.tsx:429` and `:481` — `useOutbox`/`useNeedsAttention` have no `.catch`; the wipe's emit (engine.ts:367) re-queries while `end()` goes on to `store.close()` (:395), so a tray mounted with two or more refusals on the "keep" path can reject with "Access to closed resource" as an unhandled promise. Pre-existing shape (`stop()` closed under queries too). Add `.catch(() => {})` as `useTable` has (:348).
- `frontend/libs/offline/src/engine.ts:247-304` — a `stop()` landing between the open and `this.store = store` (:274) is undone: the store re-attaches to a stopped engine and `sync('start')` still runs a manifest and pull (verifier PROBE-5). Only the forced sign-out reaches it (`waiting()` and `end()` await `opening`), and the branch is better than main (poll now gated :304). Fix in lean-libs-offline-boot with DOS-089: after the open and after the claim, `if (!this.started) { await store.close().catch(() => {}); return }`.
- One-tap sign-out with a pull in flight shows nothing for up to the 20 s deadline (the account menu closes first); optional (r) not built. `toApiError` (api-client errors.ts:175) still turns the refusal into "Something could not be completed" on the sales order screen; optional (q) not built — delivery and warehouse show the engine's sentence.
- `flushGps` (engine.ts:1303) is not gated on `ended`; harmless (system table, rows already deleted; no app calls it).
- Design §8 copy in docs/22 §8 (:311, main session's file) still says "Sign out, keep them here" and lacks the (m) clause "and from the tap on the phone refuses new writes while finishing and keeping any already in hand". Hand-off, not the lane's.

## Design and product rules — matched
Layers A/B/C as designed; identity required on the provider and `tenantId` gone from provider and engine (3a/4a now met). Amendments (a)-(l) and rulings (m)-(p) all present with their named tests (identity.test.ts :613, :705, :321, :890; leave.test.ts in all three apps). Strings: the `leave.*` block is byte-identical in sales, delivery and warehouse; leave.ts and leave-sheet.tsx differ only in comments and the sales-only `forgetDrafts`; every button ≤ 20 characters ("Sign out, keep here" = 19). Screens import only `@dos/ui` (leave-sheet.tsx); leave.ts is a lib file like queue.ts. No backend, contract, permission, schema or kit change.

## Conflicts
- With main now: none. `git diff 0f5a9b5..main` (bcc7fda) touches no branch file; its frontend changes are client.test.ts, delivery README and document-urls.test.ts. Plain three-way merge.
- lean-delivery-door (not yet branched): lean-groups.md:72/:99 removed engine.ts from it and DOS-063/149 refresh through the public `useSyncEngine().sync()`; it does not edit react.tsx. Expect at most an adjacent-line conflict in delivery `strings.ts` (DOS-167 appended `leave.*` after `app.signOut`). If it touches delivery `_layout.tsx`, the `Chrome` is rewritten (:396-560): rebase onto it, never reintroduce `void signOut()` / `void switchDistributor()` on the shell handlers.
- lean-libs-offline-boot: (1) DOS-046 rewrites `retry()` — on the branch it is wrapped in `requireStore()` + `inHand()` (engine.ts:1136-1159): keep both around the new body. (2) DOS-046's `systemSchema` state key will not compile until placed in `CLEARED_FOR_ANOTHER_IDENTITY` (engine.ts:88) — ruling: EXCLUDE it like `deviceId` (`Exclude<SyncStateKey, 'deviceId' | 'systemSchema'>`); it describes the file's DDL, not the person, and nulling it would re-run the ALTER on a table that already has the column. (3) react.tsx: DOS-068's NetInfo effect and `OutboxApi.retry` type are separate hunks from `useLeaveSession` (:285) and the legacy-file effect (:165). (4) DOS-089 adds `!hydrating` to the sales `Offline` line, now `_layout.tsx:255`; its session.ts hunk is away from `sessionIdentity` (:74-98). (5) docs/27: DOS-046's §4 opId sentence (:71) sits beside DOS-167's sign-out sentences (:74-77); §3 `_sync_state` line now lists `userId`; DOS-167 took §13 tests 12-16, so DOS-046's test becomes 17; the second to merge re-reads §4 and §13.

## READMEs
None to regenerate: no contract change. `frontend/libs/offline/README.md` is hand-written and already updated. Run `pnpm docs:readme:check` on the merged tree as usual.

## Walks (NOT TESTED on any platform; owed before the P0 closes, verifier under QA/tools/e2e)
- Web (OPFS, s-098 script): V5A/B/C plus V5D and the per-file OPFS listing (`storeFacts` :571 already lists top-level entries — assert no `dos-sales.db` and no rahul file after sign-out, exactly one kiran file after sign-in), the cold-start sign-out ('1 change has not reached the office', file kept) and the `/sync/pull` stall race (error line, never "saved on this phone"; the order exists at most once). Also one "keep" with ≥ 1 refusal to see the refused-body copy, at desk 1280 and phone 390.
- Android Pixel_7_API_36: sales steps 1-6 (file listing, six early screenshots, Shops 29/380, airplane-mode queue with 'Sign out, keep here', upgrade path deleting `dos-sales.db`), delivery and warehouse 1-3; the Sheet on the native overlay host (DOS-164) with `loading` on both buttons.
- iOS simulator via simctl + ios-drive.mjs: steps 1-5, sign-out through the product (no keychain reset).

## Defects outside this fix (one line each)
- P2 `frontend/sales-app/src/lib/queue.ts:46-61` — an offline order is queued as header + one op per line, not atomically; a sign-out between two of them (or any `stop()`, pre-existing) leaves a header-only draft that uploads while the lines are refused/lost; queue the order as one op or one transaction (S-row).
- P3 `frontend/libs/offline/src/engine.ts:247-305` — a restart with a queue and an unchanged manifest sends nothing until the first poll (≤ 60 s) or the next write: `start()` never flushes after `sync('start')` unless stale; pre-existing, verify on a walk.
- P3 `frontend/sales-app/app/_layout.tsx:415`, delivery `:457`, warehouse `:422` — a rejected `switchDistributor` escapes `void step().finally` unhandled with nothing shown; pre-existing (`void switchDistributor(tenantId)` on main).
- P3 (restated) server cursor carries no device identity (`backend/libs/core/src/modules/sync/sync.service.ts:481-493`); identity files of people who never return are never swept; pre-DOS-167 `dos.sales.draft.<retailerId>` keys stay at rest (`frontend/sales-app/src/lib/draft.ts`).
