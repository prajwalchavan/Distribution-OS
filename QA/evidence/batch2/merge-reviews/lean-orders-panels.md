# Merge review: lean group lean-orders-panels (owner), branch qa/b2-lean-orders-panels

Fable, 2026-09-20. Read-only review of f950492…7835a1f (nine commits, 25 files) against the planner's binding notes in `lean-groups.json` (no `verdicts/lean-lean-orders-panels.md` exists) and the six findings (owner 01: DOS-010, DOS-019; manager 02: DOS-027; regression 10: DOS-130, DOS-153, DOS-155). Lens: the panel says what the server will do, and whose row it is.

**Decision:** MERGE AFTER FIXES

The fixes match the notes. DOS-010: the shop rides in the identity cell whenever the register is cards — web below 1024 px (`web/list.tsx:433` draws the table only at desk density, and `theme.tsx:112` forces field density on a phone viewport) and native at every width (`native/list.tsx:368` has no table) — and where a table is drawn the Shop column stands alone; no kit file touched. DOS-027: `waitingOnKinds()` reads `orders.approvals.list` pending rows, never `approvalFlags`; on dos_qa the finding's own case now resolves — SO-0876 carries 5d89bf84 (below_floor) and 59eeee35 (credit_limit, entity `retailer`) BOTH with `order_id` set, so the primary `orderId` match prints "Below floor price · Credit limit"; both screens read at 100 on the key the Approvals screen already owns (same fetcher, one cache slot). DOS-130: `readAllReservations` follows the cursor at `limit: 200` (warehouse `CursorInput` max 200, contracts/warehouse.ts), pieces summed as integers, lots demoted to a meta line, the Release guard moved to the full list. DOS-153: `openRow`/`closePanel` cover tap, j/k and Esc on the owner; the manager Dialog's `onClose` clears. DOS-155: "last" is read from `orders.get` (owner) / `order.approvals` (manager), stated only on approve; the toast reads `DecideApprovalOutput.order.state` (`approvals.service.ts:189-190`), and `pricing.bargains.decide` answers `{ item }` (pricing.ts:413) so nothing reads an order off it. DOS-019: heading and badge from `pendingDecisions()` over the two `'top'` reads; on dos_qa each tenant holds exactly 5 pending gates + 2 requested rates, so the page has no cursor and the count is exact (5 or 6), moving with `invalidates: [['approvals'], ['bargains']]`. Tests: 12 new files, all absent from main (`git cat-file -e` on every path in the diff: 17 new, 8 owned); no pre-existing test changed. Nothing outside `ownsFiles` but the 17 new `src/lib` files (no other group owns them). No contract change, so READMEs stand.

## Blockers

1. **"across 1 lots" — the common case reads wrong in both apps.** `owner-app/src/strings.ts:181` `'o5.reservationsLots': 'across {count} lots'` and `manager-app/src/strings.ts:167` `'m2.reservationsLots'`; the translator is a plain `{key}` substitution (`ui/src/strings.ts:171`, no plural form), and a one-lot hold is what most confirmed orders carry, so the walker will file it on the first panel opened. **Fix** (owned files, no kit change): add `'o5.reservationsLot': 'across 1 lot'` / `'m2.reservationsLot'` and choose by count at `owner-app/app/orders/index.tsx:379` and `manager-app/app/orders/index.tsx:720` (`count === 1 ? t('…Lot') : t('…Lots', { count })`); extend the two `reservations.test.ts` screen guards to require both keys in the panel.
2. **Gate on the merged tree after fix 1.** Frontend, from `frontend/`: `pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm format:check`; the kit's cross-app guards with `--force` (S-155). No backend file changed, so no backend gate beyond `pnpm docs:readme:check` staying green.

## Minors

- Rail badge floor (verifier's minor stands): both `'top'` reads are `limit: 5` (`_layout.tsx:304,309`), so `waiting` caps at 10 and `NavItem.badge` (`types.ts:754`, a number) cannot say "+". A tenant with 11+ decisions sees the badge sit at 10 after deciding — the finding's own symptom, at scale. Not reachable on the seed. Later, in an owned file: raise both `'top'` limits to 100 (contract max 200), or read the Approvals keys `['approvals','pending']` / `['bargains','requested']` so one request serves badge, Today and Approvals; Today already `.slice(0, 6)`.
- `waiting-on.ts:43-45` shop-level `credit_limit` branch cannot fire: `ApprovalQueueItem.retailerId` comes only from the order left-join (`approvals.service.ts:68,88`) and is null exactly when `orderId` is null; the unit case feeds a shape the API never emits. Harmless; delete the branch and its case, or make the server fill `retailerId` from `entity_id` when `entity_type = 'retailer'` (a later contract-true change).
- Doc-comment: `waitingOnKinds` says "first raised first"; the list is `desc(approvals.id)` (`approvals.service.ts:76`), so kinds read newest-first. Words only.
- Manager DOS-027 widening does change the panel: `waiting.map(...)` renders every pending card with no slice, so the decision panel may now draw up to ~100 approval cards, not 20 — the honest outcome, but the report's "without changing what the panel shows" is wrong.
- Owner `Escape` (`approvals.tsx:321-324`) now closes the confirm dialog AND the panel and wipes the note in one key — pre-existing double close, now lossy; acceptable under "every way out clears", but a cancelled dialog by button keeps the note while Esc does not.
- Every screen claim rests on source-regex guards plus pure helper tests; nothing rendered. The walks below are the proof.

## Conflicts

- **main:** merge base 1d993b4 is main's head; `git merge-tree` clean. Fast-forward.
- **In flight (lean-manager-money, base 5f710dc):** overlaps only `manager-app/src/strings.ts`; `git merge-tree` of the two branches is clean.
- **Still to merge (branch from main AFTER this lands):** lean-sales-orders-pricing and lean-manager-order-lifecycle both own `manager-app/app/orders/index.tsx` (+146 lines here: `pending` moved above `columns`, the reservations read, `deciding.last/orderNo`, the Toast) and `owner-app/app/approvals.tsx` / `orders/index.tsx`; lean-owner-money-approvals owns `owner-app/app/index.tsx` and `orders/index.tsx`; admin-support, retailer-platform, warehouse-rules add keys to the two `strings.ts`. The three helpers under `src/lib/` (`waiting-on`, `reservations`, `pending-decisions`) are now the only way those screens may read gates and holds — the DOS-130 guard fails any `reservations.data?.items` reader.

## Walks

- **Web, desk (1280) and phone (390):** owner Orders row at 390 = "SO-… · <shop> · chip · ₹" and at 1280 the Shop column once; the same on manager Orders and both Billing desk registers; owner Orders › a confirmed multi-lot order (SO-0897's successor) › "Stock held 24 pc / across 5 lots", and a one-lot order after fix 1; manager Orders › Submitted › SO-0876 "Waiting on: Below floor price · Credit limit"; owner Approvals › type a note › close (X, Esc, tap another row, `j`/`k`) › the next request opens empty, the same row re-chosen keeps it; owner Approvals › an order's only gate › Approve › dialog carries "This is the last decision: SO-… will be confirmed and its stock held." › toast "SO-… confirmed — stock held" and Orders shows it confirmed with stock held; the same gate rejected shows no such line; Today "Needs you (N)" equals the rows and the rail badge, worker stopped, decide two › both drop by two without reload.
- **Android, Pixel 7 dev build:** owner Orders rows name the shop (the finding's evidence); manager Orders and Billing rows; the manager decision dialog inside the Sheet: note cleared on cancel, last-gate line, toast.
- **iOS (Expo Go):** the manager decision dialog inside the Sheet on the merged DOS-164 (the note asked for it); owner Approvals last-gate line and toast. **iPad simulator, landscape ≥ 1024 dp:** owner Orders and manager Orders/Billing rows show the shop in the identity cell exactly once (DOS-010's new native-tablet case).

## Defects outside this group

1. `backend/libs/core/src/modules/orders/approvals.service.ts:176-183` — rejecting ANY gate cancels the whole order (`cancelInTx`, `APPROVAL_REJECTED`), and neither dialog says so: owner `'o3.confirmReject': 'Reject {what}'` (`owner-app/src/strings.ts:132`), manager `'m2.reject': 'Reject'`. DOS-155's mirror: a silent cancel where DOS-155 cured a silent confirm. For lean-owner-money-approvals (owner) and lean-manager-order-lifecycle (manager): a `destructive` line "Rejecting cancels SO-… for <shop>", and the toast from `result.order.state === 'cancelled'`.
2. `QA/13-change-log.md:598` sends DOS-173 (billing desk double-counts a parked pack) to lean-orders-panels, but `lean-groups.json` lists it in NO group, so wave 3 never builds it. Re-queue it: `manager-app/app/billing/index.tsx:193` is owned by lean-manager-order-lifecycle next.
3. `approvals.service.ts:68` — `ApprovalQueueItem.retailerId` is documented as "the shop the gate itself is about" (contracts/orders.ts:156-161) but is null for `entity_type = 'retailer'` gates with no order; the contract promises what the row never carries.
