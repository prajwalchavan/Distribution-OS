# Lane h6-admin (DOS-106) — architect merge review (Fable, 2026-09-13)
**Decision:** MERGE AFTER FIXES

Lane `qa/b2-h6-admin` = one commit `e63ff17` on `117717c`, 25 files, read through git objects only. The design is the one amended on 2026-09-13 07:40 and it matches docs/22 §7 (founder, 2026-09-12) exactly, including the billing mapping the plan doubted. One CI-breaking omission stands between it and main.

## Blockers / majors (must be fixed before merge)
1. **READMEs not regenerated — `pnpm docs:readme:check` fails in the CI backend job** (it runs before `db:migrate` and `test`). `readme.ts:46` renders `route.summary` and the Zod sampler renders outputs, so three files change and none is in the lane: `backend/admin-service/README.md` (still `| POST | /admin/tenants | Onboard a distributor: … | platform_admin |` at :30 and :83, no "console level"), `backend/auth-service/README.md` (no `level` in the platformLogin/refresh/me samples), `frontend/admin-app/README.md` (2 old summaries). The plan's own change list named the auth README. Fix, in the lane: `cd backend && pnpm --filter @dos/contracts build && pnpm docs:readme && pnpm docs:readme:check`, then commit the three files (never hand-edit). Nothing else in the lane fails for this reason; `docs/examples.ts:3317` is an INPUT example, and `examples.spec.ts` validates inputs only.

No other blocker. Every lint-level hazard I could check by reading is clean: no leftover `requireActiveAdmin` reference anywhere, `asc`/`inArray` already imported in console.service.ts:3, `sql` still used in tenants.service.ts:92/:387.

## Minors (may follow)
- `console.service.ts:208` `metrics`: the gate runs in the first `withPlatform` transaction; `platformCounts()` then runs its COUNT/MAX queries in later `withSystem` transactions of the same request without it. Milliseconds, counts only; acceptable, but a follow-up could thread the check into `counts.ts`.
- `console.service.ts:138-151` `disableUser` re-reads `users.status` under the row lock but not `platform_admins.role`/`disabled_at`; a demotion between the gate and the lock is reachable only by SQL (no `admin.*` procedure writes `platform_admins`; the 0034 guard makes it super-only). Negligible.
- `distributors/[id].tsx` still mounts `<SubscriptionEditor>` (openable only through the gated button) while `subscriptions.tsx` unmounts it. Harmless inconsistency.
- `auth.supportPass` stays level-blind (plan residual iii). Bound to `grant.adminUserId === auth.userId` and, post-fix, billing can never obtain a grant; the only residue is an SQL-demoted admin with a pre-existing grant of their own. Follow-up, not this slice.
- DOS-107 (`admin.users.enable`) will need an `ADMIN_LEVELS` row (compile error otherwise), the spec's `toHaveLength(15)` → 16 and a probe in the spec's `probes()` table, which is compared against `allProcedures()`. Good property; note it for that lane.
- Record the fix in docs/18 after landing. docs/22 needs no register entry: §7 already carries the 2026-09-12 decision the code now enforces.

## Amendments (a)(b)(c) — satisfied? evidence
- **(a) satisfied.** `internals.ts:82-121` `requireActiveAdminLevel(tx, actorId, procedure)`: one row, `platform_admins ⋈ users`, `disabled_at IS NULL`, `users.status = 'active'` (a two-value enum, tenancy.ts:121), then `levelAllows()`. Called as the first statement inside the handler's own transaction in all 15: tenants list/get/suspend/reactivate (tenants.service.ts:77, :118, :340 before `platformIdempotent`), create (:166 pre-check in its own `withPlatform` before `validatePassword`/argon2, :175 authoritative inside `withSystem` before the tenant insert and the key), subscriptions list/get/upsert (:48, :82, :110 before the key), support request/list/revoke (:100, :117, :150 before the grant read and the key), users list/disable (console.service.ts:70 inside `withSystem`, :138 then `FOR UPDATE` on both `users` rows in id order and the actor re-read under the lock), metrics/audit (:209, :266). Under `withPlatform` the join sees the actor's own `users` row through `users_visible` (`id = app.actor_id`, tenancy.ts:157-163); under `withSystem` RLS is bypassed. Token claims stay role-only (auth.service.ts: `platformAdminLevel()` feeds the reply only; spec decodes the JWT and asserts no `level`). Spec proof that each test fails before the fix: support refusals were 200; replay of a super's key was the stored 200; the locked super's lock-back was 200; the 15-procedure locked-login sweep passed for the eight reads (five of them had no check at all); demotion mid-token was honoured only 15 min later.
- **(b) satisfied.** `permissions.ts:995-1050`: `AdminProcedurePath = Extract<ProcedurePath, 'admin.${string}'>`, `ADMIN_LEVELS: Record<AdminProcedurePath, …>` (compile-time completeness), `levelAllows()` failing closed on null level / unknown path / non-admin paths pass through. Server: `requireActiveAdminLevel` → `levelAllows`. App: `ui.tsx:48-53` `useCan = isAllowed(permissionFor(path), role) && levelAllows(path, level)`, both from `@dos/contracts`; grep of the admin app shows every mutation control behind `can()` (onboard, deep-linked /distributors/new, suspend, reactivate, edit-subscription ×2 + editor mount, disable-user, support-close, ask-access, ask-submit, hand-back + dialog) and no second list. `permissions.test.ts:1591-1610` asserts `Object.keys(ADMIN_LEVELS)` equals the contract's 15 `admin.*` leaves, super in every row, the exact per-level sets, null → false.
- **(c) satisfied.** `admin.ts`: each of the 15 summaries ends `· console level: super` / `· console levels: super, support, billing` etc.; `permissions.test.ts:1598-1604` fails when a summary and `ADMIN_LEVELS` disagree. No new generator. Only the regeneration (blocker 1) is missing.
- **Contract minimal:** `PERMISSIONS`, `PLATFORM_ROLES`, `ROLE_GROUPS`, `isAllowed` untouched (diff adds only after the table); `PlatformTokenPairOutput` and `PlatformMeOutput` gain `level` only; `PlatformAdminLevelSchema` mirrors the DB enum; no input changed. Session: `session.ts:218-236` builds `{user, role, level: null}` from a pre-fix snapshot and `applyTokens` settles the level from every login/refresh pair (test: login support → refresh billing → snapshot billing; old snapshot → null). Support pass cannot reach `admin.*`: `tenant.guard.ts:216-219` refuses a pass on admin-service, which serves no membership role, and `:204` binds the pass to `claims.sub`.

## Merge conflicts to expect and how to resolve
- `backend/libs/contracts/src/permissions.ts`: h6 adds only AFTER the last `PERMISSIONS` row (`'admin.audit.list': PLATFORM,` `}` → new `AdminProcedurePath`/`ADMIN_LEVELS`/`levelAllows` block, then the untouched `permissionFor`). h4's hunk is at :471-482 (inventory), h2's `delivery.trips.depart → DOORSTEP` is mid-file, DOS-115's `ORDER_PLACERS` lands in `ROLE_GROUPS` near the top plus order rows mid-file → all auto-merge. Only a lane that also appends a new export right after the table conflicts; keep both, h6's block first (it is the one the test and module 13 import).
- `backend/libs/contracts/src/permissions.test.ts`: h6 appends `describe('console levels (DOS-106)')` at EOF after `describe('listProcedures')` and adds four names to the two import lists. Another EOF append conflicts trivially → keep both describes; import lists → union, alphabetical. Pinned totals elsewhere (h4 adds one procedure) do not touch h6's admin-only `toHaveLength(15)`.
- `frontend/libs/api-client`: h6 = `session.ts` + `platform-client.test.ts` only; h3 = `errors.ts`; h4 = `pages.ts`, `pages.test.ts` (+ `index.ts`). Disjoint. `index.ts` already exports `PlatformSessionStore`/`PlatformSession`, so h6 needs no index change; if h4's `index.ts` hunk is near those lines, take h4's, nothing of h6's is lost.
- READMEs: after blocker 1, h6 owns admin-service, auth-service and admin-app READMEs; h4 regenerates the services carrying inventory. Disjoint files, but every lane regenerates → after the last batch-2 merge run `pnpm docs:readme && pnpm docs:readme:check` once on main and commit what moved.
- Build order after merge: `pnpm --filter @dos/contracts build` before the frontend typecheck (new exports `levelAllows`, `ADMIN_LEVELS`, `PlatformAdminLevel`).

## Regression walk and API probes
Accounts `dos.admin` (super) and `dos.support` (support), `Dos@1234`; admin app :5179 at 1400×900 and 375×812, Android `android-login.sh admin 5179 <user> ""`, iOS `ios-login.mjs 5179 <user> admin console`. Same list on all three.
- Account: "Level: Support" / "Super"; desk rail and phone account label show the same word (before: "Distribution OS staff" for both).
- Support: Distributors has no `onboard`; deep link `/distributors/new` shows `onboard-super-only` + `onboard-back`, no `onboard-submit`; detail (QA Probe `0ccdd8a0…`) has no `suspend`/`reactivate`/`edit-subscription` but `ask-access` (or `hand-back` when a window is live); Subscriptions row tap selects without an editor, no `edit-subscription`, `open-distributor` stays; People panel has no `disable-user`; Support sheet still offers `support-close`; Ask sheet offers `ask-submit`.
- Super: every control as before. Stale snapshot: a browser holding a pre-fix `dos.auth.session` shows no level-gated control until the boot refresh, then all of super's.
API (auth :3000, admin :3007; body `id` = path id, DOS-112; tokens via `QA/tools/tok.sh`):
1. dos.support login and `GET /auth/platform/me` and `POST /auth/platform/refresh` → `level: 'support'`; base64-decode the access token → no `level` claim.
2. dos.support → 403 `FORBIDDEN` "a support console account may not call admin.X; ask a super administrator": `POST /admin/subscriptions` (Sai, amountPaise 1); `POST /admin/tenants` with a weak `temporaryPassword` (403, not 400); `POST /admin/tenants/0ccdd8a0…/suspend` and `/reactivate`; `POST /admin/users/<qa.probe.owner>/disable` and `<dos.admin>/disable`.
3. Replay: dos.admin suspends QA Probe with key K body B → 200, reactivates → 200; dos.support sends byte-identical B under K → 403 (was the stored 200).
4. dos.support still 200: GET tenants, tenants/{id}, subscriptions, subscriptions/{id}, support-grants, users, metrics, audit; `POST /admin/support-grants` (Sai, 4 h, read_only) → 200; `…/revoke` → 200.
5. Stale token: with dos.support's token in hand, `update platform_admins set disabled_at = now()` → `GET /admin/tenants` → 403 "This console account is no longer active"; restore `disabled_at = null`.
6. SQL after: Sai and QA Probe subscription rows and QA Probe `tenants.status` unchanged; `count(*)` of 'QA Probe Level Pvt Ltd' = 0; dos.admin and qa.probe.owner `active`; `platform_audit` by dos.support since the fix = support.requested + support.withdrawn only.
7. dos.admin: login `level: 'super'`; suspend + reactivate QA Probe 200; upsert 200; disable qa.probe.owner 200 then restore by SQL; `cd backend && pnpm smoke` → 0 BROKEN. `GET :3007/docs/openapi.json`: every admin summary ends with its console level(s).
