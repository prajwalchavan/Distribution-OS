# Decisions Log

## Document Information

| Property     | Value                    |
| ------------ | ------------------------ |
| Document     | Decisions Log            |
| Product      | Distribution OS          |
| Version      | 2.0                      |
| Status       | Active                   |
| Owner        | Prajwal Chavan (Founder) |
| Last Updated | September 2026           |

---

# Purpose

This page is the **register of every decision that binds the Distribution OS product** — what was decided, when, why, and where in the build it is actually enforced. It is the page the Home page refers to as the Founder Decisions Register.

It exists so that no decision is re-litigated by accident. If a proposal contradicts a row below, the row wins until the founder changes it and a new dated row is added here.

**Source of truth.** `docs/22-source-of-truth.md` §8 in the repository is the master register; this page mirrors it. Engineering decisions come from `docs/15-decisions-log.md` and the architecture decision records in `docs/adr/`. Where this page and the repository disagree, **the repository wins** and this page is corrected.

As at 2026-09-05 13:45 IST: **14 backend modules verified, 1,442 automated tests, 1,004 endpoint calls exercised, 0 broken** (Build Status & Roadmap mirrors `docs/18-build-log.md`). No app screen is built yet. A decision being recorded here means it is **decided**, not that it is shipped — the "Where enforced" column says which.

---

# How to read this log

- **Newest first.** Within a date, the most consequential decision is listed first.
- **Decision** states the rule in the form a builder can obey.
- **Why** is the reason it was chosen over the alternative, not a restatement of the rule.
- **Where enforced** names the file, table, procedure or document that makes it true. `Superseded` in this column means a later row replaced it; the replacing row is named.
- Founder decisions and engineering decisions are in **one table**, because in a solo build they constrain each other.

---

# Decision register

| Date       | Decision                                                                                                                                                                                                                                                                                                       | Why                                                                                                                                                                                               | Where enforced                                                                                                           |
| ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ |
| 2026-09-05 | **All AI features are in v1, before the pilot**: WhatsApp free-text to draft order, voice order capture, demand forecasting and reorder suggestions, and delivery route sequencing. Every AI output is human-confirmed; nothing commits itself.                                                                | The three problems Confluence names as unsolved — multi-channel order capture, purchase planning, route planning — are AI problems; deferring them leaves the product looking like a billing app. | New module 12 `ai`; overrides `docs/03` "must not build" for routing and the post-pilot deferral                         |
| 2026-09-05 | **Platform console in v1**: a seventh app and service, "Distribution OS - Admin" (web, `admin-service` :3007) for distributor onboarding, plans and subscription state, and time-boxed, owner-approved, audited support access. Revenue stays subscription; no fintech.                                        | Onboarding a distributor by hand does not survive the second customer, and support without an audited, expiring grant is an access hole.                                                          | New module 13 `platform-admin`; role `platform_admin`; not yet built (`docs/18`)                                         |
| 2026-09-05 | **Positioning stays multi-industry with FMCG first.** Pharma, electricals, dairy and agri are named markets; the product stays FMCG-shaped until a customer from a second industry exists.                                                                                                                     | Widening the marketing story costs nothing; widening the data model before a real second-industry customer costs a rewrite.                                                                       | Confluence Vision and Target Market pages; no schema change                                                              |
| 2026-09-05 | **Branches: one tenant = one distributorship** for the pilot and v1. Multi-branch is v2 — each branch its own tenant, plus an owner group view.                                                                                                                                                                | A branch dimension touches every ledger, policy and report; no pilot customer needs it, and tenant-per-branch already works.                                                                      | Schema (no branch entity); Confluence Personas and Goals corrected                                                       |
| 2026-09-05 | **Product name: Distribution OS.** Apps are "Distribution OS - Owner", "- Manager", "- Sales", "- Warehouse", "- Delivery", "- Retailer", "- Admin", used on the store listing, icon label and sign-in screen only.                                                                                            | Closes the brand shortlist (Vitran / Bahi / Distribution OS) taken up the same day; the name is only ever seen at sign-in, so clarity beats cleverness.                                           | `UX-00` §10; app package names; white-label rule below                                                                   |
| 2026-09-05 | **Layout A Ledger chosen** (from A Ledger, B Instrument, C Panel, D Signal) and the design system finalised on it for all six role apps; the admin console adopts the desk density of the same system when it is designed.                                                                                     | A distributor's day is a ledger; the mockups were judged on real Tarsun data at desk and phone width.                                                                                             | `docs/design/UX-00-design-system.md` (17 review findings fixed); no frontend code before the pick                        |
| 2026-09-05 | **Accountant scope = money desk plus reads.** May record office receipts, deposits, cheque bounces and write-offs, and read and export everything. May **not** touch prices, schemes, credit limits, approvals or settings.                                                                                    | The August persona gave the accountant credit approvals, which merges the person who books money with the person who grants exposure.                                                             | `permissions.ts` (`MONEY_DESK`, `MANAGEMENT`); reverses Confluence Personas                                              |
| 2026-09-05 | **The manager approves load-out from the manager app**, not by typing a PIN on the warehouse phone. The warehouse device waits for that approval.                                                                                                                                                              | A PIN typed on the device it authorises is not an approval; it is a formality the warehouse can perform on itself.                                                                                | `warehouse.loadSheets.confirm` plus the manager approval step; `docs/23`                                                 |
| 2026-09-05 | **The `docs/23` screen inventory is binding**: the retailer app places orders (`orders.submit` for the retailer role); the salesperson may see a shop's outstanding and credit check but never collect; owner Settings needs branding, numbering and settings endpoints; owner graphs need `reporting.series`. | Screen-by-screen review found four capabilities every app assumed and no module owned.                                                                                                            | `docs/23`; platform-gaps slice, verified 2026-09-05                                                                      |
| 2026-09-05 | **`docs/22-source-of-truth.md` is the single source of truth**, updated in the same turn as any decision. Confluence mirrors it.                                                                                                                                                                               | Decisions were being lost between sessions; one file that is read first every session is the only durable place for them.                                                                         | `CLAUDE.md` session protocol; reverses the v1.0 Home page claim that Confluence is the source of truth                   |
| 2026-09-05 | **Keep developing until a hard blocker**: a turn that finishes a backend module verifies it, records it and launches the next one before it ends.                                                                                                                                                              | Work stopped once after the billing module because a turn ended with nothing running.                                                                                                             | `CLAUDE.md` session protocol; `docs/18` chain plan                                                                       |
| 2026-09-05 | **Commit at module boundaries**, with a message stating what is complete and what is in progress.                                                                                                                                                                                                              | A module is the smallest unit that is independently verifiable, so it is the right unit of history.                                                                                               | Git history                                                                                                              |
| 2026-09-05 | Any frontend under development must be **visible in a browser while it is being built**, with a screenshot to the founder.                                                                                                                                                                                     | Design decisions cannot be reviewed from a diff.                                                                                                                                                  | `launch.json` web build per app; every frontend slice ends with a browser check                                          |
| 2026-09-04 | **Only the delivery crew collects money, or the shop pays online.** The salesperson never records a receipt. The desk (owner, manager, accountant) may record a payment received at the office.                                                                                                                | The person who negotiates the order must not also hold the cash; it is the oldest control in distribution.                                                                                        | `MONEY_COLLECTORS` in `permissions.ts`; `sales-service` mounts no receipt endpoint; `docs/22` §6, §9 #2                  |
| 2026-09-04 | **Six apps, one per role** (owner; manager and accountant share; sales; warehouse; delivery; retailer), each **web + Android + iOS**, each on its own backend service, all on one Postgres database.                                                                                                           | A role can then only reach the endpoints its own service mounts; the permission matrix decides the rest inside that.                                                                              | `docs/22` §2; one service package per role in `backend/`; `TenantGuard` returns 403 before business logic                |
| 2026-09-04 | **Username and password with our own token service** (EdDSA access token 15 min, rotating per-device refresh). OTP over SMS or WhatsApp is a **later layer on top**, not a replacement.                                                                                                                        | Issuance stays in our API with no third-party dependency and no per-message cost at sign-in.                                                                                                      | `auth-service` :3000; `docs/22` §7; reverses `docs/03` "no own OTP/JWT stack"                                            |
| 2026-09-04 | **A permission matrix per endpoint**, tested for every endpoint against every role, failing closed on any undeclared route.                                                                                                                                                                                    | Role checks written inside handlers drift; a declared matrix can be tested exhaustively.                                                                                                          | `backend/libs/contracts/src/permissions.ts`; `describePermissionMatrix`                                                  |
| 2026-09-04 | **Backend first, production grade on the local database**: every endpoint of every service built and tested, then the apps one at a time.                                                                                                                                                                      | Screens built on an unfinished contract are rewritten twice.                                                                                                                                      | `CLAUDE.md`; `docs/18` status table                                                                                      |
| 2026-09-04 | **The product is white-labelled.** Inside every app and on every printed or shared document the distributor sees their own name and logo; Distribution OS branding appears only on the sign-in screen.                                                                                                         | The distributor's customer relationship is with the distributor, not with us.                                                                                                                     | `TENANT_SETTING_KEYS` (`branding.display_name`, `branding.logo_object_key`, `branding.invoice_footer`); `docs/22` §9 #10 |
| 2026-09-04 | **Shops are GST-registered**: the B2B tax invoice is the primary document. The B2C path stays for shops without a GSTIN.                                                                                                                                                                                       | Founder confirmed the pilot's shops carry GSTINs; place of supply then drives the CGST/SGST versus IGST split.                                                                                    | `retailers.gstin`; GSTR-1-shaped sales register in `billing`                                                             |
| 2026-09-04 | **Invoice numbering is per-tenant configuration, never code.** Prefix and starting number are set at onboarding and changeable until the first invoice is issued.                                                                                                                                              | Every distributor arrives mid-series from another ERP; the cut-over number is not knowable in advance.                                                                                            | `numbering_series` (`prefix`, `starting_no`, `allocation_mode`); owner Settings screen                                   |
| 2026-09-04 | **Van sales use the normal invoice series** — no per-vehicle series and no device-allocated numbers.                                                                                                                                                                                                           | Removes offline number allocation, the hardest correctness problem in the field, at no business cost. `invoices.source = 'van_sale'` still separates them for reporting.                          | `billing`; `docs/17` §D5                                                                                                 |
| 2026-09-04 | **Cash discount is reported on the invoice and realised at receipt** as a financial credit note when payment lands on time. The per-brand on-invoice variant is not built.                                                                                                                                     | Founder: "not important for now"; build the simplest correct path.                                                                                                                                | `cash_discount_conditions`; `tenant_brands.cash_discount_mode` reserved but unused                                       |
| 2026-09-04 | **Data migration is a generic importer** — upload, preview, map columns, save the mapping as a reusable profile, dry run with row-level errors, then commit. Named profiles (TradeEzee, Marg, Busy, Tally, FieldAssist) are saved mappings, not code.                                                          | Distributors arrive from any ERP or from Excel or from nothing; a hard-coded reader serves one customer.                                                                                          | `integrations` module (queued); `docs/17` §D7                                                                            |
| 2026-09-04 | **The owner app carries graphs wherever they help** — growth and performance, not tables alone.                                                                                                                                                                                                                | The owner's question is a trend, not a row.                                                                                                                                                       | `reporting.series` serves chart-ready series; reporting module queued                                                    |
| 2026-09-04 | **Swagger on every service with sample payloads built from real seeded rows**, repeatable and role-scoped.                                                                                                                                                                                                     | An example that 500s is worse than no example; `pnpm smoke` presses every one of them.                                                                                                            | `/swagger` and `/docs` per service; `backend/libs/core/src/docs/examples.ts`; `pnpm smoke`                               |
| 2026-09-04 | **English only for now**; online-first now with **offline for sales and delivery before the pilot**; Android and iOS; haptics and a considered feel.                                                                                                                                                           | The pilot's staff read English; offline is scoped to the two roles that lose signal. Reverses `docs/15` F6 "Hindi + English first".                                                               | `docs/design/UX-00`–`UX-03`; `users.locale` still defaults to `hi-IN` (a known inconsistency)                            |
| 2026-09-04 | **Layout mockups as images before any frontend code**, one letter picked for all apps, after the backend is complete.                                                                                                                                                                                          | Choosing a visual direction from a rendered screen is cheap; choosing it from built code is not.                                                                                                  | Closed 2026-09-05 by the A Ledger row above                                                                              |
| 2026-09-04 | **Demo data covers three distributors, staff under each, and shops linked to more than one distributor.**                                                                                                                                                                                                      | Tenant isolation and the retailer's multi-distributor login are only really tested by data that crosses tenants.                                                                                  | `db:seed-demo`; end-of-chain slice in `docs/18`                                                                          |
| 2026-09-04 | **Repo root holds only `backend/`, `frontend/`, `docs/` and `CLAUDE.md`**; every service and app is an independent package.                                                                                                                                                                                    | Two independent workspaces stop the Expo toolchain and the Nest toolchain fighting over one lockfile.                                                                                             | `docs/19`; two `pnpm-workspace.yaml` files                                                                               |
| 2026-09-04 | **Invoice is issued at pack by the warehouse and is immutable.** A shortfall is a credit note; a cancelled invoice keeps its number.                                                                                                                                                                           | An issued GST invoice cannot be regenerated. Reverses the "Sales Order to Invoice to Picking" order on the v1.0 Home page.                                                                        | `warehouse.packs.confirm` calling `BillingService.issueForPack`; invoice state machine; `docs/15` D15                    |
| 2026-09-04 | **Fulfilment vocabulary is fixed**: draft, submitted, confirmed, picking, packed, dispatched, delivered or partially delivered, closed or cancelled — with an ONDC mapping table. Approvals and reservations are side effects, not states.                                                                     | Every ladder invented per customer becomes a migration; ONDC needs a lookup, not a schema.                                                                                                        | `backend/libs/domain/src/state-machines/order.ts`; `docs/15` D13                                                         |
| 2026-09-04 | **Stock and money ledgers are append-only**, balances derived, every mutation carrying an idempotency key and a client-generated UUIDv7.                                                                                                                                                                       | A distributor's disputes are settled by history; an editable ledger has no history.                                                                                                               | Database triggers on `stock_ledger` and `journal_lines`; `UNIQUE(tenant_id, idempotency_key)`; ADRs 0003, 0004           |
| 2026-09-04 | **Shared-schema multi-tenancy with forced row-level security**, every access through `withTenant()` which sets `ROLE app_rw`, tenant, actor and role.                                                                                                                                                          | One database is affordable at lakhs of users; forced RLS means a forgotten `WHERE` cannot leak a tenant.                                                                                          | ADR 0002; `backend/libs/database/src/client.ts`; `rls.test.ts`                                                           |
| 2026-09-04 | **Purchase cost, landed cost and margin are unreadable by sales, warehouse, delivery and retailer roles** — a database policy, not an app rule.                                                                                                                                                                | The one secret a distributor cannot afford to leak into the field.                                                                                                                                | `tenant_product_costs` back-office policy; `rls.test.ts`; `docs/22` §9 #1                                                |
| 2026-09-04 | **Offline upload never answers 4xx.** Rejections come back 2xx with `sync_errors` and are durable in `sync_ops`.                                                                                                                                                                                               | A 4xx to a queued device either loops forever or silently drops a real order.                                                                                                                     | ADR 0007; `modules/sync`; `docs/22` §9 #8                                                                                |
| 2026-09-04 | **Document intake never commits on its own**; a human reviews before the GRN. "Zero manual entry" means zero typing except the blind gate count.                                                                                                                                                               | Extraction confidence is not a substitute for the person who is accountable for the stock.                                                                                                        | `docint` pipeline (`docs/22` §5); module queued                                                                          |
| 2026-09-04 | **The pricing engine is one function**, `priceOrder()`, run on the device and recomputed on the server at the same version; genuine deviations go to approval, not every mismatch.                                                                                                                             | Approval on every mismatch floods the owner at month-end; a notice only hides real deviations.                                                                                                    | ADR 0008; `backend/libs/domain/src/pricing/schemes.ts`; `applied_rules` stamped on every line                            |
| 2026-09-04 | **Product master is `products` + `product_variants` + `product_packs` + `supplier_pack_configs`, with `merged_into`.** Merges never rewrite ledger rows.                                                                                                                                                       | Irreversible if wrong, and it is what models the x90 / _120 / CS1 pack notation on real invoices.                                                                                                 | ADR 0005; `docs/15` D17                                                                                                  |
| 2026-09-04 | **Brand-DMS sales are imported, never re-invoiced.** A Too Yumm bill raised in FieldAssist DMS is committed as `source = brand_dms_import` — **a receivable in; no stock movement**, because the brand's field force already moved the goods on its own documents — linked to the brand's invoice number.      | Two legal invoices for one sale is a tax problem, and outstanding reconciliation fails without the import.                                                                                        | ADR 0014; `docs/22` §5, §9 #5; never-cut for the pilot (`docs/15` D7)                                                    |
| 2026-09-04 | **Van sales are supported**: the vehicle is a stock location and the trip is the aggregate; no separate app.                                                                                                                                                                                                   | Crews already sell from the van; modelling the vehicle as a location keeps one stock ledger.                                                                                                      | ADR 0013; delivery module, verified 2026-09-05                                                                           |
| 2026-09-04 | **GPS goes to a separate `/gps/points` batch endpoint** with device buffering, never through the offline queue. Live map by SSE, no WebSocket.                                                                                                                                                                 | A position must never queue ahead of a cash receipt; SSE is one fewer transport to run.                                                                                                           | ADRs 0012, 0007; `docs/15` D4, D5                                                                                        |
| 2026-09-04 | **The retailer surface is online-first** and is never an offline sync client.                                                                                                                                                                                                                                  | Per-client streaming cost scales with retailers, not with staff, and shops have no offline-write need.                                                                                            | `docs/15` D2, ADR 0010; `retailer-service` :3006                                                                         |
| 2026-09-04 | **NestJS 12 confirmed**, running dev through `@swc-node/register` and tests through `unplugin-swc`; one TypeScript version per workspace.                                                                                                                                                                      | esbuild-based runners drop decorator metadata and Nest then injects `undefined` silently; two TypeScript copies break type-aware lint.                                                            | `docs/15` E1, E2; ADR 0011 closed                                                                                        |
| 2026-09-04 | **Tally XML export plus a mapping in the pilot**; a signed desktop connector only after the pilot.                                                                                                                                                                                                             | Chartered accountants import files today with no training; a connector is a second toolchain.                                                                                                     | `docs/15` D21; `integrations` module queued                                                                              |
| 2026-09-04 | **Published pricing**: a base subscription plus a per-seat charge, unlimited retailers, an included WhatsApp quota with metered overage, and a free tier by bill volume.                                                                                                                                       | Opaque pricing costs a self-serve funnel that a solo founder cannot replace with sales calls.                                                                                                     | `docs/15` D8, D28; Confluence Target Market                                                                              |
| 2026-09-04 | **The document-intake engine is chosen by an evaluation set, not by opinion**: 50 to 100 real invoices scored before the engine is fixed; a second engine only if the scores demand it.                                                                                                                        | Vision model claims do not survive Indian supplier bills; the eval is the only evidence.                                                                                                          | `backend/docint` library, `engine_disagreements`; `docs/15` D18, D19, D25                                                |
| 2026-09-04 | **A restore drill before any real data**, with RPO 5 minutes and RTO 2 hours.                                                                                                                                                                                                                                  | A backup that has not been restored is a hypothesis.                                                                                                                                              | `docs/15` D30; deployment phase                                                                                          |
| 2026-09-04 | **Superseded — five products, two store binaries plus a web console.**                                                                                                                                                                                                                                         | Replaced the same day by six role apps, each web + Android + iOS, and a seventh admin app in 2026-09-05.                                                                                          | Superseded by the six-apps row; ADR 0009 retained as history                                                             |
| 2026-09-04 | **Superseded — Better Auth with JWKS as the identity layer.**                                                                                                                                                                                                                                                  | Replaced by our own username and password token service the same day.                                                                                                                             | Superseded by the auth row; `docs/15` D23                                                                                |
| 2026-09-04 | **Superseded — a 24-week P50 / 28-week P80 delivery plan.**                                                                                                                                                                                                                                                    | Replaced by backend-first sequencing and the continuous build protocol; `docs/18` is now the schedule of record.                                                                                  | Superseded; `docs/15` D6                                                                                                 |
| 2026-08    | **TypeScript everywhere** (Expo, Vite, NestJS, Postgres tooling).                                                                                                                                                                                                                                              | A solo founder debugs fastest in one language, and the pricing engine ships unchanged to devices.                                                                                                 | `docs/15` F1; `backend/libs/domain` has zero runtime dependencies                                                        |
| 2026-08    | **Revenue is the distributor's subscription. No fintech, no payments aggregation, no commission on trade.**                                                                                                                                                                                                    | Regulatory surface and float risk that a software business does not need.                                                                                                                         | `docs/15` F6; Goals non-goals                                                                                            |
| 2026-08    | **GPS tracking of delivery staff is required**, geo-tagging is evidence and never a block on the salesperson.                                                                                                                                                                                                  | Founder requirement for delivery; a hard geo-fence on sales punishes bad signal, not bad behaviour.                                                                                               | `delivery` module; `visits.lat/lng`; `docs/22` §4                                                                        |
| 2026-08    | **Coexist with the brand's DMS.** The pilot bills its own trade in TradeEzee (Windows ERP) and its Too Yumm trade in FieldAssist DMS; both are imported.                                                                                                                                                       | The brand owns that DMS; a distributor cannot leave it, so the product must live beside it.                                                                                                       | `docs/15` F4, F5; ADR 0014; generic importer                                                                             |

---

# Reversals worth knowing

These rows overturned something a stakeholder may still remember. Each is listed with the audit code from `docs/24-confluence-alignment.md` §5.

1. **The salesperson does not collect money** (C1). The August persona listed collections as a sales KPI; the sales service has no receipt endpoint.
2. **The invoice is issued at pack, after picking, by the warehouse** (C2). There is no data-entry-operator role and no pre-picking invoice.
3. **Six role apps, each web and mobile** (C4), not a web-only matrix per persona and not two store binaries.
4. **The accountant does not approve credit** (C5).
5. **The retailer app is current scope, not "future"** (C6): the shop places orders and pays online.
6. **Permissions are one fixed, tested matrix** (C7). There are no per-tenant custom roles and no configurable order statuses.
7. **No branch entity in v1** (C8) — one tenant is one distributorship.
8. **Route sequencing and AI order capture are back in scope** (C9, C16), reversing the earlier "must not build" line, by the 2026-09-05 AI decision.
9. **A platform console exists** (C14), reversing the earlier "no platform console" position, by the 2026-09-05 decision.
10. **The repository is the source of truth, not this space** (C12, C13).

---

# Decisions still open

These are recorded so they are not mistaken for settled. Source: `docs/22` §10 and `docs/17` §C.

1. **Invoice series at cut-over** from the pilot's existing ERP — continue the old numbers or start fresh. The system is configurable either way; an answer is needed before go-live.
2. **Sample exports** from the pilot's ERP (party master, item master, outstanding). Useful as the first saved importer profile; nothing is blocked waiting for them.
3. **GST composition scheme** — which pilot shops, if any, are under it.
4. **Return and damage policy per brand**, and the claim sheet format each brand accepts.
5. **Whether the chartered accountant already keys brand-DMS invoices into Tally**, which would create a double-posting risk at import.

---

# How this log is kept

1. A decision is written into `docs/22-source-of-truth.md` §8 **in the same turn it is made**, with its date. This page is updated from that file.
2. A decision is only recorded once it names **where it is enforced** — a file, a table, a procedure or a document. A decision with no enforcement point is an intention, and belongs on the Goals page instead.
3. A decision is never edited in place. It is superseded by a new dated row, and the old row stays with `Superseded` in its last column.
4. Architecture decisions that would be expensive to reverse also get an ADR in `docs/adr/`, listed in the irreversibility register `docs/adr/0000-irreversibility-register.md`.
5. The build status behind these decisions lives in `docs/18-build-log.md` and changes daily. This page changes only when the product changes.
