# Distribution OS — Final Architecture and Product Plan (Synthesis)

Date: 2026-09-04. Author: chief architect, synthesising proposals A (solo pragmatist), B (scale architect) and C (product differentiator), three judge panels and research reports R01–R10. Every disagreement the judges raised is resolved in the body and listed again in §15. Anything that changes a decision in CONTEXT.md is marked **[CHANGE]** with the reason.

Written for one developer building daily: 24 weeks P50 to cut-over for Tarsun Enterprises (P80: 28 weeks), on a design that reaches lakhs of users without rewriting the ledgers, the tenancy model or the sync wire protocol.

---

## 1. Positioning and what stands out

**What this is.** A distributor-owned, multi-brand system of record for Indian FMCG distribution, sold by subscription to the distributor, free for retailers. Not a brand DMS (Botree, FieldAssist, Shikhar serve the manufacturer), not a billing package (Marg, Vyapar, Busy stop at the invoice), not a marketplace (Udaan, Jumbotail, ONDC compete with the distributor). Nobody sells the distributor a system he owns across brands at SMB prices (R01 §1, R04 §4.3, R10 §2.11).

**Conversion thesis (from C; adopted as the pilot exit criterion).** Tarsun pays when three things are visibly true within two weeks of the parallel run: (1) inbound stock enters by photo with no typing except the blind gate count — no Indian DMS or billing product scans purchase invoices (R01 §4); (2) the physical pending-bills file is on his phone: per-retailer ledger, ageing 0-30/31-60/61-90/90+, UPI QR with his own VPA on every bill, WhatsApp reminders with the PDF (R09 §8, R10 §3); (3) his CA gets Tally XML that imports clean, unasked (R02 §6). Everything else is retention and is sequenced accordingly.

| Standout                                                                                                         | Why nobody else has it                                                                                                 | Evidence                |
| ---------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------- | ----------------------- |
| Photo-to-GRN with e-invoice QR/IRN verification and deterministic Indian-invoice validators                      | Vision-LLM extraction plus signed-QR facts; specialist parsers cannot express Free Qty, Disc %, GST Bnft %, "x 90"     | R05 §1, R06 §1–3        |
| One ledger across brand-mandated DMSs                                                                            | Too Yumm forces its DMS on Tarsun (invoice E); we ingest its secondary invoices so retailer outstanding is whole       | CONTEXT E/F/G, R01 §6.2 |
| Scheme-stamped invoice lines from the first order; claims later                                                  | A third of distributor income is schemes and claims; 30–50% never raised; no distributor-side tool does it cross-brand | R09 §4, §6; R10 §2.4    |
| Field apps that never block: no sync button, geo-tag as evidence, trip-scoped GPS, session survives a phone call | Six years of top-voted complaints on Bizom/FieldAssist/Botree are exactly these                                        | R01 §5                  |
| The salesperson can never see purchase price                                                                     | Vyapar's role leak made staff quit; here it is a database guarantee plus a CI test                                     | CONTEXT, R07 §12.4      |
| Trip settlement cockpit (expected vs declared cash, tolerance, reason codes)                                     | SAP DSD pattern; Ivy under-builds it; cash leakage is the owner's second cash problem                                  | R03 §9, R09 §3          |
| Published pricing and a visible feature-request channel                                                          | Opaque pricing and silent hikes are documented churn triggers                                                          | R01 §5.10               |

---

## 2. Five-apps verdict and surface map

**Verdict: keep five products; ship two store binaries plus one web console.** **[CHANGE]** to packaging, not scope; all three proposals and all judges agree. The Team app is the only artefact that needs background location, a location-typed foreground service and camera-for-documents — the permissions that trigger the Play Console declaration form, prominent-disclosure dialog and demo video (R07 §4.2). Five binaries would mean five EAS pipelines, five listings and five quarterly SDK upgrades for one person (R07 §2.3).

| Product                  | Deliverable                                                                                         | Surface                                                                                                                           | Notes                                                                                                                             |
| ------------------------ | --------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| 1. Distributor (owner)   | Owner module in **Team app** + **Owner web console**                                                | Phone: approvals, live map, day numbers. Web: price lists, schemes, Tally mapping, imports, registers, claims, curation, settings | Owners approve on the phone and configure at a PC with the CA                                                                     |
| 2. Salesperson           | Team app role module                                                                                | Mobile, offline-first (PowerSync)                                                                                                 | Beat, orders, bargain, visits; authorised product lists per rep so a manufacturer-employed rep sees only that brand (from C)      |
| 3. Manager (warehouse)   | Team app role module + **Billing desk** in the console                                              | Phone: capture, gate count, review, GRN, pick/pack. Web: A4 print, bulk billing, day-end                                          | The Marg-shaped bill prints from a PC in every office (R02 §7)                                                                    |
| 4. Delivery              | Team app role module                                                                                | Mobile, offline-first, trip-scoped GPS                                                                                            | Also the van-sales "mixed role"                                                                                                   |
| 5. Retailer              | **WhatsApp** (week 19) → **PWA** (week 20, Expo web output) → **native** (post-pilot, same project) | **Online-first**, never a PowerSync client                                                                                        | Install friction is the adoption killer (R10 §2.14); no adoption benchmark exists for an independent distributor's app (R04 §5.2) |
| Accountant (new, from C) | Console, read + exports                                                                             | Web                                                                                                                               | The CA gets registers and Tally XML with his own login                                                                            |
| Curator / support        | Console                                                                                             | Web                                                                                                                               | Global master curation, tenant support                                                                                            |

**Van sales: yes.** **[CHANGE]** to the open question. Vehicle is a stock location, Trip is an aggregate, on-spot orders are fulfilled and invoiced from the vehicle, no separate app (R09 §3, R03 §7; Tarsun already issues Campa slips, invoice G). The founder must confirm with Tarsun before week 1, because delivery crews then inherit stock-ledger responsibilities (R07 §13).

**Resolved here.**

- _Retailer codebases_ (A/B: separate Vite PWA + Expo app; C: one Expo project with web output). **Decision: C's packaging with A's data access.** One Expo Router project `frontend/apps/retailer` builds to web now and native later; it uses oRPC + TanStack Query with a persisted cache, never PowerSync. The order editor, product search and invoice viewer are React Native components shared with the Team app — the real reuse win. Judge 2's RN-web bundle concern is handled by a CI budget (≤ 600 KB gzipped initial route, per-route-group splitting) and by retailers arriving through a WhatsApp deep link to one screen.
- _Retailer on PowerSync_ (B `retailer_me`, C `retailer_scope`) vs online-first (A). **Decision: A.** PowerSync Pro bills $30 per 1,000 peak clients with a Free tier of 50 (R07 §3.1); Tarsun's ~150 retailers alone break Free during the pilot, 100 tenants would be ~15,000 clients, and lakhs would force self-hosting. Retailers order from a shop with 4G and do not need offline writes. PowerSync is reserved for the four staff roles; revisit only if self-placed orders exceed ~20% of volume and a measured offline need appears.

---

## 3. Feature scope: pilot vs v1 vs later, and must-not-build

Effort for one developer on top of the core: S < 2 weeks, M 2–6, L > 6.

### 3.1 Pilot (weeks 1–24)

| #   | Feature                                                                                                                                                                                                                           | Effort  | Why                                                                                                                                           |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------- | --------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Photo-to-GRN: QR/IRN verify, validators, SKU cascade, phone review, lot-level commit, LR document, blind gate count                                                                                                               | L       | Conversion truth 1                                                                                                                            |
| 2   | **Brand-DMS secondary invoice ingestion (Too Yumm): capture, extract, retailer match and commit** as a sales invoice with `source = brand_dms_import` posting stock-out and AR                                                    | S on #1 | Without it the parallel-run reconciliation of outstanding fails for a third of Tarsun's retail billing. Resolved for B's timing (Judges 2, 3) |
| 3   | Offline order taking: last order / suggested v1 / quick grid, case-pcs, pending-undelivered inline, MOV override logged, per-SKU max, credit control indicate/strict/stop, bargain request/approve                                | M       | Populates everything downstream                                                                                                               |
| 4   | Pure pricing engine, typed scheme rules with validity, stackable/final, funding_source/claimable; `applied_rules` on every line                                                                                                   | M       | "Scheme on app = scheme on bill"; claims reconstruct later                                                                                    |
| 5   | Billing at pack: Marg-shaped A4 (Free/Scheme %/Disc ₹/Cash Dis %), FSSAI, own-VPA UPI QR, ORIGINAL/DUPLICATE, series per FY; credit notes with four return reasons at original rate                                               | M       | Truths 2, 3                                                                                                                                   |
| 6   | Receivables: double-entry journal, receipts (cash/UPI+UTR/cheque), bill-to-bill allocation, ageing, credit hold, retailer ledger screen shaped like the pending-bills file, WhatsApp reminders                                    | M       | Truth 2                                                                                                                                       |
| 7   | Tally XML export (masters + vouchers) with ledger-mapping screen; GSTR-1-shaped register; CSV registers                                                                                                                           | S       | Truth 3                                                                                                                                       |
| 8   | Reservation-aware ATP for reps and retailers                                                                                                                                                                                      | S       | Top retailer complaint on every brand app (R01 §5.8)                                                                                          |
| 9   | Picklist per trip by SKU with FEFO warn-not-block, packing confirmation with actual lot, load sheet                                                                                                                               | M       | Precedes delivery                                                                                                                             |
| 10  | Delivery: vehicles as locations, trips with check-out PIN, stops with monotonic states and reason codes, ePOD, on-spot orders, doorstep returns → credit note, collections, expenses, counted check-in, settlement with tolerance | M–L     | Owner requirement; van sales                                                                                                                  |
| 11  | Trip-scoped GPS with consent and 90-day retention; owner live map at ~30 s                                                                                                                                                        | S–M     | Owner requirement                                                                                                                             |
| 12  | Owner dashboard as rupee outcomes (phone via PowerSync summary rows, web via SSE); approvals queue                                                                                                                                | M       | Reads everything                                                                                                                              |
| 13  | WhatsApp utility templates hi/en: confirmed, dispatched, invoice+POD+UPI QR, receipt, cut-off; "Reply 1 to confirm"                                                                                                               | S       | Retailer experiences the OS without installing; referral surface                                                                              |
| 14  | WhatsApp free text → draft order in approvals                                                                                                                                                                                     | S       | Gives the self-placed-share metric something to measure; voice/photo post-pilot after a 100-utterance benchmark                               |
| 15  | Retailer PWA v1: card per distributor, reorder, running-low, status, outstanding + UPI, request discount, directory opt-in (new links prepaid)                                                                                    | M       | First on the cut line                                                                                                                         |
| 16  | Import profiles (Tally XML masters, Marg Excel, CSV: items, parties, bill-wise outstanding, batch-wise stock) with the mandatory human checkpoint                                                                                 | M       | Migration playbook                                                                                                                            |
| 17  | Incentives v1 (targets, achievement, computed amount)                                                                                                                                                                             | S       | Second on the cut line                                                                                                                        |
| 18  | Product metrics (self-placed share, reorder rate, time-to-first-order, GRN review time, DSO) and per-tenant Anthropic/WhatsApp/SMS cost lines on the admin dashboard from week 1                                                  | S       | Decides the native app and the tiers                                                                                                          |
| 19  | Public price page (₹2,000–2,500 base + ₹150–300/seat, unlimited retailers, WhatsApp quota, free tier by bills/month) and feature-request channel                                                                                  | S       | R10 §4; R01 §5.10                                                                                                                             |

**Cut line if the schedule slips, in order:** retailer PWA → incentives v1 → WhatsApp draft orders → bargain flow → directory → live map (fall back to a trip list with "last seen"). **Never cut:** GRN-by-photo, brand-DMS ingestion, offline orders, billing/GST/print, receivables, Tally XML export, delivery + collections + GPS + settlement, owner dashboard, WhatsApp invoices, backups and the restore drill.

### 3.2 v1 (post-pilot months 1–3)

Claims aggregate with per-brand statements (data already stamped); Windows Tally Connector (Node SEA); payment-behaviour bands A/B/C; near-expiry ladder; WhatsApp voice and photo intake after the benchmark; Marathi strings (keys exist from day one); retailer native binary if PWA data justifies; second and third tenants; Multi-AZ RDS; DR copies to ap-south-2.

### 3.3 Later (month 6+)

E-way bill and outbound IRN via a GSP when a tenant needs it (₹5 crore B2B, R05 §4); EWB "expected deliveries" inbox and GST taxpayer API enrichment; Bluetooth ESC/POS slips (pilot van sales send the slip as a WhatsApp PDF and use the OS share-to-print sheet — RN thermal libraries are fragile and this is not on the cut line); rep voice ordering; ONDC NP-MSN at ~20 tenants; ClickHouse only if measured; PowerSync self-host only if residency forces it.

### 3.4 Must-not-build

Separate binaries per role or a Manager desktop app (release load, R07 §2.3); route optimisation/VRP (owner beats + Google Routes; VRP matters above ~5 vehicles, R03 §11); ONDC seller app in the pilot (vocabulary only; R04 §3.5); payments aggregator, wallet, lending, BNPL (founder; own-VPA UPI QR + UTR); own OTP/JWT stack, Firebase phone auth, Twilio Verify (30–60× the India price, R07 §8); Redis, Kafka, a second datastore, microservices (pg-boss + LISTEN/NOTIFY carry ~100 tenants, R08 §5); RDS Proxy at pilot and any WebSocket transport (§9, §11); OCR-then-LLM, specialist parsers, on-device models (R06 §1–4); Marg DBF/Busy XML readers (Excel/CSV profiles, R02 §5); shelf-photo/planogram analytics (R10 §2.15); live chat for bargaining, all-day GPS of reps, geo-fence billing gates (CONTEXT, R01 §5.2); no-code configurability; a cross-platform UI kit spanning RN and DOM (§8); per-device licensing and opaque pricing.

---

## 4. System architecture

```
                         AWS ap-south-1 (Mumbai)
   ┌──────────────────────────────────────────────────────────────────────────────┐
   │   ECS Fargate ARM                      RDS PostgreSQL 17 (t4g.small, PITR)   │
   │   ┌────────────────────┐  tx + set_config  ┌──────────────────────────────┐   │
   │   │ api  (NestJS 12 /  │ ────────────────► │ RLS FORCE on every tenant    │   │
   │   │  Fastify, oRPC,    │                   │ table; stock_ledger, journal;│   │
   │   │  SSE, /sync/upload,│ LISTEN/NOTIFY     │ pg-boss (outbox); pg_trgm;   │   │
   │   │  /gps/points,      │ (direct conn)     │ publication "powersync"      │   │
   │   │  webhooks)         │                   └───────────┬──────────────────┘   │
   │   ├────────────────────┤                               │ logical replication │
   │   │ worker (same image)│ docint, notify, rollups,      ▼                     │
   │   │  pg-boss consumers │ imports, exports, crons   PowerSync Cloud           │
   │   └────────────────────┘                          (India region verified    │
   │   S3 docs (pre-signed) │ S3 backups (Object Lock) │ wk 1; self-host = exit) │
   │   CloudFront (console PWA, retailer PWA, signed image URLs)   ALB + ACM      │
   └──────────────────────────────────────────────────────────────────────────────┘
        ▲ oRPC/SSE            ▲ oRPC (online-first)         ▲ PowerSync streams + /sync/upload
   Owner console (Vite)   Retailer PWA/native (Expo)    Team app (Expo): owner | sales |
   + Billing desk         + WhatsApp (Meta Cloud API)   warehouse | delivery  (op-sqlite)

   External: Anthropic API (Sonnet 5 default; Gemini 3.x Flash second opinion), Meta WhatsApp Cloud API,
   MSG91 SMS, Google Maps mobile SDK / MapLibre + Ola tiles on web, EAS Build/Update, Grafana Cloud + Sentry;
   later: GSP (EWB/IRN), Windows Tally Connector (Node SEA).
```

One TypeScript monorepo; one NestJS 12 modular monolith on Fastify with a worker from the same image; Postgres 17 as the only stateful system; PowerSync for the Team app only; SSE (not WebSocket) for web realtime; S3 Mumbai for documents and Object-Locked backups.

### 4.1 Modules (bounded contexts; modules talk only through exported application services or outbox events, enforced by `eslint-plugin-boundaries`)

| Module                                                   | Owns                                                                                                                                                                                             |
| -------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `platform`                                               | `tenants`, `tenant_settings`, `feature_flags`, `idempotency_keys` (online, 24 h), `sync_ops` (device_id, op_id; durable), `sync_errors`, `audit_log`, `numbering_series`                         |
| `identity`                                               | Better Auth tables, `devices`, `location_consents`, `otp_rate_limits`                                                                                                                            |
| `retailers`                                              | global `retailer_identities`; tenant `retailers`, `retailer_links`, `beats`, `beat_assignments`, `pjp`, `visits`, `directory_optins`                                                             |
| `catalog` (global)                                       | `manufacturers`, `brands`, `products`, `product_variants`, `product_packs`, `product_external_codes`, `product_aliases`, `hsn_rates` (dated), `product_proposals`                                |
| `tenant_catalog`                                         | `suppliers`, `return_policies` (tenant × brand), `tenant_products`, `supplier_pack_configs`, `tenant_product_costs` (owner/manager/accountant RLS), `rep_product_authorisations`                 |
| `pricing`                                                | `price_lists`, `price_list_items`, `retailer_price_overrides`, **`schemes`** (single typed table), `bargain_requests`, `rep_auto_approve_bounds`                                                 |
| `orders`                                                 | `sales_orders` (source enum, pricing_date_mode, payment_term, fulfil_from_location), `sales_order_lines` (`applied_rules` jsonb), `order_state_transitions`, `approvals`                         |
| `inventory`                                              | `locations` (warehouse, vehicle, damaged, in_transit), `stock_lots`, `stock_ledger`, `stock_balances`, `reservations`, `cycle_counts`                                                            |
| `procurement`                                            | `purchase_orders`, `supplier_invoices` (+ lines), `lorry_receipts`, `grns`, `grn_lines`, `inbound_discrepancies`                                                                                 |
| `warehouse`                                              | `picklists`, `pick_lines`, `pack_confirmations`, `load_sheets`                                                                                                                                   |
| `billing`                                                | `invoices` (derived; series, GST split, IRN-ready + EWB fields, UPI QR payload, FSSAI), `invoice_lines` (`applied_rules`), `credit_notes`                                                        |
| `receivables`                                            | `accounts` (seeded chart), `journal_entries`, `journal_lines`, `receipts`, `allocations`, `ageing_snapshots`, `cash_discount_conditions`                                                         |
| `delivery`                                               | `vehicles`, `trips`, `trip_stops`, `deliveries`, `delivery_lines`, `pod_evidence`, `collections`, `trip_expenses`, `trip_settlements`, `trip_points`, `vehicle_positions` (one row per vehicle)  |
| `docint` (thin module over the `backend/docint` library) | `documents`, `document_pages`, `extractions`, `extraction_checks`, `sku_match_candidates`, `review_sessions` (single-writer lock), `corrections_log`, `engine_disagreements`, `supplier_aliases` |
| `claims` (schema now, UI post-pilot)                     | `claims`, `claim_lines`, `claim_evidence`, `claim_statements`                                                                                                                                    |
| `notifications`                                          | `templates` (hi/en/mr), `messages` (channel, status, cost), `whatsapp_windows`, `inbound_messages`, `push_tokens`                                                                                |
| `reporting`                                              | `daily_tenant_stats`, `daily_rep_stats`, `retailer_behaviour`, `owner_summary` (synced), materialized views, SSE hub                                                                             |
| `integrations`                                           | `tally_mappings`, `tally_sync_ledger` (GUID, LASTVCHID), `export_jobs`, `import_jobs`, `import_rows`; later `gsp_credentials` (KMS envelope), `ewb_documents`, `external_channel_orders`         |
| `incentives`                                             | `targets`, `achievements`, `computed_payouts`                                                                                                                                                    |

### 4.2 Data model: the irreversible eight (ADRs 0001–0008, written in weeks 1–3)

**ADR 0001 — IDs.** UUIDv7 as `id text` on every synced table, generated on the client for offline creation (PowerSync requires text ids, R07 §3.2). Human numbers (`GL/1686`, `GRN-0042`) are separate server-assigned columns from `numbering_series(tenant_id, series_code, fy, next_no)` under `SELECT … FOR UPDATE` at commit, ≤ 16 characters, unique per series per FY (IRN-ready, R05 §4).

**ADR 0002 — Tenancy.** Shared schema, `tenant_id uuid NOT NULL` on every tenant table, `ENABLE` + `FORCE ROW LEVEL SECURITY`, two DB roles (`app_owner` for migrations, `app_rw` for API/worker with no `BYPASSRLS`), every unit of work inside a transaction that starts with `set_config('app.tenant_id'|'app.actor_role'|'app.actor_id', …, true)`; policies read `(SELECT current_setting(...))`; every index leads with `tenant_id`; Drizzle adds `WHERE tenant_id = ?` as defence in depth (R08 §2.2). Global tables have no `tenant_id`; writes only under `actor_role = 'curator'` or the identity service. `tenant_product_costs` carries an extra role predicate so the Vyapar failure is a database guarantee; CI dumps a salesperson device after full sync and every oRPC response for that role and asserts no cost or margin column or value appears.

**ADR 0003 — Stock ledger.** B's SQL with A's pack hierarchy and C's durable idempotency index:

```sql
stock_lots(id, tenant_id, variant_id, batch_no, mrp numeric(10,2), expiry date, mfg date,
           UNIQUE(tenant_id, variant_id, batch_no, mrp))
stock_ledger(id, tenant_id, occurred_at, lot_id, location_id, qty_delta int,
  reason enum[grn, sale, sale_return_saleable, sale_return_damaged, damage, expiry_writeoff,
              transfer_out, transfer_in, van_load, van_unload, adjustment, cycle_count, opening],
  ref_type, ref_id, actor_id, idempotency_key, created_at, UNIQUE(tenant_id, idempotency_key))
stock_balances(tenant_id, lot_id, location_id, on_hand int CHECK (on_hand >= 0 OR negative_allowed),
  reserved int, version int, PRIMARY KEY(tenant_id, lot_id, location_id))
reservations(id, tenant_id, order_line_id, lot_id NULL, location_id, qty, state enum[pending, posted, voided])
```

Pieces only; cases are display; MRP and expiry are lot attributes **[CHANGE]** (R09 §13). Balance updated in the same transaction with `RETURNING`; nightly re-derivation alarms on drift; corrections are compensating rows. ATP = `on_hand − reserved` through a `sellable_stock` view, the only stock surface reps and retailers see. Leading `(tenant_id, occurred_at)` so monthly partitioning is a data move.

**ADR 0004 — Money ledger.** `accounts` seeded per tenant (AR, Cash, UPI clearing per VPA, Sales @ rate, Output CGST/SGST/IGST, Discounts, Sales returns, Round off, Scheme receivable per manufacturer, Claims receivable); `journal_entries` + `journal_lines` balancing to the paisa (service check plus deferred constraint trigger; `UNIQUE(tenant_id, idempotency_key)`); `receipts` + `allocations` — invoices are never mutated for payment state. Outstanding = AR balance per party; ageing = open invoices minus allocations. Cash discount is a conditional realised at receipt within the window (R09 §12.20). Credit notes reverse at the original rate.

**ADR 0005 — Product master.** **Resolved for B's shape with A's pack hierarchy:**

```sql
products(id, manufacturer_id, brand_id, name, status enum[active, proposed, merged_into], merged_into NULL, ondc_category, fssai_relevant)
product_variants(id, product_id, net_qty, net_unit, promo_extra NULL, default_case_size, hsn_code, ean NULL)
product_packs(variant_id, level enum[piece, inner, case], qty_in_parent int)
supplier_pack_configs(tenant_id, supplier_id, variant_id, pcs_per_case, supplier_code, margin_basis)
tenant_products(tenant_id, variant_id, listed, local_alias, case_size_override NULL, min_order_qty, order_increment, max_per_order NULL)
tenant_product_costs(tenant_id, variant_id, lot_id NULL, purchase_rate, landed_cost, ptd, updated_from_grn_id)
```

`supplier_pack_configs` models Guru Kripa "x 90" vs Guiltfree "_120" vs Reliance "CS1". Curator merges set `merged_into` and rewrite aliases, external codes and `tenant_products`, never ledger rows. Distributor proposals are usable immediately as `proposed` and enter the console's curation queue.

**ADR 0006 — Retailer identity.** `retailer_identities` (global; E.164 phone unique; optional GSTIN/FSSAI; consent version and timestamps) ↔ `retailers` (tenant private record: code, tier, credit limit amount/bills/days, `credit_mode enum[indicate, strict, stop]`, `payment_terms enum[PRE, ON, POST_FULFILLMENT]`, beat, lat/lng from device, `gst_reg_type`, `tally_ledger_name`, brand-DMS ids) ↔ `retailer_links(identity_id, tenant_id, retailer_id, linked_by enum[rep_onboarding, directory_optin, import], status)`. Better Auth memberships list a retailer's tenants; the PWA shows one card each. Credit, tier, band and code are absent from every retailer-role write contract and from the retailer `WITH CHECK` policy. Directory links start `stop`/`PRE`.

**ADR 0007 — Sync write protocol.** `POST /sync/upload` with `X-Sync-Protocol: 1`; 2xx + `sync_errors` for business rejections, 5xx only for transient faults; `sync_ops(tenant_id, device_id, op_id)` unique, retained ≥ 180 days (§7).

**ADR 0008 — Pricing engine.** `priceOrder(order, priceLists, overrides, schemes, clock, pricingDate)` is a pure function in `backend/libs/domain` with zero runtime dependencies, identical on device and server. Order fixed by CONTEXT: price list tier → retailer override wins → schemes stack unless `final` → cash discount conditional at receipt.

```ts
type SchemeRule = {
  id; tenantId; scope: {brand?|category?|variantIds?|all};
  trigger: {kind:'qty'|'value'|'mix'; min:number; unit:'pcs'|'case'|'inr'; slabs?:Slab[]};
  reward: {kind:'free_qty'|'line_pct'|'order_pct'|'cash_discount_pct'|'net_scheme_amount'; value:number; freeVariantId?};
  applicability: {tiers?:string[]; retailerIds?:string[]; beatIds?:string[]};
  validFrom; validTo; version:number; stackable:boolean; final:boolean;
  fundingSource:'company'|'distributor'; claimable:boolean; brandId?; claimWindowDays?;
  gstOnFreeGoods:boolean; pricingDateMode:'order'|'delivery';
}
```

Every order and invoice line stores `applied_rules jsonb [{rule_id, version, kind, amount|free_qty}]` (from C) so the bill prints Free / Scheme % / Disc ₹ / Cash Dis % exactly as invoices E and F do and claims reconstruct later. **Resolved: one `schemes` table in `pricing`** read by the engine and referenced by `claims`; B's `promotion_rules` + `schemes` split is a duplication.

### 4.3 State vocabularies (fixed now, ONDC-shaped)

- **Fulfilment:** `draft → submitted → confirmed → picking → packed → dispatched → delivered | partially_delivered → closed | cancelled`. Approvals gate `submitted → confirmed`; reservations post on `confirmed`. ONDC `on_status`: Created/Accepted (submitted/confirmed), In-progress (picking…dispatched), Completed, Cancelled (R04 §6). B's `authorised` and C's `approved/reserved` are side effects, not states.
- **Delivery stop:** `pending → started → arrived → delivered | partial | failed[reason]`, monotonic, server-enforced.
- **Invoice-payment:** `issued → partially_paid → paid → written_off`; credit notes are separate documents.
- **Trip:** `planned → loading → active → closing → settled | settled_with_variance`.

### 4.4 Invoice timing (resolved; Judge 3)

Invoices are issued at **pack** for pre-sold orders — numbered, printed, possibly e-way-billed, travelling with the goods — and **never regenerated**. Shortfall or doorstep return produces a **credit note** at the original rate and posts pieces back from the vehicle. Van-sales orders are invoiced at delivery from the vehicle. A's and C's "delivered quantities regenerate the invoice" is rejected: an issued GST invoice is immutable.

---

## 5. Inbound invoice pipeline (photo/PDF → GRN)

Principle (R05 §7, R06 §6): **QR first, structured pull where a session exists, vision extraction always, human commit always.** "Zero manual entry" means zero manual **typing**; the one deliberate number is the blind gate count **[CHANGE]** (R06 §9.3). Code lives in `backend/docint/{schema,validators,matcher,engines,eval}` as a library with pure validators, a matcher and a CLI eval harness, called by the worker (B's layout, resolved).

| Step                                | Where                                           | What happens                                                                                                                                                                                                                                                                                                                                                                                                    |
| ----------------------------------- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0 Capture                           | Team app, offline OK                            | Blind gate count first. Document scanner, multi-page, JPEG q ≥ 90, originals kept; PDF share-in; each page passes QR detection. Originals to S3 `tenant/{id}/docs/{doc_id}/page-{n}.jpg` via the attachment queue; never downscaled                                                                                                                                                                             |
| 1 QR verify                         | Device, re-verified on server                   | Parse the RS256 JWT, verify against cached IRP keys by `kid`, extract the 10 fields (SellerGstin, BuyerGstin, DocNo, DocTyp, DocDt, TotInvVal, ItemCnt, MainHsnCode, Irn, IrnDt). Header card "Verified e-invoice from Reliance Retail ₹54,597" appears instantly                                                                                                                                               |
| 2 Pre-classify                      | Worker                                          | Dedupe on IRN (or supplier GSTIN + no + date); resolve supplier by GSTIN with `einvoice_expected`; `BuyerGstin == tenant.gstin`; select prompt profile (`tally`, `sap-reliance`, `guiltfree-dms`, `marg-gst-local`, `brand-dms-secondary`)                                                                                                                                                                      |
| 3 Structured pull (async, optional) | Worker                                          | EWB `GetEwayBill` if the number is printed and EWB credentials exist; `Get e-Invoice by IRN` if a GST taxpayer session is live; strip `Ph`/`Em`. **R05 §2.1 caveat carried:** the JSON gives HSN, qty, UQC, rate, discount, taxable and GST split but **not MRP, case size, scheme/GST-benefit % or batch unless the seller fills `BchDtls`** — vision remains mandatory; the pull is a cross-check skeleton    |
| 4 Vision extraction                 | Worker → `ExtractionEngine`                     | One request per invoice, all pages as labelled images at 2,576 px long edge; Claude Sonnet 5 `json_schema` output (R06 §8: raw-as-printed + normalised + per-line evidence + `handwritten_annotations[]`), cached system prompt + schema, adaptive thinking medium, streaming; **>20 images triggers the 2,000 px cap, so long invoices split with a continuity check**; Gemini 3.x Flash second opinion logged |
| 5 Validators (pure)                 | Worker                                          | GSTIN mod-36 with confusion-set retry; intra/inter-state split vs state codes; HSN length and dated rate; line arithmetic; Section 170 rounding ±₹1; Σ lines = subtotal; page completeness; QR: line count = `ItemCnt`, total = `TotInvVal` ±₹2; IRN hash `SHA256(SellerGstin + FY + DocTyp + upper(DocNo))` when QR failed; per-line agreement with step 3                                                     |
| 6 Pack-size normalisation           | Worker                                          | Parse "x 90", "_120", "(16+5.5)", "CS1", "2 Case", `Boxes`; `case_size = pcs / cases`; triangulate with name token and `supplier_pack_configs`                                                                                                                                                                                                                                                                  |
| 7 Escalation                        | Worker                                          | ≥ 2 red arithmetic checks or page mismatch → Opus 5 (or Gemini), keep the version passing more checks, log both                                                                                                                                                                                                                                                                                                 |
| 8 SKU cascade                       | Worker                                          | `supplier_aliases` exact → `product_external_codes` (494607257, Guiltfree code, DMS Item ERP Id) → HSN prefix + brand + MRP + pack filter → `pg_trgm` top-20 → logistic fusion → ≥ 0.90 green, 0.60–0.90 amber (top-3 chips), < 0.60 red. pgvector only if the eval shows the cascade stalls                                                                                                                    |
| 9 Three-way match                   | Worker + UI                                     | PO cases vs invoiced pcs ÷ case size; LR packages vs invoice cases; gate count vs LR. Gate < LR → transporter claim with mandatory damage photos before close                                                                                                                                                                                                                                                   |
| 10 Review                           | Team app                                        | `review_sessions` single-writer lock; header card (computed vs printed, IRN badge); line cards with row crop, chip, qty pcs/cases, MRP, rate; keypad edits; candidate sheet; swipe short/damaged; batch/expiry step (lot = batch + MRP + expiry); annotation panel (never alters printed values); LR panel; commit disabled while any red remains; resumable. Target: clean 10-line invoice < 60 s              |
| 11 Commit                           | API, one transaction, idempotent on document id | `supplier_invoices` + lines, `stock_lots`, `stock_ledger` `grn` rows, `stock_balances`, `tenant_product_costs`, `inbound_discrepancies`, AP journal, alias/code/pack-config upserts, `corrections_log`, outbox; 15-minute undo = compensating rows                                                                                                                                                              |
| 12 Reconciliation (later)           | Worker                                          | Monthly purchase e-invoice list → received on GST but never scanned; GSTR-2B/IMS view; never used for goods receipt                                                                                                                                                                                                                                                                                             |

**Brand-DMS invoices** (`brand_dms_invoice`, Too Yumm) run steps 0, 4, 5, 8 and a variant of 11: the retailer is matched by name/phone/beat/`Buyer ERP Id`, and commit creates a sales invoice `source = brand_dms_import` stamping SO/IN/ERP ids and posting stock out and AR in. The physical pending-bills file migrates as `retailer_ledger_migration` with the mandatory human checkpoint.

### 5.1 Failure modes (the review screen's acceptance checklist)

| Failure                                               | Detection → handling                                                                                                                       |
| ----------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------ |
| QR unreadable                                         | Decode fails → continue on vision; IRN hash recompute if printed; amber "not verified"                                                     |
| QR signature fails (`kid` rotation)                   | Refresh IRP keys; else `qr_unverified`, never block                                                                                        |
| `BuyerGstin ≠ tenant`                                 | Red; commit only with owner approval (branch/consignee)                                                                                    |
| Duplicate IRN / (supplier, no, date)                  | Show existing GRN; block; "revised copy" only via debit/credit-note flow                                                                   |
| Large supplier without QR                             | `einvoice_expected` → amber                                                                                                                |
| Missing pages ("1 of 5", 3 captured)                  | Red; capture prompt; commit blocked                                                                                                        |
| Line count ≠ `ItemCnt`; totals off > ₹2               | Red; merged/split rows highlighted via row evidence; escalation run                                                                        |
| Unknown SKU                                           | Red → "create product" as `proposed`; curator merges via `merged_into`                                                                     |
| Case-size disagreement                                | Amber with all three values; reviewer picks; `supplier_pack_configs` upsert                                                                |
| Batch-tracked line without batch                      | Batch/expiry required before commit                                                                                                        |
| Multiple MRPs for one SKU                             | Separate lots per line                                                                                                                     |
| LR packages ≠ gate count; gate = LR but pcs < invoice | Transporter claim with photos; supplier shortage per brand policy                                                                          |
| LLM down, timeout, rate limit                         | pg-boss retry ×3; then `extraction_failed` — **manual typing allowed only here and measured** on the admin dashboard; capture never blocks |
| Schema grammar latency                                | Warm the schema in staging before deploy                                                                                                   |
| Structured pull fails                                 | Skip silently; reconciliation retries; monthly "re-authorise GST" nudge                                                                    |
| Blurry page                                           | Amber "retake page n"; may proceed                                                                                                         |
| Handwritten marks                                     | Shown separately, applied or dismissed; Claude's measured weak spot (R06 §5)                                                               |
| Hallucinated line                                     | Missing `evidence.row_text`, Σ mismatch, QR line count → red with row crop                                                                 |
| Supplier format drift                                 | Per-supplier edit rate rises → nightly alert; eval set and profile extended                                                                |
| Two managers on one document                          | Second sees read-only with the holder's name                                                                                               |

### 5.2 Eval before trust (resolved timing)

Collecting and labelling Tarsun's invoices needs no code, so it starts in **week 1**: 50–100 real inbound invoices across suppliers, both e-invoice classes, bad photos and pencil marks (R06 §11). Labelled by week 6, scored in week 10 before the pipeline touches real data, on field accuracy, line recall/precision and review edits per invoice for Sonnet 5, Opus 5, Haiku 4.5 (standard-res, to learn whether 1,568 px survives 8-pt Tally text) and Gemini 3.x Flash/Pro. **Sonnet 5 is the prior, not the decision:** R06 §3.1 shows Gemini 3 Pro/Flash above Claude on scanned invoices and Claude at 70% vs GPT-5 mini 88% on handwriting. The engine sits behind one interface. C's "Vertex AI Mumbai" for Claude is unverified; Vertex regional endpoints at a 10% premium (R06 §4) are the residency path if a brand asks.

---

## 6. Order-to-cash flows per app

**Salesperson (offline).** Opens on "Aaj ka beat" with last-order and outstanding chips from `retailer_outstanding_summary`. Check-in stores the geo-tag as evidence with `distance_m`; outside the fence is amber, never a block. Order opens on "Reorder last order" or suggested v1 (median inter-purchase gap and quantity, scheme-inflated orders excluded); quick grid filtered by the rep's authorised list; case/pcs stepper ("2 case = 180 pcs"); pending-undelivered inline; ATP from `sellable_stock`. The pure engine prices each line at the effective version and stores `applied_rules` and `priced_at`; credit control runs locally (`indicate` annotates, `strict` opens an approval, `stop` blocks with the owner override path); MOV override logged; bargain request if outside the auto-approve bound. Submit → `submitted`; PowerSync uploads when connectivity exists; status dot only. Server recomputes (§7.3); the retailer's WhatsApp confirmation goes out; the rep sees `confirmed` or a "Needs attention" item. Performance tab: target, achievement, computed incentive.

**Manager / warehouse.** Inbound: capture → review → GRN (§5); stock visible to reps within one sync. Order queue grouped by beat/trip → trip + picklist consolidated by SKU with FEFO warn. Pick with actual lot; pack per order; short-packs are `pack_confirmations` rows, never edits to order lines. Billing desk (web): invoice from packed quantities — series number, GST split by place of supply, `applied_rules` copied to invoice lines, own-VPA UPI QR payload, FSSAI, ORIGINAL/DUPLICATE; PDF rendered by the API so mobile and web print identically; "dispatched" template with the PDF. Load sheet (last stop loaded first); check-out counts by the crew confirmed with the manager's PIN → `transfer_out` warehouse / `transfer_in` vehicle in one transaction. Day-end: Too Yumm DMS invoices photographed and committed so the ledger is whole.

**Delivery (offline, trip-scoped GPS).** "Start trip" → consent recorded once; foreground notification "Trip in progress — location shared with Tarsun Enterprises"; points buffered and batched to `/gps/points` (§7.5). Opens on the next stop; Google Maps hand-off; retailer phone visible until delivered. One tap `delivered`, or `partial` with reason and per-line qty, or `failed`; ePOD bundle (photo, optional OTP/signature, lat/lng, time) per tenant policy; shortfall → credit note; returns saleable/damaged → `sale_return_*` into the vehicle location. Collect cash / UPI with UTR / cheque as an append-only receipt with a client receipt number; allocation bill-to-bill oldest first unless tagged; cash counts toward expected cash. On-spot order: the rep order screen against the vehicle location; invoice at delivery; slip via WhatsApp PDF and the OS print sheet. Retailer receives invoice + POD + UPI QR, then the receipt template. Check-in: counted return of unsold stock; settlement cockpit shows expected vs declared cash, expenses, variance; outside tolerance → red; trip cannot close without owner approval.

**Owner.** Phone: `owner_summary` tiles (collected today, cash in transit, outstanding by age, orders/dispatched/delivered, at-risk stock), approvals (price variance, credit, MOV, bargain, red settlement, GRN exceptions), live map from `vehicle_positions`. Console: price lists and schemes with validity, overrides, credit modes; coverage vs universe; profit view (owner stream only); registers and exports; Tally mapping and export; imports wizard; settings (series, ePOD policy, settlement tolerance, DPDP notices); curation queue; incentives.

**Retailer (WhatsApp → PWA → native).** Every invoice, POD and receipt arrives on WhatsApp with a PWA deep link. Free text ("bhai 2 case campa 1L kal") → LLM parse against the retailer's own SKU history → draft order in the distributor's approvals → confirmation template. PWA: one card per linked distributor; reorder, running-low, deals, ATP-aware quantities; order status from the fulfilment machine; outstanding screen shaped like the pending-bills file with UPI QR; request discount; shop-detail self-edit (never credit/tier); directory opt-in with new links prepaid.

---

## 7. Offline sync design (Team app only)

### 7.1 Engine and residency

PowerSync over Postgres logical replication (R07 §3): server-authoritative, per-user partial sync via Sync Streams keyed on JWT claims, FIFO upload queue, per-field LWW with backend veto, attachment queue, `@powersync/react-native` 2.2 with op-sqlite. ElectricSQL (no write path, cloud winding down), WatermelonDB (dormant), Replicache (maintenance) and Zero (rejects offline writes) are ruled out. RDS: `rds.logical_replication = 1`, a `powersync` publication limited to synced tables (never `FOR ALL TABLES`), replication role, TLS, security group restricted to PowerSync egress IPs, `max_slot_wal_keep_size` set with a slot-lag alarm (R07 §3.2 rule 8).

**Residency (resolved).** Week 1: verify whether PowerSync Cloud offers an India region. If yes, use it. If not, proceed on Cloud for the pilot with the fact recorded in the security note (DPDP does not mandate residency today, R08 §6.1) and keep the self-hosted Open Edition on ECS Mumbai as the exit, taken only when a paying tenant or brand partner demands residency — the Open Edition has no dashboard and no custom write checkpoints (R07 §3.2 rule 9) and is real ops load for one person. `frontend/libs/offline` is the only PowerSync-touching code. Cloud Free at pilot, Pro ($49) at ~10 tenants; C's self-host-by-10 for cost is rejected.

**Auth spike (resolved).** Better Auth over R07's Supabase Auth, because the `jwt` plugin's JWKS keeps issuance in the API. Week 1 includes a one-day spike proving a Better Auth JWT with `{sub, tenant_id, role, beat_ids}` drives `auth.parameter()` and the per-beat parameter query; fallback is a tiny token-exchange endpoint minting PowerSync JWTs from the Better Auth session.

### 7.2 Streams

| Stream             | Roles                      | Selection                                                                                                                                                                                                                                                                            | Size control                                                        |
| ------------------ | -------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------- |
| `reference` (auto) | staff                      | global `products`, `product_variants`, `product_packs`, `hsn_rates`, `manufacturers`, `brands`                                                                                                                                                                                       | Changes rarely                                                      |
| `tenant_sell_side` | staff                      | `tenant_products_sell_view` (no cost columns by construction), `price_lists*`, `schemes`, `beats`, `locations`, `vehicles`, settings WHERE `tenant_id = auth.parameter('tenant_id')`                                                                                                 | Versioned; old versions pruned after 90 days                        |
| `rep_scope`        | salesperson                | parameter query `beat_assignments WHERE user_id = auth.user_id()`; `retailers` in those beats, overrides, own orders last 90 days, `visits`, `bargain_requests`, own `approvals`, `retailer_outstanding_summary`, `sellable_stock`, `suggested_orders`, `rep_product_authorisations` | Rolling 90 days; reassignment re-syncs                              |
| `warehouse_scope`  | manager                    | `documents`/`review_sessions` (30 days), open orders, `picklists`, `pack_confirmations`, `stock_balances` for warehouse and vehicles, `grns` (30 days)                                                                                                                               | Attachment metadata only                                            |
| `delivery_scope`   | delivery                   | `trips WHERE crew contains auth.user_id() AND status IN (planned, loading, active, closing)`, stops, deliveries, invoices + lines for those stops, collections, expenses, retailers via a per-stop view that hides the phone after delivery                                          | Today's trips only                                                  |
| `owner_scope`      | owner                      | `owner_summary` (90 days), open `approvals`, `trip_settlements` (7 days), **`vehicle_positions`**, `retailer_behaviour`                                                                                                                                                              | Aggregates only; never raw ledgers or raw `trip_points` (B's shape) |
| `costs`            | owner, manager, accountant | `tenant_product_costs`                                                                                                                                                                                                                                                               | The only stream with a cost column                                  |

No `GROUP BY/ORDER BY/LIMIT` in stream queries (R07 §3.2), hence the pre-aggregated tables.

### 7.3 Write path and conflict rules

The client writes domain rows locally; `/sync/upload` (protocol v1) maps each CRUD transaction's table set to a **command** (`PlaceOrder`, `RecordVisit`, `RecordReceipt`, `CompleteDelivery`, `ConfirmPack`, `RequestBargain`, `EditRetailerProfile`, `RecordTripCounts`) and dispatches it inside one DB transaction with tenant/actor settings from the JWT.

| Entity                    | Rule                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Sales order (create)      | Insert-only from devices with `quoted_unit_price`, `applied_rules`, `price_version`, `priced_at`. Server **recomputes at the same version**; unequal at the same version = tampering or engine bug → `sync_errors` + alert. If `pricing_date_mode = 'order'`, the quoted version is honoured even after a month-end scheme change (B's approval-on-every-mismatch rejected: it floods approvals). If `'delivery'`, reprice at the current version and open an approval only above the owner-set tolerance (default ₹0, adjustable) — A's rule. Credit hold and per-SKU max server-enforced; `stop` rejects, `strict` opens an approval, `indicate` annotates. Retailer confirmation only after approval. |
| Sales order (edit/cancel) | While `draft                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | submitted | confirmed`; a manager's `picking` wins over a rep edit; rep sees "already picking" |
| Line qty                  | Field LWW while `draft`; afterwards only `pack_confirmations` change quantities                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Visit / geo-tag           | Append-only; geofence is evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| Receipt                   | Append-only; immutable; correction = reversing receipt; `UNIQUE(tenant_id, device_id, client_receipt_no)`; FIFO allocation unless tagged                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| Delivery stop             | Monotonic; an out-of-order batch is accepted if valid in device timestamps (B); impossible transitions → `sync_errors` + approval; ePOD may arrive after the state ("proof pending")                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| Trip counts               | Insert-only; variance server-side; red settlement blocks close without approval                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| Retailer profile          | Field LWW on unprotected fields; protected columns rejected if present; lat/lng first-write-wins unless a manager overrides                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Bargain                   | Insert-only; decision online; auto-approve bound server-side                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| Docint review             | Single writer per session; commit idempotent on document id                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| Prices, schemes, catalog  | Download-only                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |

Business rejections return 2xx with `sync_errors(tenant_id, user_id, device_id, op_id, table, row_id, code, message_hi, message_en)` synced back to the device's "Needs attention" tray; 4xx is never returned to the uploader (it wedges the queue, R07 §3.2 rule 5). Server-owned fields flow back through the download stream.

### 7.4 Idempotency (resolved; Judge 3)

`sync_ops(tenant_id, device_id, op_id, outcome)` unique, retained ≥ 180 days; replay returns the stored outcome. `stock_ledger` and `journal_lines` carry `UNIQUE(tenant_id, idempotency_key)` so a replay after days offline cannot double-post even if `sync_ops` were pruned. Online oRPC mutations use `idempotencyKey` with Stripe semantics and 24-hour retention — the only place 24 hours applies. PowerSync does not advance a checkpoint while local mutations are unacknowledged (R07 §3.2 rule 6). CI replays a recorded upload log twice and asserts identical ledger state.

### 7.5 GPS points (the shared gap, resolved)

`trip_points` do **not** travel through the PowerSync queue: a device offline for hours would replay hundreds of GPS rows ahead of a cash receipt. The location task buffers points in a device-local table outside PowerSync and posts batches (30 s / 50 m, or on reconnect) to `POST /gps/points` with `(trip_id, device_id, ts)` for dedupe; the server drops points outside the trip window, upserts `vehicle_positions` and inserts `trip_points`. `device_id` is in every key — two-person crews carry two phones (C's `(trip_id, device_ts)` key rejected). Raw points pruned after 90 days by `dpdp.gps_retention`. The owner phone syncs `vehicle_positions`; the console reads an SSE feed of the same rows; latency ~30 s. **No WebSocket** (all judges): one fewer transport with sticky sessions behind an ALB.

### 7.6 Attachments and sync UX

Photos go through the attachment queue to S3 via pre-signed PUT; metadata rows sync normally; thumbnails cached locally. No "sync now" button: a status dot (synced / pending n / offline) and "last synced"; sync on connectivity change and app foreground; `expo-background-task` flushes on its ≥ 15-minute schedule when the app is closed.

---

## 8. Frontend architecture

| Concern                  | Decision                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Mobile                   | Expo SDK 57 (RN 0.86, New Architecture only, pinned), TypeScript, `expo-router`, EAS Build + Update with runtime-version gating, development builds from day one (Expo Go cannot run background location, op-sqlite or push)                                                                                                                                                       |
| Team app                 | `app/(auth)/ (owner)/ (sales)/ (warehouse)/ (delivery)/`; root layout mounts only permitted groups from membership roles; role switcher; routes declare `requires: ability(...)`; the sales module never bundles cost screens                                                                                                                                                      |
| Retailer app             | Separate Expo project, web output first (PWA on CloudFront), native later; online-first via oRPC + TanStack Query with persisted cache; CI bundle budget                                                                                                                                                                                                                           |
| Console                  | Vite + React 19, TanStack Router/Query/Table, oRPC client, Tailwind on shared tokens, PWA on S3 + CloudFront; MapLibre + Ola tiles for the map (Google dynamic maps cost on web); PDFs rendered by the API                                                                                                                                                                         |
| Local data               | PowerSync + op-sqlite (Team only); TanStack Query for online calls; Zustand for UI state only                                                                                                                                                                                                                                                                                      |
| Styling (resolved)       | `frontend/libs/ui/tokens.json` → generated RN `StyleSheet` theme and a Tailwind preset. RN primitives (NumberPad, QtyStepper with case/pcs, MoneyText, StatusPill, RowCrop, CandidateChips, SwipeActions, ListRow) shared **between the two Expo apps**, not with the DOM. No Tamagui, no NativeWind: a compiler toolchain on a New-Arch-only SDK is churn risk for one person |
| GPS                      | `expo-location` + `expo-task-manager`, trip-scoped, Android `foregroundServiceType="location"`, iOS background indicator, `Balanced`, 50 m / 30 s, `AutomotiveNavigation`; Play declaration, disclosure dialog and ≤ 30 s video in week 21; Transistorsoft ($399) as the OEM-battery-killer fallback (R07 §4)                                                                      |
| Maps / camera            | `react-native-maps` on Google's mobile SDK behind `MapProvider`; URL hand-off for navigation; `expo-camera` (ML Kit) for EAN/QR; document scanner at full quality                                                                                                                                                                                                                  |
| i18n                     | `i18next` + `expo-localization`; `hi`, `en` shipped, `mr` reserved; Noto Sans Devanagari; `Intl.NumberFormat('en-IN')` with a lakh/crore fallback in `backend/libs/domain`; template bodies share keys with UI strings                                                                                                                                                                   |
| Push / testing / cadence | Expo Push for staff; Vitest for domain; Maestro flows (order-offline, GRN-review, deliver-stop, settle-trip); role-leak dump in CI; one native release per quarter, the rest via EAS Update                                                                                                                                                                                        |

**Zero-training UX rules (from C), enforced as design-system lint:** every screen opens on the most likely action ("Aaj ka beat", next stop, GRN queue, "Reorder last order"); numeric input is a keypad, never a text field, stepper defaults to the last quantity, case/pcs shows both; state is colour + icon + word; no modal blocks for sync, GPS or permissions; geo-fence outside is an amber flag; the retailer sees the same object the invoice prints from; owner numbers are rupee outcomes first; Hindi/English toggle in every header, brand names untouched; the session survives a phone call and a day without signal; no "sync now" button.

---

## 9. Backend architecture and API contracts

**Stack (pinned).** Node 24 LTS; NestJS 12 on Fastify; Drizzle 0.45.x with SQL migrations and RLS policies as code, raw `sql` for ledger statements; Zod 4 in `backend/libs/contracts`; oRPC with TanStack Query bindings and OpenAPI generation; pg-boss 12 for jobs and the outbox; Better Auth (`phoneNumber`, `organization`, `jwt`); CASL in `backend/libs/domain`; OpenTelemetry via `@nestjs/observe` to Grafana Cloud; Sentry. Runner-up if decorators grate: Hono + oRPC with the same `shared/` and database (R08 §1.2).

**Stack-freshness rule (resolved).** NestJS 12 shipped on 27 Aug 2026; Expo 57, Drizzle pre-1.0, Better Auth, oRPC and PowerSync JS v2 are all "latest". Week 1 includes a half-day spike: Better Auth, the oRPC Nest adapter, pg-boss, Drizzle 0.45, Sentry and `@nestjs/observe` on ESM Nest 12. If any fails, start on NestJS 11 with an upgrade ADR. Every version pinned; framework and SDK bumps only in the quarterly native release window. R08's "Node 22 EOL July 2026" is doubtful (maintenance runs to April 2027) but immaterial: Node 24 is Active LTS.

**Module layout.** Each module: `domain/` (pure TS, imports only `backend/libs/domain`), `application/`, `infrastructure/` (Drizzle repositories, adapters), `http/` (oRPC implementations), `events.ts`, `<module>.module.ts`. Cross-cutting in `common/`: `TenantContextGuard` (refuses requests without a resolved tenant), `TxManager` (`set_config` at transaction start), `IdempotencyInterceptor`, SSE hub, hi/en error mapping.

**API style.** oRPC procedures with Zod input/output in `backend/libs/contracts`, served by Nest, emitted as OpenAPI in CI for the Tally Connector, importers and future brand/ONDC adapters. No GraphQL (ledger writes want explicit commands), no tRPC (no OpenAPI for free). Every mutating procedure declares `idempotencyKey`; a different request hash under the same key returns 409. Non-oRPC endpoints: `POST /sync/upload` (`X-Sync-Protocol: 1`), `POST /gps/points`, `GET /health`, `/webhooks/whatsapp`, later `/webhooks/gsp` and `/connector/tally/*` (polling; no inbound ports on the customer LAN). Realtime: `@Sse()` for dashboard tiles, approvals, order status and `vehicle_positions`, ids only, fan-out via `LISTEN/NOTIFY` on a **dedicated direct connection**.

**Jobs (pg-boss, same Postgres).** `outbox.*` (enqueued inside the business transaction), `docint.extract|escalate|structured_pull`, `notify.whatsapp|sms|push` (per-tenant rate limit, cost logged), `reporting.rollup` (00:30 IST), `inventory.rederive_balances` (nightly, drift alarm), `receivables.ageing_snapshot`, `gps.summarise`, `dpdp.gps_retention`, `dpdp.erasure`, `integrations.tally_export`, `integrations.import_batch` (rate-limited, checkpoint rows), `backup.verify`; later `claims.window_reminder`, `gst.ewb_expected_deliveries`, `gst.session_refresh`. Move only notification fan-out to BullMQ on Valkey if pg-boss exceeds ~10% of primary CPU (R08 §5).

**Auth and RBAC.** Phone OTP via WhatsApp authentication template (₹0.115) with MSG91 SMS fallback after ~20 s; email/passkey for owners; long-lived staff sessions with device-bound revocation from the console; OTP rate limits per phone and IP; SMS budget alarm. Roles: `owner`, `manager`, `salesperson`, `delivery`, `accountant`, `retailer`; platform `curator`, `support`. Three layers: org role → transaction settings; CASL abilities identical in apps and API (`cannot('read','Product',['purchasePrice','margin'])`, `cannot('update','Retailer',['creditLimit','tier'])`); RLS as the last word. Per-tenant KMS envelope keys for GSP and WhatsApp credentials (B).

**Pooling (resolved).** No RDS Proxy at pilot: 1–2 Fargate tasks with an in-process pool suffice; `LISTEN/NOTIFY` and the replication slot need direct connections anyway; the proxy costs ~$22/month B did not table. Adopt transaction-mode pooling only above ~3 API tasks, after verifying it does not pin connections on `set_config(..., true)`.

---

## 10. Integrations

**Tally (R02 §6).** Pilot: "Download Tally XML (masters + vouchers) for a date range" plus a ledger-mapping screen (party → ledger, sales ledger per GST rate or single per the CA's convention, Round Off ledger, free goods as Actual vs Billed quantity, batch names as strings to avoid the 14-digit Excel bug). Vouchers carry a stable `<GUID>` so re-imports never double-post, and balance to the paisa with a Round Off line because Tally rejects the whole voucher otherwise. Acceptance: the CA imports a week of vouchers into a restored backup and the Sales Register matches to the paisa. Post-pilot month 2: Windows **Tally Connector in Node SEA** (single toolchain, resolved over Go/.NET) polling our API and POSTing to `localhost:9000` while TallyPrime is open, reporting `IMPORTRESULT`; signed only if a customer's IT requires it.

**Brand-DMS coexistence.** No brand DMS exposes distributor-facing APIs (R01 §6.2). Coexistence = docint ingestion of the brand's secondary invoices (§5), per-brand secondary-sales and closing-stock CSV exports in the brand's layout, and a Tally-XML-shaped read endpoint so brand extractors that "pull from Tally" can pull from us. `source = brand_dms_import` from day one.

**WhatsApp (R07 §10, R10 §2).** Meta Cloud API direct (no BSP fee); utility templates hi/en submitted in week 18 (approval takes days); "Reply 1 to confirm" keeps replies in the free 24-hour window; inbound webhook for free-text orders; per-tenant quota and per-message cost logged; BSP-swappable adapter. **Volume (resolved):** tiers designed on R07's 300-retailer model (~11k messages ≈ ₹1,265 ≈ $15/month); a Tarsun-sized tenant (~150 retailers, ~600 orders × 4 templates ≈ 2.4k messages ≈ ₹280 ≈ $3.5) sits inside a 3,000-message quota. MyOperator's claim that in-window utility messages become billable from 1 Oct 2026 is unverified on Meta's page; budget the worst case at ₹0.5 per order conversation.

**Maps.** Google Maps mobile SDK (free on native) behind `MapProvider`; Google Routes for stop sequencing within the India free tier; MapLibre + Ola tiles on web; Ola/Mappls swap if pricing changes.

**OTP.** Better Auth send hook → WhatsApp authentication → MSG91 SMS; DLT entity and template registered once.

**E-way bill and e-invoice (R05).** Recipient side: offline RS256 QR verification, IRN hash recompute, optional EWB pull with EWB API credentials only, optional GST taxpayer API session (30-day access, monthly OTP). IRP `Get IRN` is unusable by recipients. Outbound: invoice model IRN-ready (≤ 16-char numbers, HSN, place of supply, EWB fields) but no IRN generation until a tenant crosses ₹5 crore B2B; intra-Maharashtra EWB threshold and the on-hold June 2026 changes are fields, not dependencies. GSP sandboxes post-pilot.

**Imports.** Tally XML masters, Marg Excel, CSV profiles for items, parties, bill-wise outstanding, batch-wise opening stock; rate-limited jobs with checkpoint rows; the outstanding checkpoint is a mandatory sign-off screen.

**ONDC.** Vocabulary only (payment term enum, min_qty/case_size, serviceable pincodes, FSSAI/GST on tenant, `RET10` category mapping); NP-MSN at ~20 tenants; Tarsun can join via the DigiDukaan city partner when it reaches MMR, orders entering through the external-order adapter.

---

## 11. Infrastructure, environments and cost

**Hosting (AWS ap-south-1 from day one).** RDS PostgreSQL 17 **`db.t4g.small`** Single-AZ, 20 GB gp3, 14-day PITR, `rds.logical_replication = 1`, `max_slot_wal_keep_size` set (resolved: 1 GiB on `t4g.micro` is marginal with a replication slot, pg-boss, pg_trgm and LISTEN/NOTIFY on the primary); ECS Fargate ARM `api` (0.5 vCPU/1 GB) and `worker` (0.25/0.5) from one image; ALB + ACM; CloudFront for both PWAs and signed image URLs; S3 `docs` (pre-signed, IA at 90 days) and `backups` (Object Lock); Secrets Manager with per-tenant KMS envelopes; SST v3. R2 rejected (no India jurisdiction, R08 §5.3). DigitalOcean BLR1 with Kamal is the same image if AWS setup time is unacceptable.

**Environments.** `local` (Compose: Postgres 17 `wal_level=logical`, MinIO, optional PowerSync OE, WhatsApp stub, Tarsun-shaped seed); `staging` (Mumbai, smallest sizes, anonymised data, EAS `preview`, WhatsApp test number, GSP/IRP sandboxes, separate Anthropic key); `prod`. Staging never points at production WhatsApp or GST endpoints.

**CI/CD.** GitHub Actions + Turborepo cache: PR → lint, typecheck, unit, integration on a Postgres container (RLS negatives, ledger invariants, idempotency and upload-log replay, role-leak dump), contract snapshots (OpenAPI, PowerSync schema, `/sync/upload` v1), retailer bundle budget; `main` → multi-arch image → ECR → one-shot migration task (expand/contract only) → ECS blue/green with 10-minute bake and auto-rollback; EAS Update on merge for JS-only changes; EAS Build on tag; nightly dependency audit; monthly restore-drill ticket.

**Observability.** OTel → Grafana Cloud Free; pino JSON logs with tenant/actor/request/idempotency ids; Sentry on all four surfaces; synthetic `/health` from inside and outside India. Alarms: 5xx rate, p95, pg-boss queue age, replication-slot lag and WAL retention, RDS storage/CPU, ledger drift, `sync_errors` rate per tenant, docint escalation rate and cost per page, WhatsApp delivery failures, monthly SMS/WhatsApp/Anthropic spend. Internal admin dashboard with per-tenant cost lines from week 1.

**Backups and DR.** PITR + nightly `pg_dump` to the Object-Locked bucket + `backup.verify` + monthly restore drill into a scratch instance with a checksum of `stock_balances` and AR totals — **the first drill in week 3, before any real data**. Stated **RPO 5 min / RTO 2 h**. S3 versioning. Cross-region copies to ap-south-2 at 10 tenants. Runbooks: restore, breach-72h, rotate secrets, revoke device, rollback, re-derive balances, PowerSync resync, Tally connector. k6 at 10× Tarsun volume in week 21.

### 11.1 Cost (USD/month, on-demand, ₹84/USD; infra from R08 §6.4, variable costs from R06 §4, R07 §8–10)

| Item                                                | 1 distributor  | 10                                | 100                                                            |
| --------------------------------------------------- | -------------- | --------------------------------- | -------------------------------------------------------------- |
| RDS Postgres                                        | 31 (t4g.small) | 148 (t4g.medium Multi-AZ, 100 GB) | 657 (m7g.large Multi-AZ + replica)                             |
| Fargate api + worker                                | 11             | 64                                | 255                                                            |
| ALB + CloudFront + Route 53                         | 22             | 25                                | 35                                                             |
| S3 + egress                                         | 2              | 12                                | 122                                                            |
| PowerSync (staff clients only)                      | 0 (Free)       | 49 (Pro)                          | ~250 (Pro + ~1,500 clients + data)                             |
| EAS                                                 | 0–19           | 19                                | 199                                                            |
| Observability                                       | 0              | 45                                | 150                                                            |
| Document intelligence (~120 pages/tenant, Sonnet 5) | 4              | 36                                | 360 (+30% with second opinion always on)                       |
| WhatsApp — included quota (3,000 msgs/tenant)       | 4              | 40                                | 400                                                            |
| WhatsApp beyond quota                               | pass-through   | pass-through                      | pass-through (an 11k-message tenant ≈ $15, billed in the plan) |
| OTP (WhatsApp auth first, long sessions)            | 3              | 30                                | 250                                                            |
| Maps / Routes; GSP (post-pilot)                     | 0              | ~30                               | ~200                                                           |
| **Total platform**                                  | **≈ $80–100**  | **≈ $500**                        | **≈ $2,900**                                                   |
| Per tenant                                          | $80–100        | ~$50                              | ~$29 (≈ ₹2,450)                                                |

Against R10 §4: a Tarsun-sized tenant at ₹2,500 base + ₹250 × 8 seats ≈ ₹4,500 (~$54); 100 such tenants ≈ ₹4.5 lakh revenue vs ≈ ₹2.5 lakh platform cost plus passed-through messaging. Positive but thin below ~30 tenants; marginal cost covered from roughly tenant 15. Hence: never price below ₹2,000/month, include a WhatsApp quota and meter beyond it, push OTP to WhatsApp auth with long staff sessions, buy reserved instances (30–40% off) once volume is predictable.

---

## 12. Repository layout

```
distribution-os/
├─ package.json  pnpm-workspace.yaml  turbo.json  tsconfig.base.json  .nvmrc (24)  .editorconfig
├─ .github/workflows/        ci.yml  deploy-staging.yml  deploy-prod.yml  eas-update.yml  nightly.yml  restore-drill.yml
├─ docker-compose.yml        # postgres17 (wal_level=logical), minio, mailpit, powersync-oe (optional), whatsapp-stub
│
├─ frontend/
│  ├─ apps/
│  │  ├─ team/                     # Expo SDK 57 — ONE store binary, role-gated; carries location + camera permissions
│  │  │  ├─ app/
│  │  │  │  ├─ _layout.tsx              # session, role resolution, PowerSync provider, i18n, ability injection
│  │  │  │  ├─ (auth)/                  # phone OTP, org picker
│  │  │  │  ├─ (owner)/                 # dashboard, approvals, live map, price quick-edit, team, incentives
│  │  │  │  ├─ (sales)/                 # beat today, retailer, order (last/suggested/grid), bargain, my orders, performance
│  │  │  │  ├─ (warehouse)/             # inbox (capture), review, GRN, order queue, picklist, packing, load sheet
│  │  │  │  └─ (delivery)/              # trips, stops, deliver/partial, on-spot order, collect, returns, settlement
│  │  │  ├─ src/features/<module>/      # screens + hooks per module
│  │  │  ├─ src/powersync/              # schema (from backend/libs/contracts), connector → /sync/upload, per-role streams
│  │  │  ├─ src/tasks/location.task.ts  # TaskManager.defineTask at module top level; local buffer → /gps/points
│  │  │  ├─ src/native/                 # camera, scanner, maps wrappers
│  │  │  ├─ app.config.ts  eas.json  maestro/
│  │  │  └─ store/                      # Play declaration text, disclosure video script, iOS purpose strings
│  │  ├─ retailer/                 # Expo Router — web output (PWA) first, native later; minimal permissions; ONLINE-FIRST
│  │  │  ├─ app/(onboard)/ (home)/ (orders)/ (ledger)/ (discover)/
│  │  │  ├─ src/features/               # reuses ui primitives + team's order editor / product search / invoice viewer
│  │  │  └─ app.config.ts  eas.json
│  │  └─ console/                  # Vite + React 19: owner console, billing desk, accountant exports, curation, imports, settings
│  │     ├─ src/routes/  src/features/{dashboard,approvals,pricing,billing-desk,retailers,coverage,live-map,integrations,imports,curation,claims,settings}
│  │     └─ vite.config.ts
│  └─ packages/
│     ├─ ui/                       # tokens.json → RN theme + Tailwind preset; RN primitives shared by team + retailer; fonts
│     ├─ api-client/               # oRPC client factory, auth plumbing, TanStack Query hooks, persisted cache
│     ├─ offline/                  # ONLY PowerSync-touching code: schema, streams, connector, attachment queue, sync_errors tray, UUIDv7
│     ├─ maps/                     # MapProvider: google.native.ts, maplibre.web.ts, navigation hand-off
│     └─ i18n/                     # en, hi (mr reserved), ICU plurals, formatters (keys shared with WhatsApp templates)
│
├─ backend/
│  ├─ apps/
│  │  ├─ api/                      # NestJS 12 (Fastify) modular monolith
│  │  │  └─ src/
│  │  │     ├─ main.ts  app.module.ts
│  │  │     ├─ common/                  # TenantContextGuard, TxManager (set_config), IdempotencyInterceptor, oRPC adapter, SSE hub, errors (hi/en)
│  │  │     ├─ sync/                    # POST /sync/upload (X-Sync-Protocol) → command mapper → module services
│  │  │     ├─ gps/                     # POST /gps/points → vehicle_positions + trip_points
│  │  │     ├─ webhooks/                # whatsapp, gsp (later), tally-connector polling endpoints
│  │  │     └─ modules/
│  │  │        ├─ platform/ identity/ retailers/ catalog/ tenant-catalog/ pricing/ orders/
│  │  │        ├─ inventory/ procurement/ warehouse/ billing/ receivables/ claims/ delivery/
│  │  │        ├─ docint/ notifications/ reporting/ integrations/ incentives/
│  │  │        └─ <module>/{domain,application,infrastructure,http,events.ts,<module>.module.ts}
│  │  ├─ worker/                   # same image, CMD worker: pg-boss consumers, crons, imports, exports
│  │  └─ tally-connector/          # Windows service, Node SEA (post-pilot): polls API, POSTs to localhost:9000, reports IMPORTRESULT
│  ├─ db/
│  │  ├─ schema/                   # drizzle schema per module; rls.ts (policies as code); roles.ts; views.ts (sell-side, per-stop, sellable_stock)
│  │  ├─ migrations/               # generated SQL, reviewed in PR, applied by one-shot ECS task (expand/contract)
│  │  ├─ seeds/                    # dated HSN rates, UQC, states, chart of accounts, global catalog seed, demo tenant
│  │  └─ powersync/                # sync-streams.yaml, publication.sql
│  ├─ docint/                      # LIBRARY called by the worker (no Nest dependency)
│  │  ├─ schema/                   # extraction JSON schema (R06 §8); prompt profiles: tally, sap-reliance, guiltfree-dms, marg-gst-local, brand-dms-secondary
│  │  ├─ validators/               # gstin.ts tax-split.ts hsn.ts rounding.ts irn-hash.ts qr-jwt.ts pack-size.ts page-completeness.ts
│  │  ├─ matcher/                  # alias, codes, structured filter, trgm, fusion, bands
│  │  ├─ engines/                  # ExtractionEngine interface: anthropic.ts, gemini.ts
│  │  └─ eval/                     # scoring harness CLI, disagreement tooling; fixtures in docs/fixtures (images gitignored)
│  └─ tests/
│     ├─ integration/              # testcontainers Postgres: RLS isolation, ledger invariants, idempotency replay, upload-log replay
│     ├─ role-leak/                # salesperson device dump + oRPC response dump: no cost/margin anywhere
│     └─ contracts/                # /sync/upload v1 snapshot, OpenAPI diff, Tally XML against a Tally sample
│
├─ shared/
│  ├─ domain/                      # pure TS, zero runtime deps: Money (paise), Qty/packs, GST split + rounding, pricing engine, scheme rules,
│  │                               # state machines (fulfilment/delivery/invoice/trip), credit control, suggested order, CASL abilities,
│  │                               # IRN hash, GSTIN checksum, case-size parser, en-IN formatter
│  ├─ contracts/                   # Zod 4 schemas, oRPC router types, events, docint JSON schema, PowerSync table schema,
│  │                               # sync-upload protocol v1, WhatsApp template registry types
│  └─ config/                      # tsconfig, eslint (boundaries), prettier, vitest presets
│
├─ infra/
│  ├─ sst.config.ts                # AWS Mumbai: vpc, rds (logical replication), ecs services, alb, s3 (docs, backups+object-lock), cloudfront, secrets/kms, alarms
│  ├─ docker/                      # Dockerfile (multi-stage node:24-alpine, non-root, pnpm deploy); compose.local.yml
│  ├─ kamal/                       # optional DigitalOcean BLR1 fallback
│  ├─ otel/                        # collector config
│  ├─ scripts/                     # backup.sh restore-drill.sh rotate-secrets.sh migrate.sh k6/
│  └─ runbooks/                    # restore, breach-72h, rotate-secrets, revoke-device, rollback, rederive-balances, powersync-resync, tally-connector
│
└─ docs/
   ├─ adr/                         # 0000-irreversibility-register, 0001-ids-uuidv7, 0002-tenancy-rls, 0003-stock-ledger-lots, 0004-money-journal,
   │                               # 0005-product-master, 0006-retailer-identity, 0007-sync-protocol, 0008-pricing-engine, 0009-two-binaries,
   │                               # 0010-powersync, 0011-nest12-or-11, 0012-no-websocket, ...
   ├─ domain/                      # glossary (R09 §11), invoice field spec (invoices A–G), scheme taxonomy, claim types, state machines
   ├─ fixtures/invoices/           # redacted real invoices + ground-truth JSON (docint eval set); never unredacted phones
   ├─ integrations/                # tally-xml.md, marg-excel-profile.md, whatsapp-templates.md, gsp.md, ondc-vocabulary.md, brand-dms.md
   ├─ compliance/                  # dpdp-notices (hi/en/mr), gps-consent.md, retention.md, security-note.md, play-store-declaration.md
   ├─ pilot/                       # Tarsun onboarding checklist, parallel-run scorecard, exit criteria, adoption metrics
   └─ pricing.md                   # public price page source + feature-request channel
```

Rules that keep it honest with one developer: `backend/libs/domain` has zero runtime dependencies and is the only place business math lives; `backend/db` is the only place SQL schema lives; `backend/libs/contracts` is the only place wire shapes live; `frontend/libs/offline` is the only PowerSync-touching code; modules never import another module's repository; every mutating contract carries `idempotencyKey`; every synced table has `id text` and `tenant_id`; ADRs are one page and written the same day as the decision.

---

## 13. Solo-developer roadmap and the definition of production-grade

**Honest framing.** Five months is ~22 weeks. All three proposals carry the same scope; A's 20 weeks under-budgets docint and delivery by ~2×, B's 28 leaves the retailer product outside the window. This plan is **24 weeks P50, 28 weeks P80**, using B's slice durations and acceptance criteria, A's dependency order and cut line, and C's placement of WhatsApp intake and the PWA before cut-over. Tell Tarsun "parallel run around week 22, cut-over between weeks 24 and 26". Assumes ~45 productive hours/week, no hires until the base exists.

| Weeks | Milestone                                    | Deliverables                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | Acceptance                                                                                                                                                          |
| ----- | -------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1–3   | **Foundation**                               | ADRs 0000–0012; monorepo, CI, Compose, SST staging (t4g.small, logical replication on); Nest 12 compatibility spike (else Nest 11); platform + identity schema; RLS FORCE + `TxManager`; idempotency interceptor; pg-boss outbox; Better Auth OTP (WhatsApp → MSG91), orgs, roles, JWT/JWKS; **PowerSync region check and the `auth.parameter()` spike**; oRPC + OpenAPI; OTel + Sentry; nightly `pg_dump` + `backup.verify`; **first restore drill**; Team app dev build with login, role routing, `reference` stream; `tokens.json`, i18n en/hi; price page draft; **start collecting invoices for the eval set** | RLS isolation suite green; a salesperson transaction cannot read costs; login on Expo dev build and web; restore drill recorded                                     |
| 4–5   | **Masters**                                  | Global catalog (products, variants, packs, external codes, aliases, proposals + curation queue), tenant overlay, `supplier_pack_configs`, cost table with role RLS, suppliers + return policies, retailer identity + tenant retailers + links, beats/PJP, accountant role, rep authorisations; Tally XML / Marg Excel / CSV import profiles with checkpoint UI; seed Tarsun's brands from the six invoices                                                                                                                                                                                                          | Tarsun's item and party masters import; "Campa 1L" exists once globally; x90 and _120 both represented                                                              |
| 6–8   | **Pricing + Orders (Sales module)**          | Pricing engine with typed rules, validity, versions, `applied_rules`, 100+ golden tests from invoices E/F; order aggregate, source enum, state machine, approvals; credit modes; bargain; streams `tenant_sell_side` + `rep_scope`; `/sync/upload` v1 with `sync_ops` + `sync_errors`; order screen (last/suggested v1/grid, case-pcs, pending qty, ATP stub); visits; role-leak CI test; eval set labelled                                                                                                                                                                                                         | 20 orders in airplane mode; totals match server recompute to the paisa; conflict tests for every §7.3 rule; upload-log replay                                       |
| 9–12  | **Inventory + Inbound (Warehouse module)**   | Lots, ledger, balances, reservations, locations incl. vehicle, cycle counts, opening-stock import; PO/GRN/LR; `backend/docint` steps 0–11 with review lock and undo; **brand-DMS document type: capture, extract, retailer match**; **eval scored in week 10, default engine chosen**; ATP live                                                                                                                                                                                                                                                                                                                     | Green 10-line invoice committed < 60 s; Reliance, Guiltfree, Guru Kripa and Too Yumm documents reach review; nightly re-derivation shows zero drift                 |
| 13–15 | **Billing + Money**                          | Invoices at pack, series per FY, GST split, Marg-shaped PDF via the API; credit notes; journal, receipts, allocations, ageing, UTR, cash-discount conditions; retailer ledger screen; opening outstanding import with checkpoint; **brand-DMS commit as `brand_dms_import` invoices**; Tally XML export + mapping; GSTR-1-shaped and CSV registers; billing desk                                                                                                                                                                                                                                                    | CA imports a week of vouchers into a restored Tally backup and the Sales Register matches to the paisa; invoice E commits and appears in the retailer's outstanding |
| 16–18 | **Warehouse + Delivery + Trips**             | Picklist (FEFO warn), pack confirmation, load sheet; vehicles, trips, PIN check-out/check-in, stops with state machine and reason codes, ePOD, on-spot orders, returns → credit note, collections, expenses, settlement cockpit; GPS via `/gps/points`, `vehicle_positions`; live map (phone via PowerSync, web via SSE); WhatsApp templates submitted                                                                                                                                                                                                                                                              | Two vehicles × three trips reconcile in a day; a red settlement cannot close without approval; a receipt uploaded after 1,000 buffered GPS points lands first       |
| 19–20 | **Owner + Notifications + Retailer surface** | Aggregates, `owner_summary`, SSE dashboards, approvals, coverage, profit view; WhatsApp templates live; free text → draft order; incentives v1; retailer PWA v1; product metrics and cost lines on the admin dashboard                                                                                                                                                                                                                                                                                                                                                                                              | Owner sees day numbers within seconds; every invoice reaches the retailer on WhatsApp; a retailer places one PWA order end to end                                   |
| 21–22 | **Hardening + store submission + migration** | k6 at 10× Tarsun; second restore drill; DPDP notices and GPS consent; Play declaration + video; TestFlight; Maestro journeys; security pass; Tarsun migration (items, parties, outstanding with checkpoint, lot-wise stock); parallel-run protocol                                                                                                                                                                                                                                                                                                                                                                  | Alarms fire in staging; outstanding matches the file; stock matches a cycle count                                                                                   |
| 23–24 | **Parallel run + cut-over**                  | Tarsun bills from the OS with Marg and the Too Yumm DMS as shadows; daily reconciliation; fixes via EAS Update; cut-over when the three truths hold (§1)                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Outstanding matches the file; stock matches a count; CA signs off Tally; GRN review time and self-placed share being measured                                       |
| 25–30 | **v1 value modules**                         | Claims + statements; Tally Connector (Node SEA); credit bands; near-expiry ladder; voice/photo intake after benchmark; Marathi; native retailer binary if justified; tenants 2 and 3; Multi-AZ; DR copies; hire #1 onboarded via the ADRs                                                                                                                                                                                                                                                                                                                                                                           | Second tenant onboarded without founder-only knowledge                                                                                                              |
| 31+   | **Scale**                                    | GSP EWB/IRN when needed; expected-deliveries inbox; GST API enrichment; ClickHouse only if measured; ONDC at ~20 tenants; PowerSync self-host only if residency forces it                                                                                                                                                                                                                                                                                                                                                                                                                                           | —                                                                                                                                                                   |

The cut line (§3.1) applies from week 19; nothing on the never-cut list moves.

**Definition of production-grade (the pilot does not ship without this).**

| Area       | Bar                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Tests      | `backend/libs/domain` ≥ 90% line coverage with golden cases from invoices A–G; integration on real Postgres: RLS isolation (cross-tenant fails; rep SELECT on costs returns 0 rows; retailer cannot update credit_limit), ledger invariants (balances = Σ ledger; Σ debit = Σ credit), idempotency and upload-log replay; sync conflict tests for every §7.3 rule; role-leak dump; docint eval on every model/prompt change; contract tests for `/sync/upload` v1 and Tally XML; Maestro journeys before each store release |
| Monitoring | OTel; structured logs with tenant/actor/request ids; Sentry on four surfaces with release tags; the §11 alarms; synthetic checks from India and outside; on-call = the founder's phone via Grafana OnCall; weekly cost report per tenant                                                                                                                                                                                                                                                                              |
| Backups    | PITR + nightly Object-Locked `pg_dump` + `backup.verify` + monthly restore drill with ledger checksum, first drill before first data; S3 versioning; RPO 5 min / RTO 2 h                                                                                                                                                                                                                                                                                                                                              |
| Security   | TLS everywhere; RLS forced; no `BYPASSRLS` reachable by the app; secrets in Secrets Manager with per-tenant KMS envelopes; OTP rate limits and spend alarm; device session revocation; short-TTL signed URLs; audit log on approvals, price, credit, cost views; dependency audit; DPDP notices, retention jobs, 72-hour breach runbook; no PII in URLs or logs; penetration checklist before the second paying tenant                                                                                                |
| Releases   | Blue/green with rollback; expand/contract migrations; EAS Update with runtime-version gating; one native release per quarter; changelog and feature-request channel visible to customers                                                                                                                                                                                                                                                                                                                              |
| Docs       | ADRs, runbooks, glossary, onboarding checklist — enough for hire #1 to deploy without the founder                                                                                                                                                                                                                                                                                                                                                                                                                     |

**Questions for the founder before week 1:** (1) which desktop program prints the Alan's/Masti Oye bills (Marg?) and can Tarsun export items, parties and outstanding to Excel; (2) which vendor runs the Too Yumm distributor system (FieldAssist?) and can it export invoices; what Reliance uses for Campa; (3) the CA's TallyPrime release and ledger convention (sales ledger per GST rate or single), and whether he already keys Too Yumm invoices into Tally; (4) written return/damage/expiry policy per brand plus one real claim sheet each; case sizes per pack; (5) any GST-registered retailer; ₹5 crore this FY; (6) whether Reliance and Guiltfree email PDF invoices; (7) trips per vehicle per day, stops per trip, cash per trip for two weeks; (8) acceptable GPS policy text for delivery staff; (9) confirm the van-sales model with Tarsun's crews.

---

## 14. Risks and mitigations

| #   | Risk                                                                                                            | L / I                        | Mitigation                                                                                                                                                                                         |
| --- | --------------------------------------------------------------------------------------------------------------- | ---------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Solo schedule slip; 24 weeks is P50                                                                             | High / High                  | Dependency-ordered slices with acceptance tests; explicit cut line; no feature added without removing one; EAS Update post-launch; a date range for Tarsun, not a date                             |
| 2   | Sync bugs wedge devices or lose money rows                                                                      | Medium / Severe              | Never 4xx from upload; `sync_ops` unique + ledger idempotency indexes; GPS off the PowerSync queue; beat-scoped 90-day streams; replay tests; airplane-mode Maestro flow                           |
| 3   | Purchase price leaks to a salesperson                                                                           | Low / Severe                 | Separate cost table with role RLS; sell-side views in streams; CI role-leak dump; audit log on cost reads                                                                                          |
| 4   | Ledger drift or double posting                                                                                  | Medium / Severe              | Same-transaction balances with `CHECK`; unique idempotency keys on ledger rows; nightly re-derivation with alarm; compensating entries only; deferred journal trigger; restore drill with checksum |
| 5   | Docint accuracy on Indian layouts, pencil marks, Hindi annotations                                              | Medium / High                | Eval set from week 1, scored before enabling; engine chosen by eval; validators and QR facts route, not confidence; human commit; measured manual-entry hatch; per-supplier profiles               |
| 6   | Store rejection for background location; OEM battery killers                                                    | Medium / Medium              | Trip-scoped tracking with FGS notification; declaration + video in week 21; Transistorsoft fallback; "last point n min ago" visible to the owner                                                   |
| 7   | CA rejects the system (voucher mismatch, masters, free-qty convention)                                          | Medium / High                | Paisa-exact Round Off; masters before vouchers; per-tenant toggles; dry run against a restored backup; file export before connector                                                                |
| 8   | Brand-mandated DMS keeps the distributor double-keying                                                          | High / High                  | Brand-DMS ingestion with commit in the pilot; per-brand exports; Tally-shaped read endpoint                                                                                                        |
| 9   | Retailer adoption near zero                                                                                     | High / Medium                | WhatsApp invoices/POD/receipts from day one; PWA before native; in-beat onboarding; self-placed share measured weekly; native only on data                                                         |
| 10  | Messaging, OTP and LLM costs erode the subscription                                                             | Medium / Medium              | Per-tenant cost lines from week 1; WhatsApp quota + metering; in-window replies; WhatsApp auth + long sessions; tight schema; spend alarms; ₹0.5/order worst case budgeted                         |
| 11  | Stack freshness (Nest 12 one week old, Expo 57 New-Arch-only, Drizzle pre-1.0, Better Auth, oRPC, PowerSync v2) | Medium / Medium              | Week-1 compatibility spike with Nest 11 fallback; every version pinned; quarterly upgrade window; ADR per vendor with an exit note                                                                 |
| 12  | PowerSync region, licence or business change                                                                    | Medium / High                | Region verified week 1; Cloud unless residency forces self-host; `packages/offline` is the only PowerSync code; server-authoritative design keeps logic in Postgres + Nest                         |
| 13  | Primary saturation (pg-boss + LISTEN/NOTIFY + replication slot)                                                 | Low at pilot / Medium at 100 | t4g.small floor; `max_slot_wal_keep_size` + slot-lag alarm; fan-out to Valkey/BullMQ above ~10% CPU; replica and `(tenant_id, occurred_at)` partitioning at 100                                    |
| 14  | Global master polluted by tenant proposals                                                                      | Medium / Medium              | `proposed` status, curation queue, `merged_into` merges before tenant #3                                                                                                                           |
| 15  | Compliance drift (GST 2.0 rates, EWB churn, DPDP by 13 May 2027, e-invoice at ₹5 crore)                         | Medium / Medium              | Dated `hsn_rates`; MRP per lot; original-rate credit notes; IRN-ready model; EWB fields kept, not depended on; notices, retention and breach runbook in the pilot                                  |

---

## 15. Decisions log

| #   | Decision                                                                                                                                                                 | Alternatives                                                      | Why                                                                                                                               |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- |
| D1  | Five products; two store binaries + one web console                                                                                                                      | Five binaries; one binary for everyone                            | Permission declarations and release load (R07 §2.3); the retailer binary must not carry location permissions                      |
| D2  | Retailer surface **online-first**, never a PowerSync client                                                                                                              | B/C retailer streams                                              | $30 per 1,000 peak clients, Free tier 50 (R07 §3.1); 150 retailers break Free at pilot, 15k at 100 tenants; no offline-write need |
| D3  | Retailer = one Expo Router project (web now, native later)                                                                                                               | Separate Vite PWA + Expo app (A, B)                               | One codebase; shares RN primitives and the order editor with the Team app; CI bundle budget mitigates RN-web size                 |
| D4  | Live map via `vehicle_positions` on PowerSync (phone) + SSE (web); **no WebSocket**                                                                                      | WebSocket namespace (B, C); raw `trip_points` stream (A)          | ~30 s latency is fine; one fewer transport behind an ALB; raw points on the owner phone are waste                                 |
| D5  | GPS via a separate `/gps/points` batch endpoint with device buffering; `device_id` in every key                                                                          | PowerSync FIFO queue; C's `(trip_id, device_ts)` key              | GPS rows must never queue ahead of a cash receipt; two phones per crew                                                            |
| D6  | 24-week P50 / 28-week P80; A's cut line; B's acceptance criteria                                                                                                         | A's 20 weeks; B's 28 with retailer after go-live                  | A under-budgets docint and delivery ~2×; B answers the brief late                                                                 |
| D7  | Brand-DMS ingestion in the pilot (match by week 12, commit by week 15); never-cut                                                                                        | A month 6; C post-pilot month 1                                   | Outstanding reconciliation fails for Too Yumm retailers otherwise (invoice E)                                                     |
| D8  | WhatsApp tiers on R07's 11k-message model; 3,000-message quota included, overage metered; ₹0.5/order worst case                                                          | A ~4k, C ~1.5k per tenant                                         | R07 §10 arithmetic; Oct-2026 in-window billing unverified (R10 §2)                                                                |
| D9  | Price mismatch: recompute at the same version; honour the quoted version under `'order'` mode; owner tolerance under `'delivery'`; approvals only for genuine deviations | Approval on every mismatch (B); notice only (C)                   | B floods approvals at month-end; C hides real deviations                                                                          |
| D10 | PowerSync Cloud; India region verified week 1; self-host only on a residency demand                                                                                      | Self-host by ~10 tenants (C)                                      | Open Edition lacks dashboard and custom write checkpoints (R07 §3.2 rule 9)                                                       |
| D11 | No RDS Proxy at pilot; direct connections for notifier and replication; pooler only above ~3 API tasks after verifying `set_config`                                      | RDS Proxy from day one (B)                                        | LISTEN/NOTIFY and the slot bypass it; uncosted ~$22/month; unnecessary below ~4 tasks                                             |
| D12 | RDS `db.t4g.small` at pilot with `max_slot_wal_keep_size`                                                                                                                | `t4g.micro` (B, C)                                                | 1 GiB with a slot, pg-boss, pg_trgm and LISTEN/NOTIFY is a false economy (R07 §3.2 rule 8)                                        |
| D13 | Fulfilment vocabulary `draft → submitted → confirmed → picking → packed → dispatched → delivered/partially_delivered → closed/cancelled` with an ONDC mapping table      | B's `authorised`; C's `approved/reserved`                         | Approvals and reservations are side effects; `on_status` is a lookup (R04 §6)                                                     |
| D14 | One `schemes` table in `pricing`; `claims` references it                                                                                                                 | `promotion_rules` + `schemes` (B)                                 | Engine and accrual read the same rule                                                                                             |
| D15 | Invoice at pack, immutable; shortfall = credit note; van sales invoiced at delivery                                                                                      | "Regenerate the invoice" (A, C)                                   | An issued GST invoice cannot be regenerated                                                                                       |
| D16 | `sync_ops` unique ≥ 180 days + `UNIQUE(tenant_id, idempotency_key)` on ledger rows; 24-hour pruning only for online keys                                                 | 24-hour pruning everywhere (A, B)                                 | Devices replay days later                                                                                                         |
| D17 | `products + product_variants + product_packs + supplier_pack_configs` with `merged_into`                                                                                 | `products + product_packs (nullable supplier_id)` (A)             | Irreversible; merges must never rewrite ledger rows; models x90 / _120 / CS1                                                      |
| D18 | `backend/docint` library with pure validators, matcher, engines, eval CLI                                                                                                | `tests/docint-eval` + API module (A); inside `modules/docint` (C) | Eval and validators need no Nest; engines swappable                                                                               |
| D19 | Eval set from week 1, scored week 10, decides the engine; Sonnet 5 is the prior; Gemini Flash second opinion                                                             | Eval in the build weeks (A); weeks 11–14 (B)                      | R06 §11: 50–100 invoices before choosing; starting needs no code                                                                  |
| D20 | `tokens.json` → RN `StyleSheet` + Tailwind preset; RN primitives shared only between the Expo apps                                                                       | Tamagui (B), NativeWind (C)                                       | Compiler toolchains on a New-Arch-only SDK are churn risk                                                                         |
| D21 | Tally XML export + mapping in the pilot; Connector in **Node SEA** post-pilot                                                                                            | Go/.NET signed connector in the pilot (B)                         | One toolchain; CAs import files today with zero training                                                                          |
| D22 | Stamp `applied_rules` and funding flags from the first order; claims UI first post-pilot slice                                                                           | Month 6–7 (A); weeks 33–40 (B)                                    | Stamping is free now; the UI needs pilot data                                                                                     |
| D23 | Better Auth with `jwt`/JWKS; week-1 spike proving `auth.parameter()`                                                                                                     | Supabase Auth (R07)                                               | Keeps issuance in the API; the spike closes the R07/R08 conflict                                                                  |
| D24 | NestJS 12 only if the week-1 spike passes, else Nest 11 + upgrade ADR; all versions pinned; quarterly upgrades                                                           | Adopt Nest 12 unconditionally                                     | One-week-old framework; ecosystem lag likely                                                                                      |
| D25 | E-invoice: QR verify, IRN recompute, optional pulls; vision mandatory even when the pull succeeds                                                                        | Treat the pull as authoritative                                   | JSON lacks MRP, case size, scheme % and batch unless `BchDtls` (R05 §2.1)                                                         |
| D26 | Van sales yes: vehicle as location, Trip aggregate, no separate app; founder confirms with Tarsun                                                                        | Separate van app; ignore                                          | R09 §3, R03 §7; Campa slip exists (invoice G)                                                                                     |
| D27 | Accountant role; authorised product lists per rep                                                                                                                        | Owner login for the CA; all reps see all products                 | CA workflow; manufacturer-employed reps                                                                                           |
| D28 | Public price page (₹2,000–2,500 base + ₹150–300/seat, unlimited retailers, WhatsApp quota, free tier by bills) and feature-request channel                               | Opaque pricing until scale                                        | R10 §4; R01 §5.10                                                                                                                 |
| D29 | Thermal ESC/POS post-pilot; van slips via WhatsApp PDF + OS share-to-print in the pilot                                                                                  | RN thermal library in the pilot (A)                               | Fragile libraries; not on the cut line                                                                                            |
| D30 | First restore drill in week 3, before any real data; RPO 5 min / RTO 2 h                                                                                                 | Drill at hardening (C)                                            | A backup that has not been restored is a hypothesis                                                                               |

**Flagged changes to CONTEXT, consolidated:** two binaries + console (D1); retailer sequencing WhatsApp → PWA → native (D2/D3); van sales yes (D26); Claim aggregate, scheme funding/claimability and `applied_rules` on every line (D14/D22); MRP and expiry as lot attributes, dated HSN rates, supplier pack configs (D17); pricing engine shipped to devices with server recompute (ADR 0008/D9); order `source` enum and ONDC-shaped vocabularies (D13); "zero manual entry" = zero typing except the blind gate count; invoice at pack + credit notes (D15); Tally XML export and brand-DMS ingestion as first-class integration scope (D7/D21); FSSAI on every bill and own-VPA UPI QR; geo-tag as evidence, trip-scoped GPS only; Marathi keys reserved; published pricing (D28); accountant role (D27). None contradict the ledger, aggregate, tenancy, identity, GST, document-intelligence or trade-credit decisions already made; they extend them.
