<!-- GENERATED by `pnpm docs:readme` from shared/contracts — do not edit by hand; edit the contract or backend-services/core/src/docs/readme.ts -->

# Sales service (`@dos/sales-service`)

Serves the **salesperson** role. Port **3003** (override with `SALES_SERVICE_PORT`). Stateless: run as many replicas as needed (docs/20). Shares one Postgres database and the `@dos/core` modules with the other services (docs/19).

## Sign in

Everything below except the endpoints marked **public** in the Roles column needs a token from **auth-service** (port 3000 — full flow in its README). In short: `POST http://localhost:3000/auth/login` with `{ "username": "...", "password": "...", "deviceId": "<uuid>" }` returns `accessToken` (an EdDSA JWT, ~15 minutes) and `refreshToken` (opaque, rotates on every use). Send the access token here as `Authorization: Bearer <accessToken>`; before it expires, `POST /auth/refresh` on auth-service for a new pair rather than signing in again.

A missing/expired token or a role this service or this endpoint does not allow gets a `401`/`403` — see the failure responses on each endpoint below, and the permission matrix at the end of this document.

## Run

```bash
pnpm --filter @dos/sales-service dev    # watch mode on :3003
pnpm --filter @dos/sales-service test   # smoke spec: health, docs, role gate
open http://localhost:3003/docs           # interactive API reference (Scalar); /docs/openapi.json is the spec
```

Needs the local database (`DATABASE_URL` in the repo-root `.env`; `pnpm db:migrate && pnpm db:seed` once — the seed prints a username + password per demo role; see docs/18-build-log.md). Turn those into a token with "Sign in" above.

Conventions: money is integer paise (₹40.00 = 4000), quantities integer pieces, percentages basis points (5% = 500), ids are client-generated UUIDv7, every write carries an `idempotencyKey` (same key + same payload → same answer; different payload → 409). GET inputs go in the query string. Lists return `{ items, nextCursor }`; pass `cursor` to page.

## Endpoints

| Method | Path | What it does | Roles |
|---|---|---|---|
| GET | `/health/ping` | Liveness + database reachability | public |
| GET | `/tenancy/me` | Current user, tenant and membership | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/tenancy/staff` | People who work in this distributor | owner, manager, accountant |
| POST | `/tenancy/staff` | Add a staff member with a temporary password | owner, manager |
| POST | `/tenancy/staff/set-password` | Reset a staff password; they must change it at next sign-in | owner, manager |
| POST | `/tenancy/staff/set-status` | Enable or disable a staff membership (disabling revokes their sessions) | owner, manager |
| GET | `/catalog/variants` | Search the global product master | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/catalog/manufacturers` | Manufacturers with their brands | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/catalog/proposals` | Propose a missing product; usable immediately | owner, manager, accountant, salesperson, warehouse, delivery |
| GET | `/tenant-catalog/products` | What this distributor sells (no cost) | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/tenant-catalog/products` | List/unlist a variant and set order rules | owner, manager, accountant |
| GET | `/tenant-catalog/suppliers` | Suppliers of this distributor | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/tenant-catalog/suppliers` | Create or update a supplier | owner, manager, accountant |
| GET | `/tenant-catalog/costs` | Purchase costs (owner/manager/accountant only) | owner, manager, accountant |
| POST | `/tenant-catalog/costs` | Set purchase cost (owner/manager/accountant only) | owner, manager, accountant |
| GET | `/retailers` | Retailers of this distributor (retailer role: only its own, without credit) | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/retailers/{id}` | One retailer (retailer role: only its own, without credit) | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/retailers` | Create or update a retailer; code is server-assigned | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/retailers/{id}/credit` | Set tier and credit terms (owner/manager/accountant only) | owner, manager, accountant |
| POST | `/retailers/{id}/link` | Link the retailer to its global identity by phone | owner, manager |
| GET | `/beats` | Beats of this distributor | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/beats` | Create or update a beat | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/beats/{id}/assign` | Assign a salesperson to a beat for a date range | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/visits` | Record a shop visit (own) | owner, manager, accountant, salesperson, warehouse, delivery |
| GET | `/visits` | Visits by retailer / rep / period | owner, manager, accountant, salesperson, warehouse, delivery |
| GET | `/pricing/price-lists` | Price lists with their rates | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/pricing/price-lists` | Create or update a price list | owner, manager, accountant |
| POST | `/pricing/price-lists/{priceListId}/items` | Set rates on a price list (upsert per variant) | owner, manager, accountant |
| GET | `/pricing/overrides` | Retailer-specific rates | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/pricing/overrides` | Set a retailer-specific rate | owner, manager, accountant |
| GET | `/pricing/schemes` | Schemes (the single table the engine reads) | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/pricing/schemes` | Create or update a scheme; a change to its economics bumps the version | owner, manager, accountant |
| POST | `/pricing/quote` | Price an order with the pure engine (no side effects) | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/pricing/bargains` | Ask for a lower rate; auto-approved within the rep bound | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/pricing/bargains/{id}/decide` | Approve or reject a bargain (back office) | owner, manager, accountant |
| GET | `/pricing/bargains` | Bargain requests | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/pricing/bounds` | How far a rep may discount without asking (owner only) | owner |
| GET | `/inventory/locations` | Stock locations: godown, vehicles, damaged bin | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/inventory/locations` | Create or update a stock location | owner, manager, accountant, warehouse |
| GET | `/inventory/sellable` | Available-to-promise stock (the only stock surface for reps and retailers) | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/inventory/balances` | On-hand and reserved per lot per location (stock keepers only) | owner, manager, accountant, warehouse, delivery |
| POST | `/inventory/adjustments` | Post an opening/adjustment/damage/expiry/cycle-count ledger row | owner, manager, accountant, warehouse |
| POST | `/inventory/transfers` | Move pieces of a lot between locations | owner, manager, accountant, warehouse |
| GET | `/inventory/ledger` | Append-only stock ledger | owner, manager, accountant, warehouse, delivery |
| POST | `/inventory/lots` | Find or create a lot (variant + batch + MRP) | owner, manager, accountant, warehouse |
| POST | `/orders` | Create a priced draft order | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/orders/{id}/lines` | Replace the lines of a draft and re-price it | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/orders/repeat-last` | Draft a repeat of the retailer's last order, re-priced today | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| POST | `/orders/{id}/submit` | Submit: assign the order number, raise approvals or auto-confirm | owner, manager, accountant, salesperson, warehouse, delivery |
| POST | `/orders/{id}/confirm` | Confirm and reserve stock (back office) | owner, manager, accountant |
| POST | `/orders/{id}/cancel` | Cancel an order and release its reservations | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/orders/{id}` | One order with lines, transitions and approvals | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/orders` | Orders (a retailer sees only its own) | owner, manager, accountant, salesperson, warehouse, delivery, retailer |
| GET | `/approvals` | Approval queue (back office) | owner, manager, accountant |
| POST | `/approvals/{id}/decide` | Approve or reject; the last approval approved confirms the order | owner, manager, accountant |
| POST | `/sync/upload` | Offline write batch (never 4xx; rejections are 2xx + sync_errors) | owner, manager, accountant, salesperson, warehouse, delivery |

### GET `/health/ping`

Liveness + database reachability · contract `health.ping`

**Roles:** public

**Example request**

```bash
curl "http://localhost:3003/health/ping"
```

**Success response** — `200`

```json
{
  "ok": true,
  "version": "text",
  "db": "up",
  "time": "2026-09-04T10:30:00.000Z"
}
```

**Failure responses**

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/tenancy/me`

Current user, tenant and membership · contract `tenancy.me`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Example request**

```bash
curl "http://localhost:3003/tenancy/me" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "user": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "username": "sunil.tarsun",
    "phone": "+919876543210",
    "name": "Sharma Kirana Store",
    "locale": "en-IN"
  },
  "tenant": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "slug": "text",
    "legalName": "Campa Cola 750 ml",
    "gstin": "27AAPFU0939F1ZV",
    "stateCode": "27",
    "plan": "pilot",
    "status": "active"
  },
  "membership": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "tenantId": "01a06d03-67ed-7c68-87e3-25e2fff74cc3",
    "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
    "role": "owner",
    "status": "invited"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/tenancy/staff`

People who work in this distributor · contract `tenancy.staff.list`

**Roles:** owner, manager, accountant

**Example request**

```bash
curl "http://localhost:3003/tenancy/staff" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
      "username": "sunil.tarsun",
      "name": "Sharma Kirana Store",
      "phone": "+919876543210",
      "role": "owner",
      "status": "invited",
      "lastLoginAt": "2026-09-04T10:30:00.000Z"
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call GET /tenancy/staff",
  "error": "Forbidden"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/tenancy/staff`

Add a staff member with a temporary password · contract `tenancy.staff.create`

**Roles:** owner, manager

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `userId` | uuid | yes |
| `username` | string | yes |
| `name` | string | yes |
| `phone` | string | yes |
| `role` | owner | manager | accountant | salesperson | warehouse | delivery | yes |
| `locale` | en-IN | hi-IN | mr-IN | no |
| `temporaryPassword` | string | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/tenancy/staff" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
  "username": "sunil.tarsun",
  "name": "Sharma Kirana Store",
  "phone": "+919876543210",
  "role": "owner",
  "locale": "en-IN",
  "temporaryPassword": "Dos@1234"
}
```

**Success response** — `200`

```json
{
  "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
  "membershipId": "01a06d14-9c5c-7346-8aeb-8114bed5ae60",
  "mustChangePassword": true
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /tenancy/staff",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/tenancy/staff/set-password`

Reset a staff password; they must change it at next sign-in · contract `tenancy.staff.setPassword`

**Roles:** owner, manager

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `userId` | uuid | yes |
| `temporaryPassword` | string | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/tenancy/staff/set-password" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
  "temporaryPassword": "Dos@1234"
}
```

**Success response** — `200`

```json
{
  "ok": true
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /tenancy/staff/set-password",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/tenancy/staff/set-status`

Enable or disable a staff membership (disabling revokes their sessions) · contract `tenancy.staff.setStatus`

**Roles:** owner, manager

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `userId` | uuid | yes |
| `status` | active | disabled | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/tenancy/staff/set-status" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
  "status": "active"
}
```

**Success response** — `200`

```json
{
  "ok": true
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /tenancy/staff/set-status",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/catalog/variants`

Search the global product master · contract `catalog.search`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `q` | string | no |
| `manufacturerId` | uuid | no |
| `brandId` | uuid | no |
| `includeProposed` | boolean | string | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/catalog/variants?q=campa&manufacturerId=01a06d31-cc31-7f93-8dce-8b32a928ea08&brandId=01a06db4-0f6a-71b5-8f6e-febe6219c69d&includeProposed=true&limit=50" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "productId": "01a06dde-82d5-7d2f-891a-9bf51dd787ee",
      "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
      "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
      "name": "Sharma Kirana Store",
      "productName": "Campa Cola 750 ml",
      "productNameHi": "text",
      "brandName": "Campa Cola 750 ml",
      "manufacturerName": "Campa Cola 750 ml",
      "netQty": 24,
      "netUnit": "g",
      "promoExtra": 1,
      "defaultCaseSize": 24,
      "hsnCode": "22021010",
      "ean": "text",
      "mrpPaise": 4000,
      "status": "active"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/catalog/manufacturers`

Manufacturers with their brands · contract `catalog.manufacturers`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Example request**

```bash
curl "http://localhost:3003/catalog/manufacturers" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "name": "Sharma Kirana Store",
      "legalName": "Campa Cola 750 ml",
      "gstin": "27AAPFU0939F1ZV",
      "brands": [
        {
          "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
          "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
          "name": "Sharma Kirana Store"
        }
      ]
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/catalog/proposals`

Propose a missing product; usable immediately · contract `catalog.propose`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `productId` | uuid | yes |
| `variantId` | uuid | yes |
| `manufacturerId` | uuid | yes |
| `brandId` | uuid | no |
| `productName` | string | yes |
| `productNameHi` | string | no |
| `category` | string | no |
| `variantName` | string | yes |
| `netQty` | integer | yes |
| `netUnit` | g | kg | ml | l | pcs | yes |
| `promoExtra` | integer | no |
| `defaultCaseSize` | integer | yes |
| `hsnCode` | string | yes |
| `ean` | string | no |
| `mrpPaise` | integer | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/catalog/proposals" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "productId": "01a06dde-82d5-7d2f-891a-9bf51dd787ee",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
  "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
  "productName": "Campa Cola 750 ml",
  "productNameHi": "text",
  "category": "text",
  "variantName": "Campa Cola 750 ml",
  "netQty": 24,
  "netUnit": "g",
  "promoExtra": 1,
  "defaultCaseSize": 24,
  "hsnCode": "22021010",
  "ean": "8901234567890",
  "mrpPaise": 4000
}
```

**Success response** — `200`

```json
{
  "productId": "01a06dde-82d5-7d2f-891a-9bf51dd787ee",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "status": "active"
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/tenant-catalog/products`

What this distributor sells (no cost) · contract `tenantCatalog.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `q` | string | no |
| `listedOnly` | boolean | string | no |
| `brandId` | uuid | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/tenant-catalog/products?q=campa&listedOnly=true&brandId=01a06db4-0f6a-71b5-8f6e-febe6219c69d&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "productId": "01a06dde-82d5-7d2f-891a-9bf51dd787ee",
      "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
      "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
      "name": "Sharma Kirana Store",
      "productName": "Campa Cola 750 ml",
      "productNameHi": "text",
      "brandName": "Campa Cola 750 ml",
      "manufacturerName": "Campa Cola 750 ml",
      "netQty": 24,
      "netUnit": "g",
      "promoExtra": 1,
      "defaultCaseSize": 24,
      "hsnCode": "22021010",
      "ean": "text",
      "mrpPaise": 4000,
      "status": "active",
      "tenantProductId": "01a06dcd-e550-78dd-895f-5589579c8bfd",
      "listed": true,
      "localAlias": "text",
      "caseSize": 24,
      "minOrderQty": 24,
      "orderIncrement": 1,
      "maxPerOrder": 1,
      "sortOrder": 1
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/tenant-catalog/products`

List/unlist a variant and set order rules · contract `tenantCatalog.upsertListing`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `variantId` | uuid | yes |
| `listed` | boolean | no |
| `localAlias` | string | no |
| `caseSizeOverride` | integer | no |
| `minOrderQty` | integer | no |
| `orderIncrement` | integer | no |
| `maxPerOrder` | integer | no |
| `sortOrder` | integer | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/tenant-catalog/products" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "listed": true,
  "localAlias": "text",
  "caseSizeOverride": 24,
  "minOrderQty": 1,
  "orderIncrement": 1,
  "maxPerOrder": 1,
  "sortOrder": 0
}
```

**Success response** — `200`

```json
{
  "item": {
    "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
    "productId": "01a06dde-82d5-7d2f-891a-9bf51dd787ee",
    "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
    "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
    "name": "Sharma Kirana Store",
    "productName": "Campa Cola 750 ml",
    "productNameHi": "text",
    "brandName": "Campa Cola 750 ml",
    "manufacturerName": "Campa Cola 750 ml",
    "netQty": 24,
    "netUnit": "g",
    "promoExtra": 1,
    "defaultCaseSize": 24,
    "hsnCode": "22021010",
    "ean": "text",
    "mrpPaise": 4000,
    "status": "active",
    "tenantProductId": "01a06dcd-e550-78dd-895f-5589579c8bfd",
    "listed": true,
    "localAlias": "text",
    "caseSize": 24,
    "minOrderQty": 24,
    "orderIncrement": 1,
    "maxPerOrder": 1,
    "sortOrder": 1
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /tenant-catalog/products",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/tenant-catalog/suppliers`

Suppliers of this distributor · contract `tenantCatalog.suppliers`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Example request**

```bash
curl "http://localhost:3003/tenant-catalog/suppliers" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "name": "Sharma Kirana Store",
      "gstin": "27AAPFU0939F1ZV",
      "stateCode": "27",
      "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
      "phone": "+919876543210",
      "eInvoicing": true,
      "paymentTermsDays": 7,
      "active": true
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/tenant-catalog/suppliers`

Create or update a supplier · contract `tenantCatalog.upsertSupplier`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `name` | string | yes |
| `gstin` | string | no |
| `stateCode` | string | no |
| `manufacturerId` | uuid | no |
| `phone` | string | no |
| `eInvoicing` | boolean | no |
| `paymentTermsDays` | integer | no |
| `active` | boolean | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/tenant-catalog/suppliers" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "name": "Sharma Kirana Store",
  "gstin": "27AAPFU0939F1ZV",
  "stateCode": "27",
  "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
  "phone": "+919876543210",
  "eInvoicing": false,
  "paymentTermsDays": 7,
  "active": true
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "gstin": "27AAPFU0939F1ZV",
    "stateCode": "27",
    "manufacturerId": "01a06d31-cc31-7f93-8dce-8b32a928ea08",
    "phone": "+919876543210",
    "eInvoicing": true,
    "paymentTermsDays": 7,
    "active": true
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /tenant-catalog/suppliers",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/tenant-catalog/costs`

Purchase costs (owner/manager/accountant only) · contract `tenantCatalog.costs`

**Roles:** owner, manager, accountant

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `variantId` | uuid | no |
| `limit` | integer | no |

**Example request**

```bash
curl "http://localhost:3003/tenant-catalog/costs?variantId=01a06df0-2faf-79a2-8456-92042e49f147&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
      "supplierId": "01a06d4d-b127-7ad7-815f-92d49a8a08b8",
      "purchaseRatePaise": 4000,
      "landedCostPaise": 4000,
      "ptdPaise": 4000,
      "schemeMarginBps": 500,
      "effectiveFrom": "text"
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call GET /tenant-catalog/costs",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/tenant-catalog/costs`

Set purchase cost (owner/manager/accountant only) · contract `tenantCatalog.upsertCost`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `variantId` | uuid | yes |
| `supplierId` | uuid | no |
| `purchaseRatePaise` | integer | yes |
| `landedCostPaise` | integer | yes |
| `ptdPaise` | integer | no |
| `schemeMarginBps` | integer | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/tenant-catalog/costs" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "supplierId": "01a06d4d-b127-7ad7-815f-92d49a8a08b8",
  "purchaseRatePaise": 4000,
  "landedCostPaise": 4000,
  "ptdPaise": 4000,
  "schemeMarginBps": 500
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "supplierId": "01a06d4d-b127-7ad7-815f-92d49a8a08b8",
    "purchaseRatePaise": 4000,
    "landedCostPaise": 4000,
    "ptdPaise": 4000,
    "schemeMarginBps": 500,
    "effectiveFrom": "text"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /tenant-catalog/costs",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/retailers`

Retailers of this distributor (retailer role: only its own, without credit) · contract `retailers.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `q` | string | no |
| `beatId` | uuid | no |
| `activeOnly` | boolean | string | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/retailers?q=campa&beatId=01a06d3e-cfdb-7635-85cb-ee42f205a04f&activeOnly=true&limit=100" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "name": "Sharma Kirana Store",
      "ownerName": "Ramesh Sharma",
      "phone": "+919876543210",
      "altPhone": "+919876543210",
      "address": {
        "line1": "text",
        "line2": "text",
        "landmark": "text",
        "area": "text",
        "city": "text",
        "pincode": "421301"
      },
      "lat": 19.2403,
      "lng": 73.1305,
      "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
      "gstRegType": "unregistered",
      "gstin": "27AAPFU0939F1ZV",
      "stateCode": "27",
      "paymentTerms": "PRE",
      "cashDiscountBps": 500,
      "cashDiscountDays": 7,
      "active": true,
      "code": "R-0001",
      "identityId": "01a06da2-6993-796d-8c2d-0a8fd008d8c8",
      "tier": "A",
      "creditLimitPaise": 2680000,
      "creditLimitBills": 1,
      "creditDays": 7,
      "creditMode": "indicate",
      "onboardedBy": "01a06d12-805d-700e-8caa-848bb93a39a1"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/retailers/{id}`

One retailer (retailer role: only its own, without credit) · contract `retailers.get`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `id` | uuid | yes |

**Example request**

```bash
curl "http://localhost:3003/retailers/01a06d17-0be7-794a-8dab-9b14cf78673b" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "ownerName": "Ramesh Sharma",
    "phone": "+919876543210",
    "altPhone": "+919876543210",
    "address": {
      "line1": "text",
      "line2": "text",
      "landmark": "text",
      "area": "text",
      "city": "text",
      "pincode": "421301"
    },
    "lat": 19.2403,
    "lng": 73.1305,
    "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
    "gstRegType": "unregistered",
    "gstin": "27AAPFU0939F1ZV",
    "stateCode": "27",
    "paymentTerms": "PRE",
    "cashDiscountBps": 500,
    "cashDiscountDays": 7,
    "active": true,
    "code": "R-0001",
    "identityId": "01a06da2-6993-796d-8c2d-0a8fd008d8c8",
    "tier": "A",
    "creditLimitPaise": 2680000,
    "creditLimitBills": 1,
    "creditDays": 7,
    "creditMode": "indicate",
    "onboardedBy": "01a06d12-805d-700e-8caa-848bb93a39a1"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/retailers`

Create or update a retailer; code is server-assigned · contract `retailers.upsert`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `name` | string | yes |
| `ownerName` | string | no |
| `phone` | string | yes |
| `altPhone` | string | no |
| `address` | object | no |
| `lat` | number | no |
| `lng` | number | no |
| `beatId` | uuid | no |
| `gstRegType` | unregistered | regular | composition | no |
| `gstin` | string | no |
| `stateCode` | string | yes |
| `paymentTerms` | PRE | ON | POST_FULFILLMENT | no |
| `cashDiscountBps` | integer | no |
| `cashDiscountDays` | integer | no |
| `active` | boolean | no |
| `tier` | A | B | C | D | no |
| `creditLimitPaise` | integer | no |
| `creditLimitBills` | integer | no |
| `creditDays` | integer | no |
| `creditMode` | indicate | strict | stop | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/retailers" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "name": "Sharma Kirana Store",
  "ownerName": "Ramesh Sharma",
  "phone": "+919876543210",
  "altPhone": "+919876543210",
  "address": {
    "line1": "text",
    "line2": "text",
    "landmark": "text",
    "area": "text",
    "city": "text",
    "pincode": "421301"
  },
  "lat": 19.2403,
  "lng": 73.1305,
  "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
  "gstRegType": "unregistered",
  "gstin": "27AAPFU0939F1ZV",
  "stateCode": "27",
  "paymentTerms": "POST_FULFILLMENT",
  "cashDiscountBps": 0,
  "cashDiscountDays": 0,
  "active": true,
  "tier": "A",
  "creditLimitPaise": 2680000,
  "creditLimitBills": 1,
  "creditDays": 7,
  "creditMode": "indicate"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "ownerName": "Ramesh Sharma",
    "phone": "+919876543210",
    "altPhone": "+919876543210",
    "address": {
      "line1": "text",
      "line2": "text",
      "landmark": "text",
      "area": "text",
      "city": "text",
      "pincode": "421301"
    },
    "lat": 19.2403,
    "lng": 73.1305,
    "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
    "gstRegType": "unregistered",
    "gstin": "27AAPFU0939F1ZV",
    "stateCode": "27",
    "paymentTerms": "PRE",
    "cashDiscountBps": 500,
    "cashDiscountDays": 7,
    "active": true,
    "code": "R-0001",
    "identityId": "01a06da2-6993-796d-8c2d-0a8fd008d8c8",
    "tier": "A",
    "creditLimitPaise": 2680000,
    "creditLimitBills": 1,
    "creditDays": 7,
    "creditMode": "indicate",
    "onboardedBy": "01a06d12-805d-700e-8caa-848bb93a39a1"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/retailers/{id}/credit`

Set tier and credit terms (owner/manager/accountant only) · contract `retailers.setCredit`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `tier` | A | B | C | D | yes |
| `creditLimitPaise` | integer | yes |
| `creditLimitBills` | integer | yes |
| `creditDays` | integer | yes |
| `creditMode` | indicate | strict | stop | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/retailers/01a06d17-0be7-794a-8dab-9b14cf78673b/credit" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "tier": "A",
  "creditLimitPaise": 2680000,
  "creditLimitBills": 1,
  "creditDays": 7,
  "creditMode": "indicate"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "ownerName": "Ramesh Sharma",
    "phone": "+919876543210",
    "altPhone": "+919876543210",
    "address": {
      "line1": "text",
      "line2": "text",
      "landmark": "text",
      "area": "text",
      "city": "text",
      "pincode": "421301"
    },
    "lat": 19.2403,
    "lng": 73.1305,
    "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
    "gstRegType": "unregistered",
    "gstin": "27AAPFU0939F1ZV",
    "stateCode": "27",
    "paymentTerms": "PRE",
    "cashDiscountBps": 500,
    "cashDiscountDays": 7,
    "active": true,
    "code": "R-0001",
    "identityId": "01a06da2-6993-796d-8c2d-0a8fd008d8c8",
    "tier": "A",
    "creditLimitPaise": 2680000,
    "creditLimitBills": 1,
    "creditDays": 7,
    "creditMode": "indicate",
    "onboardedBy": "01a06d12-805d-700e-8caa-848bb93a39a1"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /retailers/{id}/credit",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/retailers/{id}/link`

Link the retailer to its global identity by phone · contract `retailers.linkIdentity`

**Roles:** owner, manager

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `phone` | string | yes |
| `shopName` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/retailers/01a06d17-0be7-794a-8dab-9b14cf78673b/link" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "phone": "+919876543210",
  "shopName": "Campa Cola 750 ml"
}
```

**Success response** — `200`

```json
{
  "link": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "identityId": "01a06da2-6993-796d-8c2d-0a8fd008d8c8",
    "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
    "linkedBy": "rep_onboarding",
    "status": "pending"
  },
  "identityCreated": true
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /retailers/{id}/link",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/beats`

Beats of this distributor · contract `retailers.beats.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `activeOnly` | boolean | string | no |

**Example request**

```bash
curl "http://localhost:3003/beats?activeOnly=true" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "name": "Sharma Kirana Store",
      "area": "text",
      "visitDays": [
        1
      ],
      "active": true
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/beats`

Create or update a beat · contract `retailers.beats.upsert`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `name` | string | yes |
| `area` | string | no |
| `visitDays` | integer[] | no |
| `active` | boolean | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/beats" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "name": "Sharma Kirana Store",
  "area": "text",
  "visitDays": [],
  "active": true
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "area": "text",
    "visitDays": [
      1
    ],
    "active": true
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/beats/{id}/assign`

Assign a salesperson to a beat for a date range · contract `retailers.beats.assign`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `assignmentId` | uuid | yes |
| `userId` | uuid | yes |
| `validFrom` | date | yes |
| `validTo` | date | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/beats/01a06d17-0be7-794a-8dab-9b14cf78673b/assign" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "assignmentId": "01a06d2d-36fe-7b62-8745-2fe9da22f8b6",
  "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
  "validFrom": "2026-09-04",
  "validTo": "2026-09-04"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
    "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
    "validFrom": "2026-09-04",
    "validTo": "2026-09-04"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/visits`

Record a shop visit (own) · contract `retailers.visits.record`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `retailerId` | uuid | yes |
| `beatId` | uuid | no |
| `startedAt` | datetime | yes |
| `endedAt` | datetime | no |
| `outcome` | ordered | no_order | closed | not_found | payment_only | yes |
| `reason` | string | no |
| `lat` | number | no |
| `lng` | number | no |
| `note` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/visits" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
  "startedAt": "2026-09-04T10:30:00.000Z",
  "endedAt": null,
  "outcome": "ordered",
  "reason": null,
  "lat": 19.2403,
  "lng": 73.1305,
  "note": null
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
    "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
    "startedAt": "2026-09-04T10:30:00.000Z",
    "endedAt": null,
    "outcome": "ordered",
    "reason": null,
    "lat": 19.2403,
    "lng": 73.1305,
    "note": null
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/visits`

Visits by retailer / rep / period · contract `retailers.visits.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `retailerId` | uuid | no |
| `userId` | uuid | no |
| `from` | datetime | no |
| `to` | datetime | no |
| `limit` | integer | no |

**Example request**

```bash
curl "http://localhost:3003/visits?retailerId=01a06dbc-35ed-7760-86f2-6c701c68f2dd&userId=01a06d02-3731-7b6d-8798-5c7c2b7bf340&from=2026-09-04&to=2026-09-04&limit=100" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
      "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
      "beatId": "01a06d3e-cfdb-7635-85cb-ee42f205a04f",
      "startedAt": "2026-09-04T10:30:00.000Z",
      "endedAt": null,
      "outcome": "ordered",
      "reason": null,
      "lat": 19.2403,
      "lng": 73.1305,
      "note": null
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/pricing/price-lists`

Price lists with their rates · contract `pricing.priceLists.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `activeOnly` | boolean | string | no |
| `withItems` | boolean | string | no |

**Example request**

```bash
curl "http://localhost:3003/pricing/price-lists?activeOnly=false&withItems=true" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "name": "Sharma Kirana Store",
      "tier": "A",
      "isDefault": true,
      "validFrom": "2026-09-04",
      "validTo": "2026-09-04",
      "active": true,
      "items": [
        {
          "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
          "priceListId": "01a06d4c-b430-7575-8033-448fb7c3cd1d",
          "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
          "ratePaise": 4000,
          "inclusiveOfGst": true
        }
      ]
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/price-lists`

Create or update a price list · contract `pricing.priceLists.upsert`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `name` | string | yes |
| `tier` | A | B | C | D | no |
| `isDefault` | boolean | no |
| `validFrom` | string | no |
| `validTo` | string | no |
| `active` | boolean | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/price-lists" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "name": "Sharma Kirana Store",
  "tier": "A",
  "isDefault": false,
  "validFrom": "2026-09-04",
  "validTo": "2026-09-04",
  "active": true
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "tier": "A",
    "isDefault": true,
    "validFrom": "2026-09-04",
    "validTo": "2026-09-04",
    "active": true,
    "items": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "priceListId": "01a06d4c-b430-7575-8033-448fb7c3cd1d",
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "ratePaise": 4000,
        "inclusiveOfGst": true
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /pricing/price-lists",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/price-lists/{priceListId}/items`

Set rates on a price list (upsert per variant) · contract `pricing.priceLists.setItems`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `priceListId` | uuid | yes |
| `items` | object[] | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/price-lists/01a06d4c-b430-7575-8033-448fb7c3cd1d/items" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "priceListId": "01a06d4c-b430-7575-8033-448fb7c3cd1d",
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "ratePaise": 4000,
      "inclusiveOfGst": false
    }
  ]
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "tier": "A",
    "isDefault": true,
    "validFrom": "2026-09-04",
    "validTo": "2026-09-04",
    "active": true,
    "items": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "priceListId": "01a06d4c-b430-7575-8033-448fb7c3cd1d",
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "ratePaise": 4000,
        "inclusiveOfGst": true
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /pricing/price-lists/{priceListId}/items",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/pricing/overrides`

Retailer-specific rates · contract `pricing.overrides.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `retailerId` | uuid | no |
| `variantId` | uuid | no |
| `on` | string | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/pricing/overrides?retailerId=01a06dbc-35ed-7760-86f2-6c701c68f2dd&variantId=01a06df0-2faf-79a2-8456-92042e49f147&on=2026-09-04&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "ratePaise": 4000,
      "final": true,
      "validFrom": "2026-09-04",
      "validTo": "2026-09-04",
      "approvedBy": "01a06de1-6afb-791d-851f-c61424b0723f",
      "note": null
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/overrides`

Set a retailer-specific rate · contract `pricing.overrides.upsert`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `retailerId` | uuid | yes |
| `variantId` | uuid | yes |
| `ratePaise` | integer | yes |
| `final` | boolean | no |
| `validFrom` | string | no |
| `validTo` | string | no |
| `note` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/overrides" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "ratePaise": 4000,
  "final": false,
  "validFrom": "2026-09-04",
  "validTo": "2026-09-04",
  "note": null
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
    "ratePaise": 4000,
    "final": true,
    "validFrom": "2026-09-04",
    "validTo": "2026-09-04",
    "approvedBy": "01a06de1-6afb-791d-851f-c61424b0723f",
    "note": null
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /pricing/overrides",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/pricing/schemes`

Schemes (the single table the engine reads) · contract `pricing.schemes.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `activeOnly` | boolean | string | no |
| `on` | string | no |
| `brandId` | uuid | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/pricing/schemes?activeOnly=true&on=2026-09-04&brandId=01a06db4-0f6a-71b5-8f6e-febe6219c69d&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "name": "Sharma Kirana Store",
      "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
      "scope": {
        "all": true,
        "brandIds": [
          "01a06db4-0f6a-71b5-8f6e-febe6219c69d"
        ],
        "categories": [
          "text"
        ],
        "variantIds": [
          "01a06df0-2faf-79a2-8456-92042e49f147"
        ]
      },
      "triggerKind": "qty",
      "triggerMin": 1,
      "triggerUnit": "pcs",
      "slabs": [
        {
          "min": 1,
          "value": 1,
          "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
        }
      ],
      "rewardKind": "free_qty",
      "rewardValue": 1,
      "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4",
      "applicability": {
        "tiers": [
          "A"
        ],
        "retailerIds": [
          "01a06dbc-35ed-7760-86f2-6c701c68f2dd"
        ],
        "beatIds": [
          "01a06d3e-cfdb-7635-85cb-ee42f205a04f"
        ]
      },
      "validFrom": "2026-09-04",
      "validTo": "2026-09-04",
      "stackable": true,
      "final": true,
      "gstOnFreeGoods": true,
      "pricingDateMode": "order",
      "version": 1,
      "fundingSource": "company",
      "claimable": true,
      "claimWindowDays": 7,
      "sourceRef": "text",
      "active": true
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/schemes`

Create or update a scheme; a change to its economics bumps the version · contract `pricing.schemes.upsert`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `name` | string | yes |
| `brandId` | uuid | no |
| `scope` | object | yes |
| `triggerKind` | qty | value | mix | yes |
| `triggerMin` | integer | yes |
| `triggerUnit` | pcs | case | inr | yes |
| `slabs` | object[] | no |
| `rewardKind` | free_qty | line_pct | order_pct | cash_discount_pct | net_scheme_amount | yes |
| `rewardValue` | integer | yes |
| `freeVariantId` | uuid | no |
| `applicability` | object | no |
| `validFrom` | string | yes |
| `validTo` | string | yes |
| `stackable` | boolean | no |
| `final` | boolean | no |
| `fundingSource` | company | distributor | no |
| `claimable` | boolean | no |
| `claimWindowDays` | integer | no |
| `gstOnFreeGoods` | boolean | no |
| `pricingDateMode` | order | delivery | no |
| `sourceRef` | string | no |
| `active` | boolean | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/schemes" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "name": "Sharma Kirana Store",
  "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
  "scope": {
    "all": true,
    "brandIds": [
      "01a06db4-0f6a-71b5-8f6e-febe6219c69d"
    ],
    "categories": [
      "text"
    ],
    "variantIds": [
      "01a06df0-2faf-79a2-8456-92042e49f147"
    ]
  },
  "triggerKind": "qty",
  "triggerMin": 1,
  "triggerUnit": "pcs",
  "slabs": [
    {
      "min": 1,
      "value": 1,
      "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
    }
  ],
  "rewardKind": "free_qty",
  "rewardValue": 1,
  "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4",
  "applicability": {},
  "validFrom": "2026-09-04",
  "validTo": "2026-09-04",
  "stackable": true,
  "final": false,
  "fundingSource": "company",
  "claimable": false,
  "claimWindowDays": 7,
  "gstOnFreeGoods": false,
  "pricingDateMode": "order",
  "sourceRef": "text",
  "active": true
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "name": "Sharma Kirana Store",
    "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
    "scope": {
      "all": true,
      "brandIds": [
        "01a06db4-0f6a-71b5-8f6e-febe6219c69d"
      ],
      "categories": [
        "text"
      ],
      "variantIds": [
        "01a06df0-2faf-79a2-8456-92042e49f147"
      ]
    },
    "triggerKind": "qty",
    "triggerMin": 1,
    "triggerUnit": "pcs",
    "slabs": [
      {
        "min": 1,
        "value": 1,
        "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
      }
    ],
    "rewardKind": "free_qty",
    "rewardValue": 1,
    "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4",
    "applicability": {
      "tiers": [
        "A"
      ],
      "retailerIds": [
        "01a06dbc-35ed-7760-86f2-6c701c68f2dd"
      ],
      "beatIds": [
        "01a06d3e-cfdb-7635-85cb-ee42f205a04f"
      ]
    },
    "validFrom": "2026-09-04",
    "validTo": "2026-09-04",
    "stackable": true,
    "final": true,
    "gstOnFreeGoods": true,
    "pricingDateMode": "order",
    "version": 1,
    "fundingSource": "company",
    "claimable": true,
    "claimWindowDays": 7,
    "sourceRef": "text",
    "active": true
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /pricing/schemes",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/quote`

Price an order with the pure engine (no side effects) · contract `pricing.quote`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Request body**

| Field | Type | Required |
|---|---|---|
| `retailerId` | uuid | yes |
| `pricingDate` | string | no |
| `deliveryDate` | string | no |
| `orderId` | uuid | no |
| `lines` | object[] | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/quote" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "pricingDate": "2026-09-04",
  "deliveryDate": "2026-09-04",
  "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
  "lines": [
    {
      "lineId": "01a06db2-da39-7334-863c-3d85d8b13619",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "qtyPcs": 24
    }
  ]
}
```

**Success response** — `200`

```json
{
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "pricingDate": "2026-09-04",
  "lines": [
    {
      "lineId": "01a06db2-da39-7334-863c-3d85d8b13619",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "qtyPcs": 24,
      "caseSize": 24,
      "listRatePaise": 4000,
      "ratePaise": 4000,
      "grossPaise": 2680000,
      "discountPaise": 12000,
      "bargainPaise": 4000,
      "freeQtyPcs": 24,
      "freeItems": [
        {
          "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
          "qtyPcs": 24,
          "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
          "version": 1
        }
      ],
      "appliedRules": [
        {
          "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
          "version": 1,
          "kind": "override",
          "rewardKind": "free_qty",
          "amountPaise": 4000,
          "freeQty": 24,
          "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
        }
      ],
      "lineNetPaise": 2680000
    }
  ],
  "orderRules": [
    {
      "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
      "version": 1,
      "kind": "override",
      "rewardKind": "free_qty",
      "amountPaise": 4000,
      "freeQty": 24,
      "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
    }
  ],
  "cashDiscountBps": 500,
  "cashDiscountPaise": 12000,
  "totals": {
    "grossPaise": 2680000,
    "discountPaise": 12000,
    "bargainPaise": 4000,
    "netPaise": 2680000
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/bargains`

Ask for a lower rate; auto-approved within the rep bound · contract `pricing.bargains.request`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `retailerId` | uuid | yes |
| `variantId` | uuid | yes |
| `askedRatePaise` | integer | yes |
| `qtyPcs` | integer | no |
| `orderId` | uuid | no |
| `note` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/bargains" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "askedRatePaise": 4000,
  "qtyPcs": 24,
  "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
  "note": "Confirmed on phone with the shopkeeper"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
    "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
    "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
    "listRatePaise": 4000,
    "askedRatePaise": 4000,
    "approvedRatePaise": 4000,
    "status": "requested",
    "decidedBy": null,
    "decidedAt": null,
    "expiresAt": "2026-09-04T10:30:00.000Z",
    "note": null,
    "createdAt": "2026-09-04T10:30:00.000Z"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/bargains/{id}/decide`

Approve or reject a bargain (back office) · contract `pricing.bargains.decide`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `decision` | approve | reject | yes |
| `approvedRatePaise` | integer | no |
| `note` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/bargains/01a06d17-0be7-794a-8dab-9b14cf78673b/decide" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "decision": "approve",
  "approvedRatePaise": 4000,
  "note": "Confirmed on phone with the shopkeeper"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
    "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
    "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
    "listRatePaise": 4000,
    "askedRatePaise": 4000,
    "approvedRatePaise": 4000,
    "status": "requested",
    "decidedBy": null,
    "decidedAt": null,
    "expiresAt": "2026-09-04T10:30:00.000Z",
    "note": null,
    "createdAt": "2026-09-04T10:30:00.000Z"
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /pricing/bargains/{id}/decide",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/pricing/bargains`

Bargain requests · contract `pricing.bargains.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `status` | requested | auto_approved | approved | rejected | expired | no |
| `retailerId` | uuid | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/pricing/bargains?status=requested&retailerId=01a06dbc-35ed-7760-86f2-6c701c68f2dd&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
      "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
      "listRatePaise": 4000,
      "askedRatePaise": 4000,
      "approvedRatePaise": 4000,
      "status": "requested",
      "decidedBy": null,
      "decidedAt": null,
      "expiresAt": "2026-09-04T10:30:00.000Z",
      "note": null,
      "createdAt": "2026-09-04T10:30:00.000Z"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/pricing/bounds`

How far a rep may discount without asking (owner only) · contract `pricing.bounds.set`

**Roles:** owner

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `userId` | uuid | yes |
| `brandId` | uuid | no |
| `maxDiscountBps` | integer | yes |
| `maxOrderDiscountPaise` | integer | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/pricing/bounds" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
  "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
  "maxDiscountBps": 500,
  "maxOrderDiscountPaise": 12000
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "userId": "01a06d02-3731-7b6d-8798-5c7c2b7bf340",
    "brandId": "01a06db4-0f6a-71b5-8f6e-febe6219c69d",
    "maxDiscountBps": 500,
    "maxOrderDiscountPaise": 12000
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /pricing/bounds",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/inventory/locations`

Stock locations: godown, vehicles, damaged bin · contract `inventory.locations.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `kind` | warehouse | vehicle | damaged | in_transit | customer | no |
| `activeOnly` | boolean | string | no |

**Example request**

```bash
curl "http://localhost:3003/inventory/locations?kind=warehouse&activeOnly=true" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "kind": "warehouse",
      "name": "Sharma Kirana Store",
      "vehicleId": "01a06d9c-98d8-7445-8ff2-d0ee7a6163da",
      "negativeAllowed": true,
      "active": true
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/inventory/locations`

Create or update a stock location · contract `inventory.locations.upsert`

**Roles:** owner, manager, accountant, warehouse

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `kind` | warehouse | vehicle | damaged | in_transit | customer | yes |
| `name` | string | yes |
| `vehicleId` | uuid | no |
| `negativeAllowed` | boolean | no |
| `active` | boolean | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/inventory/locations" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "kind": "warehouse",
  "name": "Sharma Kirana Store",
  "vehicleId": "01a06d9c-98d8-7445-8ff2-d0ee7a6163da",
  "negativeAllowed": false,
  "active": true
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "kind": "warehouse",
    "name": "Sharma Kirana Store",
    "vehicleId": "01a06d9c-98d8-7445-8ff2-d0ee7a6163da",
    "negativeAllowed": true,
    "active": true
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /inventory/locations",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/inventory/sellable`

Available-to-promise stock (the only stock surface for reps and retailers) · contract `inventory.stock.sellable`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `variantId` | uuid | no |
| `locationId` | uuid | no |
| `q` | string | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/inventory/sellable?variantId=01a06df0-2faf-79a2-8456-92042e49f147&locationId=01a06d18-e60a-7abc-87f8-910189e5f14c&q=campa&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
      "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
      "batchNo": "SO-0042",
      "mrpPaise": 4000,
      "expiryDate": "2026-09-04",
      "available": 1,
      "variantName": "Campa Cola 750 ml",
      "productName": "Campa Cola 750 ml",
      "brandName": "Campa Cola 750 ml"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/inventory/balances`

On-hand and reserved per lot per location (stock keepers only) · contract `inventory.stock.balances`

**Roles:** owner, manager, accountant, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `variantId` | uuid | no |
| `locationId` | uuid | no |
| `lotId` | uuid | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/inventory/balances?variantId=01a06df0-2faf-79a2-8456-92042e49f147&locationId=01a06d18-e60a-7abc-87f8-910189e5f14c&lotId=01a06dc6-1c19-701b-8a21-982c1b2f8bc3&limit=200" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
      "batchNo": "SO-0042",
      "mrpPaise": 4000,
      "expiryDate": "2026-09-04",
      "onHand": 1,
      "reserved": 1,
      "version": 1,
      "variantName": "Campa Cola 750 ml",
      "productName": "Campa Cola 750 ml"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call GET /inventory/balances",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/inventory/adjustments`

Post an opening/adjustment/damage/expiry/cycle-count ledger row · contract `inventory.stock.adjust`

**Roles:** owner, manager, accountant, warehouse

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `lotId` | uuid | yes |
| `locationId` | uuid | yes |
| `qtyDelta` | integer | yes |
| `reason` | adjustment | damage | expiry_writeoff | opening | cycle_count | yes |
| `note` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/inventory/adjustments" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
  "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
  "qtyDelta": 24,
  "reason": "adjustment",
  "note": "Confirmed on phone with the shopkeeper"
}
```

**Success response** — `200`

```json
{
  "entry": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "occurredAt": "2026-09-04T10:30:00.000Z",
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
    "qtyDelta": 24,
    "reason": "opening",
    "refType": "text",
    "refId": "01a06dee-c83b-7a4d-837e-fb9a8786f9b5",
    "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
    "note": null
  },
  "balance": {
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
    "onHand": 1,
    "reserved": 1,
    "version": 1
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /inventory/adjustments",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/inventory/transfers`

Move pieces of a lot between locations · contract `inventory.stock.transfer`

**Roles:** owner, manager, accountant, warehouse

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `lotId` | uuid | yes |
| `fromLocationId` | uuid | yes |
| `toLocationId` | uuid | yes |
| `qtyPcs` | integer | yes |
| `note` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/inventory/transfers" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
  "fromLocationId": "01a06d54-8962-79d7-8a1e-b81195580544",
  "toLocationId": "01a06ddc-ab2d-7b02-8439-c878602510b2",
  "qtyPcs": 24,
  "note": "Confirmed on phone with the shopkeeper"
}
```

**Success response** — `200`

```json
{
  "out": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "occurredAt": "2026-09-04T10:30:00.000Z",
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
    "qtyDelta": 24,
    "reason": "opening",
    "refType": "text",
    "refId": "01a06dee-c83b-7a4d-837e-fb9a8786f9b5",
    "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
    "note": null
  },
  "in": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "occurredAt": "2026-09-04T10:30:00.000Z",
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
    "qtyDelta": 24,
    "reason": "opening",
    "refType": "text",
    "refId": "01a06dee-c83b-7a4d-837e-fb9a8786f9b5",
    "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
    "note": null
  },
  "from": {
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
    "onHand": 1,
    "reserved": 1,
    "version": 1
  },
  "to": {
    "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
    "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
    "onHand": 1,
    "reserved": 1,
    "version": 1
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /inventory/transfers",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/inventory/ledger`

Append-only stock ledger · contract `inventory.stock.ledger`

**Roles:** owner, manager, accountant, warehouse, delivery

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `lotId` | uuid | no |
| `locationId` | uuid | no |
| `from` | datetime | no |
| `to` | datetime | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/inventory/ledger?lotId=01a06dc6-1c19-701b-8a21-982c1b2f8bc3&locationId=01a06d18-e60a-7abc-87f8-910189e5f14c&from=2026-09-04&to=2026-09-04&limit=100" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "occurredAt": "2026-09-04T10:30:00.000Z",
      "lotId": "01a06dc6-1c19-701b-8a21-982c1b2f8bc3",
      "locationId": "01a06d18-e60a-7abc-87f8-910189e5f14c",
      "qtyDelta": 24,
      "reason": "opening",
      "refType": "text",
      "refId": "01a06dee-c83b-7a4d-837e-fb9a8786f9b5",
      "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
      "note": null
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call GET /inventory/ledger",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/inventory/lots`

Find or create a lot (variant + batch + MRP) · contract `inventory.lots.upsert`

**Roles:** owner, manager, accountant, warehouse

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `variantId` | uuid | yes |
| `batchNo` | string | no |
| `mrpPaise` | integer | yes |
| `mfgDate` | date | no |
| `expiryDate` | date | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/inventory/lots" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
  "batchNo": "",
  "mrpPaise": 4000,
  "mfgDate": "2026-09-04",
  "expiryDate": "2026-09-04"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
    "batchNo": "SO-0042",
    "mrpPaise": 4000,
    "mfgDate": "2026-09-04",
    "expiryDate": "2026-09-04"
  },
  "created": true
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /inventory/lots",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/orders`

Create a priced draft order · contract `orders.create`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `retailerId` | uuid | yes |
| `source` | salesperson | retailer_app | van_sale | phone | whatsapp | yes |
| `pricingDateMode` | order | delivery | no |
| `paymentTerms` | PRE | ON | POST_FULFILLMENT | no |
| `fulfilFromLocationId` | uuid | no |
| `expectedDeliveryDate` | string | no |
| `note` | string | no |
| `deviceId` | string | no |
| `lines` | object[] | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/orders" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "source": "salesperson",
  "pricingDateMode": "order",
  "paymentTerms": "PRE",
  "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
  "expectedDeliveryDate": "2026-09-04",
  "note": null,
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
  "lines": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "enteredQty": 24,
      "enteredUnit": "piece"
    }
  ]
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/orders/{id}/lines`

Replace the lines of a draft and re-price it · contract `orders.setLines`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `deviceId` | string | no |
| `lines` | object[] | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/orders/01a06d17-0be7-794a-8dab-9b14cf78673b/lines" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
  "lines": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "enteredQty": 24,
      "enteredUnit": "piece"
    }
  ]
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/orders/repeat-last`

Draft a repeat of the retailer's last order, re-priced today · contract `orders.repeatLast`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `retailerId` | uuid | yes |
| `source` | salesperson | retailer_app | van_sale | phone | whatsapp | no |
| `expectedDeliveryDate` | string | no |
| `deviceId` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/orders/repeat-last" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
  "source": "salesperson",
  "expectedDeliveryDate": "2026-09-04",
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/orders/{id}/submit`

Submit: assign the order number, raise approvals or auto-confirm · contract `orders.submit`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `deviceId` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/orders/01a06d17-0be7-794a-8dab-9b14cf78673b/submit" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/orders/{id}/confirm`

Confirm and reserve stock (back office) · contract `orders.confirm`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `deviceId` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/orders/01a06d17-0be7-794a-8dab-9b14cf78673b/confirm" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  },
  "shortages": [
    {
      "lineId": "01a06db2-da39-7334-863c-3d85d8b13619",
      "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
      "requestedPcs": 24,
      "reservedPcs": 24,
      "shortQtyPcs": 24
    }
  ]
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /orders/{id}/confirm",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/orders/{id}/cancel`

Cancel an order and release its reservations · contract `orders.cancel`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `reason` | string | yes |
| `deviceId` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/orders/01a06d17-0be7-794a-8dab-9b14cf78673b/cancel" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "reason": "Confirmed on phone with the shopkeeper",
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/orders/{id}`

One order with lines, transitions and approvals · contract `orders.get`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `id` | uuid | yes |

**Example request**

```bash
curl "http://localhost:3003/orders/01a06d17-0be7-794a-8dab-9b14cf78673b" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/orders`

Orders (a retailer sees only its own) · contract `orders.list`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery, retailer

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `state` | draft | submitted | confirmed | picking | packed | dispatched | delivered | partially_delivered | closed | cancelled | no |
| `retailerId` | uuid | no |
| `salespersonId` | uuid | no |
| `from` | string | no |
| `to` | string | no |
| `q` | string | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/orders?state=draft&retailerId=01a06dbc-35ed-7760-86f2-6c701c68f2dd&salespersonId=01a06d29-a152-76c4-87b4-301e496c0602&from=2026-09-04&to=2026-09-04&q=campa&limit=50" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "orderNo": "SO-0042",
      "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
      "state": "draft",
      "source": "salesperson",
      "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
      "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
      "pricingDateMode": "order",
      "paymentTerms": "PRE",
      "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
      "externalRef": null,
      "subtotalPaise": 2680000,
      "discountPaise": 12000,
      "taxPaise": 12000,
      "roundOffPaise": 12000,
      "totalPaise": 2680000,
      "approvalFlags": [
        "text"
      ],
      "expectedDeliveryDate": "2026-09-04",
      "note": null,
      "submittedAt": "2026-09-04T10:30:00.000Z",
      "confirmedAt": "2026-09-04T10:30:00.000Z",
      "cancelledAt": null,
      "cancelReason": null,
      "createdAt": "2026-09-04T10:30:00.000Z"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### GET `/approvals`

Approval queue (back office) · contract `orders.approvals.list`

**Roles:** owner, manager, accountant

**Query / path parameters**

| Field | Type | Required |
|---|---|---|
| `status` | pending | approved | rejected | expired | no |
| `orderId` | uuid | no |
| `kind` | credit_limit | bargain | below_floor | return | scheme_override | manual_price | no |
| `limit` | integer | no |
| `cursor` | string | no |

**Example request**

```bash
curl "http://localhost:3003/approvals?status=pending&orderId=01a06d67-52a6-70c4-8d0b-06d5bc6a56ca&kind=credit_limit&limit=50" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…"
```

**Success response** — `200`

```json
{
  "items": [
    {
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "kind": "credit_limit",
      "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
      "entityType": "text",
      "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
      "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
      "status": "pending",
      "payload": {
        "line1": "12 Station Road",
        "city": "Kalyan West",
        "pincode": "421301"
      },
      "decidedBy": null,
      "decidedAt": null,
      "decisionNote": "Confirmed on phone with the shopkeeper",
      "createdAt": "2026-09-04T10:30:00.000Z"
    }
  ],
  "nextCursor": null
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call GET /approvals",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "limit"
        ],
        "message": "Too big: expected number to be <=500"
      }
    ]
  }
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/approvals/{id}/decide`

Approve or reject; the last approval approved confirms the order · contract `orders.approvals.decide`

**Roles:** owner, manager, accountant

**Request body**

| Field | Type | Required |
|---|---|---|
| `idempotencyKey` | string | yes |
| `id` | uuid | yes |
| `decision` | approve | reject | yes |
| `note` | string | no |
| `deviceId` | string | no |

**Example request**

```bash
curl -X POST "http://localhost:3003/approvals/01a06d17-0be7-794a-8dab-9b14cf78673b/decide" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "idempotencyKey": "a3d7c1e2-…-one-key-per-tap",
  "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
  "decision": "approve",
  "note": "Confirmed on phone with the shopkeeper",
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb"
}
```

**Success response** — `200`

```json
{
  "item": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "kind": "credit_limit",
    "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
    "entityType": "text",
    "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
    "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
    "status": "pending",
    "payload": {
      "line1": "12 Station Road",
      "city": "Kalyan West",
      "pincode": "421301"
    },
    "decidedBy": null,
    "decidedAt": null,
    "decisionNote": "Confirmed on phone with the shopkeeper",
    "createdAt": "2026-09-04T10:30:00.000Z"
  },
  "order": {
    "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
    "orderNo": "SO-0042",
    "retailerId": "01a06dbc-35ed-7760-86f2-6c701c68f2dd",
    "state": "draft",
    "source": "salesperson",
    "createdBy": "01a06d85-e090-73c2-8418-e04636293a36",
    "salespersonId": "01a06d29-a152-76c4-87b4-301e496c0602",
    "pricingDateMode": "order",
    "paymentTerms": "PRE",
    "fulfilFromLocationId": "01a06dc8-c767-7943-8fdd-07b3dd890c64",
    "externalRef": null,
    "subtotalPaise": 2680000,
    "discountPaise": 12000,
    "taxPaise": 12000,
    "roundOffPaise": 12000,
    "totalPaise": 2680000,
    "approvalFlags": [
      "text"
    ],
    "expectedDeliveryDate": "2026-09-04",
    "note": null,
    "submittedAt": "2026-09-04T10:30:00.000Z",
    "confirmedAt": "2026-09-04T10:30:00.000Z",
    "cancelledAt": null,
    "cancelReason": null,
    "createdAt": "2026-09-04T10:30:00.000Z",
    "lines": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "lineNo": 1,
        "variantId": "01a06df0-2faf-79a2-8456-92042e49f147",
        "enteredQty": 24,
        "enteredUnit": "piece",
        "packSizeAtEntry": 24,
        "qtyPcs": 24,
        "freeQtyPcs": 24,
        "pickedQtyPcs": 24,
        "deliveredQtyPcs": 24,
        "listRatePaise": 4000,
        "ratePaise": 4000,
        "discountBps": 500,
        "discountPaise": 12000,
        "gstBps": 500,
        "taxPaise": 12000,
        "lineTotalPaise": 2680000,
        "appliedRules": [
          {
            "ruleId": "01a06d75-56b9-79cb-841d-eb65d18dc3e8",
            "version": 1,
            "kind": "override",
            "rewardKind": "free_qty",
            "amountPaise": 4000,
            "freeQty": 24,
            "freeVariantId": "01a06d92-594e-7ffa-82f0-6474461e10c4"
          }
        ],
        "priceLocked": true
      }
    ],
    "transitions": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "fromState": "draft",
        "toState": "draft",
        "event": "submit",
        "actorId": "01a06d81-8fbe-749f-8c31-d150f1cb90ee",
        "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
        "reason": null,
        "occurredAt": "2026-09-04T10:30:00.000Z"
      }
    ],
    "approvals": [
      {
        "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
        "kind": "credit_limit",
        "orderId": "01a06d67-52a6-70c4-8d0b-06d5bc6a56ca",
        "entityType": "text",
        "entityId": "01a06d21-94b0-7dc3-8aad-22f00b372c7e",
        "requestedBy": "01a06ded-7766-7cf6-8494-dff03b8c3334",
        "status": "pending",
        "payload": {
          "line1": "12 Station Road",
          "city": "Kalyan West",
          "pincode": "421301"
        },
        "decidedBy": null,
        "decidedAt": null,
        "decisionNote": "Confirmed on phone with the shopkeeper",
        "createdAt": "2026-09-04T10:30:00.000Z"
      }
    ]
  }
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "the salesperson role may not call POST /approvals/{id}/decide",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

404 — no such row in this tenant
```json
{
  "defined": false,
  "code": "NOT_FOUND",
  "status": 404,
  "message": "not found"
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

### POST `/sync/upload`

Offline write batch (never 4xx; rejections are 2xx + sync_errors) · contract `sync.upload`

**Roles:** owner, manager, accountant, salesperson, warehouse, delivery

**Request body**

| Field | Type | Required |
|---|---|---|
| `protocol` | integer | no |
| `deviceId` | string | yes |
| `ops` | object[] | yes |

**Example request**

```bash
curl -X POST "http://localhost:3003/sync/upload" \
  -H "Authorization: Bearer eyJhbGciOiJFZERTQSIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIwMWEwNmQ4Zi04NzY1LTc0MzItODAwOS1hYmNkZWYwMTIzNDUi…" \
  -H "content-type: application/json" \
  -d @request.json
```

request.json
```json
{
  "protocol": 1,
  "deviceId": "01a06d91-0ce4-73b4-8bda-89cbb975a4bb",
  "ops": [
    {
      "opId": "op-000123",
      "op": "PUT",
      "table": "sales_orders",
      "id": "01a06d17-0be7-794a-8dab-9b14cf78673b",
      "data": {
        "line1": "12 Station Road",
        "city": "Kalyan West",
        "pincode": "421301"
      },
      "clientTime": "text"
    }
  ]
}
```

**Success response** — `200`

```json
{
  "accepted": 1,
  "replayed": 1,
  "rejected": [
    {
      "opId": "op-000123",
      "table": "sales_orders",
      "rowId": "01a06de8-3cac-72d1-85d1-76dbdb3dbb96",
      "code": "R-0001",
      "messageHi": "Confirmed on phone with the shopkeeper",
      "messageEn": "Confirmed on phone with the shopkeeper"
    }
  ],
  "warnings": [
    {
      "opId": "op-000123",
      "code": "R-0001",
      "messageEn": "Confirmed on phone with the shopkeeper"
    }
  ],
  "upgradeRequired": true
}
```

**Failure responses**

401 — missing, malformed or expired access token
```json
{
  "statusCode": 401,
  "message": "sign in required",
  "error": "Unauthorized"
}
```

403 — role not allowed
```json
{
  "statusCode": 403,
  "message": "sales-service does not serve the owner role",
  "error": "Forbidden"
}
```

400 — input validation
```json
{
  "defined": false,
  "code": "BAD_REQUEST",
  "status": 400,
  "message": "Input validation failed",
  "data": {
    "issues": [
      {
        "path": [
          "phone"
        ],
        "message": "Indian mobile in E.164, e.g. +919876543210"
      }
    ]
  }
}
```

409 — same idempotencyKey reused with a different payload (a retry with the same payload returns the stored 200)
```json
{
  "defined": false,
  "code": "CONFLICT",
  "status": 409,
  "message": "idempotencyKey was already used with a different request"
}
```

503 — database not configured / unreachable
```json
{
  "defined": false,
  "code": "SERVICE_UNAVAILABLE",
  "status": 503,
  "message": "database is not configured"
}
```

## Permission matrix

Who may call what, from the `PERMISSIONS` table in `@dos/contracts` narrowed to the roles this service serves — ✓ = allowed, – = refused (either the matrix excludes the role, or this service does not serve it). O owner · M manager · A accountant · S salesperson · W warehouse · D delivery · R retailer.

| Procedure | O | M | A | S | W | D | R |
|---|---|---|---|---|---|---|---|
| `health.ping` | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| `tenancy.me` | – | – | – | ✓ | – | – | – |
| `tenancy.staff.list` | – | – | – | – | – | – | – |
| `tenancy.staff.create` | – | – | – | – | – | – | – |
| `tenancy.staff.setPassword` | – | – | – | – | – | – | – |
| `tenancy.staff.setStatus` | – | – | – | – | – | – | – |
| `catalog.search` | – | – | – | ✓ | – | – | – |
| `catalog.manufacturers` | – | – | – | ✓ | – | – | – |
| `catalog.propose` | – | – | – | ✓ | – | – | – |
| `tenantCatalog.list` | – | – | – | ✓ | – | – | – |
| `tenantCatalog.upsertListing` | – | – | – | – | – | – | – |
| `tenantCatalog.suppliers` | – | – | – | ✓ | – | – | – |
| `tenantCatalog.upsertSupplier` | – | – | – | – | – | – | – |
| `tenantCatalog.costs` | – | – | – | – | – | – | – |
| `tenantCatalog.upsertCost` | – | – | – | – | – | – | – |
| `retailers.list` | – | – | – | ✓ | – | – | – |
| `retailers.get` | – | – | – | ✓ | – | – | – |
| `retailers.upsert` | – | – | – | ✓ | – | – | – |
| `retailers.setCredit` | – | – | – | – | – | – | – |
| `retailers.linkIdentity` | – | – | – | – | – | – | – |
| `retailers.beats.list` | – | – | – | ✓ | – | – | – |
| `retailers.beats.upsert` | – | – | – | ✓ | – | – | – |
| `retailers.beats.assign` | – | – | – | ✓ | – | – | – |
| `retailers.visits.record` | – | – | – | ✓ | – | – | – |
| `retailers.visits.list` | – | – | – | ✓ | – | – | – |
| `pricing.priceLists.list` | – | – | – | ✓ | – | – | – |
| `pricing.priceLists.upsert` | – | – | – | – | – | – | – |
| `pricing.priceLists.setItems` | – | – | – | – | – | – | – |
| `pricing.overrides.list` | – | – | – | ✓ | – | – | – |
| `pricing.overrides.upsert` | – | – | – | – | – | – | – |
| `pricing.schemes.list` | – | – | – | ✓ | – | – | – |
| `pricing.schemes.upsert` | – | – | – | – | – | – | – |
| `pricing.quote` | – | – | – | ✓ | – | – | – |
| `pricing.bargains.request` | – | – | – | ✓ | – | – | – |
| `pricing.bargains.decide` | – | – | – | – | – | – | – |
| `pricing.bargains.list` | – | – | – | ✓ | – | – | – |
| `pricing.bounds.set` | – | – | – | – | – | – | – |
| `inventory.locations.list` | – | – | – | ✓ | – | – | – |
| `inventory.locations.upsert` | – | – | – | – | – | – | – |
| `inventory.stock.sellable` | – | – | – | ✓ | – | – | – |
| `inventory.stock.balances` | – | – | – | – | – | – | – |
| `inventory.stock.adjust` | – | – | – | – | – | – | – |
| `inventory.stock.transfer` | – | – | – | – | – | – | – |
| `inventory.stock.ledger` | – | – | – | – | – | – | – |
| `inventory.lots.upsert` | – | – | – | – | – | – | – |
| `orders.create` | – | – | – | ✓ | – | – | – |
| `orders.setLines` | – | – | – | ✓ | – | – | – |
| `orders.repeatLast` | – | – | – | ✓ | – | – | – |
| `orders.submit` | – | – | – | ✓ | – | – | – |
| `orders.confirm` | – | – | – | – | – | – | – |
| `orders.cancel` | – | – | – | ✓ | – | – | – |
| `orders.get` | – | – | – | ✓ | – | – | – |
| `orders.list` | – | – | – | ✓ | – | – | – |
| `orders.approvals.list` | – | – | – | – | – | – | – |
| `orders.approvals.decide` | – | – | – | – | – | – | – |
| `sync.upload` | – | – | – | ✓ | – | – | – |
