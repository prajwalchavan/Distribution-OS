# inventory

Append-only stock ledger with derived balances; locations include warehouses AND vehicles on trips.

Status: not started. Add `inventory.module.ts` and `index.ts` here; see docs/03-system-architecture.md for the module contract.
