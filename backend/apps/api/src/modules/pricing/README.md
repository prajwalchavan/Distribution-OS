# pricing

Price lists, retailer overrides, schemes, bargain requests; wraps resolvePrice from @dos/domain.

Status: not started. Add `pricing.module.ts` and `index.ts` here; see docs/03-system-architecture.md for the module contract.
