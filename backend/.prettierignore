node_modules
dist
.turbo
coverage
pnpm-lock.yaml
libs/database/migrations
infra/docker

# Generated by `pnpm docs:readme` from the contract — prettier must not reformat them,
# or format:check and docs:readme:check would disagree in CI.
*-service/README.md

# `pnpm smoke` writes its per-service JSON here (gitignored output, not source), so a run of the
# harness before `pnpm format:check` must not turn into a formatting failure.
.smoke
