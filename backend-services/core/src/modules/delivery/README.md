# delivery

Vehicles, trips, load sheets, stops, delivery state machines, trip-end reconciliation, GPS ingest.

Status: not started. Add `delivery.module.ts` and `index.ts` here; see docs/03-system-architecture.md for the module contract.
