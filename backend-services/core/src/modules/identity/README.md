# identity

Sessions, phone OTP, Better Auth mount, device list; a user is global, a membership is tenant-scoped.

Status: not started. Add `identity.module.ts` and `index.ts` here; see docs/03-system-architecture.md for the module contract.
