{
  "name": "@dos/offline",
  "version": "0.0.0",
  "private": true,
  "description": "Offline-first primitives for the field apps: PowerSync schema, upload connector with idempotency keys, sync error surface",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint ."
  },
  "dependencies": {
    "@dos/domain": "workspace:*"
  },
  "devDependencies": {
    "@dos/config": "workspace:*",
    "eslint": "catalog:",
    "typescript": "catalog:"
  }
}
