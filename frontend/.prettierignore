node_modules
dist
.expo
.turbo
coverage
pnpm-lock.yaml

# `expo prebuild` (and `expo run:ios` / `run:android`, which call it) writes the whole native project
# into these two folders. They are generated, gitignored, and full of JSON, plist and Gradle output —
# `pnpm format` was rewriting an 86 MB Android build tree, and `format:check` would fail on any
# machine that had ever run the app on a phone.
*/ios
*/android
libs/*/ios
libs/*/android

# Generated by backend's `pnpm docs:readme`
*-app/README.md
