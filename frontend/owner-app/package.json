{
  "name": "@dos/owner-app",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "lint": "eslint .",
    "typecheck": "tsc -b --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@dos/api-client": "workspace:*",
    "@dos/contracts": "link:../../backend/libs/contracts",
    "@dos/domain": "link:../../backend/libs/domain",
    "@dos/ui": "workspace:*",
    "@tanstack/react-query": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "typescript": "catalog:",
    "vite": "catalog:",
    "eslint": "catalog:",
    "@dos/config": "workspace:*"
  },
  "description": "Owner app (web today, Expo later): masters, costs, prices, approvals, orders. Talks to owner-service on :3001."
}
